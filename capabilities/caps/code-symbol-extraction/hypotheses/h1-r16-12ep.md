# Hypothesis: h1-r16-12ep

## Family
H1 — epoch curve (continuation)

## Claim
Doubling epochs from 6 to 12 will roughly double effective training steps
(7 → ~14), and `symbol_precision` will scale linearly from iter 1's +1.60pp
to ~+3.20pp (0.804 → ~0.836). Composite to ~0.945.

## Mechanism
Iter 1 showed a real gradient signal (+1.60pp on the target sub-score, no
regression) but was step-starved: 97% skip rate × 240 nominal = 7 effective
steps. More epochs cycles the trainer through the prompts more times so the
non-skipped prompts get more gradient passes, and (because seed varies per
sample) some prompts that EOS'd early might produce a rollout later.

## Configuration
- prompts: prompts/h1-r16-6ep.jsonl (same 40-prompt set — no change in
  prompts; this is a clean epoch-curve ablation)
- rank: 16, alpha: 32, lr: 1e-4
- epochs: **12** (was 6)
- samples_per_prompt: 1, max_tokens: 128, top_k: 8
- env: KILN_STREAMING_PREFILL=1

## What's held constant from previous iteration
Everything except epochs. Prompts identical, hyperparameters identical,
recipe identical. Single-knob change.

## Falsification plan
- Δ symbol_precision < +1pp over baseline (i.e. roughly flat from iter 1's
  +1.60pp): epochs are step-starved AND have collapsed onto the same 7
  prompts repeatedly → H1 has structural ceiling. Pivot to H6 (off-policy
  SFT cold-start) or H8 (prompt pruning).
- Δ symbol_precision in [+1.6pp, +2.5pp]: gain continues but sub-linearly;
  H1 has diminishing returns. Try epoch=18 once or pivot to H3 (samples).
- Δ symbol_precision in [+2.5pp, +5pp]: claim confirmed. Iter 3 = H1 at 18
  epochs OR triangulate with a different family.
- Δ symbol_precision > +5pp: surprising overperformance; check for
  regression on other sub-scores. If clean, push H1 harder.
- Composite < baseline OR any sub-score regressed > 1pp: H7 (Stable-OPD
  mixture) next.

## Inspection plan
Same 3 train.opd prompts as iter 1 (ts_module, py_async_set, py_service).
Specifically check whether `EventEmitter` is still being listed in the TS
output — that's the canonical 4B failure for this capability.

## Verdict
(filled after eval)

Verdict: ✗ Falsified — symbol_precision Δ from iter 1 = 0.00pp (both stuck
at 0.8199). Composite Δ = 0.00pp (both 0.9370). Doubling epochs (6 → 12)
yielded **identically** the same 7 effective training steps because the
same 7 prompts train and the other 33 EOS regardless of seed/epoch. H1
epoch curve has saturated at iter 1's gain.

Root cause is the kiln OPD trainer's rollout-prompt construction (the
`prompt_only` ends before the `<|im_start|>assistant\n` marker, so the
student samples EOS immediately on prompts where it lacks a strong
non-EOS prior). This is the same bug as v6 in the JSON-schema run; the
gen_prompt_suffix env var fix is behind `KILN_OPD_GEN_PROMPT_SUFFIX=1`
but uses raw `tokenizer.encode()` which doesn't resolve special tokens
correctly. Proper fix needs `apply_chat_template_full_with_options`.

Closing capability #2. Iter 1's adapter (`symbol-h1-r16-6ep`, composite
0.9370, +0.6%) is the best we can do with the unfixed trainer; it
captured 8.2% of headroom. Moving to capability #3.
