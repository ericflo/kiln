# calibration/ — python-algo rubric sanity fixtures

`rubric_sanity.py` asserts min(good_composite) > max(bad_composite) + 0.2.

See `good.jsonl` and `bad.jsonl` for the fixtures.

Refresh: `python3 ../rubric_sanity.py` after any rubric change.
