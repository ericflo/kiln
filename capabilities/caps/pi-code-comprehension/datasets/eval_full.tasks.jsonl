{"task_id": "task_0000_shapes_rect_area", "target_file": "shapes.py", "target_symbol": "rect_area", "gold": {"inputs": [{"name": "width", "type": "float", "source_line": 9}, {"name": "height", "type": "float", "source_line": 9}], "returns": [{"type": "float", "source_line": 9}], "mutates": [], "calls": [], "called_by": [{"file": "runner.py", "line": 10}], "invariants": [{"primary": "width:  must be non-negative.", "paraphrases": []}, {"primary": "height: must be non-negative.", "paraphrases": []}, {"primary": "Raises ValueError if either input is negative.", "paraphrases": []}, {"primary": "raises if width < 0 or height < 0", "paraphrases": ["requires not (width < 0 or height < 0)", "asserts not (width < 0 or height < 0)"]}], "side_effects": [{"primary": "raises ValueError('dimensions must be non-negative')", "paraphrases": ["raises ValueError"]}]}, "files": {"shapes.py": "\"\"\"Basic 2D geometry primitives and area/perimeter routines.\"\"\"\n\nfrom __future__ import annotations\n\nimport math\nfrom typing import List, Tuple\n\n\ndef rect_area(width: float, height: float) -> float:\n    \"\"\"Return the area of a rectangle.\n\n    Args:\n        width:  must be non-negative.\n        height: must be non-negative.\n\n    Raises ValueError if either input is negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return width * height\n\n\ndef rect_perimeter(width: float, height: float) -> float:\n    \"\"\"Return the perimeter of a rectangle.\n\n    Requires width and height to be non-negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return 2.0 * (width + height)\n\n\ndef triangle_area(a: float, b: float, c: float) -> float:\n    \"\"\"Heron's formula. All sides must be positive and satisfy\n    the triangle inequality.\"\"\"\n    if a <= 0 or b <= 0 or c <= 0:\n        raise ValueError(\"sides must be positive\")\n    if a + b <= c or a + c <= b or b + c <= a:\n        raise ValueError(\"triangle inequality not satisfied\")\n    s = (a + b + c) / 2.0\n    return math.sqrt(s * (s - a) * (s - b) * (s - c))\n\n\ndef polygon_perimeter(points: List[Tuple[float, float]]) -> float:\n    \"\"\"Closed-polygon perimeter: sum of euclidean edge lengths.\n\n    Requires at least 3 points; assumes points are in order.\n    \"\"\"\n    if len(points) < 3:\n        raise ValueError(\"need at least 3 points\")\n    total = 0.0\n    n = len(points)\n    for i in range(n):\n        x1, y1 = points[i]\n        x2, y2 = points[(i + 1) % n]\n        total += math.hypot(x2 - x1, y2 - y1)\n    return total\n\n\ndef scale_points(points: List[Tuple[float, float]], factor: float) -> None:\n    \"\"\"In-place uniform scaling of points by `factor`.\n\n    Mutates the input list; raises if factor is zero (degenerate).\n    \"\"\"\n    if factor == 0:\n        raise ValueError(\"factor must be non-zero\")\n    for i, (x, y) in enumerate(points):\n        points[i] = (x * factor, y * factor)\n", "runner.py": "\"\"\"Example consumer that imports shapes.* and assembles a small report.\"\"\"\n\nfrom __future__ import annotations\n\nfrom shapes import rect_area, rect_perimeter, triangle_area, polygon_perimeter, scale_points\n\n\ndef report_rectangle(w: float, h: float) -> dict:\n    return {\n        \"area\": rect_area(w, h),\n        \"perimeter\": rect_perimeter(w, h),\n    }\n\n\ndef report_triangle(a: float, b: float, c: float) -> dict:\n    return {\"area\": triangle_area(a, b, c)}\n\n\ndef main() -> None:\n    points = [(0.0, 0.0), (1.0, 0.0), (1.0, 1.0), (0.0, 1.0)]\n    p = polygon_perimeter(points)\n    scale_points(points, 2.0)\n    p2 = polygon_perimeter(points)\n    print(f\"perimeter before scale: {p}; after: {p2}\")\n"}}
{"task_id": "task_0001_shapes_rect_perimeter", "target_file": "shapes.py", "target_symbol": "rect_perimeter", "gold": {"inputs": [{"name": "width", "type": "float", "source_line": 23}, {"name": "height", "type": "float", "source_line": 23}], "returns": [{"type": "float", "source_line": 23}], "mutates": [], "calls": [], "called_by": [{"file": "runner.py", "line": 11}], "invariants": [{"primary": "Requires width and height to be non-negative.", "paraphrases": []}, {"primary": "raises if width < 0 or height < 0", "paraphrases": ["requires not (width < 0 or height < 0)", "asserts not (width < 0 or height < 0)"]}], "side_effects": [{"primary": "raises ValueError('dimensions must be non-negative')", "paraphrases": ["raises ValueError"]}]}, "files": {"shapes.py": "\"\"\"Basic 2D geometry primitives and area/perimeter routines.\"\"\"\n\nfrom __future__ import annotations\n\nimport math\nfrom typing import List, Tuple\n\n\ndef rect_area(width: float, height: float) -> float:\n    \"\"\"Return the area of a rectangle.\n\n    Args:\n        width:  must be non-negative.\n        height: must be non-negative.\n\n    Raises ValueError if either input is negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return width * height\n\n\ndef rect_perimeter(width: float, height: float) -> float:\n    \"\"\"Return the perimeter of a rectangle.\n\n    Requires width and height to be non-negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return 2.0 * (width + height)\n\n\ndef triangle_area(a: float, b: float, c: float) -> float:\n    \"\"\"Heron's formula. All sides must be positive and satisfy\n    the triangle inequality.\"\"\"\n    if a <= 0 or b <= 0 or c <= 0:\n        raise ValueError(\"sides must be positive\")\n    if a + b <= c or a + c <= b or b + c <= a:\n        raise ValueError(\"triangle inequality not satisfied\")\n    s = (a + b + c) / 2.0\n    return math.sqrt(s * (s - a) * (s - b) * (s - c))\n\n\ndef polygon_perimeter(points: List[Tuple[float, float]]) -> float:\n    \"\"\"Closed-polygon perimeter: sum of euclidean edge lengths.\n\n    Requires at least 3 points; assumes points are in order.\n    \"\"\"\n    if len(points) < 3:\n        raise ValueError(\"need at least 3 points\")\n    total = 0.0\n    n = len(points)\n    for i in range(n):\n        x1, y1 = points[i]\n        x2, y2 = points[(i + 1) % n]\n        total += math.hypot(x2 - x1, y2 - y1)\n    return total\n\n\ndef scale_points(points: List[Tuple[float, float]], factor: float) -> None:\n    \"\"\"In-place uniform scaling of points by `factor`.\n\n    Mutates the input list; raises if factor is zero (degenerate).\n    \"\"\"\n    if factor == 0:\n        raise ValueError(\"factor must be non-zero\")\n    for i, (x, y) in enumerate(points):\n        points[i] = (x * factor, y * factor)\n", "runner.py": "\"\"\"Example consumer that imports shapes.* and assembles a small report.\"\"\"\n\nfrom __future__ import annotations\n\nfrom shapes import rect_area, rect_perimeter, triangle_area, polygon_perimeter, scale_points\n\n\ndef report_rectangle(w: float, h: float) -> dict:\n    return {\n        \"area\": rect_area(w, h),\n        \"perimeter\": rect_perimeter(w, h),\n    }\n\n\ndef report_triangle(a: float, b: float, c: float) -> dict:\n    return {\"area\": triangle_area(a, b, c)}\n\n\ndef main() -> None:\n    points = [(0.0, 0.0), (1.0, 0.0), (1.0, 1.0), (0.0, 1.0)]\n    p = polygon_perimeter(points)\n    scale_points(points, 2.0)\n    p2 = polygon_perimeter(points)\n    print(f\"perimeter before scale: {p}; after: {p2}\")\n"}}
{"task_id": "task_0002_shapes_triangle_area", "target_file": "shapes.py", "target_symbol": "triangle_area", "gold": {"inputs": [{"name": "a", "type": "float", "source_line": 33}, {"name": "b", "type": "float", "source_line": 33}, {"name": "c", "type": "float", "source_line": 33}], "returns": [{"type": "float", "source_line": 33}], "mutates": [], "calls": [{"name": "sqrt", "file": "", "line": 41}], "called_by": [{"file": "runner.py", "line": 16}], "invariants": [{"primary": "Heron's formula. All sides must be positive and satisfy", "paraphrases": []}, {"primary": "raises if a <= 0 or b <= 0 or c <= 0", "paraphrases": ["requires not (a <= 0 or b <= 0 or c <= 0)", "asserts not (a <= 0 or b <= 0 or c <= 0)"]}, {"primary": "raises if a + b <= c or a + c <= b or b + c <= a", "paraphrases": ["requires not (a + b <= c or a + c <= b or b + c <= a)", "asserts not (a + b <= c or a + c <= b or b + c <= a)"]}], "side_effects": [{"primary": "raises ValueError('sides must be positive')", "paraphrases": ["raises ValueError"]}, {"primary": "raises ValueError('triangle inequality not satisfied')", "paraphrases": ["raises ValueError"]}]}, "files": {"shapes.py": "\"\"\"Basic 2D geometry primitives and area/perimeter routines.\"\"\"\n\nfrom __future__ import annotations\n\nimport math\nfrom typing import List, Tuple\n\n\ndef rect_area(width: float, height: float) -> float:\n    \"\"\"Return the area of a rectangle.\n\n    Args:\n        width:  must be non-negative.\n        height: must be non-negative.\n\n    Raises ValueError if either input is negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return width * height\n\n\ndef rect_perimeter(width: float, height: float) -> float:\n    \"\"\"Return the perimeter of a rectangle.\n\n    Requires width and height to be non-negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return 2.0 * (width + height)\n\n\ndef triangle_area(a: float, b: float, c: float) -> float:\n    \"\"\"Heron's formula. All sides must be positive and satisfy\n    the triangle inequality.\"\"\"\n    if a <= 0 or b <= 0 or c <= 0:\n        raise ValueError(\"sides must be positive\")\n    if a + b <= c or a + c <= b or b + c <= a:\n        raise ValueError(\"triangle inequality not satisfied\")\n    s = (a + b + c) / 2.0\n    return math.sqrt(s * (s - a) * (s - b) * (s - c))\n\n\ndef polygon_perimeter(points: List[Tuple[float, float]]) -> float:\n    \"\"\"Closed-polygon perimeter: sum of euclidean edge lengths.\n\n    Requires at least 3 points; assumes points are in order.\n    \"\"\"\n    if len(points) < 3:\n        raise ValueError(\"need at least 3 points\")\n    total = 0.0\n    n = len(points)\n    for i in range(n):\n        x1, y1 = points[i]\n        x2, y2 = points[(i + 1) % n]\n        total += math.hypot(x2 - x1, y2 - y1)\n    return total\n\n\ndef scale_points(points: List[Tuple[float, float]], factor: float) -> None:\n    \"\"\"In-place uniform scaling of points by `factor`.\n\n    Mutates the input list; raises if factor is zero (degenerate).\n    \"\"\"\n    if factor == 0:\n        raise ValueError(\"factor must be non-zero\")\n    for i, (x, y) in enumerate(points):\n        points[i] = (x * factor, y * factor)\n", "runner.py": "\"\"\"Example consumer that imports shapes.* and assembles a small report.\"\"\"\n\nfrom __future__ import annotations\n\nfrom shapes import rect_area, rect_perimeter, triangle_area, polygon_perimeter, scale_points\n\n\ndef report_rectangle(w: float, h: float) -> dict:\n    return {\n        \"area\": rect_area(w, h),\n        \"perimeter\": rect_perimeter(w, h),\n    }\n\n\ndef report_triangle(a: float, b: float, c: float) -> dict:\n    return {\"area\": triangle_area(a, b, c)}\n\n\ndef main() -> None:\n    points = [(0.0, 0.0), (1.0, 0.0), (1.0, 1.0), (0.0, 1.0)]\n    p = polygon_perimeter(points)\n    scale_points(points, 2.0)\n    p2 = polygon_perimeter(points)\n    print(f\"perimeter before scale: {p}; after: {p2}\")\n"}}
{"task_id": "task_0003_shapes_polygon_perimeter", "target_file": "shapes.py", "target_symbol": "polygon_perimeter", "gold": {"inputs": [{"name": "points", "type": "List[Tuple[float, float]]", "source_line": 44}], "returns": [{"type": "float", "source_line": 44}], "mutates": [], "calls": [{"name": "hypot", "file": "", "line": 56}], "called_by": [{"file": "runner.py", "line": 21}], "invariants": [{"primary": "Requires at least 3 points; assumes points are in order.", "paraphrases": []}, {"primary": "raises if len(points) < 3", "paraphrases": ["requires not (len(points) < 3)", "asserts not (len(points) < 3)"]}], "side_effects": [{"primary": "raises ValueError('need at least 3 points')", "paraphrases": ["raises ValueError"]}]}, "files": {"shapes.py": "\"\"\"Basic 2D geometry primitives and area/perimeter routines.\"\"\"\n\nfrom __future__ import annotations\n\nimport math\nfrom typing import List, Tuple\n\n\ndef rect_area(width: float, height: float) -> float:\n    \"\"\"Return the area of a rectangle.\n\n    Args:\n        width:  must be non-negative.\n        height: must be non-negative.\n\n    Raises ValueError if either input is negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return width * height\n\n\ndef rect_perimeter(width: float, height: float) -> float:\n    \"\"\"Return the perimeter of a rectangle.\n\n    Requires width and height to be non-negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return 2.0 * (width + height)\n\n\ndef triangle_area(a: float, b: float, c: float) -> float:\n    \"\"\"Heron's formula. All sides must be positive and satisfy\n    the triangle inequality.\"\"\"\n    if a <= 0 or b <= 0 or c <= 0:\n        raise ValueError(\"sides must be positive\")\n    if a + b <= c or a + c <= b or b + c <= a:\n        raise ValueError(\"triangle inequality not satisfied\")\n    s = (a + b + c) / 2.0\n    return math.sqrt(s * (s - a) * (s - b) * (s - c))\n\n\ndef polygon_perimeter(points: List[Tuple[float, float]]) -> float:\n    \"\"\"Closed-polygon perimeter: sum of euclidean edge lengths.\n\n    Requires at least 3 points; assumes points are in order.\n    \"\"\"\n    if len(points) < 3:\n        raise ValueError(\"need at least 3 points\")\n    total = 0.0\n    n = len(points)\n    for i in range(n):\n        x1, y1 = points[i]\n        x2, y2 = points[(i + 1) % n]\n        total += math.hypot(x2 - x1, y2 - y1)\n    return total\n\n\ndef scale_points(points: List[Tuple[float, float]], factor: float) -> None:\n    \"\"\"In-place uniform scaling of points by `factor`.\n\n    Mutates the input list; raises if factor is zero (degenerate).\n    \"\"\"\n    if factor == 0:\n        raise ValueError(\"factor must be non-zero\")\n    for i, (x, y) in enumerate(points):\n        points[i] = (x * factor, y * factor)\n", "runner.py": "\"\"\"Example consumer that imports shapes.* and assembles a small report.\"\"\"\n\nfrom __future__ import annotations\n\nfrom shapes import rect_area, rect_perimeter, triangle_area, polygon_perimeter, scale_points\n\n\ndef report_rectangle(w: float, h: float) -> dict:\n    return {\n        \"area\": rect_area(w, h),\n        \"perimeter\": rect_perimeter(w, h),\n    }\n\n\ndef report_triangle(a: float, b: float, c: float) -> dict:\n    return {\"area\": triangle_area(a, b, c)}\n\n\ndef main() -> None:\n    points = [(0.0, 0.0), (1.0, 0.0), (1.0, 1.0), (0.0, 1.0)]\n    p = polygon_perimeter(points)\n    scale_points(points, 2.0)\n    p2 = polygon_perimeter(points)\n    print(f\"perimeter before scale: {p}; after: {p2}\")\n"}}
{"task_id": "task_0004_shapes_scale_points", "target_file": "shapes.py", "target_symbol": "scale_points", "gold": {"inputs": [{"name": "points", "type": "List[Tuple[float, float]]", "source_line": 60}, {"name": "factor", "type": "float", "source_line": 60}], "returns": [{"type": "None", "source_line": 60}], "mutates": ["arg:points"], "calls": [], "called_by": [{"file": "runner.py", "line": 22}], "invariants": [{"primary": "Mutates the input list; raises if factor is zero (degenerate).", "paraphrases": []}, {"primary": "raises if factor == 0", "paraphrases": ["requires not (factor == 0)", "asserts not (factor == 0)"]}], "side_effects": [{"primary": "raises ValueError('factor must be non-zero')", "paraphrases": ["raises ValueError"]}]}, "files": {"shapes.py": "\"\"\"Basic 2D geometry primitives and area/perimeter routines.\"\"\"\n\nfrom __future__ import annotations\n\nimport math\nfrom typing import List, Tuple\n\n\ndef rect_area(width: float, height: float) -> float:\n    \"\"\"Return the area of a rectangle.\n\n    Args:\n        width:  must be non-negative.\n        height: must be non-negative.\n\n    Raises ValueError if either input is negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return width * height\n\n\ndef rect_perimeter(width: float, height: float) -> float:\n    \"\"\"Return the perimeter of a rectangle.\n\n    Requires width and height to be non-negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return 2.0 * (width + height)\n\n\ndef triangle_area(a: float, b: float, c: float) -> float:\n    \"\"\"Heron's formula. All sides must be positive and satisfy\n    the triangle inequality.\"\"\"\n    if a <= 0 or b <= 0 or c <= 0:\n        raise ValueError(\"sides must be positive\")\n    if a + b <= c or a + c <= b or b + c <= a:\n        raise ValueError(\"triangle inequality not satisfied\")\n    s = (a + b + c) / 2.0\n    return math.sqrt(s * (s - a) * (s - b) * (s - c))\n\n\ndef polygon_perimeter(points: List[Tuple[float, float]]) -> float:\n    \"\"\"Closed-polygon perimeter: sum of euclidean edge lengths.\n\n    Requires at least 3 points; assumes points are in order.\n    \"\"\"\n    if len(points) < 3:\n        raise ValueError(\"need at least 3 points\")\n    total = 0.0\n    n = len(points)\n    for i in range(n):\n        x1, y1 = points[i]\n        x2, y2 = points[(i + 1) % n]\n        total += math.hypot(x2 - x1, y2 - y1)\n    return total\n\n\ndef scale_points(points: List[Tuple[float, float]], factor: float) -> None:\n    \"\"\"In-place uniform scaling of points by `factor`.\n\n    Mutates the input list; raises if factor is zero (degenerate).\n    \"\"\"\n    if factor == 0:\n        raise ValueError(\"factor must be non-zero\")\n    for i, (x, y) in enumerate(points):\n        points[i] = (x * factor, y * factor)\n", "runner.py": "\"\"\"Example consumer that imports shapes.* and assembles a small report.\"\"\"\n\nfrom __future__ import annotations\n\nfrom shapes import rect_area, rect_perimeter, triangle_area, polygon_perimeter, scale_points\n\n\ndef report_rectangle(w: float, h: float) -> dict:\n    return {\n        \"area\": rect_area(w, h),\n        \"perimeter\": rect_perimeter(w, h),\n    }\n\n\ndef report_triangle(a: float, b: float, c: float) -> dict:\n    return {\"area\": triangle_area(a, b, c)}\n\n\ndef main() -> None:\n    points = [(0.0, 0.0), (1.0, 0.0), (1.0, 1.0), (0.0, 1.0)]\n    p = polygon_perimeter(points)\n    scale_points(points, 2.0)\n    p2 = polygon_perimeter(points)\n    print(f\"perimeter before scale: {p}; after: {p2}\")\n"}}
{"task_id": "task_0005_runner_report_rectangle", "target_file": "runner.py", "target_symbol": "report_rectangle", "gold": {"inputs": [{"name": "w", "type": "float", "source_line": 8}, {"name": "h", "type": "float", "source_line": 8}], "returns": [{"type": "dict", "source_line": 8}], "mutates": [], "calls": [{"name": "rect_area", "file": "", "line": 10}, {"name": "rect_perimeter", "file": "", "line": 11}], "called_by": [{"file": "client_0.py", "line": 4}, {"file": "client_1.py", "line": 3}], "invariants": [], "side_effects": []}, "files": {"runner.py": "\"\"\"Example consumer that imports shapes.* and assembles a small report.\"\"\"\n\nfrom __future__ import annotations\n\nfrom shapes import rect_area, rect_perimeter, triangle_area, polygon_perimeter, scale_points\n\n\ndef report_rectangle(w: float, h: float) -> dict:\n    return {\n        \"area\": rect_area(w, h),\n        \"perimeter\": rect_perimeter(w, h),\n    }\n\n\ndef report_triangle(a: float, b: float, c: float) -> dict:\n    return {\"area\": triangle_area(a, b, c)}\n\n\ndef main() -> None:\n    points = [(0.0, 0.0), (1.0, 0.0), (1.0, 1.0), (0.0, 1.0)]\n    p = polygon_perimeter(points)\n    scale_points(points, 2.0)\n    p2 = polygon_perimeter(points)\n    print(f\"perimeter before scale: {p}; after: {p2}\")\n", "shapes.py": "\"\"\"Basic 2D geometry primitives and area/perimeter routines.\"\"\"\n\nfrom __future__ import annotations\n\nimport math\nfrom typing import List, Tuple\n\n\ndef rect_area(width: float, height: float) -> float:\n    \"\"\"Return the area of a rectangle.\n\n    Args:\n        width:  must be non-negative.\n        height: must be non-negative.\n\n    Raises ValueError if either input is negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return width * height\n\n\ndef rect_perimeter(width: float, height: float) -> float:\n    \"\"\"Return the perimeter of a rectangle.\n\n    Requires width and height to be non-negative.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"dimensions must be non-negative\")\n    return 2.0 * (width + height)\n\n\ndef triangle_area(a: float, b: float, c: float) -> float:\n    \"\"\"Heron's formula. All sides must be positive and satisfy\n    the triangle inequality.\"\"\"\n    if a <= 0 or b <= 0 or c <= 0:\n        raise ValueError(\"sides must be positive\")\n    if a + b <= c or a + c <= b or b + c <= a:\n        raise ValueError(\"triangle inequality not satisfied\")\n    s = (a + b + c) / 2.0\n    return math.sqrt(s * (s - a) * (s - b) * (s - c))\n\n\ndef polygon_perimeter(points: List[Tuple[float, float]]) -> float:\n    \"\"\"Closed-polygon perimeter: sum of euclidean edge lengths.\n\n    Requires at least 3 points; assumes points are in order.\n    \"\"\"\n    if len(points) < 3:\n        raise ValueError(\"need at least 3 points\")\n    total = 0.0\n    n = len(points)\n    for i in range(n):\n        x1, y1 = points[i]\n        x2, y2 = points[(i + 1) % n]\n        total += math.hypot(x2 - x1, y2 - y1)\n    return total\n\n\ndef scale_points(points: List[Tuple[float, float]], factor: float) -> None:\n    \"\"\"In-place uniform scaling of points by `factor`.\n\n    Mutates the input list; raises if factor is zero (degenerate).\n    \"\"\"\n    if factor == 0:\n        raise ValueError(\"factor must be non-zero\")\n    for i, (x, y) in enumerate(points):\n        points[i] = (x * factor, y * factor)\n", "client_0.py": "from . import runner as m\n\ndef invoke():\n    return m.report_rectangle(w, h)\n", "client_1.py": "from . import runner as m\n\ndef invoke():\n    return m.report_rectangle(w, h)\n"}}
{"task_id": "task_0006_users_lookup_session", "target_file": "users.py", "target_symbol": "lookup_session", "gold": {"inputs": [{"name": "token", "type": "str", "source_line": 17}], "returns": [], "mutates": [], "calls": [{"name": "get_cache", "file": "", "line": 18}, {"name": "get", "file": "", "line": 21}], "called_by": [{"file": "client_0.py", "line": 4}, {"file": "client_1.py", "line": 3}], "invariants": [], "side_effects": []}, "files": {"users.py": "\"\"\"Top-level user-facing functions that consume cache.*.\"\"\"\n\nfrom __future__ import annotations\n\nfrom cache import register_cache, get_cache, clear_all_caches\n\n\ndef init_user_caches() -> None:\n    register_cache(\"sessions\", 1024)\n    register_cache(\"preferences\", 256)\n\n\ndef reset_user_caches() -> None:\n    clear_all_caches()\n\n\ndef lookup_session(token: str):\n    c = get_cache(\"sessions\")\n    if c is None:\n        return None\n    return c.get(token)\n", "cache.py": "\"\"\"Small bounded-size LRU cache with a global registry.\"\"\"\n\nfrom __future__ import annotations\n\nfrom collections import OrderedDict\nfrom typing import Any, Dict, Optional\n\n\n_REGISTRY: Dict[str, \"Cache\"] = {}\n\n\nclass Cache:\n    \"\"\"Bounded LRU cache. Capacity must be positive.\"\"\"\n\n    def __init__(self, capacity: int) -> None:\n        if capacity <= 0:\n            raise ValueError(\"capacity must be positive\")\n        self.capacity = capacity\n        self._store: \"OrderedDict[str, Any]\" = OrderedDict()\n\n    def get(self, key: str) -> Optional[Any]:\n        if key not in self._store:\n            return None\n        self._store.move_to_end(key)\n        return self._store[key]\n\n    def put(self, key: str, value: Any) -> None:\n        if key in self._store:\n            self._store.move_to_end(key)\n        self._store[key] = value\n        if len(self._store) > self.capacity:\n            self._store.popitem(last=False)\n\n\ndef register_cache(name: str, capacity: int) -> Cache:\n    \"\"\"Construct a Cache and register it globally by name.\n\n    Raises ValueError if name is already registered.\n    \"\"\"\n    global _REGISTRY\n    if name in _REGISTRY:\n        raise ValueError(f\"cache {name!r} already registered\")\n    c = Cache(capacity)\n    _REGISTRY[name] = c\n    return c\n\n\ndef get_cache(name: str) -> Optional[Cache]:\n    \"\"\"Look up a previously registered cache by name. Returns None if missing.\"\"\"\n    return _REGISTRY.get(name)\n\n\ndef clear_all_caches() -> None:\n    \"\"\"Empty every registered cache. Mutates the global registry's caches\n    in-place but does NOT remove the registry entries themselves.\"\"\"\n    for c in _REGISTRY.values():\n        c._store.clear()\n", "client_0.py": "import users\n\n\ndef handle(request):\n    result = users.lookup_session(token)\n    return result\n", "client_1.py": "import users\n\n\ndef handle(request):\n    result = users.lookup_session(token)\n    return result\n"}}
{"task_id": "task_0007_cache_register_cache", "target_file": "cache.py", "target_symbol": "register_cache", "gold": {"inputs": [{"name": "name", "type": "str", "source_line": 35}, {"name": "capacity", "type": "int", "source_line": 35}], "returns": [{"type": "Cache", "source_line": 35}], "mutates": [], "calls": [{"name": "Cache", "file": "cache.py", "line": 43}], "called_by": [{"file": "users.py", "line": 9}], "invariants": [{"primary": "Raises ValueError if name is already registered.", "paraphrases": []}, {"primary": "raises if name in _REGISTRY", "paraphrases": ["requires not (name in _REGISTRY)", "asserts not (name in _REGISTRY)"]}], "side_effects": [{"primary": "raises ValueError(f'cache {name!r} already registered')", "paraphrases": ["raises ValueError"]}]}, "files": {"cache.py": "\"\"\"Small bounded-size LRU cache with a global registry.\"\"\"\n\nfrom __future__ import annotations\n\nfrom collections import OrderedDict\nfrom typing import Any, Dict, Optional\n\n\n_REGISTRY: Dict[str, \"Cache\"] = {}\n\n\nclass Cache:\n    \"\"\"Bounded LRU cache. Capacity must be positive.\"\"\"\n\n    def __init__(self, capacity: int) -> None:\n        if capacity <= 0:\n            raise ValueError(\"capacity must be positive\")\n        self.capacity = capacity\n        self._store: \"OrderedDict[str, Any]\" = OrderedDict()\n\n    def get(self, key: str) -> Optional[Any]:\n        if key not in self._store:\n            return None\n        self._store.move_to_end(key)\n        return self._store[key]\n\n    def put(self, key: str, value: Any) -> None:\n        if key in self._store:\n            self._store.move_to_end(key)\n        self._store[key] = value\n        if len(self._store) > self.capacity:\n            self._store.popitem(last=False)\n\n\ndef register_cache(name: str, capacity: int) -> Cache:\n    \"\"\"Construct a Cache and register it globally by name.\n\n    Raises ValueError if name is already registered.\n    \"\"\"\n    global _REGISTRY\n    if name in _REGISTRY:\n        raise ValueError(f\"cache {name!r} already registered\")\n    c = Cache(capacity)\n    _REGISTRY[name] = c\n    return c\n\n\ndef get_cache(name: str) -> Optional[Cache]:\n    \"\"\"Look up a previously registered cache by name. Returns None if missing.\"\"\"\n    return _REGISTRY.get(name)\n\n\ndef clear_all_caches() -> None:\n    \"\"\"Empty every registered cache. Mutates the global registry's caches\n    in-place but does NOT remove the registry entries themselves.\"\"\"\n    for c in _REGISTRY.values():\n        c._store.clear()\n", "users.py": "\"\"\"Top-level user-facing functions that consume cache.*.\"\"\"\n\nfrom __future__ import annotations\n\nfrom cache import register_cache, get_cache, clear_all_caches\n\n\ndef init_user_caches() -> None:\n    register_cache(\"sessions\", 1024)\n    register_cache(\"preferences\", 256)\n\n\ndef reset_user_caches() -> None:\n    clear_all_caches()\n\n\ndef lookup_session(token: str):\n    c = get_cache(\"sessions\")\n    if c is None:\n        return None\n    return c.get(token)\n"}}
{"task_id": "task_0008_text_normalize_whitespace", "target_file": "text.py", "target_symbol": "normalize_whitespace", "gold": {"inputs": [{"name": "s", "type": "str", "source_line": 8}], "returns": [{"type": "str", "source_line": 8}], "mutates": [], "calls": [{"name": "strip", "file": "", "line": 22}, {"name": "isspace", "file": "", "line": 15}, {"name": "append", "file": "", "line": 20}, {"name": "join", "file": "", "line": 22}, {"name": "append", "file": "", "line": 17}], "called_by": [{"file": "audit.py", "line": 11}], "invariants": [{"primary": "raises if not isinstance(s, str)", "paraphrases": ["requires not (not isinstance(s, str))", "asserts not (not isinstance(s, str))"]}], "side_effects": [{"primary": "raises TypeError('expected str')", "paraphrases": ["raises TypeError"]}]}, "files": {"text.py": "\"\"\"String / text utilities.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict, List, Optional\n\n\ndef normalize_whitespace(s: str) -> str:\n    \"\"\"Collapse runs of whitespace to single spaces and strip ends.\"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"expected str\")\n    out: List[str] = []\n    in_ws = False\n    for ch in s:\n        if ch.isspace():\n            if not in_ws:\n                out.append(\" \")\n                in_ws = True\n        else:\n            out.append(ch)\n            in_ws = False\n    return \"\".join(out).strip()\n\n\ndef word_counts(text: str) -> Dict[str, int]:\n    \"\"\"Return a dict of word\u2192count. Words are case-folded.\n\n    Requires text to be a non-None string.\n    \"\"\"\n    if text is None:\n        raise ValueError(\"text must not be None\")\n    counts: Dict[str, int] = {}\n    for w in text.lower().split():\n        counts[w] = counts.get(w, 0) + 1\n    return counts\n\n\ndef truncate(text: str, max_len: int, suffix: str = \"\u2026\") -> str:\n    \"\"\"Truncate `text` to at most `max_len` characters; append `suffix` if cut.\n\n    Asserts max_len >= len(suffix).\n    \"\"\"\n    assert max_len >= len(suffix), \"max_len must be >= len(suffix)\"\n    if len(text) <= max_len:\n        return text\n    return text[: max_len - len(suffix)] + suffix\n\n\ndef find_first(needle: str, haystacks: List[str]) -> Optional[int]:\n    \"\"\"Return index of first haystack containing needle; None if not found.\n\n    Empty needle matches the first haystack.\n    \"\"\"\n    for i, h in enumerate(haystacks):\n        if needle in h:\n            return i\n    return None\n", "audit.py": "\"\"\"Wraps text.* to produce a small audit report on a corpus.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import List\n\nfrom text import normalize_whitespace, word_counts, truncate, find_first\n\n\ndef audit_lines(lines: List[str], probe: str = \"ERROR\") -> dict:\n    cleaned = [normalize_whitespace(l) for l in lines]\n    counts = word_counts(\" \".join(cleaned))\n    idx = find_first(probe, cleaned)\n    digest = truncate(\" | \".join(cleaned), 120)\n    return {\"counts\": counts, \"first_match\": idx, \"digest\": digest}\n"}}
{"task_id": "task_0009_text_word_counts", "target_file": "text.py", "target_symbol": "word_counts", "gold": {"inputs": [{"name": "text", "type": "str", "source_line": 25}], "returns": [{"type": "Dict[str, int]", "source_line": 25}], "mutates": [], "calls": [{"name": "split", "file": "", "line": 33}, {"name": "lower", "file": "", "line": 33}, {"name": "get", "file": "", "line": 34}], "called_by": [{"file": "audit.py", "line": 12}], "invariants": [{"primary": "Requires text to be a non-None string.", "paraphrases": []}, {"primary": "raises if text is None", "paraphrases": ["requires not (text is None)", "asserts not (text is None)"]}], "side_effects": [{"primary": "raises ValueError('text must not be None')", "paraphrases": ["raises ValueError"]}]}, "files": {"text.py": "\"\"\"String / text utilities.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict, List, Optional\n\n\ndef normalize_whitespace(s: str) -> str:\n    \"\"\"Collapse runs of whitespace to single spaces and strip ends.\"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"expected str\")\n    out: List[str] = []\n    in_ws = False\n    for ch in s:\n        if ch.isspace():\n            if not in_ws:\n                out.append(\" \")\n                in_ws = True\n        else:\n            out.append(ch)\n            in_ws = False\n    return \"\".join(out).strip()\n\n\ndef word_counts(text: str) -> Dict[str, int]:\n    \"\"\"Return a dict of word\u2192count. Words are case-folded.\n\n    Requires text to be a non-None string.\n    \"\"\"\n    if text is None:\n        raise ValueError(\"text must not be None\")\n    counts: Dict[str, int] = {}\n    for w in text.lower().split():\n        counts[w] = counts.get(w, 0) + 1\n    return counts\n\n\ndef truncate(text: str, max_len: int, suffix: str = \"\u2026\") -> str:\n    \"\"\"Truncate `text` to at most `max_len` characters; append `suffix` if cut.\n\n    Asserts max_len >= len(suffix).\n    \"\"\"\n    assert max_len >= len(suffix), \"max_len must be >= len(suffix)\"\n    if len(text) <= max_len:\n        return text\n    return text[: max_len - len(suffix)] + suffix\n\n\ndef find_first(needle: str, haystacks: List[str]) -> Optional[int]:\n    \"\"\"Return index of first haystack containing needle; None if not found.\n\n    Empty needle matches the first haystack.\n    \"\"\"\n    for i, h in enumerate(haystacks):\n        if needle in h:\n            return i\n    return None\n", "audit.py": "\"\"\"Wraps text.* to produce a small audit report on a corpus.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import List\n\nfrom text import normalize_whitespace, word_counts, truncate, find_first\n\n\ndef audit_lines(lines: List[str], probe: str = \"ERROR\") -> dict:\n    cleaned = [normalize_whitespace(l) for l in lines]\n    counts = word_counts(\" \".join(cleaned))\n    idx = find_first(probe, cleaned)\n    digest = truncate(\" | \".join(cleaned), 120)\n    return {\"counts\": counts, \"first_match\": idx, \"digest\": digest}\n"}}
{"task_id": "task_0010_text_truncate", "target_file": "text.py", "target_symbol": "truncate", "gold": {"inputs": [{"name": "text", "type": "str", "source_line": 38}, {"name": "max_len", "type": "int", "source_line": 38}, {"name": "suffix", "type": "str", "source_line": 38}], "returns": [{"type": "str", "source_line": 38}], "mutates": [], "calls": [], "called_by": [{"file": "audit.py", "line": 14}], "invariants": [{"primary": "asserts max_len >= len(suffix)", "paraphrases": ["requires max_len >= len(suffix)", "max_len >= len(suffix) must hold"]}], "side_effects": []}, "files": {"text.py": "\"\"\"String / text utilities.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict, List, Optional\n\n\ndef normalize_whitespace(s: str) -> str:\n    \"\"\"Collapse runs of whitespace to single spaces and strip ends.\"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"expected str\")\n    out: List[str] = []\n    in_ws = False\n    for ch in s:\n        if ch.isspace():\n            if not in_ws:\n                out.append(\" \")\n                in_ws = True\n        else:\n            out.append(ch)\n            in_ws = False\n    return \"\".join(out).strip()\n\n\ndef word_counts(text: str) -> Dict[str, int]:\n    \"\"\"Return a dict of word\u2192count. Words are case-folded.\n\n    Requires text to be a non-None string.\n    \"\"\"\n    if text is None:\n        raise ValueError(\"text must not be None\")\n    counts: Dict[str, int] = {}\n    for w in text.lower().split():\n        counts[w] = counts.get(w, 0) + 1\n    return counts\n\n\ndef truncate(text: str, max_len: int, suffix: str = \"\u2026\") -> str:\n    \"\"\"Truncate `text` to at most `max_len` characters; append `suffix` if cut.\n\n    Asserts max_len >= len(suffix).\n    \"\"\"\n    assert max_len >= len(suffix), \"max_len must be >= len(suffix)\"\n    if len(text) <= max_len:\n        return text\n    return text[: max_len - len(suffix)] + suffix\n\n\ndef find_first(needle: str, haystacks: List[str]) -> Optional[int]:\n    \"\"\"Return index of first haystack containing needle; None if not found.\n\n    Empty needle matches the first haystack.\n    \"\"\"\n    for i, h in enumerate(haystacks):\n        if needle in h:\n            return i\n    return None\n", "audit.py": "\"\"\"Wraps text.* to produce a small audit report on a corpus.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import List\n\nfrom text import normalize_whitespace, word_counts, truncate, find_first\n\n\ndef audit_lines(lines: List[str], probe: str = \"ERROR\") -> dict:\n    cleaned = [normalize_whitespace(l) for l in lines]\n    counts = word_counts(\" \".join(cleaned))\n    idx = find_first(probe, cleaned)\n    digest = truncate(\" | \".join(cleaned), 120)\n    return {\"counts\": counts, \"first_match\": idx, \"digest\": digest}\n"}}
{"task_id": "task_0011_text_find_first", "target_file": "text.py", "target_symbol": "find_first", "gold": {"inputs": [{"name": "needle", "type": "str", "source_line": 49}, {"name": "haystacks", "type": "List[str]", "source_line": 49}], "returns": [{"type": "Optional[int]", "source_line": 49}], "mutates": [], "calls": [], "called_by": [{"file": "audit.py", "line": 13}], "invariants": [], "side_effects": []}, "files": {"text.py": "\"\"\"String / text utilities.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict, List, Optional\n\n\ndef normalize_whitespace(s: str) -> str:\n    \"\"\"Collapse runs of whitespace to single spaces and strip ends.\"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"expected str\")\n    out: List[str] = []\n    in_ws = False\n    for ch in s:\n        if ch.isspace():\n            if not in_ws:\n                out.append(\" \")\n                in_ws = True\n        else:\n            out.append(ch)\n            in_ws = False\n    return \"\".join(out).strip()\n\n\ndef word_counts(text: str) -> Dict[str, int]:\n    \"\"\"Return a dict of word\u2192count. Words are case-folded.\n\n    Requires text to be a non-None string.\n    \"\"\"\n    if text is None:\n        raise ValueError(\"text must not be None\")\n    counts: Dict[str, int] = {}\n    for w in text.lower().split():\n        counts[w] = counts.get(w, 0) + 1\n    return counts\n\n\ndef truncate(text: str, max_len: int, suffix: str = \"\u2026\") -> str:\n    \"\"\"Truncate `text` to at most `max_len` characters; append `suffix` if cut.\n\n    Asserts max_len >= len(suffix).\n    \"\"\"\n    assert max_len >= len(suffix), \"max_len must be >= len(suffix)\"\n    if len(text) <= max_len:\n        return text\n    return text[: max_len - len(suffix)] + suffix\n\n\ndef find_first(needle: str, haystacks: List[str]) -> Optional[int]:\n    \"\"\"Return index of first haystack containing needle; None if not found.\n\n    Empty needle matches the first haystack.\n    \"\"\"\n    for i, h in enumerate(haystacks):\n        if needle in h:\n            return i\n    return None\n", "audit.py": "\"\"\"Wraps text.* to produce a small audit report on a corpus.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import List\n\nfrom text import normalize_whitespace, word_counts, truncate, find_first\n\n\ndef audit_lines(lines: List[str], probe: str = \"ERROR\") -> dict:\n    cleaned = [normalize_whitespace(l) for l in lines]\n    counts = word_counts(\" \".join(cleaned))\n    idx = find_first(probe, cleaned)\n    digest = truncate(\" | \".join(cleaned), 120)\n    return {\"counts\": counts, \"first_match\": idx, \"digest\": digest}\n"}}
