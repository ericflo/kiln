{"ts": "2026-05-18T21:11:00Z", "iter": 0, "severity": "moderate", "category": "pool-isolation", "summary": "kiln-pool pods are shared mutable state \u2014 once leased, any pre-existing scripts on the pod (other lab bootstraps, autonomous loops) can also acquire/restart kiln serve, causing mid-eval HTTP 500 / RemoteDisconnected for the new lessee. On this pod, the pi-compaction-grpo v062 autonomous loop's startup script restarted kiln serve mid-iter0-eval and 4/23 tasks errored. There's no isolation mechanism between concurrent agents sharing a pod's filesystem + kiln-serve singleton.", "repro_hint": "Acquire any kiln-pool H100; ps -ef | grep -i pi-compaction may reveal another lab's bootstrap script still running. The kiln-serve binary itself is a singleton on port 8420 \u2014 only one process can bind it."}
{"ts": "2026-05-18T22:10:00Z", "iter": 1, "severity": "major", "category": "training-too-slow-on-long-context-grpo", "summary": "cuda_grpo_ablation on pi-compaction-sized rollouts is prohibitively slow even with KILN_GRAD_CHECKPOINT_SEGMENTS=8: 4 strong-signal groups \u00d7 ~30K tokens each = ~120K total token budget; full training step did not produce a single progress= line in 6 minutes on H100 SXM. Pi-doctest training (~150K total token budget over 11 groups) completed in 2-3 min on the same hardware. The 4x slowdown isn't strictly token-count proportional \u2014 long-sequence GDN backward pass dominates wall time and scales superlinearly.", "repro_hint": "Build a GrpoGroup with completions whose combined assistant-text + source-text is ~15-30K tokens each; submit to cuda_grpo_ablation phase1 with rank=16 lr=1e-5 KILN_GRAD_CHECKPOINT_SEGMENTS=8. Pi-doctest training data has ~3K tokens per group total, which is what kiln-train is tuned for."}
{"ts": "2026-05-19T00:00:00Z", "iter": 2, "severity": "major", "category": "long-context-policy-forward-bottleneck", "summary": "Shared-prefix reference forward optimization (PR #1055, landed on main) reduces ref-forward work ~n_completions×, but the GRPO policy forward+backward path remains the dominant cost at long context (>7K tokens). Iter 2's strong-signal groups had ~9-11K-token sequences. With KILN_GRAD_CHECKPOINT_SEGMENTS=32, KILN_STREAMING_TILE_TOKENS=512, KILN_SPOOL_CHECKPOINT_BOUNDARIES=1: VRAM fits (12 GB used) but no progress= line in 10 min — CPU-bound at 99% on kernel-launch overhead from many small tiles. With defaults (segments=8, tile=2048): OOM at streaming GDN tile [4096, 6144) of 7719 (tile_size=2048) — DriverError(CUDA_ERROR_OUT_OF_MEMORY) at layer 30. The streaming GDN per-tile transient + checkpoint recompute is the next memory-and-launch-overhead bottleneck.", "repro_hint": "On H100 80GB CUDA: cuda_grpo_ablation --mode phase1 --rank 16 on GRPO groups with ~10K-token total sequences. Default env: OOMs at streaming GDN tile 2. Aggressive grad-ckpt env: CPU-bound on kernel launches, no progress in 10 min. The fix likely needs (a) larger-but-tile-internally-checkpointed GDN tiles, or (b) fused multi-layer chunked GDN+attn kernels that amortize launch cost across layers."}
