{
  "base_model_path": "/workspace/qwen3.5-4b",
  "kiln_url": "http://localhost:8420",
  "pi_bin": "/usr/bin/pi",
  "pi_model_id": "qwen-3.5-4b-kiln",
  "sandbox_root": "/tmp/pi-doctest-rollouts",
  "max_iters": 3,
  "rollout": {
    "num_generations_train": 4,
    "num_generations_eval": 1,
    "max_wall_clock_s": 120,
    "max_tokens_per_turn": 1024,
    "temperature": 0.8,
    "top_p": 0.95,
    "parallel": 1
  },
  "training_phase1_defaults": {
    "advantage_mode": "dr_grpo",
    "loss_aggregation": "token_level",
    "kl_estimator": "k1",
    "kl_coeff": 0.1,
    "clip_epsilon": 0.20,
    "clip_eps_high": null,
    "dynamic_sampling": true,
    "is_level": "token",
    "reference_policy": "base_per_step",
    "entropy_aware_kl_quantile": null,
    "lr": 1e-5,
    "rank": 16,
    "alpha": 32,
    "seed": 3141592653
  },
  "notes_v0": [
    "Single-turn rollout by cap design (one assistant turn per task).",
    "Multi-turn rollouts are now supported via kiln-train::trajectory_mask + the ECHO branch; this cap keeps the single-turn shape because that's the pi-doctest task spec, not because the trainer lacks support."
  ]
}
