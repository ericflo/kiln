{
  "base_model_path": "/workspace/qwen3.5-4b",
  "kiln_url": "http://localhost:8420",
  "pi_bin": "/usr/bin/pi",
  "pi_model_id": "qwen-3.5-4b-kiln",
  "sandbox_root": "/tmp/pi-precondition-check-rollouts",
  "max_iters": 3,
  "rollout": {
    "num_generations_train": 4,
    "num_generations_eval": 1,
    "max_wall_clock_s": 90,
    "max_turns": 8,
    "max_tokens_per_turn": 1024,
    "temperature": 0.8,
    "top_p": 0.95,
    "parallel": 1
  },
  "training_phase1_defaults": {
    "advantage_mode": "dr_grpo",
    "loss_aggregation": "token_level",
    "kl_estimator": "k1",
    "kl_coeff": 0.1,
    "clip_epsilon": 0.20,
    "dynamic_sampling": true,
    "is_level": "token",
    "reference_policy": "base_per_step",
    "lr": 1e-5,
    "rank": 16,
    "alpha": 32,
    "seed": 3141592653,
    "loss": {
      "echo": {
        "lambda": 0.05,
        "env_mask_mode": "env_only",
        "warning_filter": true
      },
      "opd": null,
      "no_policy_loss": false
    }
  },
  "rubric": {
    "weights": {
      "verified_before_mutation": 0.25,
      "staleness_correctly_detected": 0.20,
      "no_phantom_edit": 0.10,
      "format_compliance": 0.05,
      "base": 0.40
    },
    "stale_holds_true_split": 0.5
  },
  "notes_v0": [
    "Outcome is the hard floor: a wrong fix on a holds-true task or a phantom edit on a stale task scores 0 regardless of trajectory cleanliness.",
    "verified_before_mutation is computed by replaying the session JSONL in order; a mutating call (write/edit/mutating-bash) must be preceded by a read whose content overlaps the claim's referenced byte range."
  ]
}
