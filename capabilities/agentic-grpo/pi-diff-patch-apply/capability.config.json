{
  "base_model_path": "/workspace/qwen3.5-4b",
  "kiln_url": "http://localhost:8420",
  "pi_bin": "/usr/bin/pi",
  "pi_model_id": "qwen-3.5-4b-kiln",
  "sandbox_root": "/tmp/pi-diff-patch-apply-rollouts",
  "max_iters": 3,
  "rollout": {
    "num_generations_train": 4,
    "num_generations_eval": 1,
    "max_wall_clock_s": 240,
    "max_turns": 12,
    "max_tokens_per_turn": 2048,
    "temperature": 0.8,
    "top_p": 0.95,
    "parallel": 1
  },
  "training_phase1_defaults": {
    "advantage_mode": "dr_grpo",
    "loss_aggregation": "token_level",
    "kl_estimator": "k1",
    "kl_coeff": 0.1,
    "clip_epsilon": 0.20,
    "dynamic_sampling": true,
    "is_level": "token",
    "reference_policy": "base_per_step",
    "lr": 1e-5,
    "rank": 16,
    "alpha": 32,
    "seed": 3141592653,
    "loss": {
      "echo": {
        "lambda": 0.05,
        "env_mask_mode": "env_only",
        "warning_filter": true
      },
      "opd": null,
      "no_policy_loss": false
    }
  },
  "rubric": {
    "weights": {
      "minimality": 0.20,
      "no_unrelated_edits": 0.15,
      "repair_efficiency": 0.10,
      "format_compliance": 0.05,
      "base": 0.50
    },
    "minimality_span": 2,
    "repair_attempts_max": 2
  },
  "task_mix": {
    "clean_patch_pct": 0.50,
    "offset_drift_pct": 0.30,
    "incorrect_hunk_pct": 0.20
  },
  "notes_v0": [
    "The oracle re-runs the test suite in a fresh checkout that re-applies only the model's final diff; mutations to test files do not survive.",
    "no_unrelated_edits is computed from the diff of (workspace_before, workspace_after) — the touched file set must be a subset of the patch's touched file set."
  ]
}
