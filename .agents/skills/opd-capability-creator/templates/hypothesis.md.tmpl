# Hypothesis: <slug>

## Family
<one of H1–H8 (see SKILL.md §4).
H1 = epoch curve  H2 = prompt diversity  H3 = samples-per-prompt
H4 = rank capacity  H5 = top-K width  H6 = SFT cold-start
H7 = Stable-OPD mix  H8 = prompt pruning>

## Claim
<ONE sentence. Quantitative where possible.
Example: "Bumping epochs from 6→9 will lift `faithfulness` by ≥3pp.">

## Mechanism
<ONE sentence on *why* you expect this.
Example: "Faithfulness is rising monotonically with epochs (1→3: +2pp,
3→6: +2pp); more passes over teacher rollouts should compound.">

## Configuration
- prompts: <reuse prior set / new set / pruned subset>
- rank: <n>  alpha: <n>  lr: <f>
- epochs: <n>  samples_per_prompt: <n>  max_tokens: <n>  top_k: <n>
- env: KILN_STREAMING_PREFILL=1

## What's held constant from previous iteration
<List what is being kept the same so this is a single-knob change.
If you're varying more than one knob, STOP and split into two iters.>

## Falsification plan
<Concrete, ahead of time:
"If `faithfulness` Δ < +1pp, the H1 curve has saturated; pivot to H2.
If `faithfulness` Δ ≥ +1pp but composite Δ < 0, a sub-score regressed;
inspect responses and pivot to H7 (Stable-OPD mix) if regression severity major.">

## Inspection plan
<Which non-eval prompts will you sample responses on after training?
List 2–3 (do NOT use eval-set prompts).>

---

## Verdict
<Filled in AFTER eval. Required before next iteration can start.

Format: ✓ confirmed / ✗ falsified / ? inconclusive — anchored to numbers.

Examples of acceptable verdicts:

✓ Confirmed — faithfulness +3.1pp (predicted ≥3pp). Composite +1.4pp;
  headroom utilization rose 32%→47%.

✗ Falsified — faithfulness Δ = +0.4pp, well below the ≥3pp threshold.
  H1 has saturated at epoch 9; pivoting to H2 next.

? Inconclusive — composite moved +1.1pp but faithfulness was flat. Lift
  came from is_substantive, which we weren't targeting. Likely a confound;
  re-run with more iters of the same config to check.

UNACCEPTABLE (will fail the gate):
- "looks good", "decent results", "see entry"
- a verdict that doesn't reference actual sub-score numbers
- a verdict that doesn't decide what to do next>

Verdict:
