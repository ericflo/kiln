# Archived ROCm plan records

- `ROCM_PLAN.md` — the original "First-Class Parity Plan" for adding a
  ROCm/HIP backend. The work it describes is **fully landed**: `kiln-hip`,
  `kiln-rocblas`, and the rest of the ROCm backend are part of the workspace,
  and ROCm qualification lives under `qualification/receipts/rocm/` (see
  `docs/policies/ci-policy.md`). The doc is kept as a design-history record only; its
  future-tense plan and its "verified facts" snapshot of the tree predate the
  implementation and no longer describe the codebase.
