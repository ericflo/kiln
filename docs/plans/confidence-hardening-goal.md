# Kiln Confidence Hardening Goal

**Status:** Temporary working document. Keep it current while executing the
goal, then replace it with a short permanent support/qualification document
when every required item is complete.

**Created:** 2026-07-09

**Starting commit:** `961facd6d9d1452e0336f3bb8d9731b2a74938fd`

## Goal

Make Kiln a small, explainable, empirically qualified single-GPU product whose
correctness, latency, training, and evaluation claims are backed by runtime
evidence on the machines that actually matter.

This goal is complete only when every required checkbox in this document is
checked, every acceptance gate has a compact checked-in receipt, the final
receipts cover one common source tree, and the public documentation says no
more than the evidence supports.

The priority order is:

1. Prevent corruption and unexplained stalls.
2. Correct training and evaluation semantics.
3. Establish repeatable local hardware qualification.
4. Measure and improve performance against honest external baselines.
5. Reduce configuration, scheduler, module, test, and artifact complexity.
6. Finish product/API integration and documentation.

Do not add unrelated features while this goal is active.

## Operating Model

GitHub Actions is not hardware evidence. Expensive backend builds and GPU jobs
must not run automatically on every push. Automatic CI should be a cheap safety
net for formatting, dependency policy, CPU tests, UI smoke tests, and other
fast platform-independent checks. Backend qualification happens locally on
real hardware.

Platform start order:

1. ROCm on the current Strix Halo machine (`gfx1151`).
2. Vulkan on the same Strix Halo machine.
3. CUDA on the RTX 4090 Laptop GPU with 16 GB VRAM.
4. CUDA on the desktop RTX 4090 with 24 GB VRAM.
5. Metal on the M1 MacBook Air.
6. Final cross-platform rerun against one common source-tree hash.

A platform handoff starts after the preceding platform has a checked-in,
source-bound correctness and mixed-load baseline; it does not wait for every
cleanup item or the final long soak. The 24-hour/8-hour soaks below remain
release gates and are repeated against the final common source tree.

The native Kiln trainer is a bounded single-GPU online LoRA trainer. It is not
intended to replace distributed TRL/Transformers training. HF/TRL must become a
first-class interoperability route and the numerical oracle for the native
path.

## Execution Rules

- [x] Read this entire document before changing code.
- [x] Update checkboxes and the evidence log in the same commit as the work
  they describe.
- [x] Work in the order written unless a later item is required to unblock an
  earlier acceptance gate. Record any reordering in the evidence log.
- [x] Make small, reviewable commits. A backend phase is expected to contain
  many commits and pushes, not one platform-sized commit. Do not accumulate a
  multi-day or multi-feature working tree.
- [x] The goal runner has standing authorization to commit and push completed
  work directly to `origin/main` throughout this goal.
- [x] Commit and push at every reasonable green checkpoint, including each
  coherent bug fix, regression test, instrumentation slice, qualification-tool
  addition, benchmark-harness improvement, configuration migration,
  documentation correction, and compact hardware receipt. Do not wait for the
  surrounding phase or machine to be complete.
- [x] When hardware exposes a defect, normally push at least three checkpoints:
  the focused reproducer/test, the fix with focused validation, and the passing
  qualification receipt. Combine them only when they cannot be made independently
  correct and green.
- [x] Commit and push before starting a long benchmark/soak, after recording its
  result, before a risky architectural change, before changing task area, and
  before handing work to another machine.
- [x] Update relevant checkboxes and the evidence log at each checkpoint. Do not
  create artificial WIP commits that knowingly leave `main` broken when a
  coherent green checkpoint is available.
- [x] Never amend or force-push a published commit. Before starting on another
  machine, fetch and fast-forward to `origin/main` and confirm a clean tree.
- [x] Do not revert unrelated user changes. Stop and document a real conflict
  rather than using destructive Git commands.
- [x] A compile-only check, mock, source-string assertion, skipped test, or
  external CI badge does not count as runtime backend evidence.
- [x] Hardware tests must fail, not silently skip, when run in qualification
  mode and the requested device/backend is unavailable.
- [x] Do not weaken a tolerance, delete a failing test, ignore an advisory, or
  relabel a feature experimental merely to make a gate pass. Scope can be
  narrowed only when the support matrix and user-facing docs are updated in
  the same commit.
- [x] Keep raw logs, traces, and profiles out of Git. Check in compact receipts,
  summaries, schemas, and hashes only.
- [x] Do not rewrite repository history as part of this goal. Any history purge
  requires a separate explicit decision because it disrupts every clone.

## What Counts As Evidence

Create one local qualification entry point and one compact receipt schema.
Prefer a Rust or structured Python implementation over shell text parsing.

Each receipt must contain at least:

- Receipt schema version.
- `source_tree_hash`, computed over source, manifests, lockfiles, shaders,
  kernels, scripts, and runtime configuration, excluding qualification receipts
  and other generated evidence.
- Git commit and whether the worktree was clean.
- Backend, device name, device architecture, VRAM/unified memory, OS, kernel,
  driver, runtime, compiler, and relevant library versions.
- Model ID/path plus base-weight shard hashes, tokenizer hash, template hash,
  and model-config hash.
- Exact command, typed effective runtime configuration, seed, workload ID, and
  workload content hash.
- Start/end times and duration.
- Per-test result, skip count, failure count, and tolerance used.
- TTFT, end-to-end latency, p50/p99/p99.9 inter-token latency, throughput,
  SLO-goodput, peak memory, and unexplained-outlier count where relevant.
- Hashes of raw local logs/profiles without checking those raw files into Git.
- Final verdict and any explicitly unsupported scenarios.

Receipt rules:

- [x] Add a schema and validator for qualification receipts.
- [x] Add a deterministic `source_tree_hash` command and tests proving receipt
  files do not change it while source changes do.
- [x] Add a local qualification runner that writes raw logs under an ignored
  directory and emits a compact receipt.
- [x] Add a receipt comparison command that rejects incompatible source trees,
  models, workloads, and environments rather than comparing unlike runs.
- [x] Add a checked-in workload manifest for correctness, mixed-load serving,
  slow-consumer, cancellation, long-prefill, memory-pressure, and training
  cases. Do not check in generated model output or large traces.
- [x] Add documentation for running, validating, and committing a receipt on a
  new machine.

Suggested locations:

- `scripts/qualification/` for runners and validators.
- `qualification/schema/` for schemas.
- `qualification/workloads/` for small deterministic workload manifests.
- `qualification/receipts/<backend>/<device>/` for compact receipts.
- `benchmarks/receipts/<backend>/<device>/` for detailed serving-benchmark
  receipts validated by their owning driver.
- `.qualification/` for ignored raw logs, traces, profiles, and temporary data.

## Phase 0: Stop Paying For Weak Signals

### 0.1 Automatic CI cost and scope

- [x] Inventory every automatic workflow, trigger, hosted-runner platform,
  average duration, and what unique defect it can actually catch.
- [x] Remove or change to `workflow_dispatch` automatic CUDA, ROCm, Vulkan,
  Metal, release-matrix, and performance jobs that consume substantial hosted
  minutes without executing on the target hardware.
- [x] Remove the broken scheduled performance workflow or convert it into a
  local qualification command. Do not retain a permanently red nightly badge.
- [x] Keep one inexpensive automatic path for formatting, dependency policy,
  CPU/unit tests, UI smoke, and schema/receipt validation.
- [x] Ensure release packaging can be invoked deliberately after local hardware
  qualification rather than running a full matrix on every ordinary push.
- [x] Record before/after automatic job counts and expected hosted minutes in a
  small checked-in summary.
- [x] Update contributor docs so nobody treats automatic CI as backend
  qualification.

**Acceptance:** An ordinary push starts only the agreed cheap workflows.
Backend and performance workflows require an explicit human invocation. The
local receipt validator runs in cheap CI.

### 0.2 Baseline this machine

- [x] Produce a Strix Halo ROCm environment receipt at the starting source tree.
- [x] Produce a Strix Halo Vulkan environment receipt at the starting source
  tree.
- [x] Capture a short mixed-load ROCm baseline with all current defaults.
- [x] Capture controlled A/B baselines with `KILN_KV_AUTOSCALE=0`, with
  `KILN_ROCM_GRAPHS=0`, and with both disabled.
- [x] Attribute observed gaps to trace events where current instrumentation
  permits; mark the rest unexplained rather than guessing.

## Phase 1: Serving Safety And Predictable Latency

### 1.1 Stable serving profile

- [x] Add a typed, documented stable serving profile and make it the default
  for supported ROCm operation.
- [x] In the stable profile, prohibit physical KV resize, pool trim, live graph
  capture, and training GPU ownership changes while requests are active.
- [x] Add a real off switch for memory-governor automatic reclaim. Changing the
  probe interval is not an off switch.
- [x] Keep logical admission/eviction available without moving live allocation
  pointers.
- [x] Require an explicit experimental or maintenance profile for dynamic
  physical memory operations and concurrent training.
- [x] Expose the effective profile, every resolved setting, and its source
  (default, file, environment, request) in startup diagnostics and health.
- [ ] Make malformed configuration fatal with the variable/field name and
  invalid value. Do not fail open.
  - Progress: the authoritative TOML loader now denies unknown fields in every
    public section, and every environment override owned by `KilnConfig` and
    `RequestLogConfig` rejects malformed/non-Unicode input with the variable
    and raw value. Semantic validation now rejects empty identifiers/paths,
    zero capacities that runtime code would clamp, non-finite memory values,
    invalid logging filters/formats, invalid webhook URLs, inconsistent queue
    bounds, and invalid request-log limits with the field and value. A real CLI
    process rejects `KILN_PORT=definitely-invalid` before model initialization.
    The repo-wide gate remains open until the actor/model/training tuning
    variables still read outside this loader are centralized in Phase 8.1.

**Acceptance:** A stable-profile mixed-load trace contains no physical KV
resize, pool trim, live graph capture, training write lock, or unexplained
device-wide synchronization while a request is active.

### 1.2 Graph-safe, transactional KV resize

- [x] Add a monotonically increasing KV-pool generation or stable allocation
  identity.
- [x] Include pool generation/identity in graph replay validation for every
  backend that captures paged-KV pointers.
- [x] Destroy/invalidate affected graphs before any old pool allocation is
  released.
- [x] Permit physical resize only at an actor quiescence barrier with no live
  kernel and no active request referencing the pool.
- [x] Make grow and shrink failure-atomic. Commit BlockManager state only after
  every allocation and copy succeeds.
- [x] Add fault injection at every allocation, copy, synchronization, and layer
  commit boundary. Prove rollback leaves all layers and logical capacity
  consistent.
- [x] Add runtime tests for eager and captured decode before/after grow and
  shrink, including a request active when resize is requested.
  - The explicit gfx1151 qualification alternates eager-only and native HIP
    graph runners over the same physical cache before shrink, after shrink,
    and after grow. Both resize commands are issued only after a live graph has
    captured and replayed; a strong actor snapshot proves one active decode,
    and another token must arrive before the resize future can complete. The
    grown-tail phase reserves every surviving low block so eager and captured
    decode both allocate, write, and attend through newly grown blocks. Six
    consecutive runs produced exact 48-token parity for all six requests,
    generations 0 -> 1 -> 2, three captures, 135 replays, and zero failures.
- [x] Verify pool-generation mismatch refuses replay instead of launching.
  - The real ROCm qualification first captures and replays a valid graph, then
    poisons only that graph's retained generation while keeping its physical
    allocation alive. The normal decode entry records one replay failure,
    returns the exact same-cache eager result, clears and disables the graph
    runner, and leaves a unit-build probe at the `hipGraphLaunch` call
    unchanged. Six consecutive runs observed attempts 1 -> 2, failures 0 -> 1,
    and native launches 1 -> 1.

**Acceptance:** No stale graph can replay after resize; injected failures leave
the cache usable; ROCm runtime tests exercise the real device and pass.

### 1.3 Memory governor and synchronization

- [x] Do not call `hipDeviceSynchronize` from an uncoordinated periodic monitor.
- [x] Query actual pool reserved/used/free bytes before considering trim.
- [x] Report actual reclaimed bytes and suppress repeated zero-yield actions.
- [x] Add hysteresis, cooldown, and exponential backoff to any remaining
  automatic pressure response.
- [ ] Attribute each synchronization, allocation, trim, and resize with reason,
  bytes, wait time, and duration.
  - Progress: paged-KV startup/resize allocation, transactional physical
    resize, ROCm allocator trim, startup/training reclaim, and every resize
    synchronization now share bounded `gpu_memory_operation` fields. ROCm graph
    capture establishment and failure recovery are covered as well; the gate
    remains open for the CUDA graph synchronization inventory on its machine.
  - Progress: ROCm execution now classifies every host wait through one fixed
    23-reason enum and reports device/stream attempts, skipped waits, and total
    wait duration by reason. `capture_rollback` is distinct from quarantined
    `error_recovery`: recoverable capture settlement keeps the gate open only
    after an explicit rollback wait, while uncertain settlement publishes the
    restart-required quarantine. Stream-ordered mode removes proven
    same-stream waits while explicit external-yield, graph,
    allocation-lifetime, reclaim, and error-recovery boundaries stay
    observable. The qualification harness independently gates failures, 250 ms
    stalls, and 2 second pauses. This source/static checkpoint still requires
    the Strix Halo legacy-versus-stream A/B before it can close any hardware
    latency gate.
- [x] Attribute backend external-yield synchronization with a bounded boundary
  label, call/failure/slow counts, and total/max duration in health, debug
  state, Prometheus, and the 100 ms slow-sync log.
- [x] Add a pressure test where free memory stays below 10 percent without
  producing a two-second periodic inference stall.

### 1.4 Actor scheduling and backpressure

- [x] Move client delivery/backpressure outside the compute actor. A slow or
  suspended client must not sleep all decode rows.
- [x] Add deterministic slow-consumer and closed-socket tests.
- [x] Prevent exact non-block-aligned prefix-cache hits from sharing a mutable
  final KV block with decode; prove cached pages remain immutable across reuse.
- [x] Make prefix-cache lookup visibility and accounting failure-atomic: pin
  the source entry before snapshot copies, leave hit counters and LRU unchanged
  on failure, settle the provisional lease while the GPU read permit is held,
  and quarantine rather than recycle partial snapshot destinations.
- [x] Rank exact prefix-cache hits by next-token compatibility before pinning an
  entry, while allowing a shorter compatible strict-prefix fallback.
- [x] Make prefix-cache registration capacity-correct and failure-atomic across
  overlapping blocks, LRU eviction, pinned entries, and invalid block tables.
- [x] Hold each prefix-cache hit lease until decode cleanup and finish-time
  registrations are complete.
- [x] Make eviction, clear, and adapter purge honor active prefix-cache leases.
- [x] Fence late hit and miss completion with global and cache-local per-adapter
  generations so clear or purge cannot resurrect invalidated registrations.
- [x] Synchronize paged streaming, direct paged/interleaved, and batching-actor
  decode at their external-yield boundaries before publishing progress or
  recycling KV ownership; on these paths, latch an observable,
  restart-required quarantine when completion cannot be proven.
- [x] Route the legacy mutable-`BlockManager` paged streaming entry point
  through one settlement-owning reservation. Every post-allocation error,
  receiver-drop, and success exit must settle before recycling pages and retain
  GPU-owned state when settlement cannot be proven.
- [x] Make successful prefix-hit snapshots opaque until an explicit backend
  settlement, and make shared KV reservations retain pages by default unless a
  settlement boundary explicitly releases them.
- [x] Make paged stream workers own cleanup through the terminal event: run the
  sole finalizer before `Done` or `Error`, and retain GPU state plus the movable
  read permit when decode completion cannot be proven.
- [x] Carry a movable GPU read owner from threaded prefill through decode and
  cleanup, and use the same Tokio writer exclusion for portable and CUDA-native
  SFT optimizer steps.
- [x] Emit structured `generation_error` SSE payloads before `[DONE]` when a
  threaded prefill, its blocking task, or an explicit decode worker fails.
- [x] Prevent probe-only streaming completion from failing or overwriting a
  concurrently claimed deterministic-cache owner.
- [x] Make incremental detokenization and unexpected worker-channel termination
  fallible end to end; never turn tokenizer decode failure or a missing
  terminal event into empty text followed by a normal-looking `[DONE]`.
- [x] Propagate cooperative cancellation and the request deadline through
  direct threaded prefill, token delivery, and terminal delivery so timeout and
  disconnect stop the model worker rather than detaching a blocking receiver or
  waiting indefinitely on a full SSE queue.
- [x] Make prompt-logprob token rendering fallible instead of converting a
  tokenizer decode failure into an empty display token.
- [x] Make prompt-logprob numerical rendering fail closed: require the exact
  model vocabulary width, reject non-finite values before JSON serialization,
  and return exactly the requested distinct top-ranked candidates.
- [x] Match vLLM prompt-logprob protocol semantics by including the observed
  prompt token (K or K+1 entries), reporting its full-vocabulary rank, and
  rendering byte-fallback tokens with prompt context while preserving
  shift-stable F32 log-probability precision. Enforce the served-model context
  window and settle or quarantine every scorer-owned accelerator lifetime.
- [x] Make remote-teacher prompt-logprob parsing reject malformed or
  out-of-vocabulary entries instead of dropping them, and validate the
  observed-token/rank contract plus logits-row/target-position alignment before
  caching numeric results.
- [x] Establish an authoritative remote-teacher tokenizer identity handshake
  plus base-model/adapter content revision, then bind accepted/cached logits to
  that identity rather than trusting only the locally configured tokenizer hash.
- [x] Extend the backend quarantine gate to every inference worker, adapter
  mutation, prewarm, training admission, and queued training transition. A
  quarantined backend must reject rather than wait forever on its retained GPU
  read owner.
- [x] Publish one authoritative loaded-adapter tuple containing name and content
  revision atomically with the weight flip. Bind queued requests and all
  deterministic-cache owners to that revision so late completion cannot
  resurrect results invalidated by purge.
- [x] Serialize adapter upload, delete, training rewrite, gate demotion, and
  loaded-weight transitions under the same revision/barrier contract. Reject
  deletion or rewrite of physically loaded content unless the barrier swaps it
  away or reloads it first.
- [x] Implement token-budgeted/chunked prefill so a 16K prompt cannot monopolize
  the actor while other rows are decoding.
- [x] Add fair admission tests mixing short decode, 1K prefill, and 16K prefill.
- [x] Ensure adapter loading and training cannot acquire an actor-wide GPU lock
  in the stable profile.
- [x] Define explicit maintenance/drain behavior for operations that require
  exclusive GPU ownership.

### 1.5 ROCm graph behavior

- [x] Replace per-replay device-wide synchronization with stream/event ordering
  where ROCm correctness permits it.
- [x] Pre-capture supported graph buckets during an explicit warmup phase, or
  keep graphs disabled in stable mode until this is reliable.
  - Stable and maintenance profiles already prohibit live graph capture. The
    experimental profile may capture lazily, but now refuses any decode whose
    attention path has sequence-length-dependent shapes and caches a typed eager
    fallback instead of retaining an apparently successful, incorrect graph.
- [x] Do not perform warm-forward plus failed-capture plus eager-forward on a
  live request without exposing that event and latency.
- [x] Add eager-versus-graph logit and token parity across sequence buckets,
  block-table changes, prefix reuse, cancellation, and adapter boundaries.
  - A real gfx1151 test uses the production exact 64-token FA2 geometry, grows
    block tables through the boundary, releases one owner, reuses populated
    prefix pages with a new owner, invalidates at an adapter boundary, and
    requires exact hidden-state, full-logit, and greedy-token parity with an
    independent eager cache.
- [x] Treat graph fallback as a counted event with a bounded reason enum.

### 1.6 Latency observability

- [ ] Measure actor queue, admission, tokenization, prefill, decode, sampling,
  readback, client delivery, GPU-lock wait, graph capture/replay, synchronization,
  resize, trim, adapter, and training phases separately.
  - Batching paths now distinguish actor queue, admission, tokenization, prefill,
    decode, response delivery, handler queue, response-body enqueue, and
    conservative unexplained time. Each batching decode invocation now also
    carries exact shared-GPU-lock acquisition wait and external-yield
    synchronization duration through the owned token envelope; the classifier
    compares those narrower candidates with residual broad decode time without
    summing potentially overlapping phases. Direct streams distinguish
    tokenization, model-producer delivery, bridge/handler queueing,
    response-body enqueue, and otherwise unpartitioned time. Unsupported
    backend subphases remain explicit `null`; this gate stays open for sampling,
    readback, graph capture/replay, resize, trim, adapter, and training
    correlation, plus backend subphase parity on the direct path. Global graph
    counter deltas are deliberately not assigned to requests under concurrent
    inference.
- [x] Associate each emitted token with ready time, delivered time, and any
  blocking phase since the preceding token.
  - Every batching-engine and direct-model token carries producer-ready and
    delivery timestamps plus one bounded dominant blocking candidate. Opted-in
    SSE names the closed source path and exposes the exact ready, producer
    delivery, handler receipt, body enqueue, queue, and blocking values.
- [x] Export bounded-cardinality counters/histograms for TTFT, ITL p50/p99/p99.9,
  unexplained gaps, and each stall reason.
  - Prometheus uses fixed bucket and reason vocabularies, while the bounded
    rolling endpoint reports request-local p50/p99/p99.9/max ITL and the
    adaptive `max(250 ms, 5 * p50)` threshold without request/model labels.
- [x] Show the same attribution in recent-request diagnostics without requiring
  a profiler.
  - Batching and direct-stream requests retain the same closed latency object
    rendered by the recent-request drill, with unsupported phases visibly
    absent rather than replaced with invented zeroes.
- [x] Ensure measurement overhead is benchmarked and bounded.
  - A 20,000-token debug-build regression covers request tracking, Prometheus,
    the rolling ring, and final snapshots with a 100 microsecond/token ceiling;
    retained per-request sample storage is capped at 8,192 entries and 256 KiB.

### 1.7 Strix Halo ROCm qualification

- [x] Run correctness qualification with graphs disabled.
- [x] Run correctness qualification with pre-captured graphs enabled.
  - The clean source-bound `serving-rocm-graph-correctness-v1` run on
    `eacf2350` exercised eight deterministic requests in each mode across four
    prompt buckets. Graph-off recorded zero graph activity. Graph-on warmed
    eight exact FA2 geometries, then completed all 188 expected measurement
    replays with zero measurement captures, fallbacks, graph failures, request
    failures, or active slots at drain. All 192 action tokens and selected-token
    logprobs matched exactly; both modes produced canonical trace
    `sha256:5742aa0ac45e964e4147456025b120ab4598735dd69fc1a6d7bb7111de60ce9a`.
    The receipt passed strict local-artifact and known-commit validation.
- [x] Run mixed prompt lengths and concurrency 1, 8, 16, 32, and 64 where the
  supported profile permits them.
  - Source contract ready, hardware result pending: the required-device
    `serving-rocm-graph-resilience-v1` workload builds one exact binary and
    runs the same 121-request mixed-bucket ladder at concurrency 1, 8, 16, 32,
    and 64 under both 1 GiB and 64 MiB graph-byte budgets. It compares canonical
    streamed semantic deltas exactly, requires a typed tight-budget action,
    reconciles bounded graph memory and lifecycle state, and fails on either
    attributed or unexplained ITL outliers. The checkbox remains open until a
    clean source-bound Strix Halo receipt is checked in.
  - The first clean attempt on `f1654126` failed before compilation because the
    PID-namespaced case could not attach Cargo to the host user manager as a
    scope. The failed receipt is retained. `8a7de073b` keeps the case PID/network
    isolation and moves only the offline build into a private-network transient
    service with the same aggregate memory/no-swap cgroup, control-group
    teardown, and a hard deadline ordered 60 seconds inside the caller timeout.
    Real success, failure-status propagation, environment transfer, and unit
    cleanup probes passed; this is a harness repair, not graph hardware evidence.
  - The next clean attempt on `03861ffb` proved that containment reached Cargo,
    then failed closed on three ROCm-feature compile regressions: one missing
    `Arc` borrow and two unqualified `Device::Rocm` uses. Its receipt is retained.
    `af42edc91` fixes all three, replaces broad service inheritance with the
    config-recorded `closed-source-build-v1` environment policy, and proves the
    exact private-network ROCm release binary links. A first manual link attempt
    also proved the former 300-second service limit invalid by reaching it at
    only 2 GiB peak memory; every ordinary ROCm/Vulkan cold build now has an
    840/900-second service/caller envelope, and workload/case deadlines include
    that allowance. The hardware checkbox is still open pending the matrix.
  - The third clean attempt on `c00c89ff` built the exact release binary in
    250.1 seconds, loaded Qwen3.5-4B on gfx1151, allocated the 8 GiB KV cache,
    captured and launched a 32-layer HIP graph during prewarm, and shut down
    cleanly. It then failed closed before the concurrency ladder because the
    trusted debug endpoint omitted the selected `KILN_MEMORY_RECLAIM_MODE`
    attestation key, so the harness could not distinguish an absent environment
    input from unreported state. The failed receipt is retained. The endpoint
    now reports an explicit `{present:false,value:null}` state and a Rust route
    test owns that invariant; a new clean pushed-source run is still required.
  - The fourth clean attempt on `6a7587d3` cleared build, startup, attestation,
    warmup, and the headroom concurrency 1/8/16/32 waves, then failed the
    unchanged 120-second per-request deadline at concurrency 64: 41 requests
    completed and 23 timed out before their first token. Completions continued
    through 112 seconds, the longest logged decode phase was 142.7 ms, and no
    reclaim, resize, device fault, graph failure, or unexplained synchronization
    event occurred. This rules out a device freeze in this trace and exposes
    bounded 12-active-request queue/service capacity instead. The failed receipt
    is retained; the zero-filled failure metrics also show that the harness must
    preserve partial wave evidence before the next acceptance run.
  - The following source checkpoint replaces zero-filled failures with exact
    case, arm-start, arm-completion, per-arm attempted/completed, per-wave
    attempted/completed/failed, highest-completed-concurrency, graph, memory,
    and pause evidence. The 300-second client and 360-second server ceilings are
    explicitly correctness containment rather than performance acceptance;
    measured TTFT/E2E remains visible and the 120-second counterexample remains
    the performance record. The next run can now fail without discarding the
    successful prefix of the matrix.
  - The clean `da9fb3735` matrix passed both arms: 121/121 requests at each
    budget, exact streamed output, zero request/case failures, zero attributed
    or unexplained ITL outliers, and zero graph failures. Headroom captured nine
    graphs and replayed 757 times with a 98,907,888-byte peak. The 64 MiB arm
    peaked at 65,787,688 retained bytes, captured five graphs, replayed 375
    times, and made 60 pre-capture skips plus 414 typed byte-budget fallbacks.
    P99 TTFT rose from 346.9 ms at concurrency 1 to 213.7 seconds at external
    concurrency 64; this proves queued correctness and clean lifecycle under the
    declared 8-decode/4-staging/12-active envelope, not a native batch width of
    64 or competitive large-batch performance. That deficit remains owned by
    the cross-engine performance phase.
- [x] Exercise prefix reuse, cancellation, slow consumers, adapter load/unload,
  memory pressure, and maintenance-mode resize.
  - The existing mixed-load, pressure, correctness, and soak gates cover prefix,
    cancellation, slow-consumer, pressure, and graph invalidation behavior in
    separate receipts. `serving-rocm-graph-failure-containment-v1` makes the
    three real-device shape fallback, prefix/cancellation/adapter lifecycle, and
    poisoned-generation cases source-bound and non-skippable. The new
    `serving-rocm-public-mutation-lifecycle-v1` gate builds one binary for a
    content-attested public load/infer/unload/base-restore arm and a separate
    typed maintenance forced-resize/inference-rejection arm. The clean
    `a001c39fe` Strix Halo receipt completed both arms with exact adapter/base
    identity, three graph invalidations, a 4,096-to-1 block shrink, one
    fail-closed maintenance rejection, and zero request, device, output,
    shutdown, or residue failures. Together with the existing
    prefix/cancellation/slow-consumer/pressure receipts, this closes the
    lifecycle corpus for this phase.
- [x] Run a 30-minute development soak after each material serving change.
  - The source-bound `serving-rocm-development-soak-v1` gate now holds one
    graph-on, fixed-capacity server process under mixed bucket/concurrency waves
    and periodic cancellation. It fills the bounded prefix cache before the
    measurement baseline, then fails on device faults, graph/sync failures,
    unexplained ITL outliers, unaccounted blocks, cache-residency drift, dirty
    teardown, or more than 512 MiB of post-warmup GPU/RSS growth. Attributed
    gaps remain visible in the receipt.
  - The first clean run on `6da0aea9` failed after 148.5 measured seconds when
    RSS growth reached 537.45 MiB, 25.45 MiB above the unchanged limit. Its 116
    requests, three cancellations, 1,085 graph replays, and all correctness,
    fault, fallback, synchronization, attribution, ownership, and teardown
    gates otherwise passed. The failed receipt is retained; allocator/batch
    high-watermark establishment must move into warmup before rerunning. The
    follow-up harness now requires two consecutive stable memory cycles after
    at least four full concurrency cycles, failing after eight; the measured
    512 MiB bound is unchanged. A direct development probe then failed that
    gate: cycles five, six, and eight still added 146,829,312, 126,554,112,
    and 146,071,552 RSS bytes respectively. The graph runner captured and
    released request-owned graphs throughout the repeated workload, so graph
    lifecycle churn was the bounded runtime hypothesis; no later baseline may
    hide continuing growth. Commit `53a582aa` replaced request-owned native
    graphs with reusable persistent slots and joined the decode worker before
    accelerator teardown. Its clean pushed-source receipt ran 1,811 measured
    seconds and passed with 35.7 MiB RSS growth, 90.9 MiB GPU growth, 8,734
    replays, 551 measured slot reuses, zero unexplained gaps or typed failures,
    and clean unforced shutdown.
- [ ] Run a final 24-hour mixed-load soak for the ROCm phase.
- [ ] Require every ITL gap above `max(250 ms, 5 * rolling p50 ITL)` to have a
  bounded reason code. The final unexplained count must be zero.
- [ ] Require zero device faults, non-finite logits, token mismatches, leaked
  blocks, cache-generation mismatches, or unexpected process-memory growth.
- [ ] Check in the compact ROCm receipts and push them with this phase checked.

## Phase 2: Thinking Budgets And Product Contract

### 2.1 Shared budget model

- [x] Move inherit/unlimited/limit semantics, source attribution, effective
  limits, and outcomes into one shared typed model used by API, CLI, eval,
  batch, desktop, and browser code.
- [x] Remove duplicated `BudgetOverride`, `EvalBudgetOverride`, and CLI-only
  semantic implementations or reduce them to conversions around the shared
  type.
- [x] Define one canonical schema/reference and generate or validate duplicated
  documentation from it.

### 2.2 Configuration and validation

- [x] Make invalid thinking-budget environment values fail startup and
  configuration validation.
- [x] Initialize diagnostics early enough that configuration failures are always
  visible.
- [x] Document and prevalidate conflicts with `</think>` stop sequences and
  insufficient `max_tokens`.
- [x] Use strict integer parsing in browser and desktop surfaces. Do not floor
  decimals or convert malformed values into omission.

### 2.3 Transport and provenance parity

- [x] Preserve final thinking-budget metadata/outcomes when durable request logs
  reassemble SSE streams.
- [x] Add streaming versus non-streaming durable-log parity tests.
- [x] Include effective budget and source provenance in batch responses.
- [x] Make compare mode use the same SSE accumulator and outcome rendering as
  normal chat.
- [x] Add effective limits and outcome fields to recent requests and bounded
  metrics.

### 2.4 User ergonomics

- [x] Give token and time budgets independent Inherit/Unlimited/Limit controls.
  Editing one dimension must not disable the inherited other dimension.
- [x] Fetch and display effective server defaults and preview the effective pair
  before send.
- [x] Make rollout thinking tri-state or reject inert budget flags when thinking
  is explicitly disabled.
- [x] Use one shared default server port (`8420`) in server, CLI, desktop Rust,
  desktop JavaScript, tests, and docs.
- [x] Make desktop settings parsing field-tolerant, versioned, atomic, backed up,
  and visibly erroneous instead of resetting the whole configuration.

**Acceptance:** One conformance suite runs the same budget matrix through chat,
SSE, batch, compare, eval, rollout CLI, request log, recent requests, and
desktop configuration with identical effective semantics and provenance.

## Phase 3: Training Correctness And HF/TRL Interoperability

### 3.1 Correct GRPO policy semantics

- [x] Separate behavior/old-policy log-probabilities from frozen-reference
  log-probabilities in types, storage, loss functions, metrics, and receipts.
- [x] Version the rollout schema with exact token IDs, per-token behavior
  log-probabilities, behavior model/adapter content hash, tokenizer/template
  hash, sampling configuration, seed, and generation backend.
- [x] Emit that exact record from one non-streaming, single-choice real-model
  chat request, including terminal EOS decisions and fail-closed admission for
  paths that cannot provide an exact trace.
- [x] Make `kiln rollout-generate` require, validate, and persist the API's
  provenance record without synthesizing a different completion trajectory;
  prove the emitted JSONL passes strict GRPO ingestion end to end.
- [x] Reject an off-policy rollout that lacks the information needed for the
  configured importance correction. Do not silently substitute the KL
  reference.
- [x] Keep KL-reference selection independent from old-policy selection and
  refresh cadence.
- [x] Add analytic scalar/tensor fixtures where old policy and reference policy
  differ so the historical conflation cannot pass.
- [x] Compare loss, ratios, clipping, KL, gradients, and one optimizer step
  against a pinned TRL/PyTorch oracle.

### 3.2 Exact resumable checkpoints

- [x] Define a versioned training-checkpoint manifest.
- [x] Save and restore adapter parameters, optimizer moments/momentum, scheduler,
  global step, epoch, data cursor/order, all RNG states, reference/EMA state,
  precision policy, and effective configuration.
- [x] Write checkpoints atomically with an incomplete sentinel and checksum
  validation.
- [x] Distinguish resumable checkpoints from exportable PEFT adapter snapshots
  in names, API, UI, and docs.
- [x] Add crash/fault interruption tests.
- [x] Prove uninterrupted versus stop/resume training produces the same loss
  sequence, optimizer state, and adapter hash inside the declared deterministic
  envelope.

### 3.3 Seeds and provenance

- [x] Resolve and persist one effective seed at every public SFT, GRPO, OPD,
  distillation, and eval entry point.
- [x] Record base-weight shard hashes rather than only model ID/config hash.
- [x] Record backend, device, driver/runtime, build/source hash, tokenizer,
  template, precision, kernels, and effective environment/configuration.
- [x] Replace claims of exact replay with either a real replay-to-output command
  or precisely scoped integrity-verification language.
- [x] Add same-environment repeated-run adapter-hash tests.

### 3.4 SFT schema, data policy, and numerics

- [x] Unify training messages with the core inference/eval message schema,
  including tool calls, names, tool-call IDs, and tool responses.
- [x] Establish exact Qwen chat-template token and label goldens against HF for
  plain, thinking, tool-call, tool-response, and multi-turn examples.
- [x] Define assistant-only masking explicitly, including whether role headers
  and terminators are supervised, and match the chosen HF/TRL contract.
- [x] Use one explicit `fail` or `skip` invalid-row policy across inline and
  streamed ingestion. Receipts must contain stable kept/rejected row hashes.
- [x] Add ingestion-equivalence tests proving the same corpus trains the same
  rows through every transport.
- [ ] Add microbatch/gradient-accumulation semantics, scheduler/warmup, gradient
  clipping, and a documented precision/optimizer-state contract if the native
  trainer continues to expose general SFT configuration.
- [x] Otherwise, deliberately narrow the native API to a microtrainer profile
  and route general SFT to HF/TRL. Remove broader claims in the same commit.
  - Progress: native SFT now exposes only the versioned
    `native_online_lora_v1` profile. Its one-conversation/one-update shape,
    constant scheduler, no-accumulation/no-warmup/no-clipping semantics,
    precision and optimizer-state behavior, MTP boundary, receipt fields, and
    exact-resume contract are explicit and fail closed on unknown or invalid
    request fields. The CLI, browser UI, API examples, architecture guide, and
    permanent profile reference use the same name and scope.
  - Completed: the first-class SFT and recorded-GRPO export/API/CLI routes,
    task-aware pinned runner, and resident-validated PEFT import/API/CLI route
    now provide the broader-training handoff. The external production-model
    numerical round trip remains a separate Phase 3.5 gate.
- [x] Compare low-gradient and ordinary N-step AdamW updates with PyTorch,
  including the chosen master-parameter and moment precision.
  - Completed: a source-pinned PyTorch 2.13.0 eager AdamW fixture records every
    parameter and both moments across ten ordinary and ten epsilon-dominated
    updates in F32 and BF16. Portable F32 and real ROCm BF16 production kernels
    check every lane at every step; the BF16 contract has no separate F32
    master and enforces one parameter, four first-moment, and three
    second-moment ULP bounds for the fused-versus-eager rounding difference.
    The same fail-closed workload is ready for the remaining backend machines.

### 3.5 First-class HF/TRL route

- [x] Export Kiln datasets, chat-template configuration, adapters, rollout
  provenance, and eval split manifests into documented HF/TRL-compatible forms.
- [x] Import resulting PEFT adapters with full base/tokenizer/template hash
  validation.
- [x] Provide pinned reference scripts for SFT and GRPO that reproduce the
  oracle fixtures.
- [x] Document native Kiln training as bounded single-GPU online LoRA and
  HF/TRL as the path for distributed, broad-method, and highly configurable
  training.
- [x] Add round-trip tests: Kiln export -> pinned HF/TRL step -> Kiln import ->
  inference/eval.
  - Progress: a strict `kiln.hf-trl-export.v1` /
    `kiln.hf-trl-result.v1` contract now binds the complete base-shard,
    tokenizer, inference-template, native-training-template, TRL-template,
    source-execution, ordered-corpus, complete SFT-ingestion, split,
    rollout-provenance, input-adapter, environment-lock, executed-script,
    package-version, effective-config, and output-adapter identities.
    Self-digests, every referenced regular file, task-specific fields, paths,
    sizes, hashes, symlinks, unknown/duplicate fields, and result-to-export
    linkage fail closed. Typed effective-config values avoid Rust/Python
    float-serialization drift, and custom scripts remain auditable without
    being prohibited.
  - Progress: the SFT library exporter now revalidates the admitted examples,
    writes only the declared artifacts into an fsynced sibling staging tree,
    verifies the exact file set and complete manifest there, and atomically
    publishes an immutable `.kiln-hf` directory without replacing an existing
    target.
  - Progress: the standalone SFT reference runner and environment lock now pin
    the exact Torch/Transformers/TRL/PEFT stack. Before importing the training
    stack it verifies the closed export, every artifact, the admitted rows,
    tokenizer, and local base shards. It refuses package drift, missing
    assistant masks, silent truncation, and ambiguous partial results; it
    preserves the executed script and publishes a self-verifying result
    manifest last. Standard-library tests cover cross-language digests,
    tampering, symlinks, base drift, recovery, and result publication.
  - Progress: the public SFT export API now applies canonical inline,
    server-local JSONL, named-dataset, or active-corrections admission on a
    blocking worker; snapshots an optional split and revision-stable adapter;
    and publishes into a private bounded server registry. List, full-verify
    detail, lock-retaining streamed download, and durable delete routes expose
    the immutable bundle without buffering an archive or permitting
    replacement. Crash residue, aliases, symlinks, missing resident identity,
    oversized inline bodies, registry exhaustion, and malformed requests fail
    closed.
  - Progress: `kiln train hf export-sft` now creates from server-local JSONL or
    a named dataset, streams through a private same-directory staging file,
    refuses redirects and replacement, validates one link-free bounded tar
    root plus the complete pristine manifest, and atomically publishes before
    identity-conditional default server cleanup. `list --json` and `delete`
    expose retained-export lifecycle management. A real loopback test covers
    create, download, verification, no-clobber publication, cleanup, and
    collision preflight.
  - Progress: completed external SFT copies now have an exact closed file-set
    verifier, and a shared atomic writer derives a ten-file `.kiln-hf-import`
    envelope containing both manifests, the executed script, PEFT files, and
    the exact model/tokenizer/template bytes needed by a receiving server.
    Corpus, split, input-adapter, environment-lock, and reference-runner bytes
    are deliberately excluded from the upload envelope. This is contract
    foundation only; it does not by itself install an adapter.
  - Progress: `POST /v1/train/hf/peft/imports/{name}` now streams one bounded
    exact-root envelope into private staging, rejects archive/framing/resource
    ambiguity, validates both manifests and every transported byte, compares
    the complete current served-model/base-shard/model-config/tokenizer/three-
    template identity, and validates every PEFT A/B pair, target, dtype, layer,
    rank, and resident projection shape without a full weight-file heap copy.
    Publication shares adapter mutation/quota admission, uses strict registry
    measurement and a kernel no-replace rename, fsyncs the six-file installed
    adapter, and preserves a self-verifying `kiln.hf-trl-import.v1` receipt.
    The pinned SFT runner now defaults to exactly Kiln-loadable modules instead
    of PEFT `all-linear`. This closes the import-validation item, not the
    external numerical round trip.
  - Progress: `kiln train hf import-peft` now verifies a completed extracted
    bundle before connecting, applies the server's shared resource contract
    before temporary-disk use, derives the exact ten-file corpus-free envelope,
    and streams deterministic USTAR/gzip through a four-chunk bounded channel.
    It predicts the complete import receipt, PEFT content revision, installed
    size, task, script-match flag, and file count from verified local identities,
    then accepts only HTTP 201, JSON, and one matching strong ETag. Structured
    rejection, early collision, non-201 success, response corruption, and
    transport ambiguity have distinct retry guidance; the completed source is
    never modified or removed. One real TCP loopback covers successful install,
    exact receipt validation, collision, source retention, and an ambiguous
    success status. CLI/API names, resource ceilings, envelope paths, and the
    installed file set now share one contract rather than synchronized copies.
  - Progress: GRPO exports now have a deep corpus verifier rather than only a
    provenance-schema label. It streams canonical LF-terminated group JSONL
    under dataset, row, group, and completions-per-group ceilings; rejects
    aliases, whitespace alternatives, unknown/duplicate fields, missing
    provenance, non-finite rewards, and count or ordered-corpus drift; and
    reports exact completion, sampled/forced-token, and maximum-sequence
    counts. Every group is replayed through the production tokenizer/mask path
    and must bind the exported tokenizer, template invocation, served model,
    complete base-shard identity, one uniform behavior policy, and either no
    adapter or the exact exported input-adapter content revision. A
    cross-language digest golden and adversarial self-rehash tests prove that
    changing reward, prompt/payload provenance, model/tokenizer/adapter
    identity, framing, or behavior source cannot be hidden behind valid file
    and manifest hashes.
  - Progress: the synchronous GRPO library writer accepts either in-memory
    groups or already-canonical JSONL without silently normalizing file input.
    It shares the SFT writer's resident-artifact snapshot and no-replace
    publication path, bounds the source, rejects symlinks and path replacement,
    writes and fsyncs private sibling staging, and runs the complete file-set
    and deep production-tokenizer verifier before and after atomic publication.
    Both source forms yield the same manifest identity for the same groups;
    invalid framing, missing provenance, target collision, and later source
    mutation cannot leave or alter a published artifact. This is a library
    checkpoint, not a public route.
  - Progress: the task-aware pinned runner now admits GRPO only after a
    dependency-free streaming revalidation of canonical rows, exact rollout
    identities, recorded log-probabilities, forced thinking-budget closure,
    uniform group width, and a no-drop batch divisor. It replays immutable
    prompt/suffix token IDs into pinned TRL 1.8.0 without resampling, routes
    only sampled tokens into the policy mask, makes recorded behavior the PPO
    old policy, and keeps the initial/base adapter as the independent K3 KL
    reference. SFT and GRPO share result publication and one exact embedded
    script/lock pair; focused tests cover Rust digest parity, tampering,
    framing, heterogeneous groups, source mutation, exact replay tensors, and
    task-correct result identity. A pinned Torch CPU smoke completed one real
    tiny-Llama GRPO optimizer step with nonzero loss and gradient; this is
    runner-plumbing evidence, not production-model numerical agreement.
  - Progress: `POST /v1/train/hf/grpo/exports` now accepts exactly one
    provenance-complete inline group array or server-local canonical JSONL
    path, plus the shared optional split and revision-stable input-adapter
    fields. SFT and GRPO enter one publication function and therefore share
    identity preflight, immutable registry capacity, locking, adapter barrier,
    private staging, no-replace publication, full verification, download,
    conditional deletion, and crash recovery. Real-router tests construct and
    verify both GRPO source forms and prove their common lifecycle. Admission
    rejects missing provenance and heterogeneous group widths as request errors
    before publication; the Rust verifier now enforces the same uniform-width
    contract as the pinned runner.
  - Progress: `kiln train hf export-grpo` now applies the same redirect-free,
    bounded, ETag-bound create/download, strict single-root archive
    verification, no-clobber local publication, and identity-conditional
    cleanup implementation as `export-sft`. It accepts one server-local
    canonical JSONL path plus optional split and matching behavior adapter. A
    real TCP loopback covers GRPO creation, streamed download, local manifest
    verification, publication, server cleanup, and source retention.
  - Completed: the pinned scalar/tensor SFT and GRPO oracles, task-aware
    embedded runner, and exact package lock now feed one committed offline
    production-model workload. Source-bound Strix Halo ROCm and Vulkan runs each
    started and drained the real server twice, generated provenance-complete
    scored GRPO data, exported both tasks, released the GPU, ran one pinned
    HF/TRL/PEFT update per task, imported both results, and passed offline/server
    adapter verification plus hash-bound paired eval. Each strict receipt
    records two imports, two eval pairs, 16 nonzero tensors per adapter, zero
    HTTP errors, stable execution provenance across restart, no unsupported
    cases, and clean shutdown. The Vulkan arm exposed and closed five
    mixed-residency defects. The repaired same-source pair passes all eight
    declared structural comparison metrics, and the SFT adapter bytes match
    exactly. The backend-generated GRPO rollout and adapter bytes differ,
    however, so this route evidence is not numerical rollout/logit parity and
    does not close the broader Phase 6 CPU/HF oracle gate.

**Acceptance:** The oracle ladder covers tokenization/masks, scalar losses,
tiny-model logits/log-probs, one-step gradients/updates, 10-step trajectories,
and exact resume. Cross-backend agreement alone is insufficient.

## Phase 4: Evaluation Integrity

### 4.1 Correct multi-sample aggregation

- [x] Add an explicit suite-level aggregation enum such as pass@k, mean@k, and
  majority@k with precise definitions.
- [x] Reduce multiple completions to one independent per-example statistic
  before accuracy, confidence interval, tag/tool slice, regression, and
  promotion calculations.
- [x] Add known-answer n/k fixtures and property tests.
- [x] Version result schemas and migrate or clearly reject old ambiguous
  results.

### 4.2 Train/eval separation

- [x] Content-address dataset rows and preserve group/session identity.
- [x] Implement deterministic group-aware train/validation/holdout splits with
  a persisted split manifest.
- [x] Reject post-training evaluation whose suite overlaps training rows unless
  explicitly labeled `train-set-eval`.
- [x] Remove every claim that the same training JSONL automatically becomes a
  held-out suite.
- [x] Add contamination tests for exact, normalized, and grouped duplicates.

### 4.3 Reproducible evals and promotion

- [x] Materialize the eval seed before generation and derive deterministic
  per-example/per-completion seeds.
- [ ] Persist raw completion references and model, adapter, tokenizer, template,
  backend, binary/source, generation, and scorer identities.
- [ ] Add a replay command that regenerates byte-identical seeded completions
  inside the declared environment.
- [ ] Gate promotion on per-example paired statistics and a lower confidence
  bound or exact/paired test, with minimum sample size and multi-suite policy.
- [ ] Do not promote on point accuracy alone.
- [ ] Replace mock-labeled-as-e2e coverage with a small real-model runtime path.

## Phase 5: Performance Program On Strix Halo

### 5.1 One honest serving benchmark

- [x] Build one benchmark driver used unchanged for Kiln and vLLM where vLLM
  supports the hardware/model.
- [x] Pin model weights, tokenizer/template, input/output lengths, prompts,
  sampling, seeds, request arrival pattern, warmup, concurrency, runtime
  versions, and memory limits.
- [x] Measure TTFT, end-to-end latency, ITL p50/p99/p99.9, request throughput,
  token throughput, SLO-goodput, peak memory, errors, and output parity.
- [x] Include concurrency 1, 8, 16, 32, 64, and 128 where memory permits.
- [x] Separate greedy, API-default sampled, long-prefill, prefix-hit, and mixed
  workloads.
- [x] Refuse zero-token/error responses in throughput accounting.
- [x] Emit comparable compact receipts for both engines.

### 5.2 Optimize only measured bottlenecks

- [ ] Use phase attribution to rank total latency/throughput cost before each
  optimization.
- [ ] Implement fused ROCm batched LM head, penalties, top-k/top-p, and sampling
  with one token readback if profiling confirms the current serialization.
- [ ] Replace prompt-logprob full-vocabulary host readback with a device-side
  selected-logprob/rank kernel so transfer and host work scale with O(TK), while
  retaining exact finite-value validation and the bounded projection fallback.
- [ ] Autotune supported decode width after fused sampling and graph correctness,
  rather than raising the cap blindly.
- [ ] Add multi-batch graph capture only after eager parity and pool-generation
  safety pass.
- [ ] Re-run correctness, 30-minute soak, and benchmark after each optimization.
- [ ] Record rejected experiments as short summaries, not raw logs.

### 5.3 Honest performance positioning

- [ ] Define the concurrency/latency region Kiln intentionally serves.
- [ ] Make a high-concurrency competitive claim only where Kiln reaches the
  declared target, initially `>= 0.90x` vLLM SLO-goodput with no correctness or
  tail-latency regression on the pinned workload.
- [ ] Where it does not, document vLLM as the recommended high-concurrency
  backend instead of hiding the result.
- [ ] Publish current receipts and remove stale or incomparable benchmark claims.

## Phase 6: Vulkan Qualification On Strix Halo

- [x] Run the same tokenizer, logit, sampling, cache, cancellation, and eval
  correctness corpus under Vulkan.
  The clean `ea4c0775f` core receipt now covers 16 tensor/autograd/transfer/
  reduction and nine matmul CPU-parity tests with zero ignored cases. It does
  not cover the model-level routes named by this checkbox. The clean `d6d14bfaf`
  prefill receipt additionally covers literal 1K/16K actor fairness,
  cancellation cleanup, six-quantum recurrent/prefix-cache parity, following-
  token equality, and KV release. The clean `8a1edd250` inference-oracle receipt
  adds six exact HF-derived Qwen tokenizer/mask cases, two BF16 argmax cases,
  six true seeded sampling cases, six selected-logprob/loss/gradient CPU-and-TRL
  cases, and bit-identical resident/nonresident Qwen logits across all 248,320
  vocabulary entries. The clean `bcb245ac7` model-route receipt additionally
  proves retained-prefill cancellation and discard cleanup, block-aligned
  multi-turn prefix reuse with lease retirement, nonbatched KV-plus-GDN split
  replay with token-identical output, and one exact-match eval through the real
  `LiveEvalGenerator`, batching actor, and Vulkan model runner. The clean
  `4d6697c52` independent-oracle receipt now compares the same Qwen weights and
  fixed input through pinned eager Transformers/PyTorch ROCm and Kiln Vulkan
  across all 248,320 logits. It passes exact argmax, 10/10 top-token overlap,
  `0.12433958` maximum and `0.020353988` mean absolute error, and
  `0.999941539769` cosine similarity. Together these receipts close the
  declared Phase 6 correctness corpus.
- [x] Run SFT and GRPO oracle fixtures supported by the declared Vulkan scope.
  The source-pinned AdamW fixture and complete pinned HF/TRL SFT plus recorded-
  GRPO production round trip have clean Strix Halo Vulkan receipts.
- [x] Run the same short/mixed/long prompt serving workload and phase metrics.
  The clean `e2efd5dff` Strix Halo receipt completed all synchronized 1/4/8/12
  waves, 25 exact requests, and 800 completion tokens with zero request,
  device, policy, mutation, unexplained-pause, or lifecycle failures. It also
  records only a two-row maximum decode batch and 0.900 output tokens/second
  overall; this closes bounded Vulkan correctness, not the still-open
  high-concurrency performance claim.
- [ ] Run a 30-minute development soak after each Vulkan fix.
  Allocation telemetry classified the old curve: the recycler reached its
  working-set high-water during the first two cycles, later cycles had exact
  allocation/free byte conservation and zero live-buffer or process-DRM
  growth, while anonymous pages became resident inside already-live unified
  mappings. The recycler is now capped by typed
  `memory.vulkan_buffer_pool_gb`, evicts only idle buffers, exposes exact
  ownership and pressure counters, and participates in coordinated memory
  reclaim. A current-source 70.845-second measured smoke stabilized in four
  cycles with two flat endpoints, completed 22 measured requests with zero
  failures, matched 120,768 allocations to 120,768 frees and
  93,706,563,776 allocated bytes to the same freed-byte total, and ended with
  zero live-buffer or pool growth. Retention was 2,275,606,528 bytes under the
  3 GiB cap with zero eviction or uncached fallback; RSS grew 67,227,648 bytes,
  the sampled active-scratch DRM peak grew 808,316,928 bytes under its explicit
  1 GiB bound, minimum host availability stayed above 24 GiB, swap grew only
  24,576 bytes, and teardown was clean. This accepts the bounded development
  smoke and its memory model, not the required clean pushed-source 30-minute
  receipt, so the item remains open.
- [ ] Run a final 8-hour Vulkan mixed-load soak.
- [ ] Require zero silent hardware skips and zero unexplained ITL outliers by the
  ROCm attribution rule.
- [x] Compare Vulkan outputs to CPU/HF oracles, not merely to another Kiln GPU
  backend.
  The paired HF/TRL route receipts prove lifecycle and structural integrity,
  not this item: seed 42 sampled `Understood.` on ROCm versus `Understood.'` on
  Vulkan, and behavior log-probabilities differ even for shared sampled tokens.
  The `8a1edd250` receipt now supplies exact HF tokenizer goldens plus independent
  CPU/TRL sampling, selected-logprob, loss, and gradient oracles. Its full-model
  Qwen result compares two Vulkan execution paths. The clean `4d6697c52`
  workload adds the required independent full-model comparison through pinned
  Transformers eager attention and torch-fallback linear attention. Its
  deterministic HF tensor hash is
  `sha256:0b902c0d74a8ed54aefefcdab50adeb6fedd7adb3e45a2338c27276e90abeeaf`;
  every logit is compared and the declared ranking/error/cosine gates pass.
- [x] Check in compact Vulkan receipts, update this document, commit, and push.
  The current-source independent-oracle receipt is retained under
  `qualification/receipts/vulkan/strix-halo/`; the remaining Vulkan artifacts
  are the 30-minute development soak and final eight-hour soak rather than a
  missing correctness receipt.

## Phase 7: CUDA And Metal Handoffs

For each machine, first check out `origin/main`, validate receipts/schema, and
run the environment capture. Commit and push repeatedly while working on that
machine using the checkpoint rules above. Each CUDA or Metal defect, test,
fix, measurement milestone, and receipt should land as soon as it is coherent
and green; the final push on a machine is only the handoff marker, not the sole
platform commit.

### 7.1 RTX 4090 Laptop GPU, 16 GB

- [ ] Capture environment and memory-capability receipt.
- [ ] Run the full CUDA correctness/oracle subset that fits the device.
- [ ] Exercise low-memory admission and explicit failure behavior.
- [ ] Run serving performance at every concurrency that fits without changing
  the workload semantics.
- [ ] Run an 8-hour mixed-load soak.
- [ ] Check in receipts, update this document, commit, and push.

### 7.2 Desktop RTX 4090, 24 GB

- [ ] Capture environment receipt.
- [ ] Run full CUDA correctness, graph, resize, training, and eval qualification.
- [ ] Run the full Kiln/vLLM performance matrix.
- [ ] Run a 24-hour mixed-load soak.
- [ ] Check in receipts, update this document, commit, and push.

### 7.3 M1 MacBook Air, Metal

- [ ] Capture environment and unified-memory receipt.
- [ ] Run the full Metal correctness/oracle subset supported by the machine.
- [ ] Exercise memory pressure and OS coexistence without unbounded allocation
  or unexplained stalls.
- [ ] Run the supported serving performance matrix.
- [ ] Run an 8-hour mixed-load soak.
- [ ] Check in receipts, update this document, commit, and push.

## Phase 8: Reduce The Surface Area

### 8.1 Typed configuration

Current audit findings and migration order (2026-07-12):

- The environment is not a startup boundary today. Production-reachable code
  contains more than 250 model/tensor/memory/kernel tuning knobs, at least 32
  training knobs, and additional server/request-handler reads. Many are read
  per request, per decode step, per SDPA/GEMM, or per optimizer parameter.
- Several strict TOML fields remain mirrors rather than authorities.
  GPU-memory sizing and gradient-checkpoint policy are accepted and validated
  at startup, then ignored by paths that reread permissive environment parsers.
  Streaming prefill has crossed the startup boundary: its backend dispatch,
  threshold, inference/training tiles, and last-token selector resolve once and
  are injected into model and trainer execution. Speculative decoding also has
  one startup authority:
  the typed configuration is validated once, retained in immutable application
  state, and reported by `/v1/config`. Serving is fail-closed to every non-off
  method before model allocation; request dispatch contains no speculative
  branch and reports no live routing, admission, or weight-readiness authority.
- The detached transposed-weight cache writer was a confirmed live-pause
  hazard: it combined an unbounded queue, cloned weights, duplicate transpose
  work, delayed disk I/O, and request-time lazy MTP misses. It is now removed.
  Base-model misses persist synchronously before readiness from the already
  computed bytes. Native MTP serving remains fail-closed until it implements
  cancel-aware external-yield settlement. Server-owned SFT must also keep its
  deferred MTP alignment phase disabled until that phase participates in GPU
  coordination, memory admission, progress cancellation, and settlement.
  Remaining leading candidates are default large-ROCm-matmul stream synchronization and
  per-attention live-memory/tile decisions. Lazy memory-governor parsing can
  still panic when first touched instead of failing startup.
- The target boundary is strict: only startup configuration, a narrow
  credential-provider adapter, and immutable build/source provenance may read
  process environment. Model, tensor, kernel, scheduler, training, eval, UI,
  and request code receive immutable typed policy. Qualification-only controls
  use one explicit internal namespace/profile and are included in provenance.
- The startup loader now owns one declarative registry for all 78 fixed typed
  leaves with public environment overrides, alongside eight explicitly
  config-file-only leaves. Canonical names derive mechanically from section and field,
  compatibility aliases parse strictly and warn without values, every present
  alias must agree with a canonical value, and typed CLI overrides no longer
  mutate process environment. Eight structured eval/agent leaves remain
  explicitly config-file-only. Lower runtime rereads remain migration work,
  not alternate public configuration.
- The production batching actor is the first high-risk scheduler subsystem to
  cross that boundary completely. `[batching]` owns actor selection, true
  batched versus rowwise decode, strict-prefix-aware admission, and prompt
  admission quantum. The values resolve once against backend policy and the
  effective decode width, retain configured/backend/effective provenance, are
  reported identically by config/health/debug, and require restart. Four old
  actor spellings remain strict warning aliases; production actor construction
  no longer reads them directly.
- The process-wide memory policy now has an explicit device/probe identity,
  detected-and-capped capacity, cached observations, and atomic reservations.
  Direct inference and training library consumers have typed initialization
  boundaries; server-owned training binds the selected backend instead of
  inferring Vulkan execution from CPU-host weight storage.
- Training admission now retains one aggregate permit across queued and running
  work for owned request data, private GRPO snapshots, and conservatively
  projected teacher-fixture expansion. Streamed GRPO validates and copies from
  one open source descriptor, then executes only from a private read-only
  snapshot whose receipt owns cleanup.
- Migration order: install an exact no-growth source ratchet; make existing
  server TOML authoritative; inject memory, graph,
  ROCm matmul/attention, Vulkan, and training-execution policies; then delete or
  namespace the remaining tuning/debug flags and remove test env mutation.
- The source-bound ROCm serving qualification drivers now exercise that target
  boundary themselves: declared public launch policy is written to a private
  TOML file and health/debug must report `source=config_file`. Ambient `KILN_*`
  values are rejected. `KILN_DEBUG_ENDPOINTS` is the sole internal qualification
  capability. `memory.kv_autoscale` and maintenance-only
  `memory.kv_force_blocks` replace both direct autoscaler environment reads;
  the old spellings are warning compatibility aliases resolved only by the
  central registry.

- [x] Inventory every direct `KILN_*` read and classify it as public stable,
  experimental/debug, build-time, or test-only.
- [ ] Centralize public runtime configuration into typed structures with one
  parser, validation, defaults, source attribution, and effective-config dump.
- [ ] Prohibit direct runtime environment reads in model kernels, forwarding,
  scheduling, training, eval, UI, and request handlers via a repository check.
- [ ] Put experimental/debug knobs behind one explicit namespace and profile.
- [ ] Delete dead, duplicate, contradictory, and undocumented knobs.
- [ ] Ensure tests use scoped configuration rather than process-global env
  mutation wherever possible; use one global serialization helper where env is
  unavoidable.

Environment-inventory checkpoint (2026-07-13): the lexical ratchet now resolves
simple named environment constants, recognizes colocated `#[cfg(test)]` modules
and `#[test]` functions, and classifies every direct crate-owned read and
mutation with an explicit rationale. The generated permanent website reference
and schema-v2 JSON enumerate 921 read call sites, 376 mutation call sites, and
374 statically named literal `KILN_*` inputs. The real production migration
surface is 444 reads outside the typed startup boundary; 368 mutations are
test-only and eight are build-script toolchain setup, while a hard policy gate
forbids production mutation. The public typed loader uses seven syntactic reads
to resolve its complete registry, so call-site totals are deliberately not
presented as public-field totals. This closes the inventory/classification item,
not the remaining centralization, experimental-namespace, deletion, or test-
fixture migration items.

Checkpoint at `a9bd8330f`: the actor-absent direct streaming effectively-greedy rendezvous
no longer reads its four public controls during model execution. Typed
`[batching]` mode, maximum width, wait, and mixed-sequence settings resolve once
from canonical names or strict deprecated aliases, retain source/backend/final
diagnostics, and are injected into the worker. This closes that bounded runtime
read set; it does not complete the repository-wide configuration or one-
scheduler checkboxes above.

Streaming-prefill checkpoint (2026-07-13): six source-tracked
`[streaming_prefill]` fields now resolve after backend selection into one
immutable execution policy shared by inference, native SFT/GRPO/OPD, training
admission, checkpoint planning, prompt-logprob scoring, local OPD teachers,
MTP alignment, and the benchmark harness. The prompt-logprob teacher identity
hash binds the exact policy under inference-contract schema v2, so a policy
change cannot reuse numerically incompatible teacher/cache provenance.
Canonical environment names are mechanical; six old spellings and legacy TOML
`enabled` are strict compatibility inputs. The lower streaming-policy
model/trainer rereads and stale use of the exact-GDN backward tile as a
streaming fallback were removed. Server-owned training and teacher construction
fail closed if startup did not inject the resolved policy. The subsequent
dead-control checkpoint deleted `KILN_EXACT_GDN_TILE_BACKWARD` and
`KILN_EXACT_GDN_BACKWARD_TILE_TOKENS` after proving that their tile selector
had no production caller; they were not promoted to public configuration.
Config, health, and trusted debug preserve configured/backend/effective
dispatch, tile inheritance, and restart semantics. This closes that bounded
runtime read set; it does not complete the repository-wide configuration
checkboxes or constitute accelerator qualification.
Final hosted validation is green: inexpensive CPU CI run `29268429061`, the
qualification-contract run `29268429011`, and dashboard/UI smoke run
`29268887496`. These runs validate portable contracts and product surfaces;
they do not substitute for the local ROCm, Vulkan, CUDA, or Metal hardware
matrix.

Checkpoint-boundary slice (completed 2026-07-13): SFT checkpoint-boundary
planning is one immutable `TrainingRuntimeContext` policy shared by admission
and execution. Recompute mode, inclusive recompute threshold, anchor stride,
and boundary-cache target are strict source-tracked `[training]` fields with
mechanically derived canonical names and four deprecated, conflict-checked old
spellings. Startup converts the cache target to integral bytes once; preflight
and training use the same pure recompute and shape-stride functions, and no
runtime path rereads the environment. Config, health, trusted debug, and the
dashboard expose the installed policy. Exact-resume planning identity is now
`kiln.training-checkpoint-planning.v3` for GRPO and OPD. SFT subsequently
extends that object to v4 with the pinned backend loss route. A v2 identity,
an SFT v3 identity, or any applicable policy difference is drift under the
current contract. Sparse replay itself remains SFT-only, while GRPO and OPD
retain every checkpoint boundary.

The follow-up audit corrected one unsafe classification: Vulkan
`KILN_VK_FLCE_CHUNK_LEN` is not estimator-only. It controls both admission's
memory estimate and the Vulkan FLCE runtime chunk, so deleting only the
estimator read or treating it as dead would recreate admission/execution drift.
A later slice must replace both reads with one typed, shared, pure
route/device-limit policy and prove that the admitted chunk is exactly the one
executed. The unsafe SFT route switch has now been removed and bound through
admission and execution as described below. Optimizer debug-fallback and
stochastic-rounding controls are now retired by the optimizer-authority slice
below. The following typed slice owns GRPO shared-prefix-reference selection
and OPD sampler segments;
trace/debug/host-scan controls move behind explicit qualification hooks instead
of public config.

Exact-GDN dead-control slice (completed 2026-07-13): repository-wide call-site
inspection proved that `exact_gdn_reverse_tile_size` had no caller and that its
tile resolver was reachable only from that orphan and an environment-mutating
unit test. Both functions and both environment reads are deleted. The obsolete
CUDA-only tile value is no longer presented as part of
`TrainingPrecisionPolicy` or the generated backend capability report. The
runtime-environment ratchet and a focused negative source contract prevent the
two spellings from returning. This is a code-surface and authority cleanup; it
makes no accelerator performance or correctness claim.

Tape-authority slice (completed 2026-07-13):
`KILN_USE_TAPE_AUTHORITATIVE` had one production reader, only in GRPO, and no
supported disabled route. A false value was checked fresh for each completion
and produced a late error after shared or per-completion reference work; a
concurrent mutation could therefore interrupt a partially updated group. SFT
and OPD were already unconditionally kt-tape. The variable and its nine
unscoped test mutations are deleted, including two tests that left a false
value behind for unrelated parallel tests. Inline and streamed GRPO now call
one backend-route and precision-policy guard before resident model or optimizer
setup, and direct group execution repeats it before any reference forward. The
environment ratchet and a focused source contract prevent the switch from
returning. Local ROCm and Vulkan GRPO qualification must still prove the
backend-selected route on this machine; this static slice alone makes no
accelerator claim.

SFT loss-route authority slice (completed 2026-07-13): commits `307327fca`,
`4c09c043b`, and `a193db663` removed `KILN_USE_FLCE` without an alias or
replacement and made the backend's typed capability the only SFT loss-routing
authority. CUDA/ROCm report `kt_tape_flce`, Vulkan reports
`vulkan_active_rows`, and Metal reports `full_logits`; those declarations are
capability contracts, not new hardware qualification. Admission now includes
the exact route in a saturating upper bound for active-token loss workspace,
checkpoint boundaries, LoRA/optimizer state, and streaming scratch. Its HTTP
413 breakdown names the selected loss workspace, while a multi-segment
`full_logits` plan returns `training_invalid_request` before publication and is
checked again before a trainer forward.

The admitted enum is stored with `PreparedSftAdmission`, revalidated against
the resident runner before governor reservation or reclamation, installed in a
job-local `TrainingRuntimeContext`, and compared with the execution backend
before resident or trainable allocation. Every SFT loss step receives the
pinned enum. New SFT receipts expose `runtime.sft_loss_route`, and exact SFT
planning identity is v4 with that route; GRPO and OPD remain v3. The obsolete
Phase A validation example and live-script override were removed, and the
runtime-environment contract at `a193db663` records 950 reads and 539
process-mutation call sites. This remains a portable authority/correctness
checkpoint: it does not close ROCm, Vulkan, CUDA, or Metal execution gates.

Optimizer-authority slice (completed 2026-07-13): native training now resolves
one typed backend optimizer contract from exact backend name, device family,
resident base dtype, derived LoRA dtype, optimizer kind, rounding policy, and
LoRA rank. CUDA/ROCm accept F32/BF16 SGD and AdamW plus Muon ranks 2..=48;
Metal rejects SGD and accepts F32/BF16 AdamW plus Muon ranks 2..=32; Vulkan's
raw F32/BF16 hooks resolve through its F32-LoRA product policy and cap Muon at
32; CPU is the F32 reference path with Muon rank at least two. F16 remains
inference-only. Executable optimizer parameter dtypes and product-executable
base-to-LoRA tuples are reported separately so the generated capability report
does not overclaim; CPU uses its reference implementation while accelerators
require native hooks.

The four backend-specific optimizer-fallback switches, the global hot-path
fallback switch, and the BF16 stochastic-rounding environment switch are
deleted with no replacement alias. Product construction is immutable
round-to-nearest; explicit low-level reference callers retain a programmatic
rounding API.
Legacy stochastic checkpoints fail closed. Submission, dequeue, direct SFT,
inline and streamed GRPO, and OPD all validate hyperparameters and the exact
optimizer tuple before physical-memory probing, corpus scans, remote/local
teacher materialization, governor, checkpoint loading, resident-weight, LoRA,
or optimizer-state work; cheap teacher alias validation and metadata pinning
retain their established request-error ordering. Execution then revalidates
against one explicitly initialized native backend. Backend/device mismatches
and Vulkan initialization downgrades fail closed rather than acquiring CPU
authority.
This is a portable authority checkpoint, not accelerator qualification.

Optimizer product-contract slice (completed 2026-07-13): `/v1/config` now
publishes versioned `kiln.training-optimizer-support` state from the resident
runner. It separates raw backend implementation, the product-executable
optimizer tuple, per-workload admission, and request-time live-memory
admission. Rank diagnostics separately report backend, loaded-model, and
effective maxima; the effective maximum is the intersection used by controls,
so an unbounded AdamW or SGD backend no longer implies an unbounded model.
SFT, GRPO, and OPD carry individual allowlists and reasons. `distill_refresh`
is a distinct fourth workload and is deliberately unavailable until admission
pins separate exact SFT and OPD phase plans, prepares the exact SFT rows, and
reserves their maximum sequential working set.

Every direct form and recipe action in the dashboard starts fail-closed,
consumes only schema v1, disables unsupported tuples and ranks without silently
substituting another optimizer, and keeps a visible reason attached to the
control. Recipes expose the same admission descriptor through the API. Common
admission and dequeue repeat workload and tuple checks before checkpoint or
corpus materialization. Resume admission stores a compact digest of the fully
validated checkpoint manifest, including artifact hashes, plus the effective
seed; dequeue reloads and compares both before governor reservation. The
README, configuration and native-SFT references, checkpoint guide, public API
site, changelog, and static UI/docs contracts describe the complete shape and
its non-promises. This remains a portable static/product checkpoint pending
hosted compilation and local accelerator qualification.

Tape-scope and exact-gradient slice (completed in source 2026-07-13): active
training tape scope is now the sole authority for recording. The eight former
switches `KILN_USE_TAPE_FORWARD`, `KILN_USE_TAPE_FLASH_ATTN`,
`KILN_USE_TAPE_SDPA`, `KILN_USE_TAPE_LORA_ADD`, `KILN_USE_TAPE_GDN`,
`KILN_USE_TAPE_GDN_CONV`, `KILN_USE_TAPE_GDN_QK_NORM`, and
`KILN_USE_TAPE_GDN_GATED_NORM` are removed without aliases or replacement
environment controls. Outside an active scope, established inference fast
paths remain available. Inside one, fused RoPE, RMSNorm, and SwiGLU routes can
no longer return tensors that sever the training graph: they record through
the authoritative tape path or fail closed when the required recorder cannot
be constructed.

The optimizer boundary now consumes an exact LoRA gradient set. Gradient IDs
must exactly equal the configured trainable leaves, and each gradient must
match its master's shape, backward-compute dtype, and device before one final
finite-value scan and any optimizer mutation. An all-zero gradient remains a
valid observed gradient; missing and unknown IDs are errors rather than silent
skips. Bridge accumulation is deterministic, validates metadata before
combining stores, and rejects the same LoRA ID appearing in disjoint
checkpoint segments instead of double-counting it. All 36 GPU tape/parity
tests remain serialized by the shared poison-tolerant GPU test lock. This is a
portable source/static checkpoint only: backend execution and numerical
qualification remain pending on ROCm, Vulkan, CUDA, and Metal.

ROCm execution and graph-memory source/static implementation checkpoint
(compile and local hardware pending): `[accelerator]` now owns synchronization
mode, graph mode, graph entry capacity, and an independent 64 MiB..=16 GiB
retained-device-byte ceiling through typed fields and mechanically derived
canonical environment names. Startup installs one immutable per-device policy
before model or tensor allocation. Stable defaults retain legacy host barriers
and disable graphs; stream ordering and graph execution require the experimental
profile. Config, CLI, the example, and the permanent website expose the
resolved startup policy. Config, health, trusted debug state, metrics, the
dashboard, and qualification receipts expose the applicable live outcome.

Graph accounting binds every queryable graph-owned device allocation to exact
device/base identity and separately counts stable I/O, capture arenas,
private-stream hipBLASLt workspaces, and persistent slot state. Opaque graph,
exec, stream, and event allocations remain explicitly unsized and are reported
through `opaque_native_object_count`. Admission is checked before native capture
and again before publication. Owner-aware deterministic LRU eviction enforces
both entry and byte ceilings; moderate pressure allows replay but no new
capture, tight pressure first evicts idle entries, and critical pressure removes
graphs before eager execution. Candidate-alone byte and incomplete-accounting
results make that geometry non-capture-safe for the runner lifetime. Aggregate
budget suppression clears on ownership or budget relief; reservation denial
retries only after the matching device reports enough cached headroom; a memory
governor selector mismatch fails closed rather than borrowing another device's
budget. The closed result contract has four eviction causes, three post-capture
rejection causes, five pre-capture skips, and thirteen eager-fallback causes.

Five independently observable phases cover pre-candidate headroom,
candidate warmup, pre-native reservation, native capture plus first launch and
publication, and rejected-candidate cleanup. A cloneable telemetry handle lives
outside both model and graph-runner locks, so health, debug state, Prometheus,
and the dashboard can report the active phase and monotonic elapsed time while
the full snapshot is busy. Typed missing-data reasons distinguish model/graph
contention, poisoned locks, and backends without a graph runner; dashboard
health polling updates the live phase independently of the slower config
snapshot.

The low-level boundary now uses 23 reasoned waits, strong producer ownership,
fallible raw-stream acquisition, stream/context identity checks, explicit
capture rollback, and a process-lifetime per-device cleanup quarantine. Unsafe
settlement never retries partially advanced work and readiness/admission fail
closed until process restart. A single admission permit spans host-buffer copy
enqueue and settlement; failure or unwind quarantines and retains the host
allocation rather than risking use-after-free. Generic pageable-host H2D replay
uses HIP's documented synchronous-host-source contract. Graph streams, graphs,
execs, events, tensor allocations, hipBLASLt handles, and workspaces retain
uncertain resources instead of destructing them on the wrong or quarantined
device. Concurrent hipBLASLt submissions hold shared workspace allocation
ownership across FFI, so a resize cannot retire a submitted pointer.

`serving-rocm-sync-ab-v1` defines exact-provenance legacy/stream arms at
concurrency 1, 2, and 4, reconciles all 23 wait reasons, fails on 250 ms stalls
or 2 second pauses, samples memory, and requires clean teardown.
`serving-mixed-rocm-v1` validates the retained-byte, eviction, admission,
fallback, live-phase, and typed-unavailability contracts.
`serving-rocm-graph-resilience-v1` now defines the one-binary 1 GiB/64 MiB
graph-budget A/B and exact concurrency 1/8/16/32/64 corpus, while
`serving-rocm-graph-failure-containment-v1` turns the three ignored real-device
graph fallback/lifecycle/poison cases into a bounded non-skippable receipt.
`serving-rocm-public-mutation-lifecycle-v1` adds a one-binary public adapter
barrier-swap/base-restoration arm and a typed maintenance-resize arm with
fail-closed inference admission. All seven ROCm serving drivers now materialize
their declared public policy as a private typed TOML file instead of a
handwritten public `KILN_*` launch map;
only the internal trusted-debug gate remains in their scrubbed `KILN_*` process
environment. Autoscaler enablement and the disabled one-shot maintenance target
are explicit `[memory]` fields with `config_file` runtime provenance. Portable
source and 378 qualification-tooling checks are the evidence for this
checkpoint. Bounded Cargo
correctly refused to start below the unchanged 15 GiB available-memory floor,
and GPU execution was not attempted under the current host pressure, so this
makes no new compile, ROCm execution, parity, pause, memory, or throughput
claim.

Next large bite: once host memory is safely above the build floor, compile this
exact source and run the Strix Halo legacy-versus-stream A/B, graph byte-budget
stress, failure injection, and graduated concurrency. Then run the Vulkan
contract on this machine. NVIDIA handoff does not need to wait for the entire
repository cleanup, training program, or final long soaks; it starts after
these local ROCm and Vulkan baseline checkpoints produce a source-bound commit
that can be checked out on the CUDA host.

### 8.2 One scheduling model

- [ ] Map ownership and production/test use of batching engine, legacy decode
  batcher, scheduler crate, and mock scheduler.
- [ ] Select one production scheduling/admission abstraction.
- [ ] Port tests to the production abstraction or a faithful injected backend.
- [ ] Remove the idle legacy thread and duplicated policy paths.
- [ ] Add state-machine/property tests for admission, cancellation, capacity,
  prefix reuse, fairness, backpressure, and shutdown.

### 8.3 Split oversized modules by behavior

- [ ] Split `forward.rs` into owned attention, linear-attention, FFN, norm,
  cache, backend-dispatch, and training-facing modules without duplicating
  policy.
- [ ] Split `completions.rs` into schema, validation, preparation, streaming,
  finalization, batch, and shared lifecycle modules.
- [ ] Split `trainer.rs` into data/tokenization, SFT, GRPO, checkpointing,
  optimizers, provenance, and oracle-test modules.
- [ ] Split the browser application into maintainable modules using the existing
  build/embedding approach.
- [ ] Establish a reviewed production-file size budget. Exceptions require a
  specific rationale, not generated-code-style growth.

### 8.4 Replace verification theater

- [x] Inventory source-string and ad hoc Rust-source parsing tests.
- [x] Replace load-bearing source-string assertions with compile-time types,
  runtime tests, property tests, or structured metadata validation.
  - The corrected scanner found 112 source-text tests, including 51 hidden
    behind workspace-relative reader helpers and dynamic source paths. The
    source-text baseline is now exactly zero tests, zero reads, and zero text
    assertions. Backend trait/type checking, executable tape/GDN/replay and
    lifecycle tests, canonical JSON/schema validation, and source-bound local
    qualification own the corresponding claims; `docs/VERIFICATION_POLICY.md`
    publishes the permanent evidence map.
- [x] Ensure hardware qualification reports skipped tests as failures.
- [x] Rename mock tests so they cannot be mistaken for model/backend e2e tests.
- [x] Delete redundant tests that add maintenance cost without a distinct
  invariant.
  - The redundant layer was removed rather than renamed: five source-only test
    targets are gone, source-parsing unit tests were removed from seven owning
    modules, and `backend_capability_contract.rs` fell from 8,880 to 2,327
    lines while retaining structured-report and executable replay/capability
    checks. The checkpoint removes more than ten thousand net lines.

### 8.5 Artifact and documentation policy

- [x] Stop tracking raw `.log`, SSE, profiler, trace, and large CSV artifacts.
- [x] Add ignore rules and size checks that prevent recurrence.
- [x] Keep compact summaries, manifests, hashes, and reproduction commands.
- [x] Move or remove existing raw audit artifacts from the current tree while
  preserving the useful conclusions in compact documents.
- [x] Do not rewrite Git history without a separate explicit approval.
- [x] Generate API/reference documentation from canonical schemas where
  semantics are currently copied across README, QUICKSTART, site HTML, and
  guides.

Schema-publication checkpoint (2026-07-13): the permanent website builder now
validates and renders canonical JSON Schema documents directly, including root
and definition fields, requiredness, types, constraints, defaults, closed-object
state, and exact composition/conditional rules. The thinking-budget contract
and all three local-qualification schemas are searchable generated references.
This establishes the generation path but leaves the item open until public
configuration and HTTP request/response metadata move out of copied prose into
canonical schemas.

Configuration-contract checkpoint (2026-07-13):
`contracts/kiln-config-v1.schema.json` now owns the complete accepted typed TOML
shape: 15 closed sections, 92 fixed fields, two dynamic teacher-credential
templates, the deprecated TOML-only streaming-prefill compatibility field,
defaults, validation bounds, 84 mechanically named environment overrides, 63
compatibility aliases, and cross-field ROCm/KV/queue conditions. A strict
checker reconciles every schema field and annotation with the comprehensive
operator table, validates `kiln.example.toml`, and runs positive and negative
mutation cases. The website publishes a generated six-column field reference
directly from the schema and passes desktop/mobile browser checks. The item
remains open only because the HTTP request/response surface is still maintained
in router types plus copied site prose rather than one canonical OpenAPI/schema
contract.

HTTP-operation checkpoint (2026-07-13):
`contracts/kiln-http-api-v1.openapi.json` now canonically inventories all 101
production paths and 112 method bindings: 53 GET, 47 POST, and 12 DELETE
operations across 13 ownership tags. Every operation records its handler and
signature owner, HTTP versus WebSocket transport, path/header/query inputs,
request body type and media type, success status/type/media/headers, and the
structured `/v1` error envelope. The generated website page includes 34
operations absent from the old hand-written endpoint map and the product API
guide links to it. The contract checker has fail-closed path-parameter,
body/media, response, reference, count, tag, ownership, and transport mutation
coverage. The item remains open because 93 of 105 payload components are still
explicitly represented by field-open objects or arrays of field-open objects;
field-level request/response schemas and runtime router-to-contract enforcement
must land before copied semantic prose can be retired.

Inference-schema checkpoint (2026-07-14):
`contracts/kiln-inference-v1.schema.json` now owns all known fields and nested
wire types for chat completions, prompt-logprob completions, batch completions,
SSE chunks and token timing, thinking/performance/config metadata, tool and
message round trips, and exact rollout provenance. The seven inference
entrypoints now resolve from OpenAPI to 52 reachable definitions, reducing the
top-level component queue to 88 migration-pending and 17 field-complete
components. Request schemas explicitly preserve the runtime's
accepted-and-ignored OpenAI compatibility policy while emitted response
schemas are closed. Machine-readable annotations also expose two existing
footguns rather than blessing them: unknown sampling presets silently fall
back, and most sampling numbers lack request-edge range validation. A shared
dependency-free schema validator checks both configuration and inference
contracts, all published examples, external thinking-budget references, and
adversarial request/response mutations. The generated website renders every
definition and every `x-kiln-*` annotation, and the curated API guide now
delegates field semantics to the generated contracts. Phase 8.5 remains open
for the other 88 payload components and runtime router-to-contract enforcement.

Read-only serving and observability schema checkpoint (2026-07-14):
`contracts/kiln-observability-v1.schema.json` now owns the complete emitted wire
shape for health, degraded readiness, resolved configuration, gated model-state
diagnostics and their distinct error bodies, model discovery, decode statistics,
recent requests with thinking-budget outcomes, and persistent logit-cache
statistics. Ten OpenAPI components resolve to 135 reachable closed definitions,
including the shared batching, streaming-prefill, memory, ROCm graph and
synchronization, cache, training-capability, base-weight, and execution-provenance
objects. Development-time source-to-schema field audits matched 111 serialized
Rust structs exactly. The audit also corrected two transport lies: both health
routes publish the same diagnostic body with HTTP 503 when not ready, while the
debug route publishes its actual HTTP 403 disabled and HTTP 500 provenance
validation bodies instead of the unrelated generic error envelope. A
deterministic generator, stale-artifact gate, ten validated example families,
and eight adversarial mutations keep the large contract maintainable. The
generated website now publishes this field reference and the curated API guide
delegates its wire semantics to it. OpenAPI accounting is now 27 field-complete
and 80 migration-pending components out of 107. Phase 8.5 remains open for those
80 components and executable runtime router-to-contract enforcement.

Artifact-lifecycle schema checkpoint (2026-07-14):
`contracts/kiln-artifacts-v1.schema.json` now owns the complete adapter, HF/TRL,
PEFT import, legacy adapter-receipt, and teacher-registry wire workflow. Twenty-two
OpenAPI components resolve to 80 reachable definitions across all 19 lifecycle
operations. The schema distinguishes the three different runtime name policies,
raw versus `sha256:`-prefixed digests, exact handoff filenames, skipped options
versus required nullable fields, request aliases, GRPO provenance and group-width
bounds, source exclusivity, task/data-format agreement, conditional teacher
registration, and the few serde inputs that deliberately ignore unknown fields.
The audit corrected two generic-JSON API lies: teacher registration now uses a
typed fail-closed extractor with the structured Kiln error envelope, and teacher
deletion emits a typed closed response. Exact operation bindings, ETags,
conditional deletion, download headers, 22 full examples, generator drift, and
18 focused positive/negative mutations are executable portable checks. The
website publishes the entire generated reference and the README, quickstart,
HF/TRL, teacher, and adapter guides delegate wire semantics to it. OpenAPI
accounting is now 49 field-complete and 60 migration-pending components out of
109. Phase 8.5 remains open for those 60 components and executable runtime
router-to-contract enforcement.

Eval-and-judgment schema checkpoint (2026-07-14):
`contracts/kiln-evals-v1.schema.json` now owns the complete suite, generation,
scoring, execution, result, dataset, synthesis, and judgment wire workflow.
Thirty OpenAPI entrypoints resolve to 74 reachable definitions across all 25
eval and judgment operations. The source audit matches 45 serialized Rust
structs exactly; 30 full example families, deterministic generation, reference
reachability, operation bindings, and focused positive/negative mutations are
portable executable checks. Runtime responses that previously advertised
arbitrary JSON are typed and closed. Fixed scorer choices now have an
unambiguous nested discriminator, and synthesis seeds use exact decimal strings
while retaining legacy numeric input compatibility. The website publishes the
generated reference and the eval guide delegates field semantics to it. OpenAPI
accounting is now 79 field-complete and 39 migration-pending components out of
118. Phase 8.5 remains open for those 39 components and executable runtime
router-to-contract enforcement.

Training-and-agent control-plane schema checkpoint (2026-07-14):
`contracts/kiln-control-plane-v1.schema.json` now owns the complete native SFT,
GRPO, OPD, distillation, admission, preflight, training-job, embedded-agent,
trace, recipe, correction, and adapter-library wire workflow. Fifty-two public
entrypoints resolve to 115 reachable definitions across all 45 applicable HTTP
operations, with 53 validated example instances. The source audit matches 74
serialized Rust structures exactly, verifies flattened structures without
duplicate ownership, binds every operation to its exact request and response,
and exercises focused invalid mutations. Thirteen formerly generic JSON bodies
are typed and closed. The audit removed duplicate `job_type` and
`post_eval_verdict` keys from training-detail serialization and corrected two
impossible OpenAPI successes: judge drift checking is currently HTTP 501 and
library installation is currently HTTP 400. The website publishes the complete
generated reference and the training guides delegate wire semantics to it.
OpenAPI accounting is now 131 field-complete and zero migration-pending
components out of 131. Phase 8.5 remains open only for executable live-router
to-contract enforcement; no local compile or accelerator process started below
the unchanged 15 GiB host-memory floor.

Live-router and operational-configuration completion (2026-07-14): a production
router integration test now derives all 101 concrete probe paths and all 112
documented method bindings from the canonical OpenAPI contract, exercises the
real `api::router`, and requires every supported method to avoid 405 while every
unsupported probe returns 405. Invalid request bodies prevent the contract probe
from starting valid work, every request has a two-second ceiling, and each path
must retain an unsupported control method so a missing route cannot masquerade
as a handler-level 404. The same checkpoint moves terminal/agent access, pi
executable/session resolution, logit-cache placement, and adapter-library URL
into six typed config fields plus three newly mechanical existing agent fields.
Legacy spellings remain strict, warning compatibility aliases; handlers consume
one immutable startup snapshot published by `/v1/config.operational`. Two dead
undocumented switches (`KILN_DISABLE_BATCH_PREPARED_PROMPTS` and
`KILN_VK_FLCE_CHUNK_LEN`) are deleted instead of promoted. The configuration
contract now covers 98 fixed fields, 93 canonical environment overrides, and 69
compatibility aliases; the observability schema reaches 137 closed definitions;
the environment ratchet falls from 923 reads/376 mutations to 909/361. All
portable static, 421 qualification, 10 documentation-builder, 46-document site,
and Chromium gates pass. Hosted CI `29325026823` compiled the complete default
tree and passed the new live-router test plus the full default, leaf-crate,
anomaly, dependency-tree, and qualification lanes. Pages `29325026877`, the
dedicated qualification workflow `29325026824`, and repository hygiene
`29325026912` are also green. This closes Phase 8.5; bounded local Cargo still
correctly refused at 10 GiB available under the unchanged 15 GiB floor.

## Phase 9: Final Common-Tree Qualification

Receipt commits themselves do not change `source_tree_hash`. Any source,
manifest, lockfile, kernel, shader, qualification runner, or runtime-config
change does. If a source-changing fix lands on any later machine, all earlier
final receipts for affected behavior are invalid and must be rerun.

- [ ] Freeze a release-candidate `source_tree_hash` after all source changes.
- [ ] Re-run final ROCm qualification and 24-hour soak on Strix Halo.
- [ ] Re-run final Vulkan qualification and 8-hour soak on Strix Halo.
- [ ] Re-run final CUDA qualification on the 16 GB 4090 laptop.
- [ ] Re-run final CUDA qualification and 24-hour soak on the 24 GB desktop
  4090.
- [ ] Re-run final Metal qualification and 8-hour soak on the M1 MacBook Air.
- [ ] Validate that every final receipt names the same `source_tree_hash` and
  compatible model/workload versions.
- [ ] Run the complete cheap CPU/static CI path locally.
- [ ] Confirm an ordinary push invokes only the intended inexpensive automatic
  workflows.
- [ ] Update the support matrix with exact qualified device/runtime classes and
  explicitly experimental or unsupported combinations.
- [ ] Update benchmark, training, eval, thinking-budget, operations, and
  troubleshooting docs from the final evidence.
- [ ] Remove stale claims and stale benchmark tables.
- [ ] Confirm there are no unchecked required items in this document.
- [ ] Replace this temporary plan with a concise permanent qualification and
  support policy, preserving a link to the final receipts.
- [ ] Commit and push the final documentation/receipt state to `origin/main`.

## Final Release Gates

All gates are required:

- [ ] Zero known stale-pointer, use-after-free, partial-commit, or cache-state
  corruption path in supported serving profiles.
- [ ] Zero unexplained ITL outliers in final hardware soaks.
- [ ] Zero silent hardware-test skips.
- [ ] Eager/graph and backend/oracle correctness within declared tolerances.
- [ ] GRPO old-policy and KL-reference semantics match the pinned oracle.
- [ ] Exact checkpoint/resume equivalence inside the declared envelope.
- [x] Correct multi-sample eval aggregation and enforced train/eval separation.
- [ ] Thinking-budget semantics and provenance are transport/UI consistent.
- [ ] Performance claims are backed by comparable receipts; vLLM is recommended
  wherever it remains materially better.
- [x] Native training scope and HF/TRL interoperability are explicit and tested.
- [ ] Automatic GitHub-hosted work is inexpensive and is not represented as
  hardware qualification.
- [ ] Final receipts cover ROCm, Vulkan, both CUDA machines, and Metal on one
  common source tree.
- [ ] Public docs, defaults, health output, and support matrix agree.
- [ ] Worktree and `origin/main` agree at the final pushed commit.

## Evidence Log

Add one row per completed slice. Keep notes short and link to compact receipts
or focused documents. Never paste raw logs here.

| Date | Phase | Source tree | Commit | Backend/device | Evidence | Result | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-09 | Plan created | pending | pending | n/a | this document | pending | Audit converted into executable goal |
| 2026-07-09 | Receipt foundation: source identity | `a76a5e1b664b` | this commit | n/a | `scripts/qualification/source_tree_hash.py` | passed | 5 unit tests; receipts and docs excluded from identity |
| 2026-07-09 | Source identity scope correction | `sha256:ebdb799360a7` | this commit | n/a | source identity unit suite | passed | 7 tests; excludes profiling artifacts and desktop prose; uses repository hash convention |
| 2026-07-09 | Source identity index hardening | `sha256:d56d06727edd` | this commit | portable | source identity unit suite | passed | Rejects unresolved index stages; hashes symlink targets and gitlink object IDs without dereferencing |
| 2026-07-09 | Receipt schema and strict validator | `sha256:74efdd391d91` | this commit | portable | `qualification/schema/receipt-v1.schema.json` | passed | 13 receipt tests plus 7 source identity tests |
| 2026-07-09 | Atomic ROCm/Vulkan environment collector | `sha256:c4f18b397716` | this commit | Strix Halo target | `scripts/qualification/environment.py` | passed | 25 portable tests; real receipts captured after clean collector commit |
| 2026-07-09 | Initial ROCm environment | `sha256:c4f18b397716` | `33f27c5f4dc6` | ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260709t202926z-rocm-strix-halo-environment-v1.json` | passed | ROCm 7.2.4; HIP 7.2.53211; 96 GiB unified VRAM |
| 2026-07-09 | Initial Vulkan environment | `sha256:c4f18b397716` | `1538505b5129` | Vulkan/Strix Halo | `qualification/receipts/vulkan/strix-halo/20260709t203013z-vulkan-strix-halo-environment-v1.json` | passed | Vulkan 1.4.348; RADV Mesa 26.1.3; 96 GiB unified VRAM |
| 2026-07-09 | Manual-only backend compile jobs | `sha256:c4f18b397716` | this commit | GitHub Actions | `.github/workflows/ci.yml` | passed | Metal/Vulkan/CUDA/ROCm changed to explicit dispatch; cancels superseded runs |
| 2026-07-09 | Retire broken automatic perf workflow | `sha256:c4f18b397716` | this commit | GitHub Actions | `.github/workflows/perf-regression-nightly.yml` | passed | Removed nightly/PR triggers; legacy fixture and A6000 paths are explicit manual dispatch only |
| 2026-07-09 | Manual-only release packaging | `sha256:c4f18b397716` | this commit | GitHub Actions | desktop/server/Docker/RunPod workflows | passed | Removed automatic push, tag, PR, and schedule triggers; explicit dispatch remains |
| 2026-07-09 | Consolidated cheap automatic checks | `sha256:c4f18b397716` | this commit | Linux CPU | `.github/workflows/ci.yml` | passed | Folded leaf and dependency-tree guards into default job; added portable receipt contract validation |
| 2026-07-09 | CI cost inventory and operator policy | `sha256:c4f18b397716` | this commit | GitHub Actions/local hardware | `docs/ci-policy.md` | passed | Recorded all prior automatic workflows, measured cost, three-job Rust target, manual dispatch commands, and local evidence ownership |
| 2026-07-09 | Deduplicated release drift check | `sha256:d56d06727edd` | this commit | Linux CPU | `.github/workflows/release-version-drift.yml` | passed | Release workflow now owns version/CLI/link drift only; Pages and UI workflows retain their browser smoke coverage |
| 2026-07-09 | Honest legacy latency status | `sha256:d56d06727edd` | this commit | Metal/Vulkan fixtures | `docs/backend-latency-fixtures.json` | passed | Stale source-bound Metal and Vulkan artifacts are pending rerun; the legacy manifest no longer claims covered hardware evidence |
| 2026-07-09 | Capability report truth alignment | `sha256:cda13f3f84e5` | this commit | Linux CPU | `docs/backend-capability-report.{json,md}` and `backend_capability_contract` | passed | Generated report exposes fixture-required Phase 8 and pending hardware blockers; generator checks and all 51 focused contract tests pass |
| 2026-07-09 | Exact model content fingerprint | `sha256:85e21e751643` | this commit | portable | `scripts/qualification/model_fingerprint.py` | passed | Loader-aligned shards and serving-template fallback; stable hashes; traversal, symlink, reference, duplicate, and mutation tests |
| 2026-07-09 | Workload contract foundation | `sha256:c49a86951eab` | this commit | portable | `qualification/schema/workload-v1.schema.json` | passed | Exact byte identity, argv-only cases, structured results, closed comparison modes, and initial environment/core-correctness manifests; 14 focused tests |
| 2026-07-09 | Qualification-required backend tests | `sha256:31f9133268ee` | this commit | ROCm/Vulkan | core correctness hardware test binaries | passed | Exact `KILN_QUALIFICATION=1` opt-in converts missing devices and Vulkan initialization failures from developer skips into test failures |
| 2026-07-09 | Strict receipt comparison | `sha256:4fcbf0c90547` | this commit | portable | `scripts/qualification/compare_receipts.py` | passed | Committed policy binds source, model, parameters, config, environment exceptions, and metric semantics; 25 comparator tests and 88 isolated qualification tests |
| 2026-07-09 | Local qualification runner | `sha256:bedb3fc59bee` | this commit | portable + Strix Halo | `scripts/qualification/run.py` | passed | 36 runner tests and 126 qualification tests; strict bounded evidence, config/model/source attestation, exact repetition aggregation, no-network/PID isolation, and process-tree cleanup; real namespace probes preserved ROCm gfx1151 and RADV Vulkan access |
| 2026-07-09 | Shared strict JSON policy | `sha256:429d38681db8` | this commit | portable | `scripts/qualification/strict_json.py` | passed | One exact numeric, duplicate-key, integer-bound, and plain-UTF-8 policy now serves workloads, receipts, comparisons, and command results; 138 qualification tests |
| 2026-07-09 | ROCm core correctness | `sha256:429d38681db8` | `d457a89ce267` | Strix Halo ROCm | `qualification/receipts/rocm/strix-halo/20260709t225523453678z-rocm-strix-halo-core-correctness-v1-9faecc7321-v1.json` | passed | Real gfx1151 device probe, ROCm tensor parity, and ROCm matmul parity passed under loopback-only and PID-isolated execution in 88.4 seconds; receipt and all local artifact hashes revalidated |
| 2026-07-09 | Vulkan core correctness and cross-backend parity | `sha256:429d38681db8` | `2a6df2f83973` | Strix Halo Vulkan | `qualification/receipts/vulkan/strix-halo/20260709t225744609983z-vulkan-strix-halo-core-correctness-v1-0a09f3bcee-v1.json` | passed | Real RADV device probe, tensor parity, and matmul parity passed in 1.30 seconds; declared comparison against the ROCm receipt accepted only the listed backend fields and all three required case metrics were exactly equal |
| 2026-07-09 | Serving A/B configuration observability | `sha256:4ed553507ea1` | `39814f2a` | portable + ROCm | `/health`, `/v1/debug/model-state` | passed | Health now reports CUDA, ROCm, and Metal graph states independently; debug state exposes the ROCm-graph and KV-autoscale launch flags; focused ROCm-feature server tests and formatting passed |
| 2026-07-09 | KV autoscaler runtime observability | `sha256:779d5663643e` | `03cf5015` | portable + ROCm | `/health.decode_runtime.kv_autoscaler` | passed | Health exposes the resolved default/off state and bounded reason without inferring it from ambient environment |
| 2026-07-09 | Safe memory-governor default | `sha256:d4c3f94bdaf3` | `6234e764` | portable + ROCm | memory-governor unit tests and runtime state | passed | Physical reclaim is off by default; on-demand and automatic modes are explicit, strictly parsed, documented, and source-attributed |
| 2026-07-09 | Zero-yield ROCm pool-trim avoidance | `sha256:456146c5693c` | `afd40ee3` | ROCm/gfx1151 | `rocm_trim_pool` hardware tests | passed | Empty pools avoid device synchronization; trim honors the target, propagates HIP failures, and reports measured reserved-byte deltas with timing and reason |
| 2026-07-09 | Qualification variant binding | `sha256:7c60cce84c78` | `8dd88171` | portable | runner and workload unit suites | passed | Runner-owned variant identity is injected into each case, cannot be overridden by a manifest, and is recorded in the run configuration |
| 2026-07-09 | Response backpressure observability | `sha256:29b38d21102e` | `2529652b` | portable | batching-engine snapshots, debug state, and Prometheus metrics | passed | Monotonic counters distinguish channel saturation, actual wait, stall eviction, and closed receivers; exact structured events and 23 focused tests passed |
| 2026-07-09 | ROCm graph execution observability | `sha256:be0bdd7d1bef` | `15ee1f33` | ROCm/gfx1151 | `/health.decode_runtime.rocm_graphs` | passed | Health distinguishes requested, armed, and effective capture state; lifetime capture/replay outcomes and live graph count cover all decode APIs; ROCm-feature server check passed |
| 2026-07-09 | Backpressure health contract | `sha256:613bf86ee1d9` | `5db2ef19` | portable | `/health.decode_runtime.batching_engine` | passed | Health exposes the same saturation, wait, eviction, and closed-receiver counters as debug state and Prometheus; focused serialization test and formatting passed |
| 2026-07-09 | Server-side streaming token timing | `sha256:9a085c4c74aa` | `ee163c2f` | portable + ROCm serving path | opt-in `kiln.token_timing` SSE records | passed | Actor-ready timestamps survive response-channel waits; explicit performance streams expose request-relative ready and handler queue timing; 22 batching tests, focused SSE test, and server check passed |
| 2026-07-09 | Accepted-socket transport control | `sha256:9d68b2504fe4` | `115c1b18` | portable + ROCm serving path | `server.http_send_buffer_bytes`, `/health`, and debug state | passed | Strict bounded `SO_SNDBUF` opt-in is applied and read back on every accepted socket; focused config, API, loopback, portable, and ROCm-feature checks passed |
| 2026-07-09 | Local qualification operator guide | `sha256:9d68b2504fe4` | `f97f276b` | local hardware | `docs/qualification.md` | passed | Clean-tree setup, workload execution, strict validation, comparison, artifact policy, and receipt check-in are documented for new machines |
| 2026-07-09 | Pre-readiness socket-buffer proof | `sha256:9eae13441588` | `689ba61e` | portable + ROCm + Vulkan | listener preflight and accepted-socket regression tests | passed | Platform-normalized efficacy is proven before readiness; requested, kernel-readback, and effective bytes are retained; invalid clamp repro exits without a Ready line |
| 2026-07-09 | ROCm mixed-load qualification contract | `sha256:9ba9a989d350` | `7c3ded29` | Strix Halo ROCm target | `qualification/workloads/serving-mixed-rocm-v1.json` | passed | Four source-bound A/B arms cover batching, long prefill, cancellation, attributed socket pressure, actor-ready timing, graph execution, memory, and fail-closed policy checks; 29 focused and 169 total qualification tests passed |
| 2026-07-09 | ROCm default-arm graph warmup reproducer | `sha256:9ba9a989d350` | `7b49c236` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t005326407514z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | Built-in prewarm captured a graph, but four bounded request warmups produced no successful replay; the harness stopped before measurement instead of accepting unproven graph execution |
| 2026-07-09 | Production ROCm greedy graph routing | `sha256:c62f752dcd12` | `762804f5` | portable + ROCm | `GreedyBatchRoute` regression and backend capability contract | passed | Enabled single-row HIP replay now preempts the generic BF16 greedy path; graphs-off, non-ROCm, sampled, and multi-row routes retain their prior behavior |
| 2026-07-09 | Finished ROCm graph-owner cleanup | `sha256:2d7206487052` | `aafe39a6` | portable + ROCm | graph-owner unit and resource-concurrency contract tests | passed | Every batching-engine completion route releases the row's captured graphs and continuity timeline before fallible finish work, preventing the bounded graph cache from filling with dead request owners |
| 2026-07-09 | Direct ROCm graph-owner isolation | `sha256:5a7d09e0b256` | `ee058502` | portable + ROCm | allocator, owner-isolation, lifecycle-contract, and ROCm-feature server checks | passed | Direct and batched decode share a fail-closed process-wide ID namespace; every direct generation holds a scoped owner lease, all ROCm bs=1 graph APIs require a concrete owner, and recycled KV blocks cannot cross request timelines |
| 2026-07-09 | ROCm graph-owner lifecycle observability | `sha256:c70fe7a869ab` | `b6082716` | portable + ROCm | model counters, health serialization, lifecycle-contract, and ROCm-feature server checks | passed | Health reports live tracked owners plus monotonic owner and graph-release counts; bounded debug events bind owner start, recycled block identity, cleanup, and direct receiver loss to one row ID |
| 2026-07-09 | ROCm default-arm cancellation-reader reproducer | `sha256:c70fe7a869ab` | `b6082716` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t013325203346z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | Graph warmup captured and replayed without graph, resize, reclaim, trim, device-fault, or OOM events; after five seconds without a cancellation-stream byte, Python's buffered `HTTPResponse` became permanently unreadable after its first socket timeout, so the harness failed before it could report mixed-load metrics |
| 2026-07-09 | Socket-readiness qualification stream prototype | `sha256:42578a58f034` | `0a42c98d` | portable | mixed-load stream-reader and cancellation regressions | passed | Socket polling avoided retrying a poisoned timed-out reader and passed 34 focused plus 174 total qualification tests; the following real-device receipt exposed the missing already-buffered-body case |
| 2026-07-09 | ROCm buffered-body readiness reproducer | `sha256:42578a58f034` | `0a42c98d` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t024639147403z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | Server prewarm and native graph capture completed, but the first harness fix polled only kernel socket readiness and therefore starved SSE bytes already buffered by `HTTPResponse` while the keep-alive socket was idle; the bounded warmup deadline rejected the run |
| 2026-07-09 | Buffer-aware qualification stream reads | `sha256:bdaac8b3b632` | `e5587c0a` | portable + loopback HTTP | buffered-body, socket-readiness, deadline, cleanup, and cancellation regressions | passed | Each read drains `HTTPResponse`'s existing buffer before polling the socket and uses only the final request deadline for an actual blocking read; a real chunked keep-alive regression plus 36 focused and 176 total qualification tests passed |
| 2026-07-09 | ROCm batching-actor control-plane starvation | `sha256:bdaac8b3b632` | `e5587c0a` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t025524984730z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | Mixed load drove actor-backed health and metrics requests to 2.0-4.1 second latency before a five-second snapshot timeout aborted measurement; seven normal streams had emitted only 64-70 of 128 tokens after 30 seconds, while logs contained no resize, reclaim, trim, graph-failure, device-fault, or OOM event |
| 2026-07-09 | Nonblocking batching control-plane snapshots | `sha256:384bce9fb516` | `728db9b6` | portable + ROCm | blocked-forward, API, metrics, concurrency-contract, and backend compile checks | passed | Health, metrics, and debug read a timestamped actor-published cache while strong snapshots retain their ordering barrier; in-flight batch state is published before GPU work and `snapshot_age_ms` exposes staleness without rescanning prefixes on the token hot path |
| 2026-07-09 | ROCm mixed-load actor-stall baseline | `sha256:384bce9fb516` | `728db9b6` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t031209606216z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | The complete run measured p99/p99.9 ITL 4.22s/9.16s, p99 TTFT 31.8s, 33 unexplained gaps, and 23.3 tok/s despite 0.13ms p99 response-queue delay; graphs achieved 1 capture plus 45/45 replays with zero failures, KV stayed at 4096 blocks, reclaim stayed off, and the healthy pressure peer made no progress during the actor's attributed 2.012s slow-client wait |
| 2026-07-09 | Typed stream-stall startup policy | `sha256:b37a0b1d7fb9` | `cabea929` | portable + ROCm | config, batching actor, health/debug, and qualification attestation | passed | `server.stream_stall_grace_ms` is strictly parsed in 10..=2000 ms with default/config-file/environment provenance, injected immutably into the actor, and enforced against elapsed wall time; health/debug and the ROCm harness attest the effective value/source. All-target portable and ROCm checks, 23 actor tests, 30 config tests, 17 resource contracts, and 36 qualification tests passed; delivery remains actor-local and is the next checkpoint |
| 2026-07-09 | Off-actor response delivery | `sha256:a913bb0d2084` | `601a284f` | portable + ROCm | fair delivery worker, batching actor, health/debug/Prometheus, and lifecycle contracts | passed | Generation-keyed lanes preserve token/terminal order and continuous wall-clock grace without blocking peer decode or control commands; atomic result cohorts preserve sustained batch width, and cancellation-after-retirement plus concurrent-stop races are covered. All 654 server library tests, 18 resource contracts, 29 actor tests, 23 worker tests, and portable plus ROCm all-target checks passed |
| 2026-07-09 | ROCm off-actor-delivery hardware rerun | `sha256:a913bb0d2084` | `601a284f` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t045133806734z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | The validated clean-tree run removed the prior actor-local 2.012s delivery wait and reduced unexplained ITL gaps from 33 to 14, but the slow stream reached only 63 client-visible tokens and never filled its response channel, so the pressure-peer gate was not exercised. P99 ITL regressed 4.22s to 4.50s, p99 TTFT 31.8s to 38.9s, throughput 23.3 to 19.9 tok/s, and max batch 8 to 7; graphs completed 3/3 measured captures and 30/30 replays with no failures, while KV stayed fixed and reclaim stayed off |
| 2026-07-09 | Admission delivery publication barrier | `sha256:f613e0774f61` | `4456dd86` | portable + ROCm | delivery-worker watermark barrier, delayed eight-row admission regression, lifecycle contracts, and independent concurrency review | passed | The actor now waits for every first-token delivery result produced by one serial admission cohort to be published, then drains the complete FIFO result prefix before selecting decode rows. Healthy acknowledgements therefore become schedulable together while full lanes remain isolated; failed publication or a missing acknowledgement stops the actor within 5s. All 656 server library tests, 18 resource contracts, 29 actor tests, 25 worker tests, formatting, and portable plus ROCm all-target checks passed |
| 2026-07-09 | ROCm admission-barrier hardware rerun | `sha256:f613e0774f61` | `4456dd86` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t051840050197z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | Batch width and throughput recovered from the off-actor-delivery regression to max 8, mean 4.846 rows/forward, and 23.85 tok/s. The slow lane now saturated at sequence 127 and was evicted after 2.009s, but its pressure peer emitted no actor-ready token through that window and had 32.68s TTFT; p99/p99.9 ITL reached 8.28s/10.68s with 16 unexplained gaps. Graphs captured once and replayed 45/45 without failure, KV stayed at 4096 blocks, and reclaim and resize stayed inactive, isolating actor-owned preparation/prefill as the next scheduling target |
| 2026-07-09 | Backend-owned external-yield synchronization | `sha256:26723e522cfa` | `f96e5741` | portable + Strix Halo ROCm/Vulkan; CUDA/Metal contract | backend runtime facet, generated capability report, real device smoke tests, and all-target compile gates | passed | A required backend facet now drains the full CUDA context, ROCm device, Metal companion queue, or VulkanBackend-owned queue before an external scheduler may publish progress or recycle state; synchronization failure is explicitly quarantine-worthy. Metal terminal status and Vulkan sticky device loss fail closed. All 338 model tests and 52 backend contracts passed, the ROCm and Vulkan boundary tests ran on this host, and portable, ROCm, and Vulkan server all-target checks passed; CUDA and Metal hardware execution remain assigned to their target machines |
| 2026-07-09 | Immutable prefix-cache block reuse | `sha256:cc754bd8dc77` | `f4956fc5` | portable + ROCm + Vulkan | model validation, cache selection/registration, legacy-entry compatibility, and real batching integration | passed | Only block-aligned, shape-valid KV tables can cross request boundaries; non-aligned exact entries are neither produced nor retained, manually supplied unsafe hits are rejected in every exact-capable model path, and lookup skips legacy unsafe candidates before ranking so the longest safe strict prefix wins. An identical 81-token retry reproduced the first token, allocated a private suffix block, retained only the five complete cached blocks, and released the suffix on finish. All 339 model and 658 server library tests, focused integration tests, formatting, and portable, ROCm, and Vulkan server all-target checks passed |
| 2026-07-09 | Failure-atomic compatible prefix lookup | `sha256:9d147fe68f9d` | `eb386f41` | portable + ROCm + Vulkan | exact-hit sampling compatibility, strict-prefix fallback, injected snapshot failure, and all-target compile gates | passed | Exact cached logits remain reusable for sampled requests, while cached greedy tokens are eligible only for effectively greedy requests and cannot hide a shorter compatible strict prefix. State snapshots complete before hit counters, LRU order, or active-use pins mutate; an injected Vulkan-storage copy failure proved all accounting remains unchanged. All 11 focused prefix-cache tests and 660 server library tests passed, along with formatting and portable, ROCm, and Vulkan server all-target checks |
| 2026-07-09 | Atomic prefix-cache admission | `sha256:f214c03d9215` | this commit | portable + ROCm + Vulkan | overlap-capacity, pinned-LRU, duplicate-page, refcount, and all-target compile gates | passed | Registration now simulates the complete final refcount union and every required LRU removal before mutating resident state. Intrinsically oversized and duplicate-page tables are rejected, a late pinned-entry blockade cannot leave partial evictions, and successful shared-prefix admission preserves exact refcounts with disjoint retained and released block sets. Six focused adversarial tests and all 662 server library tests passed, along with formatting and portable, ROCm, and Vulkan server all-target checks |
| 2026-07-10 | Leased prefix ownership and generation fencing | `sha256:da76c63f4cc7` | this commit | portable + CPU integration | move-only request/provisional leases, tombstones, generation fences, and real batching integration | passed | Prefix sources are pinned before snapshot copies; hit counters and LRU commit only after success, while partial snapshot failure settles under the GPU permit and quarantines. Clear and purge hide live entries and defer reclamation; cache-local generations reject late registrations. This supersedes the earlier pre-pin snapshot ordering. All 669 server library tests and the real CPU multi-turn integration passed |
| 2026-07-10 | Settled paged streaming and GPU synchronization | `sha256:da76c63f4cc7` | this commit | portable; ROCm/Vulkan compile gates | worker finalizers, movable permit/writer exclusion, health/debug/Prometheus, SSE, reservation, and cache-owner contracts | passed | Paged workers run their sole cleanup before `Done` or `Error`. Decode panic or external-yield sync failure retains unknown GPU state/read ownership and latches quarantine for covered completion APIs; shared KV pages recycle only after settlement. SFT, including CUDA-native dispatch, uses the per-step writer. Bounded sync telemetry is exported, prefill and explicit worker failures emit structured errors, and probe-only completion cannot replace a concurrent cache owner. All 347 model and 669 server library tests, 19 concurrency contracts, formatting, and portable, ROCm, and Vulkan all-target checks passed |
| 2026-07-10 | Fallible streaming text and fail-closed terminals | `sha256:fdba4c099963` | this commit | portable; ROCm/Vulkan compile gates | injected decoder faults, saturated SSE queue, abrupt producer drop, tail salvage, and independent review | passed | Incremental decode and flush errors retain their offsets, roll back the accepted token, and propagate through model and batching streams instead of becoming empty text. Explicit worker failure, missing terminal events, timeout, and an unexpected producer drop emit a structured `generation_error` followed by `[DONE]` after queued deltas drain, even when the event queue is full; recent-request records preserve explicit error/timeout classification. Legacy detokenizer exits settle before page release. All 349 model and 670 server library tests, 19 concurrency contracts, formatting, and portable, ROCm, and Vulkan all-target checks passed; direct cancellation/deadline propagation remains the next checkpoint |
| 2026-07-10 | Settled direct-stream cancellation and deadlines | `sha256:8080e47d551d` | this commit | portable + CPU integration; ROCm/Vulkan compile gates | tiled-prefill/token cancellation, explicit worker settlement, panic ownership, deadline races, saturated terminal queues, and independent review | passed | Direct prefill, token enqueue, worker settlement, and success publication share one remaining absolute deadline. Timeout and disconnect signal cancellation, drain the sole bounded async bridge, and wait for explicit cleanup or intentional quarantine; terminal SSE events cannot be blocked behind the ordinary delta queue. Prefill and prefix-snapshot panics retain GPU/request ownership and quarantine; producer unwind cancels detached work, and racing progress cannot resurrect its gauge. Direct speculative streams temporarily fall back to documented single-token decode. All 354 model and 684 server library tests, the real CPU streaming integration, 19 concurrency and 3 timeout/drain contracts, the desktop default regression, formatting, and portable, ROCm, and Vulkan all-target checks passed; ROCm/Vulkan results here are compile gates, not runtime hardware evidence |
| 2026-07-10 | Settled legacy mutable paged streaming | `sha256:84c3cbb0f641` | this commit | portable + CPU integration; ROCm/Vulkan compile gates | exclusive reservation, error/receiver-drop/panic outcomes, owner retention, source invariant, and public API integration | passed | The earlier latent compatibility item was closed after the active direct-stream path. The synchronous mutable-`BlockManager` API and its shared CUDA-graph compatibility branch now allocate one retain-by-default reservation and converge every post-allocation exit through one backend-settlement epilogue. Success or settled error releases pages; failed or panicked settlement retains pages, linear state, logits, and result while releasing the CUDA coordination mutex. The API is documented as returning an already-populated receiver and deprecated toward threaded settlement. All 359 model tests, four focused reservation/panic tests, the source invariant, and a deterministic real CPU public-API integration passed, along with formatting and portable, ROCm, and Vulkan all-target checks; CUDA graph hardware execution remains assigned to the CUDA machine |
| 2026-07-10 | Fallible prompt-logprob token display | `sha256:25cce8ae2883` | this commit | portable; ROCm/Vulkan compile gates | strict token-ID mapping, injected decoder failure, special-token display, HTTP vocabulary-drift response, and independent protocol review | passed | Prompt-logprob display rendering is fallible through both mock and real response paths. Unknown tokenizer IDs and decoder faults return contextual HTTP 500 `tokenization_error` responses instead of successful empty fields, while known special tokens retain their literal vocabulary text. All 73 runnable core and 686 server library tests passed, along with formatting and portable, ROCm, and Vulkan all-target checks. The audit also narrowed the public claim to a vLLM-shaped subset and recorded numerical integrity, K/K+1 rank semantics, strict remote parsing, and tokenizer-identity handshake as explicit follow-up gates; ROCm/Vulkan results here are compile gates, not runtime hardware evidence |
| 2026-07-10 | Fail-closed prompt-logprob numerics | `sha256:c00979909bae` | this commit | portable + Strix Halo ROCm/Vulkan; CUDA deferred | exact-width and finite-value validation, deterministic K selection, fused log-softmax parity, wave/width sweep, and independent review | passed | Every local forward row, including the unused final row, is validated before ranking, decoding, or JSON; corruption returns `generation_error`, and ties resolve by token ID with exact K cardinality. CUDA/ROCm use one same-dtype output allocation and F32 fused accumulation instead of softmax-then-log or full-vocabulary F32 temporaries. All 981 tensor and 690 server library tests passed, as did portable, ROCm, and Vulkan all-target checks. Real ROCm qualification covered F32/BF16/F16 extremes, special values, multiple rows, wave boundaries, the strided path, widths through 248,320, and legacy softmax parity; real Vulkan covered extreme-range host-fallback parity. CUDA is source-mirrored but was neither compiled nor executed on this host and remains assigned to the CUDA-machine gate |
| 2026-07-10 | vLLM prompt-logprob semantics and settled scoring | `sha256:2bf0182153d4` | this commit | portable + Strix Halo ROCm/Vulkan; CUDA/Metal deferred | vLLM `7614b88ebdd9` protocol cross-check, scorer lifecycle and panic ownership, fused log-softmax, non-destructive Vulkan prewarm, deterministic real endpoints, and independent review | passed | Position zero is null; row `i-1` scores observed token `i`; K=0 returns only the observed token, and K>0 returns K or K+1 entries with full-vocabulary ranks and contextual byte fallback. One absolute deadline now covers admission, runner-owned backend execution, bounded LM-head chunks, rendering, cancellation settlement, and quarantine-worthy panic/synchronization failure. Portable suites passed (77 core with 3 ignored, 359 model, 703 server, 985 tensor, and 12 real-model integrations with 1 ignored), strict ROCm and Vulkan log-softmax hardware tests passed, and portable/ROCm/Vulkan server all-target checks passed. Real ROCm served five successful Qwen3.5-4B requests. The final production-default Vulkan build completed immutable weight prewarm in 62.5s instead of the prior destructive 211.5s path, then served 9/9 raw-ID and text requests with byte-identical repeated K=5, single-row, and text payloads across two restarts; both external-yield boundaries settled 9/9 with zero failures, the backend remained healthy and unquarantined, and no request created a new Vulkan device. The full Vulkan hardware library suite passed 396/396 with eight test threads and all 55 capability contracts passed. CUDA and Metal execution remain assigned to their target machines; the current scorer deliberately uses correctness-first O(TV) host ranking pending the checked device-side top-K gate |
| 2026-07-10 | Strict remote-teacher scoring and cache contract | `sha256:9df21078028f` | this commit | portable + Strix Halo Vulkan; ROCm compile; CUDA/Metal deferred | 326 train and 728 server library tests, 18 API integrations, strict vLLM fixtures, cache/alignment adversarial tests, atomic queue-admission races, real Qwen3.5-4B Vulkan scoring smoke, and two independent blocker reviews | passed | The only admitted remote protocol is vLLM numeric-ID `prompt_logprobs`; URL/auth/body/envelope/model/usage/single-choice/cardinality/canonical-ID/vocabulary/duplicate/rank/order/tie/mass checks fail closed, using source F64 values before F32 conversion. Logits row `p` is read from response target `p+1`, including an appended final-row probe, while OPD action target `q` uses causal logits row `q-1`; every complete batch is validated before cache writes, duplicate/out-of-order requests retain caller order, and v2 cache paths bind exact configured identity without traversal or digest-prefix collisions. Unsupported loss/stability/clipping/cost/provider/source combinations now fail before work; all submit surfaces share structural admission, effective K is reported, and multi-job publication is capacity-atomic against concurrent single submissions. The live smoke queried three rows at K=5 through kiln's strict client against kiln's real Vulkan vLLM-compatible endpoint after 63.4s immutable prewarm; it is not evidence from an actual vLLM process. Authoritative remote tokenizer/base/adapter revision is the next gate; local `model_id` still names the served base rather than loading an independent teacher, cache file replacement is not yet crash-atomic, and CUDA/Metal hardware execution remains deferred |
| 2026-07-10 | Cross-runtime tokenizer vocabulary identity | `sha256:a9c8195a72c2` | this commit | portable | complete numeric vocabulary mapping, added/special tokens, insertion-order invariance, mutation separation, and Python-compatible golden vector | passed | `KilnTokenizer` now hashes every token/ID pair with a versioned, length-framed binary contract rather than treating tokenizer JSON or a caller-supplied label as proof that raw numeric IDs mean the same thing. All 81 runnable core tests passed, including the exact two-token golden digest; this is the first independently published part of the authoritative remote-teacher identity checkpoint, not completion of that gate. |
| 2026-07-10 | Loader-owned base-model content revision | `sha256:d21323218f37` | this commit | portable | exact memory-mapped shard bytes, path/order invariance, byte and multiplicity separation, dense and GPTQ loader propagation | passed | The loader now publishes a domain-separated SHA-256 revision derived from the exact read-only shard mappings it parsed, so later remote-teacher identity cannot substitute a path, alias, mtime, or racy second disk scan for the weights actually loaded. All 362 model library tests passed; runtime identity publication and per-response pinning remain in progress. |
| 2026-07-10 | Canonical teacher identity contract | `sha256:7836d6804d93` | this commit | portable | 12 strict schema, canonical JSON, bounded base64url, digest, field mutation, adapter, duplicate/unknown-key, and malformed-input tests | passed | A dependency-free `TeacherIdentityV1` now binds the served model, exact base and optional adapter content, numeric vocabulary and tokenizer config, context/K/vocab bounds, raw-logprob protocol, implementation, and inference configuration into one canonical fingerprint. Ordinary aliases and stock vLLM fingerprints cannot parse as this identity; network discovery, per-response enforcement, and cache/receipt integration remain in progress. |
| 2026-07-10 | Post-upload model-source mutation guard | `sha256:aef49c5beeef` | this commit | portable | retained mappings/file descriptors, unchanged acceptance, same-length mutation, truncation, and unused-shard mutation tests | passed | The base revision now retains every exact loader mapping and open file until a bounded post-upload verification can recompute the revision. This closes the hash-before-copy race, does not trust paths or mtimes, and detects truncation without touching an invalid mmap. All 366 model tests and the model all-target check passed; the server startup call site lands with identity publication. |
| 2026-07-10 | Immutable vLLM teacher launcher | `sha256:802a02c22492` | this commit | portable; CUDA runtime deferred | 28 focused launcher tests, 204 qualification tests, Rust-compatible base/tokenizer golden hashes, canonical identity round trips, static-adapter revision and rank validation, override rejection, and direct `execve` construction | passed | `scripts/vllm_teacher.py` now computes an exact base/tokenizer/optional-adapter identity before launch, owns every identity-bearing vLLM option, disables dynamic LoRA and resolver plugins, and publishes the canonical fingerprint through vLLM's native custom-fingerprint option. Dry-run and manifest-only modes are dependency-free and redact credentials. vLLM and Transformers are not installed on the Strix Halo host, so an actual vLLM process remains a CUDA-machine handoff rather than claimed local evidence. |
| 2026-07-10 | Authoritative teacher identity and immutable logit provenance | `sha256:a167c72405c5` | this commit | portable; CUDA vLLM runtime deferred | exact workspace CI command, 763 server library tests, 364/365 train tests, 55 backend capability contracts, 7 remote identity integrations, 231 qualification tests, all-target model/server checks, formatting, and diff hygiene | passed | Remote registration performs a bounded two-probe identity handshake and every response, fixture row, cache entry, export, queue binding, receipt, and local adapter load is tied to the exact base/tokenizer/config/runtime/optional-adapter revision. Base models and vLLM model/adapter inputs load from private immutable snapshots; remote logit materialization happens before GPU ownership; legacy identities and cache imports fail closed. Fixed-fixture partial configs now retain their off-policy default and canonical submit-time LoRA validation. The prior CI regression in run `29113435875` was reproduced and repaired along with stale identity fixtures and brittle source-contract markers. No actual vLLM process was available on this host, so CUDA execution remains a handoff gate. |
| 2026-07-10 | Process-wide backend quarantine admission | `sha256:51d43132010a` | this commit | portable + CPU integration; accelerator runtime behavior inherited from the quarantine latch | full workspace tests, retained-owner writer and adapter-barrier races, training admission/queued-transition integrations, 231 qualification tests, all-target server check, formatting, and diff hygiene | passed | The process-lifetime health latch now gates completion/eval workers, prompt scoring, adapter barriers and disk mutations, startup prewarm, every training API and central publication transaction, queued-to-running transitions, memory preparation, job-wide GRPO/OPD/distillation writers, and each SFT step. Writers poll health instead of blocking behind intentionally retained unknown GPU ownership; APIs return 503 `backend_quarantined`, queued jobs become failed with webhook/metric accounting, health/readiness and Prometheus remain restart-required, and no reset endpoint exists. CI run `29120241674` exposed a coarse-filesystem same-length mutation hole in qualification model fingerprinting; every opened input is now content-verified by a second read and all 231 qualification tests pass deterministically. |
| 2026-07-10 | Revision-bound loaded adapters and inference caches | `sha256:12396b785a58` | this commit | portable + CPU integration | full workspace tests, 772 server library tests, 370 model tests, 17 real-model integrations with 1 ignored, 231 qualification tests, all-target server check, formatting, and diff hygiene | passed | The loader derives one canonical config/weights revision and publishes `{name, content_revision}` with the runner flip. Actor queues, direct inference, prefix KV, all deterministic cache keys and owner claims, purge generations, and response headers bind that exact tuple; stale queued work fails before prefill, direct swaps cannot cross request ownership, and late or revoked owners cannot resurrect purged results. A real loader/API test proves the wire revision, published state, and runner source identity are identical. |
| 2026-07-10 | Serialized adapter mutation and revision publication | `sha256:8e4d9d5842f5` | `ec0e4823..95a4c2f5` | portable + CPU/mock integration | 779 server library, 366 train with 1 ignored, 13 composition, 9 upload, 7 merge, 2 registry, and 19 concurrency tests; all-target server check; docs smoke and release checks | passed | Upload, merge, delete, gate demotion, training publication, per-request composition, and live weight transitions use one health-checked mutation contract. Publishers stage hidden outputs and rename atomically; training snapshots its start, compare-and-publishes the target revision, preserves intervening winners, reloads a loaded ungated same-name target inside the drained barrier, and rejects a loaded gated rewrite before GPU work. Delete and upload reject physically loaded content. Composed cache identities include exact source revisions, one guard covers synthesis through protected eviction, and CPU-heavy hashing/merge work stays off async workers. |
| 2026-07-10 | Token-budgeted prefill and fair mixed admission | `sha256:22a66f8622c` | `4103074b` | portable + Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260710t231446356203z-rocm-strix-halo-prefill-scheduling-v1-f397c29fc3-v1.json` | passed | A 32-token actor-cycle budget advances an eight-token short decode while literal 1K and 16K prefills progress round-robin, then cancellation releases the long row. The real-device deterministic nonzero GDN-plus-full-attention proof matched monolithic prefill across six quanta `[17, 17, 17, 17, 12, 1]`, including recurrent and conv state, the block-aligned split snapshot, first and following decode tokens, and KV-block release. All 3 required qualification cases passed with zero assertion failures or skips; 231 qualification-tooling tests and 19/20 runnable real-model integrations also passed. The paired Vulkan arm remains an explicit next-machine-order rerun, not claimed by this receipt. |
| 2026-07-10 | Vulkan bounded-prefill device-mismatch reproducer | `sha256:22a66f8622c` | `323c4e1e` | Strix Halo Vulkan/RADV | `qualification/receipts/vulkan/strix-halo/20260710t231551307388z-vulkan-strix-halo-prefill-scheduling-v1-6899c96516-v1.json` | failed | Device discovery and the literal short-decode/1K/16K actor contract passed, but the real hybrid-model prefill failed closed before measurement because a full-attention elementwise multiply received `vulkan:0 F32 [80,2]` and `cpu F32 [80,2]` inputs. The receipt contains zero skips or unsupported items and is retained as the exact clean-source reproducer for the following backend fix. |
| 2026-07-10 | Paired production-path bounded-prefill qualification | `sha256:c308dc525215` | `d845d402..15a547df` | Strix Halo ROCm/gfx1151 + Vulkan/RADV | `qualification/receipts/rocm/strix-halo/20260710t233006344028z-rocm-strix-halo-prefill-scheduling-v1-f397c29fc3-v1.json`; `qualification/receipts/vulkan/strix-halo/20260710t233042331023z-vulkan-strix-halo-prefill-scheduling-v1-6899c96516-v1.json` | passed | The superseding Vulkan fixture matches serving ownership: deterministic BF16 model tensors and the canonical paged pool remain CPU-backed while the Vulkan runtime seeds its separate resident cache at decode entry; the attention-output gate is enabled as in Qwen3.5. This proved monolithic versus `[17, 17, 17, 17, 12, 1]` resumable prefill, recurrent and conv state, the position-80 snapshot, first token, the following native resident decode token, and block release. The earlier device mismatch was therefore a fixture-contract error, not a production runtime defect, and no speculative RoPE transfer change was retained. Both clean-source receipts passed all three required cases with no skips or assertion failures; `compare_receipts.py` reported compatible cross-backend correctness, no rejected differences, and exact equality for every required metric. |
| 2026-07-10 | Stable runtime mutation guards | `sha256:229d9098f3c0` | this commit | portable | server/model checks, two profile graph-policy tests, and real CPU paged-cache resize rejection | passed | Production startup now resolves graph eligibility once from the immutable serving profile and passes explicit CUDA, ROCm, and Metal options into the runner. Stable and maintenance profiles cannot live-capture graphs; stable construction does not register allocator reclaim hooks, retry allocation through reclaim, start KV autoscaling, or permit the actor's physical resize operation. The compatibility constructor also resolves to stable. The real paged-cache test proves a rejected resize leaves capacity unchanged, and the profile tests prove experimental remains the only live-graph route. This is a portable enforcement checkpoint; adapter/training admission, health provenance, documentation, and the real ROCm stable-profile trace remain open. |
| 2026-07-10 | Stable adapter and training admission | `sha256:c7eb316af477` | this commit | portable + real CPU integration | 786 server library tests and 23/24 real-model integrations | passed | Every real-backend training API and central batch publication now returns `409 serving_profile_conflict` in stable mode, while the queued-to-running transition independently finalizes injected or restored jobs before memory reservation, reclamation, or GPU coordination. Every live adapter flip is guarded inside the swap primitive before disk-to-device loading and the actor barrier; load/unload endpoints reject earlier, while true no-op selections remain available. Three retained-inference-owner tests prove adapter admission, training admission, and queued-worker rejection complete without waiting for or retaining the actor-wide writer. Intentional adapter/training tests now opt into the experimental profile explicitly. |
| 2026-07-10 | Maintenance inference drain enforcement | `sha256:9bd7972295c1` | this commit | portable + real CPU integration | 788 server library tests and 24/25 real-model integrations | passed | Maintenance is a restart-only drain boundary: chat, text, batch, prompt-logprob, and agent-run admission return `503 inference_disabled_by_profile`; inference prewarm is suppressed; all eval publications are rejected before queue/tracking insertion; injected eval work becomes a fully archived failed job before its generator is invoked; and the live generator independently checks before adapter selection and generation. A real-backend test holds the exclusive GPU owner while chat and agent requests return promptly, proving they never wait for an inference read owner. Maintenance still performs a real adapter load and publishes its exact content revision, demonstrating that drained exclusive operations remain available. Health provenance and permanent operator documentation follow in the next checkpoint. |
| 2026-07-10 | Serving-profile diagnostics and operator contract | `sha256:ec21510b0e58` | this commit | portable + real CPU integration | 791 server library tests, 24/25 real-model integrations, all-target server check, formatting, shell syntax, and diff hygiene | passed | One typed resolution report now drives startup diagnostics, `/health`, `/v1/health`, and `/v1/config`, publishing the selected profile, default/file/environment provenance, request-override prohibition, and every effective ownership policy. Maintenance reports a healthy backend separately while readiness returns 503 with `status=maintenance`; stable governor health distinguishes requested reclaim from the profile-forced effective `off`. The permanent policy matrix and restart-only drain runbook are linked from the README, quickstart, training/eval guides, example config, and demo scripts; interactive training demos opt into experimental while inference-only demos retain stable. The stable process still admits logical inference scheduling, cancellation, eviction, and prefix reuse while all physical mutation gates remain closed. |
| 2026-07-10 | Stable ROCm mixed-load qualification contract | `sha256:72bc152f7931` | this commit | portable; Strix Halo ROCm execution follows from the clean commit | 232 qualification-tooling tests, strict workload validation, Python compilation, and diff hygiene | passed | The four historical autoscale/graph A/B arms now pin `experimental`, preserving their actual dynamic-runtime semantics after stable became the default. A distinct `stable` arm requests autoscaling, automatic reclaim, and ROCm graphs but requires health to prove that the profile suppresses every effective mutation path. The receipt contract now records backend external-yield synchronization calls, failures, slow calls, total time, and maximum duration by bounded boundary; stable fails on any failed or >=100 ms sync, resize/reclaim/graph event, changed KV capacity, attributed or unexplained ITL outlier, or missing synchronization evidence. This is a committed pre-benchmark harness checkpoint, not device evidence. |
| 2026-07-10 | Stable ROCm padded-vocabulary startup reproducer | `sha256:72bc152f7931` | `6f7b860474a7` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t002223202949z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | Clean-source stable startup loaded and uploaded Qwen3.5-4B, then rejected its legitimate padded model vocabulary: the tokenizer has 248,070 assigned IDs while the embedding/logit width is 248,320. No workload request ran, all dynamic mutation paths were effectively off, and the private model snapshot was cleaned up. The receipt is retained as the exact reproducer for the following identity-contract fix. |
| 2026-07-10 | Padded model-vocabulary identity contract | `sha256:8b49019be45a` | this commit | portable; ROCm rerun follows from the clean commit | 56 vLLM launcher tests, 233 qualification tests, 82/85 runnable core tests, and 793 server library tests | passed | Local and vLLM teacher identities now record the model embedding/logit width while allowing a smaller tokenizer map for reserved padding rows. Both paths still fail closed when the tokenizer entry count exceeds the model width or any assigned numeric ID falls outside it; the vLLM launcher also resolves nested `text_config.vocab_size` and rejects conflicting declarations. Documentation states the distinction explicitly. This portable fix checkpoint does not substitute for the following Qwen3.5-4B ROCm receipt. |
| 2026-07-10 | Stable ROCm post-startup stall and teardown reproducer | `sha256:8b49019be45a` | `1265cfbf5713` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t003239968059z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The padded-vocabulary fix reached readiness and completed ten measured requests with zero request failures, resize, reclaim, or graph activity and fixed 4,096-block KV capacity. All 340 external-yield synchronizations succeeded in 3.709 ms total with a 0.369 ms maximum, ruling out that boundary as the observed pause source. The run still produced 37 unexplained ITL gaps (943 ms p99), 24.16 s pressure-peer TTFT, and no peer progress during the attributed slow-consumer window. The server logged clean SIGTERM shutdown, but its worktree-local 9.3 GB snapshot made the run dirty and teardown exceeded the harness's 10 s grace, leaving one 5.0 GB shard; lifecycle cleanup and scheduling attribution are separate follow-up fixes. |
| 2026-07-10 | Run-owned model snapshot teardown contract | `sha256:102b4e658186` | this commit | portable; Strix Halo ROCm teardown rerun follows | 38 focused mixed-load tests, all 234 qualification tests, strict workload validation, and Python compilation | passed | The mixed-load server now places immutable model snapshots below its ignored per-run directory, grants real-model destruction a bounded 60-second SIGTERM window, records exit code/elapsed time/forced-kill state/residual payload paths, and makes a forced or incomplete payload cleanup fail the case. The harness remains final owner of the run directory after child exit, so even a killed child cannot leave multi-gigabyte worktree state. This fixes evidence contamination and residue recovery only; it does not resolve or waive the measured scheduler stalls. |
| 2026-07-10 | Snapshot payload residue attestation | `sha256:9b79f50c8201` | this commit | portable + Strix Halo ROCm teardown observation | 39 focused mixed-load tests, all 235 qualification tests, and Python compilation | passed | A clean hardware rerun exited the server normally in 264 ms, kept Git clean, and removed both 9.3 GB snapshot payload files; only an empty temporary directory remained until the run owner removed its ignored tree. The lifecycle gate now distinguishes that harmless empty control directory from any file, symlink, forced kill, or nonzero exit, all of which still fail. The intermediate duplicate latency receipt was not retained; the next clean commit produces the source-bound scheduler reproducer. |
| 2026-07-10 | Stable ROCm isolated stall and snapshot-residue reproducer | `sha256:9b79f50c8201` | `aa4c2271b6b9` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t004500068632z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The clean run reproduced 37 unexplained ITL gaps (976 ms p99), 23.45 s pressure-peer TTFT, and no pressure-window peer progress while fixed KV capacity, zero graph/resize/reclaim activity, 0.79 ms p99.9 response-queue delay, and 343 successful external-yield synchronizations totaling 3.489 ms exclude those suspected paths. SIGTERM returned 0 without force in 164 ms and Git stayed clean, but Kiln's silent `TempDir` drop left one 5.3 GB snapshot shard; the run owner removed it. The receipt is the exact common reproducer for explicit snapshot destruction followed by scheduler attribution. |
| 2026-07-10 | Explicit immutable-snapshot destruction | `sha256:e9a27f934dcb` | this commit | portable; Strix Halo ROCm teardown rerun follows | all 371 model library tests, read-only last-owner recovery regression, server all-target check, formatting, and diff hygiene | passed | `ModelSnapshotLease` now explicitly closes its temporary directory at the last source/deferred-MTP owner, emits structured removal timing, and retries three times after restoring private owner deletion permissions. Exhaustion emits an error with the exact residual path instead of silently accepting `TempDir` best-effort failure. Callers use the lease path accessor, and operator documentation describes cleanup recovery. This portable checkpoint requires the following full-size snapshot hardware proof. |
| 2026-07-10 | Explicit snapshot-drop hardware counterexample | `sha256:e9a27f934dcb` | `90880ea18354` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t005129233232z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The full-size run proved destructor cleanup is not a sufficient server guarantee: SIGTERM returned 0 without force in 164 ms, but no lease cleanup event appeared and one 5.3 GB shard remained for the run owner to remove. The same run reproduced 42 unexplained ITL gaps (949 ms p99, 1.05 s p99.9), 25.44 s p99 TTFT, and no pressure-window peer progress while synchronization totaled only 3.550 ms and mutation paths stayed inactive. Cleanup ownership must move to an explicit drained server-shutdown handle; the destructor remains only a fallback. |
| 2026-07-10 | Drained server snapshot cleanup ownership | `sha256:cc3a299adce5` | this commit | portable; Strix Halo ROCm teardown rerun follows | all 372 model library tests, five focused snapshot tests, server all-target check, formatting, and diff hygiene | passed | The loader now returns an opaque idempotent cleanup handle that does not keep mapped weights alive. Real server startup retains it, and shutdown invokes it only after Axum and the batching engine drain; any residual cleanup error makes shutdown nonzero with the exact path. Lease drop remains fallback coverage for startup failures and library use. The tests prove explicit cleanup while other lease owners remain, repeated cleanup, last-owner cleanup, and read-only recovery. Full-size hardware remains the acceptance gate. |
| 2026-07-10 | Drained snapshot cleanup hardware proof and latency baseline | `sha256:cc3a299adce5` | `6d1e45095174` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t005850623279z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The explicit drained owner removed the full 9.3 GB snapshot in 225 ms before `server stopped cleanly`; SIGTERM returned 0 without force in 414 ms, snapshot residue was empty, and Git stayed clean. With lifecycle contamination eliminated, the only gate failures were no pressure-window peer progress and 30 unexplained ITL gaps (823 ms p99, 955 ms p99.9); 379 synchronization calls totaled 4.402 ms with a 0.377 ms maximum, response-queue p99.9 was 0.98 ms, and graphs/resize/reclaim remained inactive. This is the clean source-bound scheduler baseline. |
| 2026-07-10 | Idempotent snapshot fallback diagnostics | `sha256:a31d22962ad9` | this commit | portable | five focused snapshot lifecycle tests, formatting, and diff hygiene | passed | After the drained shutdown owner removes a snapshot, the later lease fallback now treats `NotFound` as idempotent success instead of emitting a misleading recovery warning. Cleanup behavior and the hardware latency baseline are unchanged; future shutdown logs contain one authoritative removal event. |
| 2026-07-10 | Batching actor phase attribution | `sha256:8b3683c2f209` | this commit | portable + ROCm compile gate; Strix Halo runtime attribution follows | 34 batching/health/debug tests, Prometheus rendering, 235 qualification tests, portable and ROCm all-target checks, formatting, and diff hygiene | passed | Admission, bounded prefill, and decode forwards now publish cumulative and maximum wall time plus 100 ms slow-phase counters through health, debug state, and Prometheus. Each slow phase emits one bounded structured event with phase/work size; the mixed-load receipt contract records phase deltas and requires event/counter agreement. This passive checkpoint will distinguish the repeated 505-654 ms actor-ready gaps between prefill and decode without logging normal forwards or request content. |
| 2026-07-10 | Stable ROCm actor-phase attribution proof | `sha256:8b3683c2f209` | `d40d02099078` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t011556512375z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The source-bound run converted all 30 unexplained ITL gaps into causal phase evidence: every outlier overlapped a bounded prefill, sometimes followed by a decode, and zero remained unexplained. Admission totaled 3.34 ms with a 0.64 ms max; 43/43 measured prefills crossed 100 ms, totaled 12.88 s, and maxed at 656.6 ms; decode maxed at 157.0 ms. This rules out admission, response delivery (0.36 ms p99.9 queue delay), external-yield sync (3.98 ms total, 0.36 ms max), resize, reclaim, and graphs. The 512-token prefill remainder is monopolizing the single actor between decode cohorts; the next checkpoint must bound prefill independently below the 250 ms ITL gate. Snapshot teardown remained clean and unforced. |
| 2026-07-10 | Independent prefill-cycle ceiling | `sha256:1a7db6a7a002` | this commit | portable + ROCm compile gate; Strix Halo runtime proof follows | 796 server library tests, 235 qualification tests, portable and ROCm all-target checks, formatting, JSON/Python validation, and diff hygiene | passed | A typed `server.max_prefill_tokens_per_cycle` policy now bounds admission plus resumable prompt work independently of the combined actor-cycle budget after ready decode rows reserve their tokens. The stable default is 64 tokens, chosen from the measured 110-135 ms small-prefill floor and 157 ms decode maximum rather than by weakening the 250 ms ITL gate; strict TOML/environment validation, source provenance, startup diagnostics, health/debug state, Prometheus, qualification attestation, operator docs, and a literal short-decode/1K/16K fairness regression all share the same contract. This is a committed scheduler checkpoint, not hardware acceptance; the next clean-source ROCm receipt must still show zero attributed and unexplained ITL outliers plus pressure-window peer progress. |
| 2026-07-10 | Stable ROCm 64-token prefill-ceiling counterexample | `sha256:1a7db6a7a002` | `2bb1e4d8f198` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t013434402779z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The clean source-bound run reduced attributed ITL outliers from 30 to 5 and kept unexplained outliers at zero, but a 249.3 ms late-prompt prefill followed by decode as high as 154.8 ms still produced 322-363 ms gaps. P99/p99.9 ITL fell to 460/568 ms, while the added small-forward overhead reduced throughput to 19.30 tok/s. The slow lane reached backpressure only near the end of its 45-second observation window, so no 2-second eviction or three-sided pressure-peer overlap was proven. All 492 external-yield synchronizations succeeded in 4.995 ms total, response-queue p99.9 was 1.18 ms, mutation paths stayed inactive, and unforced shutdown removed all snapshot payloads in 415 ms. The next clean checkpoint halves the prompt ceiling instead of weakening any acceptance gate. |
| 2026-07-10 | Calibrated 32-token stable prefill ceiling | `sha256:0411e68eb442` | this commit | portable + ROCm compile gate; Strix Halo runtime proof follows | 796 server library tests, 235 qualification tests, portable and ROCm all-target checks, formatting, JSON/Python validation, and diff hygiene | passed | The only scheduler change from the preserved 64-token counterexample is a 32-token stable default, propagated through config, examples, operator docs, and every qualification variant/attestation. The override range remains 1..=65,536 and historical receipts remain immutable. This is a committed calibration checkpoint; hardware must still prove that shorter late-position prefills eliminate the five attributed gaps, allow the slow lane to reach a full two-second eviction window, preserve peer decode progress on both sides of that window, and complete the long prompt within the existing deadline. |
| 2026-07-10 | Stable ROCm 32-token prefill-ceiling counterexample | `sha256:0411e68eb442` | `c47e41ec3c23` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t014234610452z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | Halving the fixed token ceiling is not a viable latency policy. At 32 tokens, one attributed 322 ms gap remained, a late prefill still reached 232.5 ms, and even a three-token prefill took 118.7 ms; fixed layer-launch and sequence-position cost dominate token count. Prefill wall time more than doubled to 57.58 s across 385 calls, throughput fell to 9.55 tok/s, and long-prefill TTFT rose to 77.79 s. The slow lane did finally prove one 2.003 s attributed eviction, but the pressure peer failed during chunked prefill with `hipblasLtMatmulAlgoGetHeuristic returned no algos`, producing the second batching error and preventing peer-overlap acceptance. Unexplained outliers remained zero, response-queue p99.9 was 0.30 ms, mutation paths stayed inactive, and shutdown was clean and unforced in 514 ms. Preserve this as evidence against further blind halving; investigate the ROCm matmul failure and replace the fixed-ceiling calibration with cost-aware or mixed-forward scheduling. |
| 2026-07-10 | Fail-open ROCm BLAS heuristic hardening | `sha256:1f91cfa4b62c` | this commit | portable + Strix Halo ROCm/gfx1151 | 29 ROCm-BLAS unit tests, 985 portable tensor tests, 9 real-device matmul parity tests, portable and ROCm server all-target checks, formatting, and diff hygiene | passed | A hipBLASLt cache miss now searches with the handle's full documented 32 MiB workspace budget instead of accidentally constraining first-use shapes to 1 MiB. If the explicit heuristic still returns no candidate for a valid descriptor, the wrapper executes hipBLASLt's supported implicit/default algorithm, leaves the result uncached, and increments a handle fallback counter rather than failing the request. Terminal errors now include exact M/N/K, logical batch/index, dtypes, and layouts. Real parity covers BF16/F32, transposes, batching, bias, and the pressure peer's 32-token attention sequence plus 14-token `[14,256] x [256,238]` tail. That isolated sweep also passed before the fix, so it narrows but does not falsely claim reproduction; the next full mixed-load run remains the intermittent production-path acceptance proof. |
| 2026-07-10 | Restore measured-better 64-token interim default | `sha256:8bfa8d8cdcaa` | this commit | portable; ROCm policy unchanged pending scheduler replacement | 2 focused server config tests, 39 mixed-load contract tests, formatting, and diff hygiene | passed | The disproven 32-token default is rolled back in config, examples, operator docs, and qualification expectations while its failed receipt remains immutable. This does not claim that 64 tokens meets the final latency gate: its preserved receipt still has five attributed gaps. It simply keeps `main` on the materially better measured interim behavior (19.30 versus 9.55 tok/s, no measured request failure, and 44.27 versus 77.79 s long-prefill TTFT) while the next checkpoint replaces token-only calibration with a scheduler policy that accounts for fixed launch and sequence-position cost. |
| 2026-07-10 | Layer-resumable prefill scheduling | `sha256:d1b00df4a314` | this commit | portable + CPU runtime; ROCm/Vulkan compile gates | 799 server, 372 model, and 235 qualification tests; CPU monolithic/resumable parity and retained-state cleanup; portable, ROCm, and Vulkan all-target checks | passed | The actor keeps the measured-better 64-token chunk but now yields after a typed default of four transformer layers, retaining the chunk's hidden, position, rotary, paged-KV, and linear-attention progress so ready decode can run without replaying completed layers. A forced eight-layer/two-layer-quantum regression proves decode interleaving across zero-token yields; CPU parity covers six real layer yields, and cancellation/discard release retained ownership only after backend settlement. Strict TOML/environment validation and provenance, startup logs, health/debug state, Prometheus counters, examples, operator docs, and every mixed-load variant share one contract; qualification now fails if it observes no inter-layer yield. This is a committed scheduler checkpoint, not ROCm latency acceptance; the next clean-source Strix Halo run must prove the existing ITL, peer-progress, error, and teardown gates. |
| 2026-07-10 | Layer-resumable prefill ROCm parity | `sha256:d1b00df4a314` | `745b3446c82d` | Strix Halo ROCm/gfx1151 | strict real-device hybrid prefill parity integration | passed | Qualification mode required a real ROCm device and ran the shared actor path through one GDN and one full-attention layer in one-layer groups. Six retained layer yields across token quanta `[17, 17, 17, 17, 12, 1]` matched monolithic first-token and next-decode output, recurrent state, split snapshot, sequence length, and block cleanup. This proves backend correctness and ownership for the mechanism on this device; it is not the full Qwen3.5-4B mixed-load latency acceptance receipt. |
| 2026-07-10 | Layer-resumable prefill Vulkan parity | `sha256:d1b00df4a314` | `64aa52ba9285` | Radeon 8060S / RADV STRIX_HALO, Mesa 26.1.3 | strict production-runtime hybrid prefill parity integration | passed | Qualification mode required the real Vulkan runtime and exercised the production CPU-backed model/paged-pool path with BF16 weights and attention output gating. Six retained layer yields across token quanta `[17, 17, 17, 17, 12, 1]` matched monolithic first-token and next-decode output, recurrent state, split snapshot, sequence length, and block cleanup. This is Vulkan mechanism and ownership evidence, not a full-model serving throughput receipt. |
| 2026-07-10 | Stable ROCm layer-yield accounting counterexample | `sha256:d1b00df4a314` | `5172d5eb01d4` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t023311359387z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | Layer scheduling eliminated the pause failure: 1,001 inter-layer yields processed 4,576 layers with a 35.6 ms maximum prefill group, 86.5 ms maximum decode, 87/116 ms p99/p99.9 ITL, and zero attributed or unexplained outliers. The run then exposed a scheduler-accounting bug rather than a ROCm math failure: five requests were rejected when a retained 64-token chunk completed in a later cycle whose remaining admission budget was 16-55 tokens, yielding six total batching errors including the intentional stalled-client eviction and invalidating pressure-peer overlap. External synchronization stayed clean at 19.4 ms total/0.097 ms max, mutation paths stayed inactive, and unforced shutdown removed all snapshot payloads in 414 ms. Preserve the selected chunk width across layer yields and defer its continuation until the current cycle can honor that reservation; do not weaken the token-budget invariant. |
| 2026-07-10 | Retained prefill token-width reservation | `sha256:bccc49b9f908` | this commit | portable + Strix Halo ROCm/Vulkan | 800 server, 372 model, and 235 qualification tests; exact-width actor regression; CPU/ROCm/Vulkan resumable parity; portable, ROCm, and Vulkan all-target checks | passed | A layer-resumable chunk now exposes the token width selected when it began. If admission or decode leaves a smaller remainder in a later actor cycle, the scheduler defers that chunk without executing it, shrinking it, replaying completed layers, or weakening the strict post-forward token-budget check. A deterministic 64-token/eight-layer regression proves a 32-token remainder performs no model work or request failure, preserves the reservation, remains schedulable, and completes exactly once when 64 tokens fit. Health, debug state, Prometheus, qualification receipts, examples, and operator docs expose the cumulative deferral count. Real CPU, ROCm, and Vulkan hybrid parity still produced identical monolithic/resumable first and next tokens across six layer yields; cancellation released retained ownership. This is a committed correctness checkpoint; the next clean-source full ROCm run must prove request success and pressure-window overlap while retaining the already-achieved zero-outlier latency result. |
| 2026-07-10 | Stable ROCm retained-width correctness and admission-starvation counterexample | `sha256:bccc49b9f908` | `adb901293b3e` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t024917687277z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The retained-width fix closed the accounting defect: all ten measured requests succeeded, 128 narrower-cycle deferrals produced no request error, and the sole batching error exactly matched the intentional stalled-client eviction. Layer scheduling retained zero attributed and unexplained ITL outliers across 1,463 yields; max prefill/decode were 36.2/92.9 ms and p99/p99.9 ITL were 96/137 ms. The remaining failure is admission starvation rather than an execution pause: max decode width was only two rows, the dedicated pressure peer did not receive its first token until 24.7 s after dispatch and therefore began only after the attributed 2.007 s pressure window ended, despite later completing successfully. Mean rows per forward were 1.118, throughput was 12.16 tok/s, and the long-prompt TTFT was 67.9 s. All 2,562 external-yield synchronizations succeeded in 33.3 ms total with a 0.396 ms maximum; stable mutation paths stayed inactive; unforced shutdown returned zero in 516 ms and left no snapshot residue. Preserve this receipt, then make ready-prefill admission fair enough for a newly queued peer to reach decode without regressing the proven sub-250 ms inter-token bound. |
| 2026-07-10 | ROCm width-16 admission diagnostic | `sha256:bccc49b9f908` | `adb901293b3e` | Strix Halo ROCm/gfx1151 | exact stable mixed workload with an isolated `KILN_MAX_DECODE_BATCH=16` runtime override | failed | Raising the active/decode cap admitted the pressure peer earlier and reduced its TTFT from 24.7 s to 17.3 s without an ITL regression (89/132 ms p99/p99.9, zero outliers), but it still missed the start of the pressure window by about 0.4 s and max observed decode width remained two. Throughput moved only from 12.16 to 12.59 tok/s. This rules out a blind ROCm cap increase as the complete fix; prefill service order and true multi-row formation remain the measured bottlenecks. The diagnostic result stayed local because the in-memory override was not a source-bound supported configuration; only this compact conclusion is retained. |
| 2026-07-10 | Charge retained prefill tokens at chunk selection | `sha256:14420f772346` | this commit | portable + Strix Halo ROCm/Vulkan | 800 server, 372 model, and 235 qualification tests; exact reservation regression; CPU/ROCm/Vulkan hybrid parity; portable, ROCm, and Vulkan all-target checks | passed | Prefill progress now separates tokens newly scheduled from tokens whose final transformer layer completed. The actor charges a chunk once when selecting it, validates every later completion against the immutable retained width, and yields after the resumed layer group without making that completion compete for a second new-token budget. A deterministic 64-token/eight-layer case resumes under a 32-token new-work remainder with no replay, shrink, deferral, or error; real parity proves each chunk is scheduled and completed exactly once across six yields, and cancellation still releases retained ownership only after settlement. The now-impossible deferral counter and its API, Prometheus, qualification, and documentation surface were removed rather than preserved as dead telemetry. This corrects accounting and simplifies the contract; bounded short-prefill service is still required to resolve the hardware pressure-peer TTFT counterexample. |
| 2026-07-10 | Bounded short-prefill service lane | `sha256:ef09a0e918f2` | this commit | portable; ROCm/Vulkan compile gates | 801 server, 372 model, and 235 qualification tests; deterministic mixed-tail scheduling regression; portable, ROCm, and Vulkan all-target checks | passed | Three of every four eligible prefill dispatches remain strict round-robin. The fourth may select the smallest remaining tail only when it fits within four configured token chunks; if none qualifies, that dispatch also remains round-robin. A five-row regression proves four 1K prompts each retain round-robin progress while a late 128-token prompt receives the bounded opportunities and reaches decode-ready first. Priority selection preserves the independent round-robin cursor across success and failure, so continuous short arrivals cannot consume more than 25% of dispatch capacity. Health, debug state, Prometheus, qualification receipts, examples, and operator docs expose the counter, and the mixed workload now requires the path to execute. This is a committed scheduler checkpoint; the next source-bound ROCm receipt must prove pressure-window peer progress, zero request failures/outliers, and clean teardown before the policy is accepted on hardware. |
| 2026-07-10 | Stable ROCm bounded-prefill acceptance | `sha256:ef09a0e918f2` | `674c150b1fcf` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t032233364936z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | passed | The clean source-bound run exercised 171 bounded short-tail dispatches and the dedicated peer emitted actor-ready tokens before, inside, and after the attributed 2.016 s stalled-client window. All ten measured requests succeeded; the sole batching error exactly matched the intentional eviction. Across 1,344 layer yields, max prefill/decode were 37.0/87.6 ms, p99/p99.9 ITL were 77.6/143.1 ms, and attributed plus unexplained outliers remained zero. The pressure peer TTFT fell from 24.7 s to 16.6 s while the 4,653-token long prompt still completed in 66.8 s. All 2,465 external-yield synchronizations succeeded in 30.3 ms total with a 0.359 ms maximum; stable resize/reclaim/graph paths stayed inactive; response-queue p99.9 was 0.76 ms; unforced shutdown returned zero in 614 ms and left no snapshot residue. This closes the isolated pause/backpressure correctness gate, not the throughput program: max decode width remained two, mean rows per forward were 1.071, and output throughput was only 12.48 tok/s, so true multi-row formation and the vLLM comparison remain explicit follow-up work. |
| 2026-07-10 | Effective decode-width observability | `sha256:77d3085658ce` | this commit | portable; ROCm/Vulkan compile gates | actor snapshot, health/debug JSON, Prometheus, and focused serialization tests | passed | The batching engine now publishes its effective concurrent decode-row ceiling after backend selection and the combined token budget constrain it. Operators and the benchmark can therefore distinguish a configured cap from an observed formation failure instead of interpreting `max_observed_batch_size` in isolation. Focused actor, health, debug, and Prometheus tests passed, as did formatting and portable, ROCm, and Vulkan server all-target checks. The direct `KILN_MAX_DECODE_BATCH` parser is still permissive and untyped; strict startup configuration remains follow-up rather than being hidden by this evidence. |
| 2026-07-10 | Fail-closed shared serving benchmark v2 | `sha256:61197fed3078` | `db68b153dc62` | portable; Kiln/vLLM protocol contract | 246 qualification tests including 11 focused socket, parser, receipt, counter, memory, and failure tests; local vLLM `30f519e94` source audit | passed | The former ad hoc non-streaming nonce loop is now one deterministic OpenAI chat-stream driver for both engines. A real launch barrier, fixed prompt multiset, explicit sampling/template fields, strict SSE/usage/finish validation, fixed-length and uniform-input gates, R7 TTFT/ITL/E2E percentiles, request/token throughput, SLO-goodput, DRM peak memory, optional Kiln phase/width deltas, exact output comparison, strict JSON, clean-source/runtime identity, self-hashing atomic receipts, and overwrite refusal share one contract. The adjacent vLLM source confirms every request extension used by the driver. Documentation marks old nonce tables historical. This is harness acceptance, not hardware performance evidence: real ROCm rows, vLLM execution, concurrency 128, sampled/long/prefix/mixed workloads, and external runtime/binary provenance remain open. The earlier pre-commit evidence draft omitted the then-untracked new test file from the tracked-tree hash; this row records the committed source hash instead. |
| 2026-07-10 | ROCm equal-shape scaling counterexample | `sha256:61197fed3078` | `db68b153dc62` | Strix Halo ROCm/gfx1151 | `benchmarks/receipts/rocm/strix-halo/20260711t040223714292z-rocm-strix-halo-serving-equal-shape-db68b153-v1.json` | passed diagnostic | The clean exact-binary run completed every fixed 146-token prompt and fixed 64-token output with zero request or batching errors at concurrency 1/8/16/32/64. Throughput rose only from 21.93 tok/s at c1 to 34.03 at c8, then plateaued at 32.29/32.13/31.34 tok/s; c64 p99 TTFT reached 123.9 s and SLO-goodput fell to 0.49 tok/s. The effective cap was eight, but observed width topped out at six for c8 and seven thereafter, while mean rows per decode forward stayed 2.40-2.61. At c64 the actor spent 104.6 s in 1,544 decode forwards and 26.0 s in 1,536 layer-bounded prefill forwards; admission itself took only 40 ms. Peak device memory was 39.12 GB, all 64 requests succeeded, and output/receipt hashes are intact. This proves the multi-row ROCm path works but does not sustain its cap; serialized layer-round-robin prefill and decode overlap stagger row readiness, and the short-tail priority tie policy is the next focused A/B before any width increase or kernel work. The receipt also exposed that the driver inherited `OPENAI_API_KEY` even for a local endpoint (`authentication_configured=true`); remove that secret-bearing default before the next official run. |
| 2026-07-10 | Explicit benchmark authentication provenance | `sha256:71a620eda808` | this commit | portable | 247 qualification tests including 12 focused harness tests | passed | The serving driver no longer inherits the generic `OPENAI_API_KEY` secret for arbitrary benchmark targets. Authenticated endpoints must use a mutually exclusive explicit value or named environment source; empty or missing sources fail preflight, while receipts record only `none`, `argument`, or `environment`. A regression proves a populated generic key cannot enter headers, and the CLI receipt round trip proves unauthenticated local runs report `authentication_configured=false`. Documentation prefers the named-environment form to keep credentials out of process listings. |
| 2026-07-10 | Keep equal prefills on round-robin service | `sha256:e6d987d7cf8f` | this commit | portable; ROCm/Vulkan compile gates | 802 server library tests, 247 qualification tests, two focused scheduler regressions, formatting, and portable/ROCm/Vulkan all-target checks | passed | The bounded short-tail lane now admits only work no larger than four chunks and strictly less than half the round-robin row's remaining work. Equal-shape cohorts therefore remain aligned under ordinary round-robin service instead of consuming every fourth priority opportunity and creating an artificial decode-readiness staircase. The existing 128-token-versus-1K mixed-tail test still proves bounded interactive acceleration and long-row progress, while a new five-row equal-work trace proves twenty dispatches stay exact round robin and the priority counter remains zero. This is a source checkpoint for the focused ROCm A/B, not a throughput claim; the clean exact-binary run must improve row formation without regressing the accepted mixed-load latency and pressure-window gates. |
| 2026-07-10 | Separate and validate detailed benchmark receipts | `sha256:1e79f87749f6` | this commit | portable + GitHub Actions contract | 248 qualification tests, every standard qualification receipt, the detailed ROCm receipt, Python compilation, and exact CI command reproduction | passed | CI run `29139245290` correctly rejected the first detailed serving artifact because it had been placed in the compact `receipt-v1` tree. Detailed `kiln.serving-benchmark.v1` artifacts now live under `benchmarks/receipts/`; CI discovers and validates that tree with the owning driver while continuing to validate every compact receipt with the standard validator. The serving validator enforces a closed top-level/run contract, strict JSON, finite counters and latency values, request/gate/verdict consistency, clean passed source, exact workload/run coverage, and the canonical self-hash. The driver also rechecks repository identity after measurement so a mid-run edit cannot produce false clean-source evidence. Documentation distinguishes both receipt families and their check-in paths. |
| 2026-07-10 | ROCm multi-row greedy reproducibility counterexample | `sha256:1e79f87749f6` | `9905f1c64d8e` | Strix Halo ROCm/gfx1151 | `benchmarks/receipts/rocm/strix-halo/20260711t043044767129z-rocm-strix-halo-serving-equal-shape-round-robin-9905f1c6-v1.json` | failed | The exact original workload, prompts, output lengths, sampling, sizes, and baseline receipt proved that keeping equal prefills round-robin improves performance: c8/c16/c32/c64 throughput moved from 34.03/32.29/32.13/31.34 to 37.70/34.89/32.88/32.25 tok/s, mean decode width from 2.56/2.40/2.60/2.61 to 3.45/3.03/2.74/2.77, and p99 TTFT from 6.22/24.37/57.56/123.89 to 3.98/20.14/55.85/119.90 seconds. All 121 requests succeeded with zero batching errors and c1 output matched, but every multi-row output-set hash differed from the baseline, so the fail-closed reference gate rejected the run. Same-binary c8 repeats also changed substantive continuations for identical prompts; disabling prefix caching did not help, while effective decode width one produced 0/8 divergences across two repeats at 21.62 tok/s. This isolates scheduler-dependent BF16 output variation to multi-row decode and blocks treating the throughput win as correctness acceptance. |
| 2026-07-10 | ROCm deterministic serving repeatability | `sha256:d07c3e4d3ab0` | `f4a662b2a362` | Strix Halo ROCm/gfx1151 | `benchmarks/receipts/rocm/strix-halo/20260711t045708958307z-rocm-strix-halo-deterministic-reference-f4a662b2-v1.json`; `benchmarks/receipts/rocm/strix-halo/20260711t045953182327z-rocm-strix-halo-deterministic-restart-f4a662b2-v1.json` | passed | The clean release binary `sha256:55243ebb8202` started with only `KILN_DETERMINISTIC=1`; startup and `/health` reported effective decode width one despite the wider ROCm performance policy. Every measured run reported mean, maximum observed, and effective decode width exactly one with zero request or batching errors. The fixed c1/c8 prompts, 64-token outputs, sampling fields, and workload fingerprint produced identical output-set hashes before and after a fully drained process restart, and the strict reference comparison reported zero mismatches. C1/c8 throughput was 21.65/21.86 tok/s in the reference and 21.86/21.89 tok/s after restart; c8 p99 TTFT was 20.62/20.60 seconds, making the reproducibility-versus-queueing tradeoff explicit rather than presenting this as the performance configuration. Both shutdowns removed their full private snapshots without force or residue. All portable gates and ordinary-push CI run `29140299408`, including UI smoke and both receipt validators, passed; no accelerator CI was used. |
| 2026-07-10 | ROCm mutable-remainder priority regression | `sha256:d07c3e4d3ab0` | `3a9d40b5145f` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t050149904346z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | The full stable mixed workload rejected the equal-prefill half-remainder heuristic. It preserved the pause fix: p99/p99.9 ITL were 80.5/141.6 ms, prefill/decode maxima were 36.2/87.1 ms, and attributed plus unexplained outliers remained zero. All ten requests succeeded, the only batching error was the intentional 2.003-second stalled-client eviction, and synchronization, mutation, shutdown, and snapshot-residue gates were clean. However, bounded short-prefill dispatches fell from the accepted 171 to 114; the 238-token pressure peer's TTFT regressed from 16.6 to 21.1 seconds and it missed the attributed pressure window. Throughput rose only from 12.48 to 12.96 tok/s, which cannot justify the correctness regression. Immutable admission-time prompt work, rather than a mutable remainder ratio, must distinguish equal cohorts from genuinely shorter late arrivals. |
| 2026-07-10 | Admission-work short-prefill classes | `sha256:d7e454b92440` | `f74a3d52b9a3` | portable; ROCm/Vulkan compile gates | 803 server library tests, 248 qualification tests, focused equal/shorter-work scheduler regressions, every receipt validator, formatting, and portable/ROCm/Vulkan all-target checks | passed | Each active prefill now retains its immutable admission-time work class. The first source checkpoint required that class to be strictly smaller than the current round-robin row while still enforcing the four-chunk remaining-work ceiling. Equal 128-token cohorts therefore used zero priority selections across twenty dispatches, while a focused 238-versus-432 case proved the late shorter row was selected when the longer row owned the cursor. A separate lightweight `Qualification contract` workflow now triggers on qualification tooling, workloads, and both receipt trees, closing the path-filter hole that skipped validation for the preceding receipt-only commit without making those commits rebuild the Rust workspace or use accelerator CI. The following hardware receipt shows why comparison against only the cursor was still insufficient. |
| 2026-07-10 | ROCm shorter-cursor priority counterexample | `sha256:d7e454b92440` | `f74a3d52b9a3` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t051221221574z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | failed | Immutable work identity alone did not restore the pressure contract: the run again recorded only 114 priority dispatches, the pressure peer's TTFT regressed further to 22.63 seconds, and it missed the pressure window. The reason is structural: when the genuinely shorter row itself owns the ordinary round-robin cursor, comparing its class to that same row can never mark the bounded opportunity as priority service, so the cursor advances instead of preserving the other 75% lane. P99/p99.9 ITL remained 100.6/143.7 ms with zero attributed or unexplained outliers, all ten requests succeeded, the intentional eviction was the sole batching error, throughput reached 13.43 tok/s, and synchronization, mutation, shutdown, and residue gates stayed clean. Priority eligibility must compare the candidate to the largest eligible active work class, while all-equal cohorts still decline the lane. |
| 2026-07-10 | Largest-class short-prefill eligibility | `sha256:c91b72c227d7` | `bf021bc32533` | portable; ROCm/Vulkan compile gates | 803 server library tests, 248 qualification tests, focused shorter-cursor/equal-cohort regressions, every receipt validator, formatting, and portable/ROCm/Vulkan all-target checks | passed | The bounded lane now asks whether a candidate's immutable admission work is smaller than any eligible active class, rather than comparing only with the ordinary cursor. A 238-token row that already owns that cursor is therefore still marked as the one-in-four priority dispatch beside a 432-token row, preserving the other 75% round-robin cursor for its normal turn. An all-equal 128-token cohort has no larger class and still consumes zero priority opportunities across twenty dispatches. The next exact-source ROCm mixed workload remains the acceptance gate; this row is not hardware evidence. |
| 2026-07-10 | Stable ROCm equal-cohort and mixed-load acceptance | `sha256:c91b72c227d7` | `bf021bc32533` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260711t051837168473z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json` | passed | The exact-source run recovered the pressure contract while retaining the equal-cohort policy. It exercised 163 bounded priority dispatches; the 238-token peer reached its first token in 16.59 seconds and emitted actor-ready progress before, inside, and after the attributed 2.002-second stalled-client window. All ten measured requests succeeded, and the intentional eviction was the sole batching error. Across 1,536 prefill forwards, prefill/decode maxima were 36.4/88.1 ms, p99/p99.9 ITL were 79.3/141.0 ms, and attributed plus unexplained outliers remained zero. Throughput was 12.58 tok/s with mean/max decode width 1.085/2, matching the prior mixed-load envelope rather than treating the focused equal-shape improvement as a large-batch claim. All 2,466 external-yield synchronizations succeeded in 31.8 ms total with a 0.38 ms maximum; response-queue p99.9 was 0.29 ms; KV stayed at 4,096 blocks with no graph, resize, or reclaim activity; unforced shutdown returned zero in 364 ms and left no snapshot residue. |
| 2026-07-10 | Typed deterministic and decode-width configuration | `sha256:59185a815a1a` | this commit | portable + ROCm/Vulkan compile + real Strix Halo ROCm process | 808 server, 373 model, 5 focused tensor, 55 capability-contract, and 248 qualification tests; every receipt validator; strict startup failures; real health/config/request smoke | passed | `server.deterministic` and `server.max_decode_batch` are first-class TOML settings with strict environment precedence, primitive ergonomic syntax, source attribution, and bounded validation. Startup fixes the tensor determinism selector exactly once and rejects a conflicting late change. One resolved hard ceiling now bounds actor admission, model decode-buffer allocation, resident-pool warmup, and the fallback rendezvous worker; none rereads the shared decode-width environment variable. Resolution preserves the configured and backend-policy widths while reporting the final authority (`backend_policy`, file, environment, `deterministic`, or `max_batch_tokens`) through startup logs, `/v1/config`, health, and debug. Final ROCm processes rejected `KILN_DETERMINISTIC=sometimes` and `KILN_MAX_DECODE_BATCH=0` with exit 1, variable name, and invalid value. A real 4.206B-parameter ROCm server started from TOML with deterministic=true and configured width 16; startup and both API surfaces reported backend policy 8 and effective width 1 from `deterministic`, a greedy chat request returned the expected `kiln` response with maximum observed width one and zero batching errors, and drained shutdown removed the private snapshot. Documentation now states the narrower proven serving-repeatability contract instead of falsely implying that this selector already makes every accelerator kernel or training path bitwise deterministic. This is a scoped configuration migration, not completion of the repo-wide malformed-configuration or Phase 8.1 gates. |
| 2026-07-10 | Fail-closed default thinking-budget environment | `sha256:4ade1c918acf` | this commit | portable + ROCm/Vulkan compile + real process startup | 810 server library tests; strict grammar/load regressions; portable, ROCm, and Vulkan all-target checks; process exit checks; formatting and diff hygiene | passed | `KILN_DEFAULT_THINKING_BUDGET_TOKENS` and `KILN_DEFAULT_THINKING_BUDGET_MS` now resolve on the fallible startup path instead of warning and retaining a TOML/default value. Their entire documented grammar is a non-negative base-10 integer or case-insensitive `unlimited`; zero still means immediate close. Empty strings, the old undocumented `off`/`none`/`null` aliases, negatives, decimals, unit-suffixed values, overflow, and non-Unicode values fail closed. Real binaries rejected `"64 tokens"`, `"2.5"`, and `"off"` with exit one plus the exact variable and invalid value before model or listener initialization. |
| 2026-07-10 | Pre-validation structured diagnostics | `sha256:e0487aa8e173` | this commit | portable + ROCm/Vulkan compile + real process failures | 811 server library and 7 server binary tests; bootstrap parser regression; portable, ROCm, and Vulkan all-target checks; serve/config process checks; formatting and diff hygiene | passed | The CLI now resolves only string-valued `[logging]` fields plus env/verbosity precedence and installs tracing before any command can load the full configuration. The bootstrap parser remains field-tolerant when another typed field is invalid and falls back safely when TOML syntax or file I/O prevents extraction; authoritative `KilnConfig` parsing is unchanged. Every `KilnConfig::load` error emits one `configuration_load_failed` event containing the selected path and full error chain. Real `serve` and `config` processes emitted valid JSON diagnostics for malformed environment values and a TOML `u16` type failure before exiting one, while retaining the terminal error fallback. |
| 2026-07-10 | Thinking-close compatibility preflight | `sha256:2cc02211435e` | this commit | portable + ROCm/Vulkan compile + Qwen-template HTTP smoke | 812 server library tests; focused overlap/capacity regressions; portable, ROCm, and Vulkan all-target checks; mock HTTP 400; formatting and diff hygiene | passed | Budget setup now reports the exact offending `stop` string when it can match, contain, or overlap any forced `</think>` boundary. A shared preflight validates the effective completion capacity after context-window clamping against the tokenizer-specific close-token count before decode; synthetic coverage proves equal capacity is accepted and insufficient capacity fails with both counts. Operator docs distinguish invalid insufficient capacity from a valid budget larger than the completion cap, where the controller reserves close slots and reports trigger `max_tokens`. A real mock server using the local Qwen tokenizer/template rejected `stop=["</think>\n"]` with HTTP 400 before generation, while the fallback non-reasoning template correctly left the budget inert. |
| 2026-07-10 | Strict browser and desktop budget parsing | `sha256:9f4a57f6e668` | this commit | browser + desktop portable surfaces | four-scenario headless Chromium server smoke; executable desktop UI contract; 812 server library and 11 desktop settings tests; portable/ROCm/Vulkan all-target checks; JavaScript syntax, formatting, and diff checks | passed | Both surfaces accept only base-10 whole token counts within their representable bounds. Decimal seconds are decomposed as strings and combined with integer arithmetic, so up to three fractional digits become exact milliseconds without floating-point rounding. The readers reject exponent, sign, decimal-token, excess-precision, overflow, and native number-input `badInput` states before blank can mean an unused limit. Browser integration proves malformed custom values focus the offending field, show an error, issue no completion request, and cannot overwrite the last valid persisted budget; valid zero tokens plus 1.25 seconds still serialize as `0` and `1250`, while Unlimited remains explicit null. The inexpensive UI workflow now runs the desktop contract whenever its UI changes; no accelerator CI was added. |
| 2026-07-10 | Durable thinking-budget stream parity | `sha256:bd0fb18f7414` | this commit | portable request-log path + ROCm/Vulkan compile gates | 813 server library tests; 8 focused request-log unit tests; 4 on-disk capture integration tests; portable/ROCm/Vulkan all-target checks; formatting and diff hygiene | passed | SSE reassembly now retains the finish chunk's complete metadata object and reconstructs `choices[0].thinking_budget` only when final outcome telemetry is present. A parity matrix compares streamed and non-streaming durable shapes for token-triggered close, natural close, partial forced close, and configured-but-inert budgets across message, finish reason, outcome, usage, and effective limit/source metadata. The middleware integration consumes a real SSE body, flushes the asynchronous logger, and verifies the persisted JSONL row. Streams interrupted before finish metadata remain marked interrupted without a fabricated outcome. Public request-log documentation records the shared mining paths. |
| 2026-07-10 | Batch thinking-budget configuration provenance | `sha256:f5b0d27f2db3` | this commit | portable API + ROCm/Vulkan compile gates | 814 server library tests; focused resolution, cache-rehydration, and HTTP serialization regressions; real Chromium docs smoke; portable/ROCm/Vulkan all-target checks; formatting and diff hygiene | passed | Chat and batch now share one effective-budget resolver. Every batch response path, including zero-token and all deterministic-cache rehydration paths, reports the request-wide resolved pair at `metadata.thinking_budget` with independent `request`, `server_default`, `request_unlimited`, or `unlimited` provenance. Per-completion outcomes remain at `completions[].thinking_budget`, avoiding a false aggregate trigger/close state for heterogeneous rows. Cache hits preserve original outcomes while recomputing provenance from the current request. Public API, quickstart, and site documentation define the split explicitly. |
| 2026-07-10 | Playground compare stream and outcome parity | `sha256:919dea80cf4d` | this commit | browser UI + portable/ROCm/Vulkan compile gates | four-scenario real Chromium UI smoke; normal/compare budget and failure fixtures; 814 server library tests; portable/ROCm/Vulkan all-target checks; JavaScript syntax, formatting, and diff hygiene | passed | Normal chat and both compare sides now use one incremental UTF-8/SSE consumer and one chunk-state accumulator for reasoning, content, TTFT, finish reason, and final thinking-budget metadata. Both views use the same budget/finish outcome formatter; a browser regression proves independent natural-close versus token-cap/truncated results on concurrent sides. Structured `generation_error` events fail only their side and remain visible instead of becoming successful empty output, premature EOF fails closed, and every terminal/error path cancels its reader. |
| 2026-07-10 | Bounded thinking-budget telemetry substrate | `sha256:dad6bcaef5b5` | this commit | portable + ROCm/Vulkan compile gates | 816 server library tests; focused recent-request serialization and Prometheus cardinality/outcome regressions; portable, ROCm, and Vulkan all-target checks; formatting and diff hygiene | passed | Recent requests now have a typed optional budget record that distinguishes unresolved applicability, inert configuration, natural close, forced close, and partial outcomes without fabricating absent state. The central recorder can publish fixed-source counters, a closed outcome counter, and numeric token/time histograms; hostile source text is collapsed to `unknown` and never becomes a label. This is deliberately an infrastructure checkpoint, not Phase 2.3 completion: existing generation, cache, stream, and error paths still need to populate the record, and the dashboard and public documentation still need to expose it. |
| 2026-07-10 | End-to-end thinking-budget request observability | `sha256:43a158aeafc5` | this commit | browser + portable/ROCm/Vulkan compile gates | 819 server library tests; focused effective/inert, cache, streaming-setup failure, metrics, and recent-request serialization regressions; four-scenario real Chromium UI smoke; portable, ROCm, and Vulkan all-target checks; JavaScript syntax, formatting, and diff hygiene | passed | Every chat record path now carries effective token/time limits, independent source provenance, applicability, and the final shared runtime status when one exists. Unary API metadata and recent records use the same captured status; cache hits retain their original outcome; live streams snapshot status on success, timeout, generation error, or client disconnect; setup failures preserve established applicability without inventing token counts. The central recorder publishes one bounded-label outcome and finite-limit histogram observation per physical record, while multi-choice records deliberately use the same first/root outcome convention as response metadata. The request drill renders configuration, provenance, application state, outcome, and measured thinking without adding list-row clutter, and additive old records remain renderable. Public API and operator documentation define field absence, closed label sets, and histogram cardinality safety. |
| 2026-07-10 | Shared Rust thinking-budget configuration model | `sha256:954bf357dd4b` | this commit | portable + ROCm/Vulkan compile gates | 86 core, 215 eval, 819 server, and 4 `kiln-eval` binary tests; focused override/provenance matrices; portable, ROCm, and Vulkan all-target checks; formatting and diff hygiene | passed | `kiln-core` now owns omitted/inherit, explicit unlimited, finite-limit, independent token/time resolution, explicit CLI value, and typed provenance semantics. Chat, batch, eval suite/example/run overrides, rollout/eval CLI flags, and cache policy all consume that implementation. The former server `BudgetOverride`, eval `EvalBudgetOverride`, CLI parser/serde implementation, and eval source-string resolver were removed; the two public names remain compatibility aliases. This is a bounded Phase 2.1 checkpoint, not checkbox completion: runtime outcome records and the browser/desktop schema still need to converge on the canonical model. |
| 2026-07-10 | Canonical thinking-budget terminal outcome | `sha256:40ac51b6873a` | this commit | portable + ROCm/Vulkan compile gates | 87 core, 215 eval, and 819 server library tests; eval all-target check; focused API/cache outcome regressions; portable, ROCm, and Vulkan server all-target checks; formatting and diff hygiene | passed | One `kiln-core` outcome now defines the terminal `triggered`, typed trigger, closure state, measured reasoning tokens, and elapsed milliseconds serialized by chat choices, batch items, eval results, and their durable consumers. It is constructed directly from the shared runtime status, and deserialization fails when `triggered` disagrees with trigger presence. The duplicate server wire struct and eval outcome struct were removed; `EvalThinkingBudgetOutcome` remains a compatibility alias. Phase 2.1 remains open for the effective/applicability record and browser/desktop schema. |
| 2026-07-10 | Typed thinking-budget provenance | `sha256:55818f62335b` | this commit | portable + ROCm/Vulkan compile gates | 88 core, 215 eval, and 819 server library tests; eval all-target check; focused unknown-source/cardinality regressions; portable, ROCm, and Vulkan server all-target checks; formatting and diff hygiene | passed | API metadata, batch metadata, eval results, recent requests, and Prometheus observation now carry the shared provenance enum instead of converting to arbitrary strings between layers. JSON remains the same stable snake-case vocabulary. Deserialization maps unknown legacy/external text to the explicit `unknown` variant, and the metrics label table is enum-backed, so hostile text cannot become a label. Phase 2.1 remains open for the combined effective/applicability record and browser/desktop contract. |
| 2026-07-10 | Canonical effective thinking-budget record | `sha256:26b42c95429f` | this commit | portable + ROCm/Vulkan compile gates | 90 core, 215 eval, and 819 server library tests; eval all-target check; flat/legacy-nested schema and invalid-state regressions; portable, ROCm, and Vulkan server all-target checks; formatting and diff hygiene | passed | Chat metadata and eval results now use one validated `kiln-core` record for effective limits, typed sources, applicability, and the optional canonical outcome. Serialization preserves the established flat chat shape and rejects inconsistent configured/limit, source/limit, applied/configured, or outcome/applied states. Deserialization accepts both that shape and the earlier nested eval-only outcome for backward compatibility; new eval results write the shared flat form documented in the eval guide. The duplicate server and eval record structs were removed behind compatibility aliases. Recent requests retain a small conversion wrapper only for their legitimate unresolved-applicability state. Phase 2.1 remains open for a machine-readable cross-runtime reference and browser/desktop conformance. |
| 2026-07-10 | Versioned thinking-budget contract foundation | `sha256:f44b59dece39` | this commit | portable contract/core | 91 core tests; JSON syntax, formatting, and diff hygiene | passed | A Draft 2020-12 schema now defines request overrides, effective configuration, sources, terminal outcomes, application records, and unresolved recent records. A versioned conformance file covers independent dimensions, zero, inherit, explicit unlimited, request/suite/run/example provenance, flat terminal records, legacy nested eval input, and invalid states. The core suite executes every vector and cross-checks the schema's complete source and trigger vocabularies. A concise human reference links the normative artifacts from the README. This is a foundation checkpoint: Phase 2.1's schema/reference item remains open until browser and desktop runners execute the same vectors. |
| 2026-07-10 | Desktop thinking-budget contract vectors | `sha256:a6631d186c4f` | this commit | desktop portable UI | executable desktop VM smoke plus core v1 contract test; JSON syntax and diff hygiene | passed | The v1 contract now includes server-default vectors for unlimited, token-only, time-only, and dual finite limits. The desktop smoke extracts and runs the production settings parser against every vector, including exact fractional-seconds-to-milliseconds conversion and zero preservation. This closes desktop's cross-runtime portion without adding accelerator CI; browser request vectors remain before the Phase 2.1 schema/reference item can close. |
| 2026-07-10 | Independent Playground thinking-budget controls | `sha256:4c92ea3d97f3` | this commit | real Chromium + portable/ROCm/Vulkan compile gates | five shared request-contract vectors in four-scenario Chromium smoke; 390 px responsive regression; desktop and core contract runners; 819 server library tests; portable, ROCm, and Vulkan all-target checks; JavaScript syntax, formatting, and diff hygiene | passed | Token and time now have separate Inherit, Unlimited, and Limit selectors, so each request dimension independently omits its field, sends explicit null, or sends a validated finite value. Production parsing and request serialization execute every request-scope vector from the same v1 conformance file used by Rust and desktop. Finite fields appear only for their selected dimension, zero and exact fractional milliseconds remain intact, thinking-off preserves but disables the chosen controls, and the 390 px layout has no horizontal overflow. Existing combined `server`/`unlimited`/`custom` local-storage settings migrate dimension by dimension without losing prior values. The shared typed implementation and independent-control checklist items close here; the canonical-documentation item remains open until cheap CI enforces public prose linkage. |
| 2026-07-10 | Canonical thinking-budget documentation guard | `sha256:80e6c38166ba` | this commit | portable docs/contract CI | schema-derived reference vocabulary; required public-document links; real Chromium docs and server UI smoke; desktop and core contract runners; release-version validation; 10 source-identity tests; JSON, JavaScript, YAML, and diff hygiene | passed | The versioned schema and conformance file now drive an executable documentation check for contract version, independent override states, every source, and every trigger. README, Quickstart, eval, and site API explanations must link the canonical reference; contract edits trigger the existing browser/desktop smoke, while prose drift uses the existing lightweight version-drift job rather than launching unrelated GPU work. Pages publishes both JSON artifacts at the schema's declared URL. Qualification source identity now includes `contracts/`, with a regression proving a contract edit changes the hash; prior behavior could leave receipts looking source-identical after a semantic schema change. This closes Phase 2.1. |
| 2026-07-10 | Playground effective thinking-budget preview | `sha256:c183d5a3a8e1` | this commit | real Chromium + portable/ROCm/Vulkan compile gates | four-scenario Chromium UI smoke with config failure/retry and 390 px maximum-value coverage; 819 server library tests; focused config API regression; docs-site smoke; portable, ROCm, and Vulkan all-target checks; visual desktop/mobile review; JavaScript syntax, formatting, and diff hygiene | passed | Playground and Runtime Config now share one cached `GET /v1/config` request. Before send, a quiet status row resolves each independent dimension to its finite value or unlimited state and labels it `server` or `request`; finite edits, explicit unlimited, inheritance, malformed values, and thinking-off state update immediately. Missing or invalid generation defaults remain visibly unavailable rather than being assumed unlimited, with a local retry that recovers after API failure. The toolbar groups each label with its selector, the demo fixture exposes the real config shape, and maximum token/time labels remain overflow-free at 390 px. Public README, Quickstart, canonical contract, and API documentation identify the preview and its source endpoint. This closes the effective-default preview item in Phase 2.4. |
| 2026-07-10 | Fail-closed rollout thinking-budget applicability | `sha256:a2dd2d06dbec` | this commit | portable CLI + ROCm/Vulkan compile gates | five shared request-scope conformance vectors; focused flag/template and no-side-effect regressions; real CLI help/error smoke; 823 server library tests; contract documentation guard; portable, ROCm, and Vulkan all-target checks; formatting and diff hygiene | passed | `kiln rollout-generate` now rejects finite, zero, and explicit-unlimited budget flags whenever thinking is disabled, before reading inputs, creating output, invoking the scorer, or contacting a server. Top-level template budget fields, including JSON null, are rejected after parsing but before output or network activity, and the request renderer enforces the same invariant defensively for internal callers. With `--thinking true`, the rollout renderer executes the canonical request-scope Inherit/Unlimited/Limit matrix and preserves each dimension independently. Long help, option help, the actionable runtime error, and the canonical contract all state the requirement. This closes the rollout applicability item in Phase 2.4. |
| 2026-07-10 | Shared `8420` runtime default | `sha256:19b7d0652709` | this commit | portable desktop/CLI + local ROCm/Vulkan compile gates | versioned cross-surface contract and drift guard; 824 server library tests; 149 desktop tests; 5 eval binary tests; real `kiln config`, `kiln health --help`, and `kiln-eval --help` smoke; desktop VM smoke; portable, ROCm, and Vulkan all-target checks; release/docs guard; shell syntax, formatting, and diff hygiene | passed | A versioned runtime-defaults contract now anchors the server bind host, local client host, and port. Server configuration and every CLI URL default share one Rust definition; desktop Rust and JavaScript each have one guarded default consumed by settings, supervision, and onboarding. Fresh installs now use `127.0.0.1:8420`, while explicitly persisted ports remain user overrides. README, Quickstart, desktop guidance, example configuration, and the Phase 2 validation script agree on `8420`; the validation script's broader hardware workflow was not exercised in this checkpoint. The existing lightweight release-drift job enforces the contract without adding GPU CI. This closes the shared default-port item in Phase 2.4. |
| 2026-07-10 | Durable desktop settings recovery | `sha256:dddae63404a7` | this commit | portable Linux desktop + real Chromium | 159 locked desktop tests including 22 focused settings cases; field-level corruption, invalid JSON, legacy migration, future-schema, backup recovery, semantic validation, private Unix permissions, exact malformed-byte preservation, and injected promotion-failure coverage; Clippy; executable desktop VM smoke; real Chromium docs smoke and 520/390 px settings probes; JavaScript/YAML syntax, release/default/contract guards, formatting, and diff hygiene | passed | `settings.json` now carries schema version 1 and is decoded field by field, so one malformed value preserves every other valid field. Writes are process-serialized, staged beside the destination with mode `0600` on Unix, fsynced, and promoted only after the prior supported document moves to `settings.json.bak`; malformed primary bytes move to `settings.json.invalid`, and an injected promotion failure proves rollback and temporary-file cleanup. Missing or corrupt primary data recovers from backup, while any damage suppresses automatic server launch and OS autolaunch reconciliation until an explicit repair Save. The app logs and notifies, Settings keeps a structured warning visible and Save-enabled for repair, and a newer schema is read-only. Unrelated binary installation cannot clear an unresolved warning. Port and inference-fraction inputs now reject coercion, and the constrained layout has no horizontal overflow. The existing lightweight UI job gains source-contract triggers without adding a hosted Rust build or accelerator work. This closes the final Phase 2.4 checklist item. |
| 2026-07-10 | Distinct GRPO behavior and KL loss inputs | `sha256:e2c48fcb7ff3` | this commit | portable CPU; ROCm/Vulkan feature builds | exact scalar loss and analytic coefficient fixtures with policy, behavior, and frozen-reference log-probabilities all distinct; portable, ROCm, and Vulkan checks; formatting and diff hygiene | passed | PPO token/sequence/CISPO ratios now consume only behavior-policy log-probabilities, while K1/K3 consume only the frozen reference through scalar, tape, host-analytic, and CUDA/ROCm device-coefficient routes. The current one-reference Vulkan fused ABI declines distinct tensors and falls back to the separated generic route. Production still passes its historical reference into both inputs until versioned rollout provenance and independent policy selection land, so the broader semantics and ingestion items remain open. These feature runs used CPU fixtures and are not ROCm/Vulkan hardware evidence; CUDA could not compile locally because `nvcc` is unavailable. |
| 2026-07-10 | Strict rollout provenance v1 foundation | `sha256:e78713870ae2` | this commit | portable | 22 trajectory/provenance compatibility and adversarial tests; train and server all-target checks; formatting and diff hygiene | passed | `ScoredRollout` can now carry a validated `kiln.rollout-provenance.v1` record with exact full input IDs, prompt boundary, ordered sampled/forced action decisions, selected-token behavior log-probabilities, base/adapter content identity, tokenizer/config/template hashes, effective sampling and thinking-budget controls, seed, and generation backend. Unknown fields, unsupported versions, malformed hashes, non-finite or positive log-probabilities, index/token drift, unproven forced tokens, and invalid sampling ranges fail during deserialization. Legacy rollouts remain readable without provenance. This is a type/parser foundation only: the API/CLI do not emit it and the trainer does not consume it yet, so the Phase 3.1 rollout-schema and ingestion items remain open. |
| 2026-07-10 | Independent KL in no-correction GRPO | `sha256:c3acffc64c2e` | this commit | portable + Strix Halo ROCm; Vulkan compile | 374 train tests with 1 ignored; exact scalar fixture; qualification-mode ROCm host/device coefficient parity; Vulkan test compile; formatting and diff hygiene | passed | Fixing the importance ratio at one no longer short-circuits the whole loss and silently deletes an independently configured KL penalty. Scalar, host-analytic, CUDA/ROCm device-coefficient, and generic tape routes retain K1/K3 value and gradient from the frozen reference while ignoring behavior-policy values in no-correction mode. The focused ROCm test executed on device with unrelated behavior and KL-reference tensors under `KILN_QUALIFICATION=1`; Vulkan was compiled but not executed in this slice, and CUDA remains deferred to its target machines. Configuration still derives this internal mode from the historical combined reference field, so the policy-selection checklist remains open. |
| 2026-07-10 | Independent GRPO policy selection and strict provenance ingestion | `sha256:0ef718c555a6` | this commit | portable + Strix Halo ROCm; ROCm/Vulkan compile | 382 passed/1 ignored portable train tests; 3 focused server submission regressions; legacy-receipt compatibility; exact tokenizer/template/prompt/payload/action-position fixtures; qualification-mode ROCm device loss; ROCm/Vulkan feature checks; formatting and diff hygiene | passed | `behavior_policy` now independently selects recorded rollout probabilities or an intentional fixed-one ratio, while `kl_reference_policy` independently selects base, none, or validated EMA cadence. Recorded mode binds canonical prompt messages and the scored payload to exact input IDs, prompt boundary, tokenizer/config/template hashes, sampled-token log-probabilities, and sampled/forced action positions; forced controller tokens remain context and never become policy targets. Missing provenance, identity drift, prompt or payload drift, incomplete action enumeration, and enabled KL without a reference fail before a forward; tokenizer-aware API admission, dry-run, inline, streamed JSONL, shared-prefix routing, loss inputs, and receipts use the split contract. The rendered prompt prefix is verified exactly, while the generated suffix stays authoritative because re-rendering a completed chat can append another generation prefix; trajectory data additionally fails closed when exact observation masks cannot be reconstructed. Legacy JSON accepts `reference_policy` only as an input alias, legacy receipts map it to an unknown-behavior KL selection, and legacy rollouts remain available under the default no-correction mode. Rollout emitters, aggregate policy metrics, provenance identity receipts, and the pinned oracle remain open. |
| 2026-07-10 | Exact post-filter behavior log-probability sampler | `sha256:80c9f314dc5d` | this commit | portable + Strix Halo ROCm; Vulkan compile | 376 portable model tests; 30 focused sampling tests; seeded token-path identity and analytic full/filtered probability fixtures; on-device ROCm top-k token/log-probability parity; Vulkan feature check; formatting and diff hygiene | passed | Sampling can now return the selected token together with its natural-log probability after repetition/presence/frequency penalties, temperature, top-k, min-p, top-p, and final renormalization. Deterministic paths report zero, stochastic trace capture fails without a resolved seed, invalid effective probabilities fail rather than producing invented provenance, and seeded traced tokens remain identical to the ordinary sampler. The seeded draw and probability lookup share one effective distribution, so trace capture does not repeat filtering or candidate transfer. Normal token-only serving paths are unchanged. Inference integration must still route provenance-enabled rows through this result; fused backends should eventually return token plus log-probability directly before trace-mode performance is considered closed. |
| 2026-07-10 | Batched inference action-trace foundation | `sha256:4041f20f9d9f` | this commit | portable + Strix Halo ROCm/Vulkan | 92 core tests with 3 ignored; 376 model tests; 828 server tests; sampled/forced/missing-probability actor regressions; trace-path tiny-model token parity on CPU, ROCm, and Vulkan; portable/ROCm/Vulkan all-target checks; formatting and diff hygiene | passed | Paged prefill and decode can now opt into exact selected-token behavior log-probabilities. The batching engine partitions traced and ordinary rows into independent batched forwards, so rollout capture bypasses token-only fused sampling without sending unrelated serving rows through the trace path. The thinking-budget controller reports the true decision source, including an already-forcing close token whose ignored model candidate happens to match; accepted action records therefore distinguish sampled tokens with finite non-positive log-probabilities from forced tokens without invented probabilities. Missing trace output fails before token acceptance. Ordinary API/eval requests still opt out, and no public response emits provenance yet, so rollout generation and the Phase 3.1 end-to-end emission item remain open. |
| 2026-07-10 | Replayable rollout template invocation | `sha256:18ba7330ef87` | this commit | portable + ROCm/Vulkan compile gates | 92 core tests with 3 ignored; 385 train tests with 1 ignored; 11 focused provenance compatibility/adversarial tests; ROCm/Vulkan train all-target checks; formatting and diff hygiene | passed | Rollout provenance now records the bounded tools, tool choice, and effective template kwargs used to render the prompt. Strict GRPO ingestion replays that invocation against the pinned template/tokenizer before accepting the exact prompt prefix, so request options such as Qwen's `enable_thinking` cannot make a valid rollout spuriously fail or let an altered prompt pass. Empty invocation data remains omitted, and earlier v1 payloads deserialize to the historical default invocation. The runtime thinking-budget record also exposes its exact close token IDs for the public emitter. API/CLI emission remains the next checkpoint. |
| 2026-07-10 | Allocation-free KV block cleanup | `sha256:8de42ef72620` | `b8102e0f` | portable serving core | 93 core tests with 3 ignored; million-block double-free regression; formatting and diff hygiene | passed | `BlockManager` now maintains an explicit Free/InUse/Retired state table across allocation, retirement, physical shrink, and growth. Request teardown and live high-water lookup no longer rebuild pool-sized hash sets while decode is paused, and invalid or duplicate frees fail immediately in every build profile. This removes a terminal response pause whose cost scaled with reserved KV capacity. |
| 2026-07-10 | Live-page-bounded Vulkan KV upload | `sha256:7492b4d70d31` | `546edd06` | Vulkan/Strix Halo | three focused scalar/batched/invalid-table prefix tests; Vulkan server all-target check; exact provenance HTTP reproducer | passed | Vulkan paged decode no longer serializes and uploads the entire auto-sized K/V pool on every token. It validates the pages reachable by each active row, narrows both pools to the smallest physical-slot prefix covering those pages, and dispatches against that prefix. The previously stalled real HTTP reproducer now completes both provenance cases in about 0.5 seconds on this host. |
| 2026-07-10 | Exact chat rollout-provenance emission | `sha256:74bb1ceb34f3` | this commit | portable + Strix Halo ROCm/Vulkan | 832 server library tests; 26 real-model integration tests with 1 ignored; exact seeded and terminal-EOS HTTP fixtures on CPU, ROCm, and Vulkan; portable/ROCm/Vulkan all-target checks; formatting and diff hygiene | passed | Opt-in `rollout_provenance` responses now bind the resolved seed, exact prompt and action IDs, sampled post-filter log-probabilities, forced-token sources, terminal EOS, behavior/base/adapter identity, tokenizer/template identity, effective sampling and thinking-budget controls, template invocation, scored payload, and generation backend. Trace requests bypass text-only caches and reject streaming, multiple choices, mock or non-batched execution, missing policy identity, unrepresentable prior tool metadata, zero generation capacity, and a budget close sequence that leaves no sampled action. The public API half of Phase 3.1 emission is complete; `kiln rollout-generate` persistence and strict ingestion proof remain open. |
| 2026-07-10 | Provenance-bound GRPO rollout generation | `sha256:2535216a96d0` | this commit | portable + Strix Halo ROCm/Vulkan compile gates | 10 focused rollout CLI tests; 835 server library tests; 26/27 real-model integrations; trainer recorded-policy ingestion proof; rebuilt CLI help; real Chromium docs smoke; release/docs guard; portable, ROCm, and Vulkan server all-target checks; formatting, Python compilation, and diff hygiene | passed | `kiln rollout-generate` now forces one non-streaming provenance-enabled choice, validates the exact seed, requested adapter, prompt and scored-content hashes, action coverage, usage counts, tokenizer/template/sampling/backend identity, and stable content-addressed behavior policy across every seed before invoking the scorer. It persists the server-issued record without inventing a trajectory, and the emitted JSONL passes strict `behavior_policy=recorded` ingestion. Each dataset and summary is staged, synced, and atomically replaced only after the full run succeeds; a missing-record regression proves existing outputs survive and the scorer is never invoked. Public API admission now rejects tool definitions and tool choice as well as prior tool-call metadata until that scored payload can be represented exactly. README, Quickstart, GRPO guide, CLI help, and API site distinguish this correctness path from the text-only batch endpoint. The lightweight release guard now parses multiline Clap attributes so documented CLI flags cannot silently escape validation; this addresses the observed cheap-CI regression without adding GPU CI. |
| 2026-07-10 | Versioned GRPO policy-audit receipt contract | `sha256:82c46e7af9cc` | this commit | portable; ROCm/Vulkan compile gates | three exact numeric and behavior-identity fixtures; full train library suite; ROCm and Vulkan train all-target checks; formatting and diff hygiene | passed | The additive `kiln.grpo-policy-audit.v1` receipt keeps importance ratios derived from behavior-policy log-probabilities independent from K1/K3 values derived from the frozen KL reference. It specifies ratio scope, observation counts, clip-tail counts and fraction, ratio range and mean, reference log-ratio and masked/unmasked KL means, entropy-mask coverage, and exact recorded-policy source identities. The canonical behavior-source manifest is order-independent, content-addressed, and distinguishes adapters with the same name but different revisions. No-correction mode records exact unit ratios without deleting independently configured KL observations, and legacy receipts continue to deserialize with no audit. This is a receipt/math foundation only: live training tensors and rollout provenance are not wired into the accumulator yet, so the Phase 3.1 separation item remains open. |
| 2026-07-10 | Reuse selected policy log-probabilities from GRPO loss roots | `sha256:575b5eac526f` | this commit | portable + Strix Halo Vulkan; ROCm compile | 403 passed/1 ignored Vulkan train tests; 5 real-device Vulkan FLCE/GRPO parity tests; exact generic loss-return fixtures; portable, ROCm, and Vulkan train checks; formatting and diff hygiene | passed | Checkpointed and tape-authoritative GRPO loss roots now return the exact selected policy log-probabilities they already computed, so policy diagnostics will not require a second model or vocabulary-head forward. Vulkan retains the selected-logit buffer beside its saved max/sumexp state and derives the returned values through the same shader used by the standalone path; a real-device assertion proves saved-state and standalone outputs are identical. The full Vulkan train run also exposed two CUDA-era CPU-storage SFT smoke tests whose stale feature gate admitted the explicitly unsupported non-resident Vulkan hybrid substrate; their gate now matches their documented CUDA-only contract instead of failing after a Vulkan forward. Aggregate receipt wiring remains the next checkpoint, so the Phase 3.1 separation item remains open. |
| 2026-07-10 | Live GRPO policy audit and complete behavior/reference separation | `sha256:9f4a8e49fee5` | this commit | portable + Strix Halo ROCm/Vulkan | 389 portable, 393 ROCm, and 405 Vulkan train tests passed with one pre-existing ignore per suite; real BF16 ROCm and F32 Vulkan recorded-policy benchmark steps; persisted-receipt/API fixture; release/docs guard; real Chromium smoke; formatting and diff hygiene | passed | Inline, streamed JSONL, and benchmark training now aggregate exact selected policy log-probabilities already returned by the loss root, recorded behavior probabilities, and the independently selected KL reference into `grpo.policy_audit`; no extra model/head forward is introduced. Receipts and structured logs expose token/sequence ratio counts, clip tails, K1/K3 and entropy-mask denominators, and compact content-addressed behavior-source identities. Dry runs omit the audit, while a successful run fails closed if audit finalization fails. The full Vulkan run exposed a causal-convolution byte bridge returning CPU tensors into a device-resident GDN recurrence; `d4de9cb2` restores output/state residency, all three affected F32/BF16 SFT/OPD tests pass, and cheap CI run `29157249808` is green. The public Markdown, benchmark, API, and web GRPO guides now distinguish provenance-bound recorded correction from the text-only fixed-one batch path and document receipt retrieval. This closes the behavior/reference separation item; the pinned TRL/PyTorch oracle remains open. |
| 2026-07-10 | Source-pinned TRL/PyTorch GRPO oracle corpus | `sha256:b33905a6b100` | this commit | local CPU reference | byte-exact regeneration through TRL 1.8.0 `GRPOTrainer._compute_loss`, PyTorch 2.13.0 autograd and AdamW; 5 distinct policy fixtures; 252 qualification-tool tests; Python compilation and diff hygiene | passed | The compact canonical fixture pins TRL commit `95809b9`, the full `grpo_trainer.py` SHA-256, and PyTorch commit `cf30153`, then records token/asymmetric-clip, negative-advantage lower-clip, sequence/GSPO, upper-only CISPO, and no-correction-with-K3 cases. Outputs include loss, token and observed ratios, clip metrics, per-token K3, policy-logprob gradients, AdamW parameters, and both moments. Regeneration uses the official CPU wheel so it downloads no accelerator libraries; automatic CI validates the checked-in contract without installing TRL/PyTorch. Rust does not consume these numbers yet, so the Phase 3.1 oracle item remains open. |
| 2026-07-10 | Production GRPO parity with pinned TRL/PyTorch | `sha256:ef6c13d04929` | this commit | portable + Strix Halo ROCm/Vulkan | 392 portable, 397 ROCm, and 408 Vulkan train tests passed with one pre-existing ignore per suite; all 5 oracle cases matched production loss, ratios, clip tails, K3, policy-logprob gradients, and CPU AdamW state; all 5 matched the ROCm device coefficient path; pinned CISPO policy loss/gradient matched the Vulkan shader; 6/6 Vulkan FLCE tests; API admission; portable/ROCm/Vulkan/server all-target checks; release/docs contracts and real Chromium smoke | passed | Consuming the independent fixture exposed two real semantic defects: GSPO divided by completion length both inside its broadcast surrogate and again in the outer normalizer, while CISPO used a two-sided additive PPO interval instead of MiniMax-M1/TRL's detached absolute upper-only weight cap. Every scalar, host analytic, ROCm device, and Vulkan shader route now applies one GSPO length normalization and `min(ratio, cispo_max_weight)` with no lower floor. The new public cap defaults to 5.0, is independently validated at API admission, survives old config/receipt deserialization, is recorded in receipts and ablation output, and drives policy-audit tail counts without overloading PPO's `clip_epsilon` fields. Public Markdown, Quickstart, API, site, and changelog documentation define the distinction. This closes the Phase 3.1 pinned-oracle item; broader backend qualification and soak gates remain open. |
| 2026-07-10 | Versioned exact-resume checkpoint foundation | `sha256:ecf1f8e70ddb` | this commit | portable; ROCm/Vulkan compile gates | 10 focused adversarial storage tests; 400 portable train tests with one pre-existing ignore; ROCm/Vulkan all-target checks; formatting and diff hygiene | passed | Resumable `.kiln-checkpoint` directories are now a distinct immutable artifact type from PEFT exports. Schema v1 types exact config, precision, progress/order, RNG streams, optimizer/scheduler, objective state, and checksummed files; the writer stages behind an incomplete sentinel and fsync/rename boundary, while the loader rejects unknown fields, traversal, symlinks, hard links, untracked files, invalid item order, incomplete state, and checksum/size drift. Trainer-loop save/restore integration remains open. |
| 2026-07-10 | Stable-name adapter and optimizer checkpoint codec | `sha256:d4642d046862` | this commit | portable + Strix Halo ROCm; Vulkan compile gate | exact AdamW and Muon continuation on CPU and ROCm; 404 portable train tests with one pre-existing ignore; complete optimizer suite; ROCm/Vulkan train all-target checks; formatting and diff hygiene | passed | Adapter tensors and stateful optimizer buffers now serialize by stable PEFT-compatible parameter name rather than process-local tensor identity. Restore validates the exact tensor set, shapes, dtypes, finite values, and per-parameter step before mutation; seeds both host and resident-device copies; and explicitly tracks which copy each subsequent update made authoritative. The ROCm gate exposed and fixed stale-host serialization after a resumed device step. Both AdamW and Muon now produce byte-identical adapter and optimizer files after the next uninterrupted versus resumed step. Scheduler/cursor/RNG/objective state and public loop/API wiring remain open. |
| 2026-07-10 | Exact SFT stop/resume integration | `sha256:0a575c6ee13d` | this commit | portable + Strix Halo ROCm; ROCm/Vulkan compile gates | 405 train library tests with one pre-existing ignore; 839 server library tests; 66 parameter tests; complete optimizer suite; 10 adversarial checkpoint-storage tests; real ROCm uninterrupted versus cancelled/resumed exact-equivalence qualification; portable, ROCm, and Vulkan server all-target checks; JavaScript syntax, formatting, and diff hygiene | passed | SFT now checkpoints and restores exact adapter, AdamW/Muon, scheduler, shuffled order/cursor, RNG, partial-objective, diagnostics, precision, effective configuration, base-weight shard, tokenizer, runtime, and derived gradient-checkpoint-plan identity. Immutable checkpoints publish independently of final PEFT staging; strict API admission validates full checksums before GPU work; job status, CLI, Playground, Quickstart, and the checkpoint guide expose the resume workflow. The real ROCm gate matched every loss plus final adapter and intermediate optimizer bytes. GRPO/OPD exact loop state and process-kill fault qualification remain open. |
| 2026-07-10 | Coordinated SFT checkpoint publication | `sha256:c7bb2ebd2f95` | this commit | portable + Strix Halo ROCm; ROCm/Vulkan compile gates | focused backend-capability contract; full portable train library suite; real ROCm uninterrupted versus cancelled/resumed exact-equivalence qualification; default/ROCm/Vulkan server all-target checks; formatting and diff hygiene | passed | Checkpointing now copies authoritative adapter and optimizer buffers to CPU under the interruptible per-step serving write lock, then releases that lock before safetensors encoding, checksumming, fsync, and atomic rename. Final adapter export uses the same coordinated snapshot boundary and no longer hides a resident-sync failure. Structured timings split lock wait, device snapshot, and filesystem publication so a reported inference pause can be attributed instead of guessed at. This checkpoint also promptly fixes the broad-backend identity call caught by cheap CI run `29161184092`; the focused contract passes and the correction was pushed separately as `c6ca6c6e`. |
| 2026-07-10 | Strict GRPO diagnostic resume-state codec | `sha256:30080630e277` | this commit | portable | 406 train library tests with one pre-existing ignore; focused round-trip and unknown-field rejection; formatting and diff hygiene | passed | GRPO policy-audit sums, clip/KL counters, recorded behavior-source accumulators, echo measurements, and phase timings now have typed strict serde state suitable for an exact loop checkpoint. Ratio scope is owned data rather than a process-lifetime string reference, so deserialization cannot depend on leaked/static text. This is a deliberately small foundation checkpoint: no existing GRPO PEFT snapshot is advertised as resumable yet; adapter/optimizer, EMA tensors, cursor, loss history, and public admission remain open. |
| 2026-07-10 | Stable GRPO EMA reference checkpoint codec | `sha256:773688c2a9a1` | this commit | portable + Strix Halo ROCm; Vulkan compile gate | 408 train library tests with one pre-existing ignore; CPU and real ROCm byte-identical next-refresh continuation; non-finite adapter rejection-before-mutation regression; Vulkan train all-target check; Rust 1.94/1.96 formatting and diff hygiene | passed | Frozen EMA reference tensors now serialize under the same stable PEFT-compatible names as policy parameters and restore only after exact tensor-set, shape, dtype, and finite-value validation. An uninterrupted and restored reference produce byte-identical safetensors after the next EMA refresh on CPU and the resident ROCm AdamW path. The shared adapter loader also now rejects non-finite values before mutating any live parameter, closing a gap between its documented and actual validation contract. The pending commit also resolves the only failure in cheap CI run `29161659781`, a Rust 1.94/1.96 line-wrap disagreement; all build and test jobs in that run passed. Loop manifests, cadence/cursor state, and public GRPO resume remain open. |
| 2026-07-10 | Settled per-group GRPO GPU coordination | `sha256:979b14210bb8` | this commit | portable + Strix Halo ROCm; ROCm all-target gate | 411 train library tests with one pre-existing ignore; 839 server library tests; 20 concurrency/resource invariants; deterministic writer-release, sync-failure, and panic-quarantine tests; real BF16 ROCm recorded-policy optimizer group with inference ownership between setup, step, and cleanup; Rust 1.94/1.96 formatting and diff hygiene | passed | Server GRPO no longer retains one process GPU writer across reward filtering, JSONL reads, tokenization, progress callbacks, checkpoint encoding, or the full job. Inline, streamed, generic, and CUDA-delegated routes coordinate model setup, each optimizer group plus EMA transition, device snapshots, and smoke/cleanup as separate phases, synchronizing the backend before every release. A failed settlement or panic quarantines the backend before inference can reuse unknown device state; requested checkpoint snapshot failures now fail closed rather than publishing stale PEFT state. Receipts expose aggregate writer wait, held time, and acquisition count, while safetensors and filesystem publication stay outside writer ownership. The real ROCm step proved actual accelerator settlement and read-owner admission between all three phases. A single very large group remains an explicitly attributed atomic interval; exact GRPO manifest/cursor/optimizer integration remains open. |
| 2026-07-10 | Vulkan settled GRPO writer qualification | `sha256:792c35809afc` | this commit | Strix Halo Vulkan; Vulkan all-target gate | 427 train library tests with one pre-existing ignore; real Vulkan recorded-policy optimizer group with inference ownership between setup, step, and cleanup; Vulkan server all-target check; Rust 1.94/1.96 formatting and diff hygiene | passed | The same bounded writer protocol qualified on the local Vulkan backend with a real accelerator optimizer group. Backend settlement completed before each release, inference read ownership was admitted between all three phases, and aggregate acquisition and timing evidence remained finite and non-zero. This closes the local Vulkan qualification for the settled per-group GRPO protocol; exact GRPO manifest/cursor/optimizer integration remains open. |
| 2026-07-10 | Exact GRPO checkpoint bundle foundation | `sha256:eb8c193aa603` | this commit | portable | 413 train library tests with one pre-existing ignore; focused strict loop-state and complete atomic-bundle round trips; Rust 1.94/1.96 formatting and diff hygiene | passed | A versioned route-aware GRPO loop-state contract now owns the committed group cursor, JSONL seek coordinates, loss history, data/token/ECHO/gradient/policy diagnostics, phase and writer timings, and EMA cadence cursor. One immutable manifest-driven bundle atomically publishes checksummed adapter, AdamW/Muon, EMA-reference, and loop artifacts under stable names; validation binds optimizer/scheduler steps, RNG streams, trainable order, precision, effective configuration, model/tokenizer/backend identity, and the exact artifact set. The focused bundle test restored every tensor class and rejected unknown or route-incoherent loop state. This remains internal until the inline and streamed loops prove exact continuation; legacy PEFT snapshots are still not advertised as resumable. |
| 2026-07-10 | Exact inline GRPO stop/resume integration | `sha256:38c8c11109ef` | this commit | portable + Strix Halo ROCm/Vulkan | 414 portable, 422 ROCm, and 431 Vulkan train library tests passed with one pre-existing ignore per suite; real ROCm and Vulkan uninterrupted versus cancelled/resumed exact-equivalence qualification; 55 focused backend-capability tests; default/ROCm/Vulkan server all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | Inline GRPO now restores the immutable bundle before GPU setup, validates its exact data/filter/order/gradient-plan/model/tokenizer/backend/precision/config/RNG identities, resumes at the next committed optimizer group, and preserves loss history, AdamW state, EMA tensors and cadence, token/data/ECHO/gradient/policy diagnostics, phase timings, and the final adapter. Cancellation publishes under a durable root independent of failed final-adapter staging; periodic and cancellation snapshots copy device state under the bounded writer and encode/fsync after release. Both local backends matched every loss, final adapter bytes, and intermediate adapter/optimizer/reference artifacts. The Vulkan proof exposed and fixed a CPU scalar broadcast in LoRA reference forwards, resident policy/optimizer tensors returning on the wrong owner device, and stale policy data before EMA refresh; its unqualified paged shared-prefix reference route now uses the exact per-completion fallback. The full Vulkan suite also caught a portable codec fixture incorrectly inheriting the runtime Vulkan CPU sentinel, so it now binds a concrete CPU backend. Streamed JSONL, server admission, and OPD exact resume remain open, so the phase-wide checkpoint items stay unchecked. |
| 2026-07-10 | Exact streamed JSONL GRPO stop/resume integration | `sha256:e9d4097f7edd` | this commit | portable + Strix Halo ROCm/Vulkan | 417 portable, 426 ROCm, and 435 Vulkan train library tests passed with one pre-existing ignore per suite; real ROCm and Vulkan uninterrupted versus cancelled/resumed exact-equivalence qualification; 20 server concurrency and 55 backend-capability contracts; default/ROCm/Vulkan server all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | Streamed GRPO now performs a memory-bounded CPU preflight before resident model upload, binds the exact trainable order and derived gradient-checkpoint plan, and resumes by physical JSONL line and byte offset while revalidating each line hash, token count, and plan before its optimizer step. Blank lines around three real groups proved step-one and step-two cursors; deleting failed final-adapter staging proved cancellation bundles remain durable. Both local backends matched every loss, final adapter, intermediate adapter/optimizer/EMA bytes, EMA cadence, and accumulated diagnostics. The ROCm proof exposed a one-ULP JSON round-trip defect in gradient-norm sums, fixed with an exact IEEE-754 checkpoint codec that still accepts legacy numeric state; Vulkan exposed information-free host `f64` tail variation after device `f32` reductions, fixed by canonicalizing observations to their source precision. Cheap CI run `29164262573` also caught a stale source invariant that still required deleted PEFT-only checkpoint directories; the contract now requires coordinated exact snapshot capture followed by publication outside GPU ownership. Server admission, CLI/UI/docs, completed-route qualification, and OPD exact resume remain open, so the phase-wide checkpoint items stay unchecked. |
| 2026-07-10 | Durable GRPO checkpoint server admission | `sha256:469726c35892` | this commit | portable + ROCm/Vulkan/CUDA compile gates | 840 server library tests; exact-kind SFT/GRPO admission and type-aware status discovery; durable-root concurrency contract; default, ROCm, Vulkan, and toolkit-free CUDA server all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | Inline and streamed GRPO jobs now publish exact checkpoints directly beneath the adapter registry instead of transient final-adapter staging on generic and CUDA-delegated routes. Dequeue admission accepts only one direct `.kiln-checkpoint` basename, validates the complete immutable artifact set and checksums, rejects traversal, wrong training kind, and wrong adapter, and normalizes the path before any GPU phase. Job detail discovers only the newest manifest-valid checkpoint matching its SFT or GRPO job type and reports the checkpoint/data route explicitly; zero checkpoint cadence now fails at GRPO submission. The CUDA all-target gate exposed an unrelated stale benchmark progress callback that returned `()` instead of `TrainControl`; it was repaired immediately and the full compile gate passed without a CUDA toolkit. Cheap CI run `29165069604`, including the preceding regression repair, is green with accelerator jobs skipped. CLI, browser ergonomics, permanent docs, completed public-route hardware qualification, and OPD exact resume remain open. |
| 2026-07-10 | GRPO exact-resume CLI | `sha256:235a13bd83fa` | this commit | portable + ROCm/Vulkan/CUDA compile gates | 841 server library tests; inline and JSONL payload regressions; Clap positive/zero cadence boundary; rendered CLI help; ROCm, Vulkan, and toolkit-free CUDA server all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | `kiln train grpo` now exposes exact checkpoint cadence and immutable resume basename flags for both inline JSON batches and memory-bounded JSONL sources, preserving the server's configuration object instead of creating a second request dialect. A typed non-zero cadence rejects zero before file I/O or network submission, and help text states that resume requires the same source and configuration. The preceding server-admission CI run `29165374855` is fully green with accelerator jobs skipped. Browser ergonomics, permanent docs, completed public-route hardware qualification, and OPD exact resume remain open. |
| 2026-07-10 | Exact training resume browser workflow | `sha256:bd1cd161688b` | this commit | Chromium desktop/mobile + portable source checks | full server UI smoke across normal, empty-adapter, cold-start, and API-failure scenarios; 1280x900 and 390x844 expanded-control non-overlap/overflow gates; JavaScript syntax; Rust 1.94/1.96 formatting and diff hygiene | passed | SFT and GRPO advanced forms now expose optional positive checkpoint cadence and a direct immutable resume basename, omit blank fields, and reject zero/non-integer cadence or path-shaped checkpoint names before submission. Job drill status distinguishes SFT epoch/example cursors from inline/JSONL GRPO group cursors, copies the exact basename, and can prepare the matching form from replay metadata. Preparation restores recorded visible settings and named datasets when available but clears unverifiable inline data before enabling submit, so a stale browser source cannot accidentally accompany a valid checkpoint. Browser tests prove fresh/cadenced payloads, route-aware status, clipboard behavior, safe preparation, source clearing, traversal rejection, and stable responsive layout. The preceding CLI CI run `29165551915` is fully green with accelerator jobs skipped. Permanent docs, completed public-route hardware qualification, and OPD exact resume remain open. |
| 2026-07-10 | Canonical SFT/GRPO checkpoint documentation | `sha256:bd1cd161688b` | this commit | documentation + release contract | release-version/link drift check; stale resumability-claim scan; diff hygiene; pushed UI workflow's dedicated Chrome CI green and cheap CI jobs green with accelerators skipped | passed | The native checkpoint contract, README, quickstart, GRPO operator guide, and changelog now treat SFT plus inline/streamed GRPO as exact-resumable and OPD alone as PEFT-only. They distinguish resumable bundles from serving snapshots; enumerate optimizer, reference/EMA, cursor/RNG, JSONL byte/line, loss/diagnostic, identity, and route state; document cancellation/crash boundaries, status discovery, browser preparation, API/CLI examples, fail-closed admission, immutable checkpoint collisions, and weights-only `base_adapter` semantics. Documentation-only files remain excluded from source identity, so the source hash correctly matches the preceding UI checkpoint. Static site copies, completed public-route hardware qualification, and OPD exact resume remain open. |
| 2026-07-10 | Static exact-resume product documentation | `sha256:1da814f1938d` | this commit | Chromium docs site + source contract | complete docs-site mobile/browser smoke; release-version/local-link drift check; focused CLI parser regression; Rust 1.94/1.96 formatting and diff hygiene | passed | The public API, CLI, quickstart, and GRPO pages now share the same inline/streamed JSONL route contract, checkpoint/resume flags, immutable basename examples, exact restored-state claim, job identity/cursor fields, browser handoff, and explicit OPD limitation. The smoke contract now requires those facts and copy-paste examples. Pages run `29166091524` promptly caught that the earlier CLI example had dropped the literal `one group per line` format guarantee; the CLI help and its source invariant were repaired in this checkpoint and the exact local Pages command is green. Canonical and static documentation are complete; completed public-route hardware qualification and OPD exact resume remain open. |
| 2026-07-10 | Public GRPO exact-resume route qualification | `sha256:279d01899d17` | this commit | Strix Halo ROCm + Vulkan | qualification-mode real-device HTTP integration for inline and streamed JSONL GRPO; public submit/status/cancel/resume routes; Rust 1.94/1.96 formatting and default/ROCm/Vulkan server gates | passed | On both local backends, each public source route completed an uninterrupted 12-group control, submitted the same job again, cancelled it through `DELETE /v1/train/queue/{id}` after it entered `running`, loaded the exact immutable basename exposed by job status, and resumed through `POST /v1/train/grpo`. The combined interrupted/resumed loss sequence, final PEFT adapter bytes, final checkpoint adapter and optimizer bytes, EMA-reference bytes, committed cursor, JSONL physical-line/byte/hash state, RNG streams, effective configuration, precision, scheduler/optimizer manifest, and semantic diagnostics matched the uninterrupted control exactly; blank JSONL lines exercised the physical cursor and backend health remained unquarantined. ROCm used a production-shaped BF16 identity-bearing base with the production-default tape flags, while Vulkan exercised its supported resident F32 route. Replacement Pages run `29166231196` and cheap CI run `29166231193` are green after the preceding documentation regression repair. OPD exact resume and process-kill/fault qualification remain open. |
| 2026-07-10 | Preserve OPD prompt identity after filtering | `sha256:8cb5f0cc63e1` | this commit | portable + ROCm/Vulkan compile gates | focused filtered-row source-index regression; 418 trainer tests with one pre-existing ignore; ROCm/Vulkan train all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | OPD previously compacted valid tokenized prompts after filtering but later indexed rollout prefixes and asymmetric teacher contexts with the compacted position. If an earlier row was invalid, a retained row could train against another source row's prompt or privileged context. Prepared prompts now carry their original source index through every downstream lookup, and a regression filters row zero while proving row one remains bound to its own tokenization and teacher-only context. Exact checkpoint cursor work can now bind the real source order rather than preserving an already-corrupted index. |
| 2026-07-10 | Strict OPD recovery-state contract | `sha256:5ef0e1508adf` | this commit | portable | 27 focused loop-state and diagnostics regressions; unknown-field, cursor, history, and non-finite corruption rejection; Rust 1.94/1.96 formatting and diff hygiene | passed | OPD recovery state distinguishes consumed source/sample candidates from committed optimizer steps and preserves loss, data/token, ECHO, gradient-diagnostic, and stateful collapse-guardrail accumulators. Tensor artifacts and runtime restore are intentionally the next checkpoint. |
| 2026-07-10 | Exact OPD core stop/resume integration | `sha256:d889872feea5` | this commit | portable + Strix Halo ROCm/Vulkan | 423 portable trainer tests with one pre-existing ignore; real ROCm BF16 and Vulkan F32 uninterrupted-versus-cancelled/resumed equivalence; requested-base-adapter byte-equivalence regression; default/ROCm/Vulkan server all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | OPD now atomically checkpoints and restores exact adapter, AdamW/Muon, scheduler, distinct optimizer-step and source/sample cursors, rollout/init RNG streams, effective seed/configuration, loss and diagnostic state, model/tokenizer/base-weight/backend/precision identity, and authoritative teacher revision. Both local backends matched every loss plus final and intermediate adapter/optimizer bytes. The loop now honors stop callbacks, rejects zero cadence and non-finite loss, records resolved seeds and auto-scaled sample counts, makes final synchronization fallible, and evicts resident state on every exit. This work also exposed that `base_adapter` was recorded but never loaded; the requested weights now initialize OPD before optimizer registration. Server ownership, durable public admission, CLI/UI/docs, and process-kill fault qualification remain separate checkpoints. |
| 2026-07-10 | Settled per-candidate OPD core ownership | `sha256:103751102869` | this commit | portable + Strix Halo ROCm/Vulkan | 423 portable trainer tests with one pre-existing ignore; real coordinated ROCm BF16 and Vulkan F32 exact-resume gates; inference read admission at every optimizer boundary; injected settlement-failure waiter; Rust 1.94/1.96 formatting and diff hygiene | passed | OPD now treats allocation, restore/registry setup, on-policy rollout, each optimizer candidate, checkpoint capture, final device snapshot, and cleanup as bounded healthy-writer phases with explicit backend settlement. Panic or failed settlement quarantines before releasing ownership, and local-teacher fixture construction can settle per prompt. Exact checkpoint encoding, checksumming, fsync, rename, final PEFT export, and receipts run after device ownership is released. The real tests delete failed final-adapter staging before exact resume from a separate checkpoint root, retain byte-identical continuation, admit inference between every candidate, and prove cancellation/completion release the writer. Cheap CI run `29167996227` for the preceding exact-state checkpoint is green with accelerator jobs skipped. Server call sites remain job-wide until the next checkpoint. |
| 2026-07-10 | Durable OPD server admission and exact source identity | `sha256:88b89f24396d` | `e764ec1c` + this commit | portable + Strix Halo ROCm/Vulkan; toolkit-free CUDA compile | 425 train tests with one pre-existing ignore; 844 server library tests; 21 resource/concurrency invariants; real ROCm BF16 and Vulkan F32 exact cancel/resume; default, ROCm, Vulkan, and CUDA server all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | Direct OPD and all four distillation recipes now admit only a validated direct OPD checkpoint basename, publish exact bundles under the durable adapter registry rather than final-adapter staging, expose the newest valid OPD checkpoint through job detail, and retain only bounded settled GPU phases. Local teacher adapter load, fixture scoring, every OPD phase, and all teacher/device releases are coordinated; primary and cleanup failures remain visible together. Fixed/composite fixtures and the deterministic test source derive order-independent revisions from their exact numeric rows or algorithm contract, so the default 25-step checkpoint no longer fails late merely because a generated teacher is not one model identity. Server receipts read the trainer's successful OPD receipt and propagate its actual resolved seed instead of substituting zero for an omitted request seed. Strict kind/adapter/traversal admission, status discovery, seed provenance, source-revision mutation/order, and source-shape invariants are covered. Cheap CI run `29168349786` exposed a stale GRPO source invariant after coordination was centralized; `1416a036` repaired it immediately and replacement run `29168553309` is fully green with accelerator jobs skipped. CLI/browser ergonomics, permanent documentation, public HTTP hardware qualification, and process-kill/fault qualification remain open. |
| 2026-07-10 | First-class OPD exact-resume CLI | `sha256:d5ac1ac52bb9` | this commit | portable; ROCm/Vulkan compile gates | 67 CLI tests; 848 server library tests; rendered command help; release/CLI drift guard; default, ROCm, and Vulkan all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | `kiln train opd` accepts either the typed `/v1/train/opd` request object or a prompt array, requires one prompt/dataset source and a registered teacher from the request or `--teacher`, preserves advanced request configuration, and gives explicit adapter, LoRA-rank, checkpoint-cadence, and immutable-resume overrides. It rejects missing teachers, ambiguous sources, malformed/unsupported OPD configuration, and zero CLI cadence before network work. SFT, GRPO, and OPD now share one submission/status rendering path, and job status already prints OPD's newest exact basename. Preceding server CI run `29169320203` is fully green with accelerator jobs skipped. Browser preparation, permanent documentation, public HTTP hardware qualification, and fault interruption remain open. |
| 2026-07-10 | OPD exact-resume browser workflow | `sha256:9068849f9ef8` | this commit | Chromium desktop/mobile + portable; ROCm/Vulkan compile gates | complete server test-target suite including 848 library tests and 21 resource invariants; repeated full Chrome 148 passes across normal, empty-adapter, cold-start, failure-state, and mobile scenarios; default/ROCm/Vulkan all-target checks; release/runtime guards; Rust 1.94/1.96 formatting and diff hygiene | passed | Checkpoint status now exposes its validated effective configuration, exact data hash/count, and OPD teacher identity/revision. The job drill prepares the OPD form with authoritative settings and basename, selects only the exact usable teacher revision, deliberately clears unverifiable prompts, and keeps the source warning visible; legacy checkpoints missing either binding fail closed in the browser. Fresh and resumed submission prove positive cadence, path-safe basenames, teacher-revision drift rejection, queue navigation, and OPD candidate-cursor status. The browser run also exposed a long-lived action toast intercepting controls after scroll; toast surfaces are now click-through except for explicit action/dismiss buttons, and the full suite passes repeatedly. Release run `29169568729` caught the new CLI URL-default count; `e7055783` repaired the guard immediately and replacement run `29170007351` is green. Permanent documentation, public HTTP hardware qualification, and fault interruption remain open. |
| 2026-07-10 | Permanent OPD exact-resume documentation | `sha256:329624383622` | this commit | documentation + Chromium site contract | full docs-site Chrome 148 smoke; 67 CLI tests; release-version/local-link and runtime-default guards; stale resumability-claim scan; Rust 1.94/1.96 formatting and diff hygiene | passed | The canonical checkpoint contract, README, Quickstart, changelog, off-policy JSONL guide, public API/CLI/quickstart/architecture pages, product summary, Rust config docs, and top-level CLI help now treat OPD as exact-resumable. Copy-paste API and CLI examples preserve the same request/config, immutable basename, prompt or dataset identity, and exact teacher revision; status, candidate cursor, cancellation, browser prompt clearing, legacy/PEFT rejection, and ROCm/Vulkan evidence are explicit. The docs smoke now requires the OPD endpoint, command, payload shape, cadence, resume fields, teacher binding, candidate cursor, and serving-only PEFT distinction, so the removed limitation cannot return silently. Public HTTP hardware qualification and fault interruption remain open. |
| 2026-07-10 | Public OPD exact-resume route qualification, ROCm arm | `sha256:84e00b94a345` | this commit | Strix Halo ROCm/gfx1151; Vulkan compile gate | real-device public teacher registration, OPD submit/status/cancel/resume, and exact control equivalence; existing inline/JSONL GRPO route qualification; 849 server library tests; focused admission and checkpoint-discovery tests; full Chrome 148 UI matrix and docs smoke; default/ROCm/Vulkan server all-target checks; release/runtime guards; Rust 1.94/1.96 formatting and diff hygiene | passed | A real BF16 ROCm public OPD run registered the local teacher, completed an uninterrupted 12-candidate control, cancelled a repeated run through the HTTP queue endpoint at a committed candidate boundary, loaded the exact immutable basename from job status, and resumed through `POST /v1/train/opd`. Combined losses, final PEFT bytes, checkpoint adapter/optimizer bytes, effective configuration, data identity, candidate cursor, manifest, loop state, and backend health matched the control. Qualification first rejected the fixture's unsupported top-K 4 and exposed a GRPO-only hint on generic training errors; the fixture now uses native top-K 16 and remediation is route-neutral. It then exposed that materialized-row content revision had been conflated with the registered model identity in checkpoint status and browser preparation. Status now reports distinct `teacher_identity_revision` and `teacher_content_revision` fields, malformed embedded identities fail closed, the browser compares only identity revisions, and server resume still verifies exact numeric-source content. The generalized harness reran both GRPO source routes successfully. Vulkan runtime execution and process-kill/fault qualification remain open. |
| 2026-07-10 | Public OPD exact-resume route qualification, Vulkan arm | `sha256:84e00b94a345` | `b23ed39e` | Strix Halo Vulkan/RADV | real-device public teacher registration and OPD uninterrupted/cancel/resume equivalence; inline and streamed JSONL GRPO regression qualification | passed | The same committed public-route harness ran without a Vulkan-only code change. A resident F32 Vulkan OPD control and its HTTP-cancelled/resumed counterpart matched combined losses, final PEFT bytes, checkpoint adapter/optimizer bytes, identity/content provenance, candidate cursor, manifest, and loop state, and the backend remained healthy. Both pre-existing GRPO public source routes also retained exact equivalence under the generalized harness. This completes the local ROCm-then-Vulkan public HTTP OPD checkpoint arm; process-kill/fault qualification remains open. |
| 2026-07-10 | Crash-atomic checkpoint publication qualification | `sha256:7817c5f87de7` | this commit | portable Unix process/filesystem | injected error and real `SIGKILL` at all seven durable publication boundaries; canonical-name rejection; orphan-staging retry; complete trainer and server checkpoint gates | passed | The unchanged public writer now delegates to a privately observable publication state machine. Both deterministic faults and separate child processes killed after staging creation, sentinel sync, artifact write, manifest/artifact sync, sentinel removal and tree sync, atomic rename, and parent-directory sync prove the final `.kiln-checkpoint` path is absent before rename or fully checksum-valid afterward. Pre-rename kills retain realistic orphan staging; even the fully written sentinel-free pre-rename directory is rejected because the loader now requires the canonical suffix, while a retry publishes and loads the intended target beside it. In-process faults clean staging through the existing guard. Together with the public ROCm/Vulkan uninterrupted-versus-cancel/resume evidence, this closes the Phase 3.2 crash/fault gate for SFT, GRPO, and OPD's shared checkpoint substrate. |
| 2026-07-10 | Admission-time training and eval seed provenance | `sha256:393c6249114a` | this commit | portable; ROCm/Vulkan compile gates | 220 eval tests; 859 server library tests; focused end-to-end, agentic, real-model, and resource-contract tests; full Chrome 148 UI matrix and docs smoke; default/ROCm/Vulkan all-target checks; release guards; Rust 1.94/1.96 formatting and diff hygiene | passed | Every public SFT, GRPO, OPD, distillation, recipe, self-improve, post-training, and eval admission now resolves an omitted seed once before queue publication, preserves an explicit seed, and reports the exact unsigned decimal value through API, CLI, status, browser drill, copy actions, raw JSON, and JSONL export without JavaScript precision loss. Resume inherits the checkpoint's authoritative LoRA-init seed and rejects mismatch or missing RNG provenance. Paired comparisons share one job seed; the versioned SHA-256 derivation produces stable per-example/per-completion seeds, filtered reruns reuse the original seed, and duplicate resolved example identities fail admission. Documentation scopes deterministic completion replay to the same model artifacts, binary, backend, driver/runtime, tokenizer/template, and generation environment rather than claiming cross-build byte identity. The seed contract is backend-independent and required no accelerator runtime, while both local backend feature surfaces compiled successfully. |
| 2026-07-10 | Canonical process execution provenance | `sha256:feaf7e705301` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 99 core tests with 3 expected ignores; 864 server library tests; focused schema, startup-builder, health, and gated-debug adversarial tests; default, ROCm gfx1151, and Vulkan server all-target checks; full Chrome 148 docs-site smoke; release/local-link guards; Rust 1.94/1.96 formatting and diff hygiene | passed | Production startup now creates one strict self-verifying `kiln.execution-provenance.v1` record over backend/device, bounded driver/runtime evidence, the exact running executable and optional source metadata, model/tokenizer/template identity, inference and training precision policy, compiled kernel contract, and effective configuration/environment digests. Secret-bearing `KILN_*` values contribute only a redacted presence marker. The record remains attached to resident weights and `AppState`; real-backend health fails readiness when it is absent or invalid and exposes only bounded digests, while the gated debug endpoint returns the full validated record and rejects tampering. A permanent contract documents evidence sources, source-versus-binary authority, redaction, API behavior, and the integrity-only scope. Exact checkpoint, receipt, adapter, and eval persistence remain the next slice, so the Phase 3.3 checkbox stays open. |
| 2026-07-10 | Durable base-weight provenance binding | `sha256:94b2e4162d07` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 100 core tests; 220 eval library tests plus integration/example suites; 376 model library tests; 431 train tests with one pre-existing ignore; 860 server library tests; focused eval end-to-end and 6 CLI tests; full server-UI and docs-site Chrome 148 matrices; default, ROCm gfx1151, and Vulkan server all-target checks; release/local-link guards; Rust 1.94/1.96 formatting and diff hygiene | passed | Exact SFT, GRPO, and OPD checkpoints now require the complete validated shard manifest at capture and resume, retain the aggregate consistency binding, reject legacy aggregate-only exact continuation, accept audit-only filename relocation, and reject byte/size/multiplicity drift before GPU ownership. Successful train receipts and adapter manifests persist and validate the full identity. Eval admission snapshots it before queue publication; restart archives reject corruption; list/detail/raw JSON, human and JSON CLI output, dashboard summaries/copy controls, and standalone outcome JSONL preserve it. Adversarial tests cover missing, inconsistent, renamed, byte-changed, and tampered artifacts. A canonical permanent contract defines the schema, aggregate algorithm, lifecycle, legacy policy, and limits; README, checkpoint, adapter, eval, quickstart, and API docs agree. |
| 2026-07-10 | Loader-owned base-weight shard manifest | `sha256:92bc46a3c8f3` | this commit | portable; ROCm/Vulkan compile gates | 99 core tests; 376 model library tests; focused health/debug API tests; default, ROCm, and Vulkan server all-target checks; Rust 1.94/1.96 formatting and diff hygiene | passed | The immutable model snapshot's existing one-time mmap hash pass now retains a strict versioned manifest of every path-safe shard filename, exact byte length, and complete SHA-256, including shards whose tensors are not extracted. Its pinned aggregate remains byte- and multiplicity-sensitive while staying relocation- and index-order-independent under the existing content-identity contract; filenames separately provide auditable artifact identity. Post-upload verification reports the exact changed shard, GPU weights retain the manifest, and production startup fails closed if its aggregate or transfer differs. `/health` exposes a bounded aggregate/count/byte summary while gated `/v1/debug/model-state` exposes the complete manifest, both without re-reading weight files. Training/checkpoint/eval persistence remains the next slice, so the Phase 3.3 checkbox stays open. |
| 2026-07-09 | First reduced-CI measurement | `sha256:cda13f3f84e5` | `3c71cc4002f8` | GitHub Actions | run `29049575526` | passed | Three active jobs completed in 3m52s wall and about 4m36s aggregate time; all GPU backend jobs were skipped |

| 2026-07-10 | Training artifact execution-provenance binding | `sha256:74120bee2991` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 434 train library tests with one expected ignore; 864 server library tests; exact-checkpoint, receipt, and adapter-manifest adversarial tests; default, ROCm gfx1151, and Vulkan server all-target checks; full Chrome 148 docs-site smoke; release/local-link guards; Rust 1.94/1.96 formatting and diff hygiene | passed | Exact SFT, inline and streamed GRPO, and OPD checkpoints now require the complete validated startup execution record, retain it in auxiliary state, and compare its canonical digest before GPU ownership on resume. Successful model-backed receipts and adapter manifests persist and validate that record plus the trainer's concrete parameter, optimizer-state, activation, gradient, and stochastic-rounding precision; early failures, dry runs, and legacy non-resume artifacts may omit the optional audit fields, while legacy partial-runtime checkpoints fail closed for exact continuation. Eval persistence and product surfaces remain the next slice, so the Phase 3.3 checkbox stays open. |

| 2026-07-10 | End-to-end execution-provenance artifacts and product surfaces | `sha256:13b0226e2089` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 217 eval library tests with 3 expected Python-dependent ignores plus all eval targets; 434 train library tests with one expected ignore; 869 server library tests; 7 CLI and 2 eval end-to-end tests; full desktop/mobile server-UI and docs-site Chrome 148 matrices; default, ROCm gfx1151, and Vulkan server all-target checks; release/local-link guards; Rust 1.94/1.96 formatting and diff hygiene | passed | Eval admission now validates and snapshots both resident artifact identities before publication; list/detail/raw JSON, terminal archives, human and JSON CLI output, dashboard drill-ins, exact-copy controls, and standalone outcome JSONL retain the full self-verifying execution record. Archive save/load, CLI consumption, real-backend admission, checkpoint discovery, receipt-backed training detail, and adapter metadata fail closed on tampering while legacy/synthetic non-resume artifacts remain readable without optional fields. Training drill-ins also show the concrete dtype and rounding contract. The preceding Pages run caught a protected cold-reader phrase regression; `66d86bbc` restored it immediately and replacement Pages run `29175782840` is green. This closes the Phase 3.3 provenance-recording item; replay-language and repeated-run gates remain separate. |
| 2026-07-10 | Integrity-scoped replay and receipt contract | `sha256:e1b3d7a77c3b` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 435 train library tests with one expected ignore; replay CLI test; 869 server library tests; 22 resource/concurrency contracts; full desktop/mobile server-UI and docs-site Chrome 148 matrices; default, ROCm gfx1151, and Vulkan server all-target checks; release/runtime/local-link guards; Rust 1.94/1.96 formatting and diff hygiene | passed | The historically named `kiln-replay`, `replay.jsonl`, and `replay_hash` surfaces now state and enforce their actual boundary: they recompute request-lineage hashes and never load a model, train, infer, compare tensors/outputs, or prove reproducibility. CLI help and success output say this directly; README, checkpoint, eval, vignette, site, Rust API, legacy receipt, library UI/API, and the active OPD plan agree. A permanent contract enumerates bound and missing inputs, scopes exact continuation to validated `.kiln-checkpoint` state inside its declared envelope, and corrects the former false `PIPE_BUF` regular-file atomicity claim to single-writer synchronized JSONL with malformed partial records rejected. A repository invariant forbids the removed rebuild, replay, per-PR GPU, and cross-backend byte-identity promises. This closes the Phase 3.3 replay-language item; same-environment repeated-run adapter-hash testing remains separate. |
| 2026-07-10 | Same-environment training repeatability qualification | `sha256:e21ac0b8a4e8` | `91bbd78b` + this commit | Strix Halo ROCm/gfx1151 BF16 and Vulkan/RADV F32; portable compile/docs gates | real-device public SFT, inline GRPO, streamed JSONL GRPO, and OPD repeatability plus cancel/resume tests on both backends; default/ROCm/Vulkan server checks; full Chrome 148 docs-site smoke; release/local-link and diff guards | passed | Each public route now runs two clean jobs with the same resident model bytes, execution provenance, data, effective config, and seed, and then one HTTP-cancelled/resumed job. The gate requires exact losses, receipt-backed final adapter SHA-256 and bytes, checkpoint adapter/optimizer/reference tensors, semantic manifest, and objective loop state. Qualification first failed because the old synthetic fixture no longer met the production exact-checkpoint provenance contract; it now serializes its actual tiny base tensors into a deterministic safetensors shard, derives the loader manifest from those bytes, uses the real test executable/runtime to build the startup provenance record, and keeps the local teacher identity aligned. All six ROCm/Vulkan route gates pass. Permanent documentation limits the result to the recorded same-process/backend/device envelope and makes no cross-backend or cross-machine byte-identity claim. This closes Phase 3.3. |
| 2026-07-10 | Canonical agentic training message schema | `sha256:bde7cd1ed564` | `79821a73` + this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 101 core tests with 3 expected network ignores, 217 eval tests with 3 expected Python-dependent ignores, 437 train tests with one expected ignore, and 870 server tests; inline/local-JSONL/named-dataset ingestion fixtures; SFT render/mask and rollout-provenance goldens; Qwen3.5 agentic template integration; default/ROCm/Vulkan all-target checks; full Chrome 148 docs smoke; release/local-link guards; repository-pinned Rust 1.96.1/rustfmt 1.9 formatting and diff hygiene | passed | `kiln_core::tokenizer::ChatMessage` is now the one inference-tokenizer, eval, synthesis, and training type. Plain messages retain the exact two-field wire shape, while OpenAI assistant tool calls, tool names, call IDs, and `role: "tool"` responses survive every SFT/GRPO/OPD conversion and all three SFT transports. Missing/null and text-part-array content normalize consistently. SFT chat-template rendering sees the complete agentic fields and assistant masking remains role-based. Exact rollout provenance now accepts prior tool context, binds every canonical field into the prompt hash, and persists it in trainer-compatible JSONL; request tool definitions/choice remain rejected because generated tool actions are still a separate scored-payload gate. ROCm/Vulkan evidence is compilation only because this slice changes schema and CPU-side tokenization rather than device execution. The first hosted run exposed that floating `stable` selected rustfmt 1.9 while local validation had resolved rustfmt 1.8; the repository now pins Rust 1.96.1 plus rustfmt, CI honors that pin, contributor setup documents it, and qualification source identity includes it. Replacement hosted CI run `29178318558` and qualification-contract run `29178318589` are fully green. This closes the first Phase 3.4 item. |

| 2026-07-10 | Source-pinned Qwen SFT token and assistant-label contract | `sha256:ad823a5179c4` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 104 core tests with 3 expected network ignores, 217 eval tests with 3 expected Python-dependent ignores, 439 train tests with one expected ignore, 871 server tests, and 257 qualification tests; exact Transformers 5.13.1/TRL 1.8.0 fixture regeneration; six-case real Qwen tokenizer integration; default/ROCm gfx1151/Vulkan all-target checks; full Chrome 148 docs smoke; release/local-link/contract guards; Rust 1.96.1/rustfmt 1.9 formatting and diff hygiene | passed | Native SFT now renders complete conversations with `add_generation_prompt=false` and, for the pinned Qwen3.5-4B template, selects TRL's exact prefix-preserving thinking training template. The source-pinned oracle records exact rendered text, token IDs, assistant masks, and `-100` labels for plain, explicit-thinking, tool-call, tool-response, multi-turn, and adversarial delimiter-literal rows against Qwen revision `851bf6e8`, Transformers commit `4626421d`, and TRL commit `95809b94`. Assistant role headers plus system/user/tool turns are masked; thinking, answer, tool-call body, `<|im_end|>`, and its newline are supervised. Internal generation-boundary markers prevent role/terminator literals in dataset content from moving loss spans and fail closed on marker collision. Preflight and public examples use the same production tokenizer/mask path. The effective training-template digest is distinct from the inference-template digest and is retained in process provenance, exact SFT checkpoints, train receipts, and adapter manifests with cross-record validation. The vendored TRL template has exact source, digest, and Apache-2.0 attribution. ROCm/Vulkan evidence is compilation only because this slice changes CPU-side rendering, tokenization, labels, and artifact identity rather than device kernels. This closes the Qwen golden and assistant-only masking items in Phase 3.4. |

| 2026-07-10 | Canonical SFT row admission and transport-independent receipts | `sha256:e6754697e79c` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 447 train tests with one expected ignore, 873 server tests, 26 real-model integration tests with one expected heavy ignore, 257 qualification tests, source-pinned real-Qwen tokenization, focused queue/tracked-cap/name/LoRA/UI/eval integration suites, default/ROCm gfx1151/Vulkan all-target checks, full Chrome 148 server-UI and docs-site smoke, committed receipt validation, release/runtime/local-link guards, Rust 1.96.1/rustfmt 1.9 formatting and diff hygiene | passed | Every public SFT source now reduces through one pre-GPU admission contract: inline JSON, local JSONL, named datasets, corrections, recipes, and direct Rust calls share an explicit fail-by-default or skip-and-record policy and the exact production chat-template/tokenization/mask path. Parsed rows receive domain-separated canonical JSON hashes; malformed JSON receives a bounded raw-byte hash without retaining content; kept hashes are ordered and duplicate-preserving; rejected positions and stable reason codes plus a kept-corpus aggregate persist in train receipts and bind exact-resume auxiliary identity. Streamed files are reparsed in the worker and must reproduce the submit-time manifest before GPU ownership; materialized rows are likewise rehashed, preventing queue-time mutation or caller-forged evidence. Equivalence fixtures prove identical selected examples, kept/rejected hashes, and aggregate identity across all transports, including blank-line handling, malformed JSON, agentic nested fields, duplicates, policy parity, and tampering. The CLI and Playground expose the policy and permanent API/receipt documentation defines limits. Broader integration testing caught two error-precedence regressions before publication: direct SFT tokenization had moved ahead of stable mock rejection, and recipe tokenization had moved ahead of whole-recipe name/capacity/mock validation. Both paths now perform static, atomic admission before corpus work while real backends still reject invalid rows before queue publication. ROCm/Vulkan evidence is compilation only because this slice changes CPU-side ingestion and artifact identity rather than device execution. This closes the invalid-row policy and ingestion-equivalence items in Phase 3.4. |

| 2026-07-10 | Versioned bounded native SFT profile foundation | `sha256:6d29cb89b528` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 451 train tests with one expected ignore; 875 server library tests plus all integration targets, including 26 real-model tests with one expected heavy ignore; strict dedicated/front-door/recipe/central-admission regressions; 257 qualification tests; default/ROCm gfx1151/Vulkan all-target checks; full Chrome 148 server-UI and docs-site smoke; committed qualification and benchmark receipt validation; release/runtime/thinking-budget/desktop/local-link guards; Rust 1.96.1/rustfmt 1.9 formatting and diff hygiene | passed | Native SFT now names and persists one exact `native_online_lora_v1` shape: one admitted conversation per microbatch and optimizer update, deterministic epoch permutations, constant learning rate, accumulation 1, no warmup or clipping, and checked step-count arithmetic. Unknown request/config/optimizer fields and non-finite or invalid learning-rate, LoRA, checkpoint, and optimizer values return structured `training_invalid_request` errors before publication. CLI and browser payloads select the profile explicitly; receipts and exact-checkpoint scheduler state bind it; permanent documentation records actual backend parameter/activation/gradient/state dtypes, the absence of an F32 master on BF16 paths, the non-portable legacy stochastic-rounding switch, and the separate soft-failing MTP pass without claiming broad trainer parity or Muon superiority. Review also found that tagged `POST /v1/train` SFT had bypassed canonical row admission, memory preflight, and ingestion evidence; it now delegates to the same atomic admission function as `POST /v1/train/sft`, with a focused no-publication regression. The HF/TRL handoff remains explicitly unshipped, so the Phase 3.4 narrowing/routing checkbox stays open. |

| 2026-07-10 | Strict HF/TRL handoff identity foundation | `sha256:3d221b78ccaa` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 456 train tests with one expected ignore; 104 core tests with three expected network ignores; focused manifest/path/template/cross-language-hash adversarial tests; default/ROCm/Vulkan server all-target checks; release/local-link guard; real Chrome 148 docs smoke; formatting and diff hygiene | passed | The additive `kiln.hf-trl-export.v1` and `kiln.hf-trl-result.v1` contracts bind complete base shards, model/tokenizer configuration, distinct inference/native/TRL template bytes, exact corpus and selection/provenance identity, optional split and input adapter, reference and executed scripts, package versions, typed effective configuration, and exact PEFT output. Semantic self-digests and every referenced nonempty regular file fail closed on unsupported versions, task mismatch, unknown or duplicate fields, traversal, path aliases, symlinks, size drift, and hash drift. Qwen's marker-stripped native identity can no longer be mistaken for TRL's generation-marked template. Decimal configuration travels as exact text to avoid Rust/Python JSON exponent drift. Custom scripts are preserved and reported distinctly instead of being prohibited. The contract and validators are foundation only: no public exporter, reference runner, import API, or round-trip claim exists yet, so every Phase 3.5 checkbox and the Phase 3.4 routing item remain open. |

| 2026-07-10 | Source-provenance and complete SFT handoff binding | `sha256:12b508efffd9` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 457 train tests with one expected ignore; six focused HF/TRL contract tests; full-ingestion cross-field and source-provenance mismatch adversarial cases; default/ROCm/Vulkan server all-target checks; formatting and diff hygiene | passed | The export now carries the source process's complete self-verifying execution provenance and cross-checks its model-config, tokenizer-vocabulary, tokenizer-config, inference-template, and native-training-template hashes against the exact exported files. A pinned Python environment lock is a required hashed artifact. SFT exports declare generation-span assistant-only labels and retain the complete `kiln.sft-ingestion.v1` receipt rather than only aggregate counts; verification reparses it and requires the source, policy, row counts, per-row identities, and ordered corpus digest to agree with the compact manifest. File tampering, internally valid source substitution, and provenance/artifact substitution all fail closed. This is still pre-API contract hardening; the builder, public transport, scripts, import, and round trip remain open. |

| 2026-07-10 | Atomic immutable HF/TRL SFT bundle writer | `sha256:f89a57b27a98` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 461 train tests with one expected ignore; four focused bundle publication tests; default/ROCm/Vulkan server all-target checks; formatting and diff hygiene | passed | The new synchronous library writer revalidates every prepared row, receipt hash, sequence/supervision bound, source provenance, base-shard manifest, and generation-span template before snapshotting. It writes only the exact declared model, tokenizer, three templates, canonical messages JSONL, full ingestion receipt, optional split and input adapter, reference script, and environment lock into a new `.incomplete-<uuid>` sibling. Every file uses create-new plus fsync; the exact recursive file set and all manifest/artifact cross-checks pass before one rename publishes an immutable `.kiln-hf` directory and syncs its parent. Existing targets are never replaced. In-process failures remove staging; tests cover input drift, invalid split JSON, symlinked adapter artifacts, optional adapter byte identity, immutable-target rejection, and post-publication tamper detection. This is a library checkpoint only; public transport and the real pinned reference script remain open. |
| 2026-07-10 | Pinned self-verifying HF/TRL SFT reference runner | `sha256:15d3db80ff94` | this commit | portable CPU contract tests; Strix Halo ROCm/Vulkan compile gates; external accelerator execution pending | 463 train tests with one expected ignore; 264 qualification tests; eight focused standard-library runner tests; Python compilation/help smoke; default/ROCm/Vulkan server all-target checks; hosted CI run `29182898109`; formatting and diff hygiene | passed | The standalone runner and embedded environment lock pin Torch 2.13.0, Transformers 5.13.1, TRL 1.8.0, PEFT 0.19.1, and their direct training stack. Before importing Torch it rejects schema, digest, file-set, symlink, artifact, ingestion, template, tokenizer, base-shard, and package-pin drift. It uses the exported generation-marked template with assistant-only loss, normalizes tool arguments through the source-pinned HF policy, derives a no-truncation sequence bound, materializes seed and importer-bounded effective trainer configuration, and distinguishes continued input adapters from newly configured LoRA. The v1 correctness reference rejects multi-process launch rather than racing its single-writer result publication; custom distributed scripts remain explicit and auditable. Rank zero preserves the actual executed script and streams exact PEFT artifacts into durable create-new files before a cross-language-stable self-verifying result manifest; the manifest is the completion marker, partial state is recoverable only with its explicit sentinel, and unattributed artifacts fail closed. Standard-library tests require no Torch installation and cover Rust/Python digest agreement, tampering, duplicate/unknown fields, symlink substitution, base drift, typed-config bounds, partial recovery, immutable completion, and result identity. The complete gate exposed a race in a recently added GRPO coordination test: its waiter briefly retained a read owner while the main thread asserted writer availability. Commit `c8f8a514` now joins that owner first and passed 200 stress repetitions plus the full suite. Real external training, public transport, import, and round-trip evidence remain open. |
| 2026-07-10 | Public immutable HF/TRL SFT export and download API | `sha256:650b86b04a1c` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 464 train tests with one expected ignore; 879 server tests; four focused create/list/detail/download/delete/capacity/recovery tests; default/ROCm/Vulkan server all-target checks; full Chrome 148 docs-site smoke; formatting and diff hygiene | passed | `POST /v1/train/hf/sft/exports` now applies the canonical SFT row admission contract to exactly one inline, server-local JSONL, named-dataset, or active-corrections source on a blocking worker, then snapshots the complete resident identity, optional object-valued split, and optional adapter under its revision barrier. A dedicated export lock serializes create, fully verified detail/download, and durable tombstone deletion. The private Unix-0700 registry rejects aliases and replacement, caps inline bodies at 256 MiB and published exports at 256, cleans only recognized crash residue, and refuses symlinks or special entries. List reads self-validating summaries without rehashing large corpora; detail and streamed tar.gz download require the exact recursive file set and all artifact hashes. Download retains ownership until the bounded channel closes and uses no-store caching; deletion remains available for an explicitly named damaged real directory so corruption cannot permanently consume capacity. The archive recreates one `{name}.kiln-hf` root and never buffers the full output. README, permanent interoperability documentation, and the public API site expose the exact routes, sources, limits, locks, archive shape, and still-open CLI/import boundary. GRPO export, CLI transport, validated import, and real external round trips remain open. |
| 2026-07-10 | Verified atomic HF/TRL SFT CLI handoff | `sha256:de4a59ef0279` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 891 server library tests; nine focused CLI/archive safety tests; nested-command parser tests; real loopback create/stream/verify/publish/conditional-delete/collision workflow; default/ROCm gfx1151/Vulkan all-target checks; full Chrome 148 docs smoke; release/runtime-default/local-link guards; formatting and diff hygiene | passed | `kiln train hf export-sft` accepts exactly one server-local JSONL or named-dataset source plus optional input adapter and local object split file. It rejects bad names, policies, URLs with redirects or embedded credentials, and existing/missing-parent outputs before creation. Download uses a name-constructed endpoint, bounded JSON and idle-read handling, a 64 GiB compressed-byte ceiling, streamed same-directory private staging, compressed-byte hashing, durable sync, and no-clobber publication. Creation and download ETags must equal the manifest identity. Verification admits only regular files/directories under one exact root, bounds entries and expanded bytes, rejects traversal, links, special files, duplicate and case-insensitive aliases, forces gzip checksum completion, and requires the complete pristine manifest to equal the creation identity. Failure retains the server export and prints only an identity-bound cleanup command. Successful publication deletes the exact export by default with `If-Match`; a reused name returns HTTP 412 without sacrificing the local artifact or deleting the replacement. Explicit unconditioned deletion remains available for operator cleanup of damaged manifests. `list --json` and `delete --export-sha256` cover retained lifecycle. The new nested command initially exposed that the cheap release guard understood only two command levels; it now registers the nested HF enum and longest-matches command paths, while the guard plus static-site contract require the HF workflow rather than skipping it. Hosted run `29185243883` then caught a separate hard-coded count of 16 centralized URL defaults after the three HF commands raised the correct total to 19; the runtime-default guard now structurally requires every `url: String` CLI field to carry `default_server_url()` instead of maintaining another synchronized magic number. README, permanent interop docs, API, CLI reference, and source help agree on server-local path semantics and still-open GRPO/import work. |
| 2026-07-10 | Closed completed-bundle and corpus-free PEFT import envelope | `sha256:4f900333f943` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 466 train library tests with one existing documented ignore; eight focused HF/TRL bundle tests; default/ROCm gfx1151/Vulkan server all-target checks; formatting and diff hygiene | passed | A completed external-training copy is now accepted only when the pristine export remains byte-identical and the exact executed script, PEFT config, PEFT weights, and result manifest are added with no partial sentinel or undeclared file. The derived `.kiln-hf-import` transport contains exactly ten files: both self-verifying manifests, the executed script, PEFT config/weights, and exact model config, tokenizer, inference template, native training template, and TRL training template. It deliberately excludes corpus, ingestion, split, input-adapter, environment-lock, and reference-runner bytes. Materialization verifies the full source before copying, uses private create-new staging, revalidates the allowlist and every included hash, fsyncs the tree, atomically publishes without replacement, and verifies again. Tests reject result/model/adapter drift, extra private files, partial results, and target replacement. This is shared contract foundation only; public upload and resident-model import remain open. |
| 2026-07-10 | Resident-identity-validated atomic HF/TRL PEFT import API | `sha256:84e0d7c7eeaf` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 468 train tests with one existing documented ignore; 897 server library tests; six focused import API tests; 11 focused LoRA loader tests; four resource-publication tests; nine pinned-runner tests; 266 qualification tests; default/ROCm gfx1151/Vulkan server all-target checks; full Chrome 148 docs smoke; release/runtime-default/local-link guards; formatting and diff hygiene | passed | The raw gzip API accepts only one exact `{name}.kiln-hf-import` root and streams it to private staging with compressed, expanded, entry, per-file, safetensors-header, zero-tail, and idle-progress bounds. Duplicate framing headers, traversal, links, special entries, directory payloads, case aliases, nonzero decoded tails, trailing compressed bytes, and concatenated members fail closed. Both self-verifying manifests and all ten transported files must agree before the current served model ID, complete base-shard manifest, model config, tokenizer vocabulary/configuration, and inference/native/TRL templates are compared exactly. Metadata-only mmap validation is restricted to immutable private staging and rejects unsupported/duplicate targets, missing A/B pairs, nonfloating or mixed dtypes, bad ranks/layers, and every resident projection-shape mismatch; the pinned runner now defaults to the same ten supported module names rather than `all-linear`. The six-file installed adapter includes a self-digesting `kiln.hf-trl-import.v1` receipt binding name, task, export/result identities, resident identity, and reference-script match. Publication rechecks collision and backend health under the adapter mutation barrier, measures quota strictly, fsyncs every file and directory, and uses a shared kernel-enforced no-replace rename that also repaired the prior check-then-rename race in both HF bundle writers. Tests prove empty-directory collision preservation, identity and shape drift rejection without publication, exact receipt/file installation, malformed archives/headers, stalled-body timeout, and strict quota handling. The first-party upload CLI and real pinned external-training/inference round trip remain open. |
| 2026-07-10 | Verified bounded HF/TRL PEFT import CLI | `sha256:ea691127671c` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 378 model tests; 468 train tests with one existing documented ignore; 903 server library tests; 12 focused CLI tests; real TCP success/collision/source-retention loopback and non-201 ambiguity regression; default/ROCm gfx1151/Vulkan server all-target checks; rendered help; full Chrome 148 docs smoke; release/runtime-default/local-link guards; formatting and diff hygiene | passed | `kiln train hf import-peft` locally verifies the completed `.kiln-hf` source and predicts the exact resident content revision, import receipt, installed bytes, task, script match, and six-file installation before opening a connection. Shared library name validation, resource limits, and file contracts keep the client and API aligned. The client copies only the exact ten-file corpus-free envelope into private temporary staging, emits a sorted reproducible USTAR/gzip stream through a four-chunk bounded channel, and enforces compressed, expanded, manifest, script, configuration, and auxiliary-file ceilings without buffering the archive; the API independently reapplies those limits plus safetensors-header and decoded-tail bounds. It rejects redirects and credentials, requires exact HTTP 201, one JSON content type, one matching strong ETag, a bounded strict response, and equality with every local prediction. Network failures, non-201 success, malformed successful responses, and unfinished producers explicitly report potentially completed installation and preserve the immutable source for inspection; ordinary API errors retain their structured diagnosis. The real-router loopback proves successful installation, receipt identity, collision behavior, and byte-for-byte source retention, while a raw TCP regression proves 204 cannot be misreported as success. README, permanent interoperability guidance, CLI/API references, rendered help, and static-site smoke now present a continuous export, external runner, and import workflow while distinguishing the safer first-party command from the raw upload API. Real pinned external SFT/GRPO training and numerical round trips remain open. |
| 2026-07-10 | Deep canonical HF/TRL GRPO corpus verification | `sha256:ab4b774e6d23` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 475 train library tests with one existing documented ignore; 903 server library tests; 24 focused HF/TRL tests including seven new corpus/digest/adversarial cases; default/ROCm gfx1151/Vulkan server all-target checks; release/local-link guard; formatting and diff hygiene | passed | GRPO manifests no longer gain trust from a provenance-schema string plus a dataset file hash. The verifier streams canonical compact, final-LF JSONL under 64 GiB dataset, 256 MiB row, ten-million-group, and 2..=1024-completion bounds; rejects aliases, alternate whitespace, blank/unframed rows, unknown or duplicate fields, missing provenance, non-finite rewards, and count or ordered-corpus drift; and computes the domain-separated `kiln.hf-trl-grpo-corpus.v1` identity with a Python-reproduced golden. Every completion revalidates its prompt and scored-payload hashes, sampled/forced actions and behavior log-probabilities, exact token sequence, tokenizer/vocabulary/template identity, served model, complete base-shard aggregate, and either no adapter or the exported input adapter's digest-derived content revision. Every group then replays through the production tokenizer/mask path, and the whole corpus must use one behavior-policy identity. Adversarial tests rehash both the changed dataset identity and manifest self-digest before proving that framing, reward, provenance, tokenizer, base, adapter, behavior-source, and row-count changes still fail closed; a positive adapter test proves exact revision acceptance. Backend evidence is compilation because this slice changes streamed CPU-side handoff verification rather than device kernels. The atomic GRPO writer, public transport, pinned runner, and external numerical round trip remain open. |
| 2026-07-10 | Atomic immutable HF/TRL GRPO bundle writer | `sha256:179fe2168739` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 480 train library tests with one existing documented ignore; 903 server library tests; 12 focused shared bundle tests including four new GRPO publication/bounds cases; default/ROCm gfx1151/Vulkan server all-target checks; contract/release/local-link guards; formatting and diff hygiene | passed | The synchronous library writer snapshots provenance-complete in-memory groups or an already-canonical real JSONL file into the same private, fsynced, no-replace publication state machine as SFT. File input is bounded, must be compact and final-LF, and rejects symlinks, special files, path replacement while opening, blank rows, alternate whitespace, and truncation instead of silently normalizing them; in-memory serialization also fails before allocating past the row ceiling. Both source forms produce the same manifest identity for the same groups. The complete exact file set and deep production-tokenizer/provenance verifier run before rename and again after parent sync; existing targets are preserved, failures remove staging, and later source mutation cannot alter the immutable bundle. Tests also cover missing provenance, tampering, source retention, and SFT publication/import regressions. This is a backend-independent library checkpoint; the public GRPO API/CLI, pinned runner, and external numerical round trip remain open. |
| 2026-07-10 | Pinned recorded-rollout HF/TRL GRPO runner | `sha256:549ed1e82375` | this commit | portable + pinned Torch CPU smoke; Strix Halo ROCm/Vulkan compile gates | 18 focused standard-library runner tests; 10,000 random Rust/Python finite-float serialization comparisons; 479/480 train tests with one documented ignore; 903 server tests; 275 qualification tests; one real pinned TRL 1.8.0 tiny-Llama optimizer step; default/ROCm gfx1151/Vulkan server all-target checks; contract/release/runtime guards; Python compilation, formatting, and diff hygiene | passed | One task-aware embedded script now verifies SFT and GRPO before importing Torch. GRPO verification streams typed canonical JSON with serde-compatible f32/f64 formatting, exact corpus framing and identity, recomputed base-model aggregate, prompt/payload/model/tokenizer/template/adapter/behavior/sampling/thinking-budget provenance, strict sampled/forced actions, one behavior policy, and a uniform group width. Training snapshots the corpus privately, rechecks it per epoch, rejects TRL's silent incomplete-batch drop, and replays exact prompt/suffix IDs without vLLM or resampling. Only sampled tokens receive `env_mask=1`; recorded behavior log-probabilities become `old_per_token_logps` for PPO, while the frozen initial/base adapter remains the independent K3 KL reference. Results record task-correct trainer identity and the complete effective policy configuration. The real pinned CPU step produced nonzero loss/gradient, recorded reward statistics, clipping from current/behavior mismatch, a PEFT safetensors adapter, and a self-verifying GRPO result. That smoke proves runner plumbing, not the still-open native-versus-TRL numerical oracle or production-model inference/import round trip. |
| 2026-07-10 | Public immutable HF/TRL GRPO export API | `sha256:06481f6da2dc` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 480/481 train tests with one documented ignore; 904 server tests; six focused registry/API tests plus a direct heterogeneous-width writer regression; default/ROCm gfx1151/Vulkan server all-target checks; Chrome 148 docs smoke; release/runtime-default/thinking-budget/local-link guards; formatting and diff hygiene | passed | `POST /v1/train/hf/grpo/exports` accepts exactly one provenance-complete inline group array or server-local canonical JSONL path, with the shared optional split and revision-stable input adapter. A task-neutral publication function now owns SFT and GRPO resident-identity preflight, registry capacity, lock ordering, adapter barrier, staging, no-replace publication, verification, response identity, and lifecycle. GRPO admission streams or bounds the source, recomputes resident adapter identity, replays exact recorded-policy tokenizer/mask semantics, and returns malformed provenance or group shape as `hf_trl_invalid_request` before registry creation; post-admission publication failures remain distinct. Router tests construct, publish, verify, list, and compare both source forms and reject ambiguous sources, missing provenance, and heterogeneous group widths without residue. The width test exposed that Rust previously admitted a corpus the pinned runner could not batch; the deep bundle verifier now requires the same uniform width as the runner. The public GRPO CLI and production external round trip remain open. |
| 2026-07-10 | Verified first-party HF/TRL GRPO CLI handoff | `sha256:7c0b44814c92` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 905 server tests; focused nested-command parser coverage; real TCP SFT regression plus GRPO create/download/verify/publish/conditional-delete loopback; default/ROCm gfx1151/Vulkan server all-target checks; Chrome 148 docs smoke; release/runtime-default/thinking-budget/local-link guards; formatting and diff hygiene | passed | `kiln train hf export-grpo` accepts one server-local canonical recorded-rollout JSONL path, a path-safe immutable name, optional behavior adapter/split, optional no-clobber output, and explicit server-copy retention. SFT and GRPO now share one redirect-free client, creation ETag validation, bounded idle download, compressed/expanded/archive-entry limits, safe single-root extraction, complete bundle verification, atomic local publication, identity-conditional default cleanup, and retry guidance. Before local publication the downloaded manifest must agree with the creation summary's task, source, row count, ordered corpus identity, and input adapter, closing a response-consistency gap for both task routes. The real router loopback proves GRPO task identity, archive publication, source retention, and exact server cleanup while retaining the existing SFT regression. CLI help, README, API/CLI sites, and the permanent interoperability contract now describe both routes, so the general native-training scope and first-class export/documentation checklist items are closed. Actual external SFT/GRPO optimizer, import, and inference/eval round trips remain open. |
| 2026-07-10 | Canonical rollout-generator to HF/TRL export composition | `sha256:ab764f189da9` | this commit | portable + Strix Halo ROCm production-model reproduction | 905 server tests; loopback generation followed by strict HF/TRL JSONL admission; default/ROCm gfx1151/Vulkan server all-target checks; release/local-link guard; formatting and diff hygiene | passed | The first real production-model route composition exposed that `kiln rollout-generate` added diagnostic `metadata` fields which native GRPO silently ignored but the closed HF/TRL schema and pinned Python runner correctly rejected. Generated JSONL now serializes the canonical typed `GrpoGroup` directly and therefore contains only prompt messages, scored completions, optional trajectory, and exact provenance. Usage, latency, seed, server performance, and raw scorer output remain in the separate summary rather than contaminating training data. The loopback regression now requires byte equality with typed reserialization and immediate strict export admission, preventing the generator and exporter from drifting independently again. The actual external optimizer, import, and inference/eval round trips remain open. |
| 2026-07-10 | Semantic local-tokenizer binding for pinned HF/TRL preflight | `sha256:962d1f625c4a` | this commit | portable + Strix Halo ROCm production-model bundles | 20 focused runner tests; 276 qualification tests; 480 train tests with one documented ignore; 905 server tests; both real SFT and GRPO exports verified against local Qwen3.5-4B; default/ROCm gfx1151/Vulkan server all-target checks; Python compilation and diff hygiene | passed | The first embedded-runner preflight against the real model exposed an impossible raw-byte check: Kiln exports Tokenizers' canonical serialization, while the official local model uses the semantically equivalent legacy string form for BPE merge pairs. Python Tokenizers reproduced the exported 10,078,828 bytes and digest exactly from the 12,807,982-byte local file, proving that the mismatch was framing rather than tokenizer drift. The dependency-free preflight now parses bounded duplicate-free JSON and normalizes only string-pair versus two-token-array merge representation before comparing the complete tokenizer object. Vocabulary, merge content/order, normalizer, pre-tokenizer, post-processor, decoder, added tokens, malformed pairs, oversized files, and every other structural change still fail. Focused tests cover equivalent framing, changed merges, malformed merges, and full-preflight mutation rejection. Existing pre-fix bundles were not trained because a successful result must retain the corrected embedded script; fresh exports remain required before the external optimizer round trips. |
| 2026-07-12 | Production HF/TRL SFT and recorded-GRPO round trip | `sha256:b00acd14ba42` | `57472c4e` | Strix Halo ROCm/gfx1151 + Torch ROCm 7.2 | `qualification/receipts/rocm/strix-halo/20260712t123220110075z-rocm-strix-halo-hf-trl-production-roundt-0be7b6a1e5-v1.json` | passed | The committed loopback-only workload exported canonical SFT plus two real provenance-complete GRPO completions, stopped Kiln before external GPU ownership, ran one pinned BF16 rank-1 `q_proj` HF/TRL/PEFT update per task, restarted the same binary with identical execution provenance, imported both self-verifying results, and passed structural/nonzero/server adapter verification plus hash-bound paired eval. The receipt binds the clean source tree, workload, two exact Qwen shards, binary, ROCm environment, Torch 2.13.0+rocm7.2, Transformers 5.13.1, TRL 1.8.0, every export/result/import/weight identity, two imports, two eval pairs, 16 nonzero tensors per adapter, zero HTTP errors, a drained scheduler, and no unsupported cases. The harness also passed 285 qualification tests and a separate real dry run; Vulkan remains an explicit paired rerun. |
| 2026-07-12 | Source-pinned N-step AdamW numerical oracle | `sha256:1ad4fd75f808` | `aa885140` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t130202426843z-rocm-strix-halo-adamw-pytorch-oracle-v1-e4da535e10-v1.json` | passed | PyTorch 2.13.0 source and eager implementation hashes, seed 0, four canonical F32/BF16 cases, all parameters and both moment trajectories, and the no-hidden-master precision contract are committed. The portable F32 reference and real ROCm BF16 fused kernel passed every lane of 20 updates; BF16 bounds are 1 parameter, 4 first-moment, and 3 second-moment ULPs. The fail-closed receipt records three required passes, zero assertion failures, no unsupported cases, ROCm 7.2.4, and a clean exact source tree. Vulkan and the off-machine CUDA/Metal executions remain final-platform gates rather than being inferred from this receipt. |
| 2026-07-12 | Vulkan N-step AdamW numerical oracle | `sha256:1ad4fd75f808` | `c7fe0cbe` | Vulkan/Strix Halo | `qualification/receipts/vulkan/strix-halo/20260712t130347515845z-vulkan-strix-halo-adamw-pytorch-oracle-v1-322470bf50-v1.json` | passed | The real RADV Vulkan F32 fused kernel passed every lane of both ten-step ordinary and epsilon-dominated trajectories under the source-pinned PyTorch mixed tolerance. The receipt has three required passes, no assertion failures or unsupported cases, Mesa 26.1.3 and Vulkan 1.4.348 device identity. `compare_receipts.py` accepted the ROCm/Vulkan pair under the committed cross-backend policy: all three exact correctness metrics matched and every environment difference was explicitly allowlisted, with no rejected or unexplained difference. CUDA and Metal remain off-machine hardware gates. |
| 2026-07-12 | Vulkan mixed-residency LoRA round-trip repair | `sha256:a1f19c8d0f53` | `66f4a04c` | portable + Vulkan/Strix Halo; ROCm compile; toolkit-free CUDA compile | exact workspace test; 378 model tests; 56 capability contracts; 292 qualification tests; four real Vulkan device regressions; portable, ROCm, Vulkan, and CUDA model all-target checks; default-environment dry production SFT/GRPO round trip | passed | The real Qwen3.5-4B round trip exposed five independent mixed-residency failures in sequence: embedding activation precision, RMSNorm weight placement, portable LoRA A/B placement, recurrent conv-state migration, and materialized K/V placement. Each boundary is now centralized and has a focused real-Vulkan regression. Ordinary Vulkan decode remains native-required, while active LoRA has one typed `allow_portable_lora_decode` exception with sparse power-of-two `vulkan_lora_paged_decode_fallback` warnings. With no debug fallback environment, both pinned SFT and recorded-GRPO adapters trained, imported, verified, and passed paired eval in the dry harness. The CUDA check validates Rust feature wiring only because this host has no CUDA toolkit; the clean committed-tree Vulkan receipt follows separately. Cheap CI run `29195651428` passed formatting, dependency policy, the full CPU workspace, substrate and determinism checks, CUDA dependency-tree policy, and portable qualification evidence with accelerator jobs skipped. |
| 2026-07-12 | Vulkan production HF/TRL SFT and recorded-GRPO round trip | `sha256:a1f19c8d0f53` | `66f4a04c` | Strix Halo Vulkan/RADV + Torch ROCm 7.2 | `qualification/receipts/vulkan/strix-halo/20260712t140855954401z-vulkan-strix-halo-hf-trl-production-roundt-af0c916883-v1.json` | passed | The clean committed-tree run completed both pinned external optimizer steps, imported and structurally verified both adapters, and passed two hash-bound paired evals with 16 nonzero tensors per adapter, zero HTTP errors, no unsupported cases, stable execution provenance, and clean snapshot/server teardown. Sparse `vulkan_lora_paged_decode_fallback` warnings make the correctness-qualified adapter route visible; it remains a performance target rather than a silent native-path claim. The following same-source ROCm receipt supplies the declared cross-backend comparison arm. |
| 2026-07-12 | Paired production HF/TRL route correctness | `sha256:a1f19c8d0f53` | `66f4a04c..235a53c3` | Strix Halo ROCm/gfx1151 + Vulkan/RADV | `qualification/receipts/rocm/strix-halo/20260712t141602891782z-rocm-strix-halo-hf-trl-production-roundt-0be7b6a1e5-v1.json`; `qualification/receipts/vulkan/strix-halo/20260712t140855954401z-vulkan-strix-halo-hf-trl-production-roundt-af0c916883-v1.json` | passed | Both clean receipts bind the same source tree and execution environment, and `compare_receipts.py` accepted every allowlisted backend/runtime difference with no rejected difference. All eight declared lifecycle/structural metrics match exactly; both SFT adapters are byte-identical. This is deliberately not logged as GRPO numerical parity: the backend-generated seed-42 completion and behavior log-probabilities differ, producing different GRPO adapter hashes, while each backend's import and paired eval remains valid. The deterministic Vulkan-versus-CPU/HF logit and sampling corpus stays open. |
| 2026-07-12 | Current-default ROCm mixed-load baseline | `sha256:a1f19c8d0f53` | `cd08b2ae` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t142302974612z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | passed | The experimental-profile baseline exercised enabled physical KV autoscaling and ROCm graphs with normal, 1,536-word long-prefill, cancellation, slow-consumer, and pressure-peer traffic. It recorded zero request failures, zero zero-token responses, zero unexplained or attributed ITL outliers, one confirmed cancellation, one backpressure/stall eviction, 13/13 measured graph captures, 828/828 graph replays, and no resize or reclaim event. ITL was 51.9 ms p50, 91.8 ms p99, and 132.2 ms p99.9; maximum decode/prefill phases were 88.5/37.1 ms. The intentionally mixed long-prefill/pressure workload made TTFT p99 59.2 s, so that aggregate is retained for directional A/B use rather than presented as ordinary-request latency. |
| 2026-07-12 | Variant-invariant ROCm A/B workload contract | `sha256:e98cd587fd39` | this commit | portable | 294 qualification tests; workload and receipt validators; negative comparator regression | passed | The first autoscale-off attempt was discarded before commit because the variant name appeared in model-visible markers, producing 11,745 baseline prompt tokens versus 11,775 and 912 versus 1,034 completion tokens while the comparator still passed. Markers now depend only on seed and request role, every variant declares the same prompt identity and format, and required exact prompt, long-prefill, completion, and request counts make that class of invalid comparison fail closed. Controlled hardware A/B receipts remain open and will be regenerated from this contract. |
| 2026-07-12 | Variant-invariant ROCm default A/B anchor | `sha256:e98cd587fd39` | `7bd635bd` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t143605345272z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | passed | The clean experimental-profile anchor fixes the comparison workload at 11,735 prompt tokens, 4,651 long-prefill tokens, 931 completion tokens, and 10 measured requests. It recorded zero request failures, zero zero-token responses, zero explained or unexplained ITL outliers, one confirmed cancellation, one stall eviction, 13/13 measured graph captures, 888/888 graph replays, and no KV resize or memory-reclaim event. ITL was 51.5 ms p50, 95.2 ms p99, and 134.3 ms p99.9; the pushed qualification-contract run `29196539343` also passed. The disabled-feature arms must match every exact workload-count invariant before their performance deltas are considered. |
| 2026-07-12 | Rejected ROCm autoscale-off A/B attempt | `sha256:e98cd587fd39` | `851173b1` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t143919238123z-rocm-strix-halo-serving-mixed-rocm-v1-184c082f9e-v1.json`; strict comparator | rejected | Prompt, long-prefill, and request counts matched the anchor exactly, but the new fail-closed gate rejected 931 versus 939 completion tokens. Under identical prompts, seeds, `temperature=0`, and `top_k=1`, completion lengths changed for `normal-00` (73 to 71), `normal-01` (65 to 75), `normal-02` (76 to 73), and `pressure-peer` (74 to 77). Neither arm resized KV or reclaimed memory. No latency or throughput delta from this pair is treated as evidence; a repeated default arm must first distinguish ordinary concurrent-run nondeterminism from an autoscale-specific semantic change. |
| 2026-07-12 | Reproduced concurrent ROCm greedy-output nondeterminism | `sha256:e98cd587fd39` | `613200a9` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t144318820808z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json`; per-request raw-artifact comparison | reproduced | Repeating the unchanged graph-on/autoscale-on arm produced 934 rather than 931 completion tokens. Greedy completion lengths changed for `normal-01` (65 to 67), `normal-02` (76 to 84), `normal-03` (128/length to 112/stop), and `pressure-peer` (74 to 83), proving that the autoscale toggle did not cause the earlier semantic drift. Prompts, seeds, binary, source, requested configuration, prompt-token counts, and request counts were identical. Performance A/B closure is blocked until the workload has a fixed output denominator and the separate deterministic model corpus directly gates batch-invariant logits/tokens. |
| 2026-07-12 | Rejected ROCm graphs-off A/B attempt | `sha256:e98cd587fd39` | `b59b1f72` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t144722344749z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; strict comparator | rejected | The graph-free arm matched all prompt and request identities but produced 903 rather than 931 completion tokens, with `normal-06` ending at 99 instead of 128 and `normal-07` at 92 instead of 99. It had zero failures and unexplained ITL outliers and correctly recorded zero graph activity, but its output-token throughput and latency deltas are not comparable to the anchor because the generated workload changed. A same-configuration graph-free repeat remains required before assigning any semantic drift specifically to graph execution. |
| 2026-07-12 | Reproduced graph-free ROCm greedy-output nondeterminism | `sha256:e98cd587fd39` | `dc2febc3` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t145028397034z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; per-request raw-artifact comparison | reproduced | Repeating graphs-off unchanged produced 906 rather than 903 completion tokens. `normal-00/01/02` changed from 71/75/73 to 73/65/76 and `pressure-peer` from 77 to 85, while the longer fixed rows happened to match. This rules out ROCm graph capture as the sole source of the concurrent greedy drift and leaves batch composition or a broader decode path as the leading unproven hypotheses. Performance A/B runs now require an explicit fixed-output workload contract; deterministic batch/logit correctness remains a separate blocking corpus rather than being inferred from length-only evidence. |
| 2026-07-12 | Fail-closed fixed-output ROCm A/B contract | `sha256:94a27815b70f` | this commit | portable | 295 qualification tests; workload validator; all committed receipt validation | passed | Every measured prompt now requests an uninterrupted ascending integer stream, and the pressure peer uses the same 128-token bound as normal requests. Effective config declares exactly 1,184 expected measured tokens and `finish_reason=length`; the runner checks every named request and the aggregate, records the length-terminated request count, and fails locally on any early EOS, missing/duplicate request, or token-count drift. The comparator also requires exact prompt, long-prefill, completion, length-termination, and request counts. This makes throughput and latency denominators comparable without pretending to close the separate batch-invariant logit/token correctness gate. |
| 2026-07-12 | Prompt-only fixed-output ROCm attempt | `sha256:94a27815b70f` | `ac4d06ef` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t145806700028z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed as designed | Nine measured requests reached their declared limit, but `normal-00` emitted EOS at 112/128 tokens, so the receipt failed at 1,168/1,184 completions and 9/10 length terminations rather than publishing a biased baseline. The longer generation also missed the strict pressure-peer overlap condition; zero requests otherwise failed and zero ITL outliers were unexplained. Prompt wording is therefore insufficient as a correctness control. A typed bounded EOS policy is required, with this workload applying it to every measured request. |
| 2026-07-12 | Bounded ignore-EOS serving contract | `sha256:62e5ce0093fb` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 907 server tests; 378 model tests; 295 qualification tests; default, ROCm gfx1151, and Vulkan server all-target checks; thinking-budget contract; full Chrome 148 docs-site smoke; workload validation; formatting and diff hygiene | passed | Chat and batch requests now expose the typed, default-off `ignore_eos` policy. Batching, direct generation, and every speculative verifier treat EOS ids as ordinary accepted tokens only when requested; explicit stop sequences remain active and the effective maximum remains a hard bound. Synthetic multi-choice and batch requests preserve the policy, deterministic cache identity binds its normalized value, and recorded rollout provenance fails closed because its current schema cannot identify the altered policy. The fixed-output workload declares and sends the policy for all five A/B variants under workload hash `sha256:d1821a715c19`; a clean real ROCm receipt must still prove 1,184 completions and all 10 length terminations before the baseline is accepted. |
| 2026-07-12 | Qualification Cargo discovery hardening | `sha256:99911cad81ed` | this commit | portable | 297 qualification tests; focused missing-PATH and invalid-override regressions; workload validation; Python compilation and diff hygiene | passed | The first post-policy hardware invocation stopped before build because the inherited Codex environment omitted rustup's Cargo directory from `PATH`. The source-bound builder now resolves an explicit `CARGO` override first, then `PATH`, `CARGO_HOME/bin`, and `HOME/.cargo/bin`, and fails with an actionable error when none is executable. The build argv receives the resolved executable directly; no shell or ambient command-string evaluation is introduced. The zero-work receipt was discarded rather than checked in as hardware evidence. |
| 2026-07-12 | Fixed-output ROCm proof and pressure-peer starvation reproducer | `sha256:99911cad81ed` | `b72bf730` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t152338011760z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | The clean default arm proved the bounded EOS policy on real hardware: all ten measured requests finished by length with exactly 1,184 completion tokens, zero request failures, and zero attributed or unexplained ITL outliers. It retained one intentional 2.000 s stalled-client eviction, 836 successful graph replays with no failure, fixed 4,096-block KV capacity, and no reclaim or resize event. The sole gate failure was genuine ready-prefill starvation: the dedicated peer was bound before pressure, but its 24.40 s TTFT placed its first client-visible token after the attributed backpressure window, which began about 21.46 s after binding and ended about 23.51 s. Preserve the fixed-output contract; add compact before/inside/after timing evidence, then restore bounded short-prefill fairness rather than weakening the overlap gate. |
| 2026-07-12 | Pressure-peer timing evidence contract | `sha256:5f06b86c1ee9` | this commit | portable | 297 qualification tests; exact overlap/timing regressions; workload validation; Python compilation, formatting, and diff hygiene | passed | The mixed-load receipt now retains pressure-window start and duration, peer first actor-ready latency, and actor-ready token counts before, inside, and after the attributed window. The existing three-sided overlap gate is unchanged, but failures report all six values directly instead of requiring raw server-log reconstruction. Every A/B variant declares the expanded metric set under workload hash `sha256:b9252d98a38f`; driver/manifest drift remains fail closed. Scheduler policy is intentionally unchanged in this checkpoint. |
| 2026-07-12 | Bounded short-prefill service candidate | `sha256:2c1e21eae502` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 907 server tests; 297 qualification tests; focused mixed-work and equal-work scheduler regressions; ROCm gfx1151 and Vulkan server all-target checks; formatting and diff hygiene | passed | The reproduced 276-token peer needed roughly forty bounded layer dispatches while sharing the actor with a 4,691-token row and fixed-length decode traffic. The short-tail lane now receives one opportunity per three eligible prefill dispatches instead of one per four, while two ordinary round-robin dispatches remain guaranteed. The deterministic mixed-work regression makes the short row decode-ready by dispatch nine and proves all four long rows advance; equal-work rows still consume no priority. Admission width, combined token budget, per-cycle prefill token/layer ceilings, and delivery policy are unchanged. Real ROCm overlap and latency/throughput evidence remain required before accepting the candidate. |
| 2026-07-12 | One-in-three short-prefill ROCm counterexample | `sha256:2c1e21eae502` | `bc124e8b` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t153726958173z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | The candidate retained the exact 1,184-token/10-length denominator, zero request failures and ITL outliers, one intentional stall eviction, fixed KV capacity, and clean graph/memory behavior. Short-priority dispatches rose from 185 to 219 and the peer's first actor-ready token arrived at 23.907 s, about 0.49 s earlier than the preceding client-visible TTFT; p99/p99.9 ITL improved from 106.8/197.5 ms to 99.8/138.0 ms while throughput stayed effectively flat at 16.61 tok/s. The pressure window ran from 21.155 s to 23.187 s after peer dispatch, so ready counts remained exactly 0/0/128 before/inside/after and the gate correctly failed. At cancellation cleanup the only waiting rows are the 4,691-token long prompt followed by the 276-token peer; bounded waiting-admission priority is the next target rather than further unmeasured active-lane tuning. |
| 2026-07-12 | Bounded short-waiting admission candidate | `sha256:a596c89a2868` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 909 server tests; 297 qualification tests; resumable, non-resumable, burst-refill, FIFO-bound, prefix-deferral, health/debug/Prometheus, and workload-contract regressions; default, ROCm gfx1151, and Vulkan server all-target checks; full Chrome 148 docs smoke; formatting and diff hygiene | passed | On non-burst backends only, a long FIFO admission head may yield to the shortest resumable request found within the next eight queue entries when that request fits the one admission chunk plus four-chunk active short class. Two consecutive overtakes force FIFO service; a short/equal head stays FIFO, non-resumable work must fit the current budget, prefix deferral remains authoritative, and CUDA burst refill never scans or reorders. Health, debug state, and Prometheus expose a dedicated selection counter. The mixed workload requires that counter to advance and declares it under workload hash `sha256:cf46897cd74f`; permanent operator docs now describe both the one-in-three active lane and bounded waiting lane. Real ROCm three-sided pressure overlap remains the acceptance gate. |
| 2026-07-12 | Short-waiting admission ROCm counterexample | `sha256:a596c89a2868` | `f7b3e43e` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t155100559051z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json` | failed | The exact denominator, request, pause, graph, memory, and teardown invariants remained clean, but the new waiting-selection counter was zero: the peer was already FIFO-eligible and never used the candidate branch, falsifying the inferred queue-order bottleneck. Run-to-run timing moved first actor-ready to 23.255 s while pressure ran from 21.212 s to 23.260 s, producing 0/1/127 ready tokens before/inside/after; one token at the final edge still correctly fails the three-sided progress gate. Throughput was 16.71 tok/s, p99/p99.9 ITL were 99.5/134.3 ms, and no outlier was attributed or unexplained. Remove the unexercised waiting policy, counter, qualification requirement, and documentation rather than retaining dead scheduler complexity; active prefill service remains the demonstrated bottleneck. |
| 2026-07-12 | Remove disproven waiting-admission policy | `sha256:4b1358a0c57a` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 907 server tests; 297 qualification tests; workload validation; default, ROCm gfx1151, and Vulkan server all-target checks; full Chrome 148 docs smoke; formatting and diff hygiene | passed | The unused lookahead branch, streak state, two tests, health/debug fields, Prometheus series, qualification metric/gate, five manifest declarations, and operator copy are removed together. This deletes 212 lines from the rejected candidate while retaining the one-in-three active short-tail policy, exact fixed-output denominator, and six pressure timing counters. Workload identity returns to `sha256:b9252d98a38f`. The historical candidate and counterexample remain in this log and their receipt remains valid; production does not carry the disproven mechanism. |
| 2026-07-12 | Two-in-three active short-prefill candidate | `sha256:a94e7c12ea99` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 907 server tests; 297 qualification tests; focused mixed-work, ordinary-turn, and equal-work scheduler regressions; workload validation; default, ROCm gfx1151, and Vulkan server all-target checks; full Chrome 148 docs smoke; formatting and diff hygiene | passed | The falsified waiting-queue hypothesis leaves active prefill service as the measured bottleneck. Two of every three eligible dispatches may now accelerate a genuinely shorter immutable work class, while every third dispatch is forced through ordinary round-robin service. The deterministic mixed-work trace makes the short row decode-ready by dispatch five and proves all four long rows progress by dispatch eight; equal-work cohorts still consume no priority, and an ordinarily selected short row retains the priority classification. Admission order, width, token budgets, per-cycle layer limits, and delivery policy are unchanged. The fixed-output workload remains `sha256:b9252d98a38f`; real ROCm three-sided pressure overlap and latency/throughput evidence are still required before accepting the candidate. |
| 2026-07-12 | Two-in-three ROCm default A/B anchor | `sha256:a94e7c12ea99` | `2d393923` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t160537457367z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json`; strict receipt validation | passed | The clean committed candidate is the first fixed-output arm to pass the unchanged three-sided pressure gate. It produced exactly 1,184 completion tokens with 10/10 length terminations, zero request failures, and zero attributed or unexplained ITL outliers. The peer became actor-ready at 17.778 s, 3.214 s before the pressure window began at 20.993 s; it delivered 44, 40, and 44 ready tokens before, inside, and after the 2.002 s window. The scheduler exercised 288 short-priority forwards; throughput was 16.85 tok/s and p99/p99.9 ITL was 99.1/141.3 ms. One intentional stalled-client eviction and its engine error were confirmed, 760 measured graph replays had zero failure, KV stayed at 4,096 blocks, and reclaim/resize/teardown remained clean. This receipt is the default anchor for the autoscale-off, graphs-off, and both-off arms; no toggle effect is accepted without the strict comparator. |
| 2026-07-12 | ROCm autoscale-off compatible control | `sha256:a94e7c12ea99` | `f6bdb873` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t160940027516z-rocm-strix-halo-serving-mixed-rocm-v1-184c082f9e-v1.json`; strict declared-A/B comparator against the default anchor | passed | The arm records requested/effective KV autoscaling off while retaining graphs; those are the only accepted configuration differences. Exact prompt, long-prefill, completion, length-termination, and request denominators match, with zero request failures and unexplained ITL outliers. Pressure progress remained 45/40/43 before/inside/after. Comparator compatibility and every required metric rule passed; optional zero-tolerance directional probes were mixed: throughput moved -0.28%, p99/p99.9 ITL -1.44%/-2.23%, TTFT p99 +0.26%, and E2E p99 +0.28%. Neither arm resized KV or reclaimed memory, so this pair does not exercise the autoscaler and supplies no causal autoscale performance claim. The result is a clean neutral control, not a reason to change the default. |
| 2026-07-12 | ROCm graphs-off cross-phase starvation counterexample | `sha256:a94e7c12ea99` | `7429dc9c` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t161305677644z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; receipt validation; rejected strict comparison | failed | The graph-free arm correctly recorded zero capture/replay activity, exact 1,184-token/10-length output, zero request failures and ITL outliers, one intentional stall eviction, and clean memory/teardown. It failed only the unchanged pressure gate: the peer became ready at 23.355 s, after the 20.892-22.878 s window, for 0/0/128 ready tokens before/inside/after. Its 286 short-priority forwards nearly match the anchor's 288, and both arms executed the same 1,616 prefill forwards/6,464 layers; graph-free prefill time was slightly lower at 27.94 s versus 28.34 s. The material change is decode actor time, 50.94 s versus 41.86 s (+21.7%), which delayed peer readiness by 5.58 s and reduced throughput 10.95%. The strict comparator correctly rejects the failed receipt. Within-prefill dispatch ratio is no longer the target; bounded wall-clock service across decode and ready-prefill phases is required before rerunning this arm. |
| 2026-07-12 | Request-scoped actor first-token attribution | `sha256:90843e7c5993` | this commit | portable + Chromium; Strix Halo ROCm/Vulkan compile gates | 908 server tests; 298 qualification tests; actor-command wait regression; strict performance parser tests; default, ROCm gfx1151, and Vulkan server all-target checks; Chrome 148 docs smoke; workload validation; thinking-budget and release contracts; formatting and diff hygiene | passed | Opt-in batching responses now partition TTFT into API-enqueue-to-admission queue time, slot preparation, and admission-to-first-sampled-token wall time while retaining accumulated model prefill/decode durations. Actor-ready timestamps drive non-streaming and streaming TTFT consistently; batching streams publish the complete performance object once on their terminal chat chunk without letting a server default silently enable custom per-token events. The actor carries the phase boundaries through every active-prefill remove/reinsert path, and an end-to-end gated-forward regression proves command-channel wait is included. The mixed-load parser rejects missing, duplicate, nonnumeric, negative, non-finite, shape-drifted, or impossible phase envelopes and records all three pressure-peer values under workload `sha256:f8bcd6770868`. Scheduler policy is unchanged; the next graph-free ROCm run must use these phases to select a bounded cross-phase fix. |
| 2026-07-12 | Graph-free ROCm request-phase attribution | `sha256:90843e7c5993` | `7b760112` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t164248119983z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; strict receipt validation | failed | The clean exact-source run failed only the unchanged pressure-overlap gate while preserving 1,184 completions, 10/10 length terminations, zero request failures, zero ITL outliers, one intentional stall eviction, fixed 4,096-block KV capacity, zero reclaim/resize, zero graph activity, and clean teardown. Its new request phases account for essentially the entire 23,321.382 ms peer first-token time: 15,853.960 ms queued before admission, 0.333 ms in admission, and 7,466.538 ms from admission to first sampled-token readiness, leaving about 0.55 ms outside the actor envelope. The peer was admitted 4.977 s before pressure began but became ready 2.490 s after pressure began and 0.477 s after it ended, producing 0/0/128 ready tokens before/inside/after. Graph-free decode occupied 50.800 s across 1,030 forwards while prefill occupied 27.566 s across the same 1,616 forwards/6,464 layers as the prior arms; 287 short-priority forwards did not prevent the miss. Throughput was 15.10 tok/s and p99/p99.9 ITL was 85.8/144.2 ms. Queueing is 68 percent of peer TTFT, but post-admission cross-phase service also independently misses the gate; admission work, response delivery, memory mutation, synchronization, and an unexplained device pause are ruled out. |
| 2026-07-12 | Fail-fast mixed-load environment ownership | `sha256:4c8d690a6e02` | this commit | portable | 299 qualification tests; focused runner-owned and forbidden-control regressions; workload validation; Python compilation and diff hygiene | passed | A diagnostic invocation prefixed with `KILN_MAX_DECODE_BATCH=12` completed a full hardware run at the unchanged effective width of eight because the source-bound driver intentionally sanitized ambient Kiln controls; its duplicate receipt was discarded and no width-12 conclusion is retained. The driver now accepts only its runner-owned result-path and variant-ID variables, strips them before server launch, and fails before build on every other inherited `KILN_*` name with a sorted actionable error. Diagnostic configuration must be represented by a committed workload variant, so an operator can no longer mistake a silently ignored shell override for exact-source evidence. |
| 2026-07-12 | Bounded short-prefill staging candidate | `sha256:f198ba6601cd` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 911 server tests; 47 batching regressions; focused health, debug, and Prometheus serialization; default, ROCm gfx1151, and Vulkan server all-target checks; formatting and diff hygiene | passed | The attributed graph-free trace showed eight ordinary requests consuming the active-set ceiling even though decode width never exceeded three, leaving the 276-token peer queued for 15.854 seconds. Latency-oriented actors now retain the backend decode ceiling while exposing at most four separately accounted staging slots for prompts that can enter the four-chunk short-tail class after one prefill quantum. Staged rows cannot consume ordinary FIFO capacity; a freed ordinary slot still admits the waiting long prompt, total active width remains bounded, deterministic width one and CUDA burst refill retain their prior capacity, and generation-ordered decode cohorts rotate across ready staged rows without changing slot-to-sampling alignment. Health, debug state, startup logs, and Prometheus expose configured capacity, live staged occupancy, observed active width, and staging admissions. The fixed-output mixed workload remains `sha256:f8bcd6770868`; its qualification contract and a clean graph-free ROCm acceptance run are intentionally separate next checkpoints. |
| 2026-07-12 | Staged-active ROCm qualification contract | `sha256:6f4df31fe696` | this commit | portable | 300 qualification tests; all checked-in receipt validation; workload validation; Python compilation and diff hygiene | passed | All five mixed-load arms now bind an effective decode width of eight, four short-prefill staging slots, and a total active ceiling of twelve under workload `sha256:71d928b5b7d0`. Health and debug state must agree on those exact capacities at startup, after warmup, and after measurement. The measured interval must advance the staging-admission counter and observe more than eight but no more than twelve active requests; result metrics retain the capacity, observed width, and admission delta. Cancellation drain now requires decode, prefill, staged occupancy, and the waiting queue all to reach zero, so an active staged prefill cannot masquerade as a drained actor. The unchanged fixed-output, three-sided pressure, latency, error, graph, memory, and teardown gates still decide hardware acceptance. |
| 2026-07-12 | Graph-free ROCm staged-admission counterexample | `sha256:6f4df31fe696` | `bd3a1692` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t171953555268z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; strict receipt and local-artifact validation | failed | The bounded lane executed three staging admissions and raised observed active width from the ordinary ceiling of eight to eleven under the declared bound of twelve. It reduced pressure-peer actor queue time from the prior graph-free 15,853.960 ms to 114.467 ms, proving that active-set saturation caused the queue phase. Staging alone did not bound post-admission service: the peer then spent 14,814.053 ms admitted before first-token readiness, which arrived at 14,929.235 ms after dispatch, 1,113.446 ms after the 11,807.904-13,815.789 ms pressure window. Counts remained 0/0/128 before/inside/after, so the unchanged gate correctly failed. The run otherwise retained exactly 1,184 completions, 10/10 length terminations, zero request failures or ITL outliers, one intentional stall eviction, zero graph/resize/reclaim activity, and clean unforced teardown. Decode used 50.610 s across 997 forwards, prefill used 28.001 s across 1,616 forwards/6,464 layers, throughput was 15.05 tok/s, and p99/p99.9 ITL was 95.2/146.6 ms. The staging lane now needs a bounded share of prefill dispatches rather than competing indistinguishably with ordinary short tails. |
| 2026-07-12 | Rotating staged-prefill service candidate | `sha256:4f77c5fa4022` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 912 server tests; 48 batching regressions; exact staged/ordinary dispatch trace; focused health, debug, and Prometheus serialization; default, ROCm gfx1151, and Vulkan server all-target checks; formatting and diff hygiene | passed | On the two existing short-prefill priority turns out of every three, eligible staged prefills now rotate by immutable request generation before ordinary short-tail acceleration is considered. The third turn remains global round robin, so a continuously full four-slot staging lane cannot hide ordinary prompt progress. A six-row deterministic trace produces staged `A,B`, ordinary `A`, staged `C,A`, ordinary `B`, staged `B,C`, ordinary `C`; the dedicated counter is an observable subset of all short-priority forwards. Once staged rows become decode-ready, the separate bounded decode-cohort rotation remains in force. Capacity, admission eligibility, token and layer budgets, and CUDA burst/deterministic exclusions are unchanged. The following qualification-contract checkpoint must require measured staged-priority service before another graph-free ROCm run can accept this candidate. |
| 2026-07-12 | Staged-service ROCm qualification contract | `sha256:2b4815078bf6` | this commit | portable | 300 qualification tests; all checked-in receipt validation; workload validation; Python compilation and diff hygiene | passed | The mixed-load result now records the measured delta of `total_prefill_staging_priority_forwards`, requires it to be positive, and rejects any value above the total bounded short-priority count. All five variants declare the new metric under workload `sha256:7fc4417f61a7`; exact 8/4/12 capacity, staging admission, observed active width, zero-drain, fixed-output, three-sided pressure, latency, graph, memory, error, and lifecycle gates remain unchanged. A hardware arm therefore cannot accept the service candidate by merely admitting staged requests or by publishing an incoherent counter. |
| 2026-07-12 | Graph-free ROCm rotating-service near miss | `sha256:2b4815078bf6` | `2194739c` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t173625119930z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; strict receipt and local-artifact validation | failed | The measured lane executed three staging admissions and 76 rotating staged-priority forwards, with active width eleven under the declared twelve-request bound. Pressure-peer queue time stayed bounded at 112.254 ms and admitted-prefill wall time fell from 14,814.053 to 13,586.056 ms. First readiness moved inside the pressure window at 13,698.993 ms after dispatch, 1,367.202 ms after pressure began and 667.422 ms before it ended; ready counts improved from 0/0/128 to 0/8/120. The gate still correctly failed because it requires progress before, inside, and after the window. Exact 1,184-token/10-length output, zero request failures and ITL outliers, one intentional eviction/error, zero graph/resize/reclaim activity, and clean unforced teardown remained intact. Decode used 50.605 s across 996 forwards, prefill used 27.848 s across the unchanged 1,616 forwards/6,464 layers, throughput was 15.09 tok/s, and p99/p99.9 ITL was 98.8/165.6 ms. Admission and staged selection are now demonstrated; the bounded staged-to-ordinary prefill share needs one further adjustment without reducing decode cadence or weakening the global progress floor. |
| 2026-07-12 | Four-turn staged-prefill burst candidate | `sha256:926ed1790cec` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 912 server tests; 48 batching regressions; exact fifteen-dispatch staged/ordinary trace; focused health, debug, and Prometheus serialization; default, ROCm gfx1151, and Vulkan server all-target checks; formatting and diff hygiene | passed | While eligible staged work exists, at most four rotating staged-priority turns now precede a mandatory global round-robin prefill turn; without staged work, the prior two-priority/one-global short-tail cadence is unchanged. Decode still executes every actor cycle, per-forward token and four-layer ceilings are unchanged, and the bounded lane still has only four slots. The deterministic trace advances staged `A,B,C,A`, ordinary `A`, staged `B,C,A,B`, ordinary `B`, staged `C,A,B,C`, then ordinary `C`; deterministic width one and CUDA burst refill expose a zero staged-priority burst. Startup logs, health, debug state, and Prometheus publish the resolved maximum burst. The next contract checkpoint must pin four before hardware acceptance. |
| 2026-07-12 | Four-turn ROCm qualification contract | `sha256:2f47b4184130` | this commit | portable | 300 qualification tests; all checked-in receipt validation; workload validation; Python compilation and diff hygiene | passed | Every mixed-load arm now requires health, debug state, and result metrics to report a maximum staged-priority burst of exactly four, in addition to the positive staged-admission and staged-service deltas and their subset invariant. Workload `sha256:9aeb97160602` retains the exact 8/4/12 active envelope, zero-drain proof, fixed-output denominator, three-sided pressure gate, latency/error attribution, graph/memory state, and clean lifecycle requirements. The hardware run cannot silently execute the preceding two-turn policy or an unbounded service burst. |
| 2026-07-12 | Graph-free ROCm staged-entry counterexample | `sha256:2f47b4184130` | `f8215cc5` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t175152650257z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; strict receipt and local-artifact validation | failed | The exact four-turn policy was attested, but staged-priority forwards rose only from 76 to 77. Peer queue time remained 112.091 ms and admitted-prefill wall time improved by 498.204 ms to 13,087.852 ms. First readiness arrived at 13,200.890 ms, 944.640 ms after pressure began and 1,087.326 ms before it ended; counts improved to 0/14/114 but still correctly failed the required pre-window segment. All fixed-output, failure, outlier, graph, memory, capacity, service-counter, and teardown gates remained clean; throughput was 15.06 tok/s and p99/p99.9 ITL was 98.0/164.8 ms. The source exposes the remaining contract mismatch: staging admission accepts a prompt of at most five 64-token quanta because one dispatch will enter the four-quantum short-tail class, while staged-priority selection accepts only prompts already at four quanta. The actual 276-token peer is therefore admitted at the intended boundary but must wait for a global turn before it can consume staged service. The next fix must give an admitted boundary row its entry quantum, not further increase the burst or weaken the gate. |
| 2026-07-12 | Staged entry-quantum service fix | `sha256:c8789f3acb7a` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 913 server tests; 49 batching regressions; exact staged-versus-ordinary 276-token boundary selection; default, ROCm gfx1151, and Vulkan server all-target checks; formatting and diff hygiene | passed | Admission and service selection now share one overflow-safe five-quantum staging-entry limit. An admitted staged row receives priority while completing the entry quantum that brings it into the ordinary four-quantum short-tail class; ordinary short-tail acceleration remains capped at four quanta. The deterministic regression uses the measured 276-token peer size: it is selected immediately when staged under the 320-token entry limit, then rejected from ordinary short-tail priority under the unchanged 256-token limit. Capacity, four-turn burst, decode cadence, per-forward token/layer ceilings, CUDA/deterministic exclusions, and every qualification gate are unchanged. |
| 2026-07-12 | Accepted graph-free ROCm staged-service arm | `sha256:c8789f3acb7a` | `1ea3632d` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t180445314362z-rocm-strix-halo-serving-mixed-rocm-v1-c5256e3c8c-v1.json`; strict receipt and local-artifact validation | passed | The exact four-turn policy executed three staging admissions and 83 staged-priority forwards, with active width eleven under the declared twelve-request bound. Peer queue time was 114.239 ms and admitted-prefill wall time fell from 13,087.852 to 6,471.065 ms. First readiness arrived at 6,586.056 ms after dispatch, 7,058.608 ms before pressure began; it produced 82/24/22 ready tokens before/inside/after the 13,644.664-15,686.211 ms pressure window. The run produced exactly 1,184 completions and 10/10 length terminations, zero request failures or ITL outliers, one intentional stalled-client eviction/error, zero graph activity, zero resize/reclaim, zero synchronization failures, fixed KV capacity, a fully drained actor, and clean unforced snapshot teardown. Decode used 50.383 s across 986 forwards and prefill used 28.055 s across the unchanged 1,616 forwards/6,464 layers; throughput was 15.09 tok/s and p99/p99.9 ITL was 104.1/172.5 ms. This accepts the graph-free arm on its own gates; strict A/B conclusions still require fresh default and remaining toggle arms on this same source and workload. |
| 2026-07-12 | Fresh ROCm default anchor and graph A/B | `sha256:c8789f3acb7a` | `08a8c6fe` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t180942059940z-rocm-strix-halo-serving-mixed-rocm-v1-f2e983b84c-v1.json`; strict receipt validation and declared comparator against graph-free | passed | The graph-on/autoscale-on arm passed every individual gate with exact 1,184-token/10-length output, zero request failures and ITL outliers, one intentional eviction/error, three staging admissions, 83 staged-priority forwards, active width eleven, fixed KV capacity, no resize/reclaim, clean drain, and unforced teardown. The peer became ready at 6,113.916 ms and produced 81/25/22 ready tokens around the 12,954.441-14,994.240 ms pressure window. Nine measured graph captures and 773 replays all succeeded. Throughput was 17.22 tok/s and p99/p99.9 ITL was 103.4/168.2 ms. The strict comparator accepted only the declared graph requested/effective differences and matched prompt, long-prefill, completion, length, request, failure, and unexplained-outlier requirements. Optional graph-free deltas were throughput -12.38%, p99/p99.9 ITL +0.71%/+2.55%, TTFT p99 +13.99%, E2E p99 +14.24%, decode-forward wall +25.54%, and prefill-forward wall -1.75%. This pair supports a graph performance effect without attributing any correctness or memory behavior to graphs. |
| 2026-07-12 | Fresh ROCm autoscale-off neutral control | `sha256:c8789f3acb7a` | `242fbc8f` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t181430457656z-rocm-strix-halo-serving-mixed-rocm-v1-184c082f9e-v1.json`; strict receipt validation and declared comparator against default | passed | The graph-on/autoscale-off arm passed every individual gate with the exact output denominator, zero request failures/outliers, one intentional eviction/error, three staging admissions, 83 staged-priority forwards, active width eleven, successful graph execution, fixed KV capacity, no resize/reclaim, full drain, and clean teardown. The peer became ready at 6,131.918 ms and produced 84/24/20 ready tokens around the 13,257.707-15,282.799 ms pressure window. The comparator accepted only the declared autoscale requested/effective differences and matched every required metric. Optional deltas versus default were throughput -0.50%, p99/p99.9 ITL +1.47%/+1.29%, TTFT p99 +0.49%, E2E p99 +0.47%, decode-forward wall +0.55%, and prefill-forward wall +0.43%. Neither arm resized or reclaimed memory, so this is a clean neutral control and no causal autoscaler performance claim. |
| 2026-07-12 | Fresh ROCm both-off control | `sha256:c8789f3acb7a` | `a3da6bd4` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t181836351772z-rocm-strix-halo-serving-mixed-rocm-v1-b68874d2e9-v1.json`; strict receipt validation and declared comparator against default | passed | The graph-off/autoscale-off arm passed every individual gate with exact 1,184-token/10-length output, zero request failures or ITL outliers, one intentional eviction/error, three staging admissions, 83 staged-priority forwards, active width eleven, zero graph activity, fixed KV capacity, no resize/reclaim, full drain, and clean teardown. The peer became ready at 6,554.223 ms and produced 85/24/19 ready tokens around the 13,759.517-15,794.587 ms pressure window. The declared default comparison accepted only the two requested/effective toggle pairs and matched every required metric. Optional deltas versus default were throughput -11.54%, p99/p99.9 ITL +2.04%/+1.08%, TTFT p99 +12.91%, E2E p99 +13.16%, decode-forward wall +23.92%, and prefill-forward wall -2.06%, consistent with the separately accepted graph-free slowdown. The manifest correctly rejects a direct graphs-off-to-both-off comparison because that directional pair is undeclared; neither accepted arm resized or reclaimed memory, so this run does not isolate or exercise autoscaling and supplies no causal autoscale claim. |
| 2026-07-12 | Stable-profile ROCm mixed-load proof | `sha256:c8789f3acb7a` | `b7536d22` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t182424766420z-rocm-strix-halo-serving-mixed-rocm-v1-8dd0211c5b-v1.json`; strict receipt and local-artifact validation | passed | The stable arm requested KV autoscaling, automatic reclaim, and ROCm graphs while health and the receipt proved all three effectively off. It retained the exact 1,184-token/10-length output, zero request failures or ITL outliers, one intentional eviction/error, three staging admissions, 83 staged-priority forwards, active width eleven, fixed 4,096-block KV capacity, full drain, and clean teardown. Across 2,616 externally bounded backend yields it recorded zero synchronization failures, zero calls at or above 100 ms, a 0.838 ms maximum, and 31.413 ms total. Maximum decode/prefill forwards were 100.938/34.095 ms; p99/p99.9 ITL was 104.0/172.5 ms. The peer became ready at 6,552.580 ms and produced 83/24/21 ready tokens around the 13,580.469-15,570.869 ms pressure window. The apparent 218.943-second runner duration includes an explicitly reported 85.245-second release-binary build before server launch; it is not an unaccounted inference pause. This accepts the stable suppression and mixed-load contract on current source, not the still-open long-duration soak or forced memory-pressure gates. |
| 2026-07-12 | Failure-atomic KV pool replacement foundation | `sha256:f07b09f0334a` | this commit | portable; Strix Halo ROCm/Vulkan compile gates | 379 model library tests; focused pool identity, shrink, grow, copy, zero-tail, and no-op tests; default, ROCm gfx1151, and Vulkan server all-target checks; formatting and diff hygiene | passed | Every paged-KV cache now has a process-unique allocation ID and a generation that advances only after a successful physical replacement. Shrink and grow stage every layer, preserve the old tensors and published capacity through allocation/copy/final-synchronization failure, then perform one pool/capacity/generation commit. No-op resize preserves identity, separately constructed pools cannot alias identity, and successful shrink/grow preserve the KV prefix while zeroing the grown tail. This intentionally replaces the old host-staged shrink's low transient high-water with an `old + new` headroom requirement: portable failure atomicity is the correctness priority, and the autoscaler must account for staging headroom before the device gate can pass. Graph replay binding, actor-level `BlockManager` publication, fault injection, and real ROCm acceptance remain open, so no Phase 1.2 checkbox is closed here. |
| 2026-07-12 | Paged-KV graph allocation identity binding | `sha256:7bf11e90199a` | this commit | portable; ROCm/Vulkan compile; toolkit-free CUDA compile | 379 model library tests; 22 resource-concurrency invariants; focused stale/current identity rejection; default, ROCm gfx1151, Vulkan, and `CUDARC_CUDA_VERSION=12080` CUDA checks; formatting and diff hygiene | passed | CUDA single-row and opt-in batched graphs plus ROCm HIP graphs now retain the exact paged-cache allocation ID, generation, and block count captured by their kernels. Every replay compares live identity before any native launch; a mismatch destroys that cached graph and takes eager fallback rather than touching stale pointers. The production physical-resize route additionally acquires the exclusive GPU barrier and model write lock, checks backend health, and invalidates CUDA, ROCm, and Metal decode graph state before the cache can replace or release an allocation. The cache-level guard proves an old generation is rejected while the current generation remains accepted. Actor drain, transactional logical-capacity publication, injected failures, and real graph-before/after-resize hardware proof remain open, so this is defense-in-depth implementation evidence rather than Phase 1.2 acceptance. |
| 2026-07-12 | Drained FIFO KV resize barrier | `sha256:38fbc4d35646` | this commit | portable; ROCm/Vulkan compile; toolkit-free CUDA compile | 914 server library tests; deterministic active-resize-admission ordering and existing adapter-order regressions; default, ROCm gfx1151, Vulkan, and `CUDARC_CUDA_VERSION=12080` CUDA server all-target checks; formatting and diff hygiene | passed | Physical resize no longer runs merely between two actor steps. Resize and adapter swaps share one FIFO-exclusive mutation queue; a pending mutation pauses admission, lets every active prefill/decode row finish, and runs only at the between-requests barrier. A gated two-step request proves resize cannot fire while active, the queued request cannot prepare early, both decode steps precede resize, and admission resumes only afterward. The production route then holds the global GPU writer and model writer, invalidates graphs, prepares a cloned logical `BlockManager`, commits the physical pool transaction, and publishes the logical state with one infallible assignment. A physical error therefore cannot leave a pre-published logical shrink or grow. Direct inference workers retain the shared GPU read owner through cleanup, so the same writer excludes their live accelerator ownership. This closes the quiescent-barrier implementation item; injected boundary failures and current-source ROCm resize/graph evidence remain required before accepting transactional resize as a whole. |

| 2026-07-12 | Exhaustive KV resize rollback proof | `sha256:14d216460385` | this commit | portable; ROCm/Vulkan compile; toolkit-free CUDA compile | 104 core tests with 3 ignored; 382 model and 918 server library tests; 42 injected shrink/grow transaction failures plus no-op, logical rollback, successful publication, and capacity-mismatch regressions; default, ROCm, Vulkan, and `CUDARC_CUDA_VERSION=12080` CUDA server all-target checks; formatting and diff hygiene | passed | The physical transaction exposes a unique deterministic checkpoint before the initial synchronization; each key and value allocation; each zero-fill synchronization; each key and value prefix copy; every staged-layer boundary; the final synchronization; and the single commit. Three layers produce 21 distinct checkpoints per direction. Injecting every shrink and grow failure preserves exact pool tensor identities, dimensions, values, allocation identity, generation, and block count, and every failed cache remains retryable. Production logical publication now uses the same tested transaction helper: a cloned `BlockManager` is assigned only after physical success, injected shrink/grow failures leave it exactly unchanged, a successful operation publishes matching logical/physical capacity, and a preexisting mismatch fails before physical work. This closes portable failure atomicity and fault injection; transient `old + new` headroom accounting plus current-source ROCm eager/captured resize execution remain required before Phase 1.2 acceptance. |

| 2026-07-12 | Transactional KV staging-headroom policy | `sha256:bc885feaa270` | this commit | portable; ROCm/Vulkan compile; toolkit-free CUDA compile | 923 server library tests; 10 autoscaler staging, direction, floor, step, hysteresis, and bounded-backoff regressions; training-reclamation staging regression; default, ROCm, Vulkan, and `CUDARC_CUDA_VERSION=12080` CUDA server all-target checks; formatting and diff hygiene | passed | Every production resize caller now budgets the full replacement pool while the old generation remains live; grow is no longer incorrectly admitted from delta-only headroom. Policy decisions use the stricter governor/allocator signal, while staging uses the backend allocator's actual safe bytes and includes reusable ROCm pool spare. A typed plan may cap grow, may deepen an automatic shrink only within its explicit step/floor, and refuses an exact forced shrink rather than silently crossing the operator target. Accepted plans hold a temporary governor reservation for all replacement bytes through the drained physical transaction. Training reclamation separately tracks required bytes and the exact capped reservation it actually holds, preserves unrelated reservations, and may deepen to the smallest stageable target above its one-block floor. Insufficient headroom, allocation failure, and zero-yield resize retry with bounded 8-128 second exponential backoff instead of a two-second loop. Current-source real ROCm shrink/grow and graph replay evidence remain required. |

| 2026-07-12 | ROCm replacement-tail corruption fix | `sha256:daa06785a1f0` | `2f448a2d` | Strix Halo ROCm/gfx1151; portable; backend compile matrix | five consecutive 2 GB physical shrink/reuse/grow qualifications; 1 GB independent shrink proof; 11 ROCm storage smoke tests with 2 ignored; 382 model, 987 tensor, 6 HIP, and 923 server library tests; default, ROCm, Vulkan, and `CUDARC_CUDA_VERSION=12080` toolkit-free CUDA server all-target checks; hosted CI run `29207419461` | passed | The clean transactional implementation reproducibly copied a marker into the grown layer-0 key pool and later returned zeros after additional staged allocations touched the multi-gigabyte replacement generation. A bounds-checked raw range D2H path removed the temporary device allocation from compact-view observation and localized the loss to replacement staging rather than readback. ROCm replacement pools now allocate uninitialized, copy the complete surviving prefix before commit, and leave grown free blocks inaccessible until block assignment plus their mandatory first KV write; constructors remain zeroed and other backends retain zeroed resize tails. This removes a needless multi-gigabyte mid-resize write and its observed corruption mode. Every repeated run preserved the marker through nine 200 MiB training-like allocations, restored 8,000 blocks, and round-tripped the first grown slot after write. Hosted CI passed formatting, cargo-deny, default build/tests, substrate tests, and qualification-tool validation while expensive GPU lanes remained skipped by policy. This is the physical-substrate evidence paired with the serving-path runtime proof below. |

| 2026-07-12 | ROCm eager/captured decode across transactional resize | `sha256:cbe8aee7d2c7` | `8e3a8000` | Strix Halo ROCm/gfx1151; portable backend compile matrix | exact ignored `real_model_integration` qualification; six consecutive hardware runs; 26 default real-model integrations; 923 server library tests; default, ROCm, Vulkan, and `CUDARC_CUDA_VERSION=12080` toolkit-free CUDA server all-target checks | passed | One BF16 full-attention serving fixture executes six 48-token requests over the same paged cache: eager and HIP-graph parity before shrink, after shrink, and after grow. Shrink 64 -> 32 and grow 32 -> 64 are requested only after a native graph is live and has replayed; the actor reports one active decode and emits another token before either resize can complete. Stable cache identity advances generation 0 -> 1 -> 2, while graph invalidation forces one fresh capture after each replacement. Every run records three captures, 135 successful replays, zero graph failures, and exact eager/captured token equality. Reserving all 32 surviving blocks forces both post-grow requests to allocate, initialize, and attend through the uninitialized ROCm replacement tail. This closes the real eager/captured before/after shrink/grow item; Metal pool identity remains a separate open gate. |

| 2026-07-12 | ROCm stale-generation native-launch refusal | `sha256:5dfdf3a71f2f` | this commit | Strix Halo ROCm/gfx1151; portable; unit-build launch instrumentation | exact ignored `kiln-model` qualification; six consecutive hardware runs; 382 portable model tests; default and ROCm model all-target checks | passed | The test establishes one valid capture and native replay, computes the next eager oracle on the same cache, and corrupts only the graph's retained pool generation. That leaves the baked pointers physically valid, so a missing guard would be safe to execute but would increment the probe immediately before `hipGraphLaunch`. Normal replay instead reports the identity mismatch, takes exact eager fallback, trips the circuit breaker, and clears the cached graph. Every run observed replay attempts 1 -> 2, replay failures 0 -> 1, and native launches 1 -> 1. The launch probe and fixture are compiled only into the unit-test binary; production code retains the existing fail-closed identity check. |

| 2026-07-12 | Coordinated ROCm automatic pool trim | `sha256:624b2ebc9460` | this commit | portable; ROCm implementation; backend compile matrix | 924 server library tests; focused idle/releasable policy regressions; default, ROCm, Vulkan, and `CUDARC_CUDA_VERSION=12080` toolkit-free CUDA server all-target checks | passed | Allocator reclaim is now wired only after the batching actor, backend-health latch, and shared GPU coordination lock exist. A periodic ROCm attempt first reads actual pool reserved/used/spare bytes, refuses active prefill or decode, and uses a nonblocking exclusive acquisition so the monitor never queues a writer behind inference. After acquisition it rechecks health, actor activity, and pool statistics before the only path that can reach `hipDeviceSynchronize`; busy, active, unhealthy, and zero-spare paths return without synchronization. Completed trims retain measured reserved-byte yield plus target, pool state, coordination wait, reason, and duration. Bounded automatic retry/backoff and the real below-10-percent pressure latency gate remain open. |

| 2026-07-12 | Bounded automatic memory reclaim schedule | `sha256:189c8045e55b` | this commit | portable; backend compile matrix | 39 memory-governor tests with one hardware smoke ignored; 924 server library tests; focused health and Prometheus contracts; default, ROCm, Vulkan, and `CUDARC_CUDA_VERSION=12080` toolkit-free CUDA server all-target checks | passed | Automatic reclaim now uses a pressure episode with distinct 10-percent trigger and 25-percent recovery thresholds. Moderate pressure remains armed until recovery, successful yield receives an eight-second cooldown, and zero-yield attempts back off at 2/4/8/16/32/64/128 seconds with only the first zero-yield event at info level. Health and Prometheus expose attempts by outcome, suppressed retries, total actual reclaimed bytes, last target/yield/duration, next retry, and current zero-yield streak; logs carry the same target, actual yield, before/after pressure, free bytes, duration, and retry fields. The operator table documents the exact policy. Cheap CI run `29208772460` exposed a stale source-contract ordering assertion from the preceding coordination change; `5b5f604e` repaired it immediately and replacement run `29208949084` is fully green. Real sustained-pressure latency evidence remains open. |

| 2026-07-12 | Bounded external ROCm pressure peer | `sha256:7986d4c39e03` | this commit | Strix Halo ROCm/gfx1151 + portable policy tests | 7 allocation-policy, DRM-accounting, safety-floor, and atomic-ready tests; real 256 MiB HIP allocation/budget-exhaustion cleanup smoke; all 306 qualification-tooling tests | passed | The focused peer uses direct HIP allocations in a separate process, mirrors the governor's DRM VRAM-plus-GTT accounting, approaches an 8-percent free target in aligned bounded chunks, enforces a 5-percent free safety floor during allocation and hold, caps total allocation and hold time, and publishes readiness without replacing an existing artifact. Signals and every failure path release the retained pointers before process exit; process termination provides deterministic driver-context cleanup. The real bounded smoke deliberately exhausted its 256 MiB budget far above the pressure target, failed closed, freed both allocations, and did not publish readiness. This is qualification tooling, not evidence for the still-open sustained below-10-percent serving-latency gate. |

| 2026-07-12 | Source-bound ROCm pressure qualification contract | `sha256:2f65e99fdee5` | this commit | portable contract; ROCm execution pending clean-source checkpoint | strict workload validation; 74 focused driver/workload tests; all 315 qualification-tooling tests; pushed qualification-contract run `29209713657` green on the preceding peer checkpoint | passed | The committed `serving-rocm-memory-pressure-v1` workload builds current ROCm source, starts an isolated experimental server with fixed KV capacity, graphs off, and automatic reclaim on, waits for all four deterministic 384-token streams to decode, then drives a separate HIP process to an 8-percent free target with a 5-percent floor. The driver requires at least 20 seconds and 40 samples wholly below 10 percent, keeps all four requests active, gates every wholly enclosed server-ready ITL gap below 1,000 ms, proves monitor attempt/zero-yield/backoff counters in both health and Prometheus, forbids physical reclaim/resize/graph activity/quarantine/errors, and requires complete length-terminated responses plus memory recovery and clean process teardown. The manifest inventory now covers correctness, mixed serving, slow-consumer, cancellation, long-prefill, memory-pressure, and training cases. This is a source-bound harness checkpoint; Phase 1.3 remains open until its real receipt passes. |

| 2026-07-12 | Qualification authentication-payload redaction | `sha256:a106c6f11bf3` | this commit | portable security contract | 43 focused environment/runner tests; all 316 qualification-tooling tests; tracked-receipt secret scan | passed | Preflight for the first pressure receipt exposed that ignored local `run-config.json` artifacts treated an authentication JSON environment variable as public because the two capture paths maintained separate sensitivity lists without an `AUTH` marker. Both paths now use one shared fail-closed classifier covering authentication, tokens, keys, secrets, passwords, credentials, webhooks, and cookies; sensitive values are stored only as SHA-256 digests. Regression fixtures prove authentication JSON and cookies cannot appear verbatim while ordinary diagnostics remain readable. No tracked receipt contained the affected variable or refresh-token material. The ignored historical local artifacts are not rewritten or claimed as evidence. |

| 2026-07-12 | ROCm pressure-target ceiling reproducer | `sha256:a106c6f11bf3` | `9acd21ca` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t214458333195z-rocm-strix-halo-serving-rocm-memory-pres-285fdb5a13-v1.json` | failed | The clean-source server loaded and prewarmed successfully, started all four measured streams, and the external peer made 128 successful 512 MiB HIP allocations (64 GiB total). The next allocation failed with HIP status 2 before the peer's unnecessarily strict 8-percent readiness target, so no latency measurement was admitted and teardown released every peer allocation and stopped the server cleanly. The final emitted pre-allocation snapshot was already at 9.98-percent DRM free; the last successful allocation would move the same counter farther below 10 percent, but that inferred value is not accepted as sampled evidence. This is a harness-target defect rather than a server failure: the Phase 1.3 gate requires sustained free memory below 10 percent, not 8 percent. The follow-up changes the peer target to a sustainable sub-10-percent value while retaining the independent 5-percent floor and strict sample gate. |

| 2026-07-12 | Sustainable sub-10-percent ROCm pressure target | `sha256:d9fe3a07f3dc` | this commit | portable contract derived from Strix Halo failure boundary | 27 focused driver/workload tests; strict workload validation (`sha256:ba4562720437`) | passed | The workload readiness target is now 9.75 percent, inside the all-process DRM range reached by the final successful HIP allocation in the clean-source reproducer. The acceptance condition is unchanged and stricter than readiness: every sustained sample must remain below 10 percent for at least 20 seconds, with at least 40 samples, while the separate 5-percent safety floor remains active. This is a narrow harness correction based on an observed allocator ceiling; it does not weaken the latency limit, request-liveness requirement, counter invariants, or prohibition on physical reclaim during active inference. |

| 2026-07-12 | Sustained sub-10-percent ROCm serving latency | `sha256:d9fe3a07f3dc` | `4aec1ec4` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t215033057397z-rocm-strix-halo-serving-rocm-memory-pres-285fdb5a13-v1.json` | passed | The clean-source workload held 63.76 GiB of external HIP allocations for 30,000.1 ms while all four 384-token streams remained active. All 60 Prometheus samples were below 10-percent free (9.239-9.785 percent), and 348 server-ready ITL gaps wholly inside that window had p99 439.1 ms, maximum 453.7 ms, and zero gaps at or above 1,000 ms. The automatic monitor made four zero-yield attempts and suppressed eleven retries under backoff; the last attempt took 0.014 ms, no attempt reclaimed bytes, and no physical reclaim, KV resize, graph event, batching error, backend quarantine, or request failure occurred. All four requests completed by length with 1,536 output tokens, 426 attributed external-yield synchronizations had zero failures/slow calls and a 0.383 ms maximum, memory recovered above 20 percent in 507.8 ms after peer exit, and both processes shut down cleanly. This closes the Phase 1.3 sustained-pressure stall gate on gfx1151. |

| 2026-07-12 | Dynamic KV allocation and resize-sync attribution | `sha256:125ee06840e2` | this commit | portable + ROCm/Vulkan compile | 382 model library tests; default model runtime; ROCm gfx1151 and Vulkan model checks; formatting and diff hygiene | passed | Every paged-KV pool allocation now emits one bounded `gpu_memory_operation` record with operation, initial-versus-resize reason, outcome, device, layer, key/value pool, requested/actual bytes, submission duration, zero-initialization state, and an explicit zero coordination wait. Routine successful startup-layer records are debug-level; resize allocations are info-level and every failure is a warning. Every resize-only device synchronization separately records the bounded initial-drain, per-layer allocation-completion, or final-copy-completion phase plus affected replacement bytes, blocking wait/duration, device, layer, and outcome. This covers the model-owned allocation/synchronization substrate; caller reason, actor-barrier wait, exclusive-GPU wait, whole-resize duration, and aggregate startup allocation remain in the next server checkpoint, so the Phase 1.3 attribution item stays open. |

| 2026-07-12 | Server-owned dynamic GPU memory attribution | `sha256:0432bfda2076` | this commit | portable + ROCm gfx1151/Vulkan/CUDA compile gates | 924 server library tests; 47 focused qualification tests; default, ROCm gfx1151, Vulkan, and toolkit-free CUDA server all-target checks; formatting and diff hygiene | passed | Aggregate initial KV allocations and their same-size reclaim retries now record bounded attempt, byte, outcome, wait, and duration fields. Actor-owned physical resize carries a closed automatic-policy, forced-configuration, training-preparation, or maintenance reason from enqueue through the quiescence barrier; its record separates barrier, GPU-coordination, model-lock, mutation, and total duration while reporting requested and actual blocks/bytes. Training reclaim and coordinated ROCm pool trim use the same event schema, including zero-yield and failure outcomes. The qualification parser recognizes the authoritative physical-resize event. The first all-target run caught that replacing the public `DecodeForward::resize_kv` signature broke an integration caller; the final design preserves that method and adds a defaulted hidden contextual hook, keeping existing implementations and consumers source-compatible. Graph capture/replay synchronization attribution remains open. |

| 2026-07-12 | HIP cross-stream event-ordering substrate | `sha256:07c32e97fca4` | this commit | Strix Halo ROCm/gfx1151 + portable compile | 7 real HIP tests; portable `kiln-hip` check; formatting and diff hygiene | passed | The bounded HIP wrapper now owns device-bound, timing-disabled events with RAII destruction and rejects cross-device stream/event use before FFI. Streams can record and wait on reusable events without blocking the host. A real two-stream test uploads on the default stream, orders a non-default-stream device copy through an input event, orders the download through a completion event, and proves all 4,096 bytes exactly. This is the independently tested primitive for removing replay host synchronization; the graph runner is intentionally unchanged in this checkpoint. |

| 2026-07-12 | Event-ordered ROCm graph replay | `sha256:858b4f69d946` | this commit | Strix Halo ROCm/gfx1151 + portable contract | exact stale-generation hardware test; exact eager/graph active-resize hardware qualification; 135 native replays, 3 captures, and generations 0 -> 1 -> 2; focused replay-route contract; ROCm model all-target check; formatting and diff hygiene | passed | Each captured graph retains reusable input-ready and replay-complete events plus both streams. Steady-state replay records refreshed inputs on the kt default stream and makes the capture stream wait, then records launch completion on the capture stream and makes eager lm-head work on the default stream wait. Neither handoff blocks the host, while external-yield settlement still confirms device completion before progress publication. The structural contract rejects reintroduction of either host synchronization. The real resize qualification produced exact eager/captured 48-token parity before shrink, after shrink, and after grow across 135 replays with zero failures; the separate poisoned-generation test still refused native launch and returned the exact eager fallback. CUDA single-row and batched graphs plus ROCm graphs all already retain and validate exact allocation ID, generation, and block count before launch, so the stale Phase 1.2 implementation checkbox is now closed. Capture-establishment and failure-recovery synchronization remain host-blocking and will be attributed separately. |

| 2026-07-12 | Attributed ROCm graph synchronization | `sha256:e908e7afefa9` | this commit | Strix Halo ROCm/gfx1151 + portable qualification contract | 399 ROCm model tests with one explicit hardware ignore; exact 135-replay eager/graph active-resize hardware qualification; 316 qualification-tooling tests; ROCm model all-target check; formatting and diff hygiene | passed | Every remaining ROCm graph host wait now passes through one bounded `gpu_memory_operation` record. Warmup input drain/completion, capture-begin input/capture-stream drain, first-launch completion, and default-stream failure recovery carry closed reason and phase values, outcome, wait/duration, and either the exact stable graph-I/O byte count or an explicit zero with `unknown_in_flight_device_work` scope. Steady-state replay has no host wait after the event-ordering checkpoint. The previous server-attribution change also exposed a classifier regression before this push: structured records use `event=gpu_memory_operation`, so message-only resize matching could never fire. Classification is now field-driven for resize, reclaim/trim, and the closed ROCm graph-sync reason set; unknown operations and operator-supplied reasons remain unattributed. Graph sync is included in ITL causality and graphs-off policy checks. The real graph/resize path again produced exact parity across 135 replays and zero failures. CUDA graph synchronization attribution remains an off-machine gate. |

| 2026-07-12 | Typed ROCm graph fallback contract | `sha256:7ffa25ba4936` | `d0775a6a` + this commit | Strix Halo ROCm/gfx1151; portable health/qualification/browser contract; default/ROCm/Vulkan/CUDA compile gates | 400 ROCm model tests with one explicit hardware ignore; 924 server library tests; exact stale-generation and 135-replay active-resize hardware qualifications; 316 qualification-tooling tests; release/link guard; Chrome 150 docs smoke; formatting and diff hygiene | passed | All eager graph fallbacks now use one closed seven-reason model: shaped-warmup failure, cold-cache host round-trip, persistent host round-trip, graph-cache capacity, critical memory pressure, capture failure, or replay failure. The shared wrapper records first/slow/failed structured events with attempt, eager, and end-to-end duration; lifetime health exposes total and per-reason counts plus slow, total-microsecond, and max-microsecond counters. Qualification rejects missing, negative, non-summing, or contradictory counters, recognizes only the closed structured event, attributes it to exact ITL windows, and treats graph synchronization as a graphs-off violation. The normal real route retained exact eager parity across three captures, 135 replays, and generations 0 -> 1 -> 2 with every fallback counter zero. The poisoned-generation route incremented exactly one replay-failure fallback while native launches stayed unchanged. The API site and permanent qualification guide document the health path and reason vocabulary. Hosted CI on the preceding synchronization commit exposed a stale string-matching resource invariant; `d0775a6a` changed it to require the typed wrapper, exact phase labels, and both underlying stream waits, and the focused reproduction passes. |

| 2026-07-12 | Fail-closed ROCm graph geometry and lifecycle parity | `sha256:d9bd35ee0188` | this commit | Strix Halo ROCm/gfx1151; default, ROCm, and Vulkan compile gates | 400 ROCm model tests with three explicit hardware ignores; 924 server tests; 316 qualification-tooling tests; 22 resource/concurrency invariants; exact unsupported-geometry, 91-replay lifecycle-parity, and stale-generation hardware qualifications; CUDA runtime dependency-tree and release-version guards; formatting and diff hygiene | passed | The new parity corpus exposed a real false-success case: a 1:1, head-dim-64 fixture declined graph-stable native paged attention, so HIP capture silently recorded the generic attention fallback with the current K/V length embedded in tensor shapes. Replay updated token, position, rotary, slot, and bit-identical K/V pools but reused the captured attention length and returned wrong hidden state. ROCm graph-backed forward now returns a typed marker before any shape-dependent attention fallback can be captured; the runner treats it as a per-geometry deferral, remains armed for supported buckets, caches `shape_dependent_attention`, and executes exact eager output. Health and qualification extend the closed fallback vocabulary to eight reasons. A negative hardware test produced three counted fallbacks, one deferral, zero captures/replays/failures, and exact eager parity. The production-valid native fixture then crossed 64-token buckets and growing block tables, released one decode owner, reused populated prefix pages under a new owner, invalidated across an adapter boundary, and completed four captures plus 91 replays with exact hidden-state, full-logit, and final greedy-token parity and zero fallbacks. The poisoned-generation test still prevented a native launch and recorded exactly one replay failure. Stable and maintenance profiles already disable live capture; this fail-closed guard makes experimental lazy capture reject unsupported geometry rather than fabricate correctness. This closes both remaining ROCm graph-behavior gates. |

| 2026-07-12 | Continuous-process ROCm development-soak gate | `sha256:1ea8b37d4f8f` | this commit | portable + Strix Halo ROCm/gfx1151 | 323 qualification-tooling tests; all eight workload manifests; release-version and formatting guards; 67-second real-device harness validation after steady-state warmup | passed | The new non-comparative soak workload builds once, holds one graph-on/fixed-capacity server, fills the bounded 39-entry recurrent prefix cache before establishing memory baselines, and then runs fixed-output concurrency waves with multiple sequence buckets and periodic cancellation. It fails closed on request/cancellation errors, structured or message-carried device faults, graph failures/fallbacks, failed or 100 ms backend settlement, unexplained ITL gaps, unaccounted KV blocks, active/pending prefix ownership, cache-residency drift, dirty teardown, snapshot residue, and more than 512 MiB of post-warmup GPU or RSS growth. The final direct smoke measured 38 requests over six waves, one confirmed cancellation, 26 prefix hits reusing 331 blocks, 546 graph replays, zero faults/fallbacks/request failures/unaccounted blocks/unexplained gaps, three bounded actor-decode ITL attributions, stable 39-entry residency, 287 MiB GPU growth, and 219 MiB RSS growth. This smoke validated the dirty development harness and is not a qualification receipt; the 30-minute gate remains open until the committed workload runs from clean pushed source. |

| 2026-07-12 | ROCm soak post-warmup RSS counterexample | `sha256:1ea8b37d4f8f` | `6da0aea9` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260712t234918603177z-rocm-strix-halo-serving-rocm-development-053e89eca9-v1.json`; strict receipt validation | failed | The first clean source-bound run filled all 39 prefix entries before measurement, then completed 116 requests across 16 waves, three confirmed cancellations, 65 captures, 1,085 replays, 106 prefix hits reusing 1,414 blocks, and clean teardown. It recorded zero request failures, device faults, graph failures/fallbacks, backend-sync failures/slow calls, unexplained ITL gaps, unaccounted blocks, active leases, pending releases, or cache-residency drift. GPU growth was 438.86 MiB, but RSS grew 537.45 MiB after only 148.5 measured seconds and crossed the unchanged 512 MiB bound by 25.45 MiB. Filling prefix entries alone therefore does not establish the graph/batch allocator high-watermark; the next harness checkpoint must exercise repeated fixed-prompt concurrency cycles before taking the baseline rather than weakening the limit. |

| 2026-07-12 | Fail-closed ROCm soak memory convergence | `sha256:31e9a1667393` | this commit | portable + Strix Halo ROCm/gfx1151 development probe | 28 focused qualification tests; strict workload validation; eight-cycle real-device convergence probe | failed as designed | Before the measured clock starts, the harness now requires at least four complete workload cycles and two consecutive endpoints within 64 MiB GPU-used and 16 MiB server-RSS deltas. It fails after eight cycles and preserves partial cycle, request, cancellation, final-delta, and maximum-delta evidence rather than emitting misleading zeros. The first dirty-tree probe reached the full 39-entry prefix-cache state and then failed after all eight cycles with no forced/nonzero shutdown or snapshot residue. RSS remained nonconvergent: observed cycles five, six, and eight added 146,829,312, 126,554,112, and 146,071,552 bytes. The unchanged 512 MiB measured bound was never entered. Repeated request-owned HIP graph capture/release is now the bounded runtime hypothesis to test; this row is harness evidence, not a passed soak or a causal claim. |

| 2026-07-12 | Bounded reusable ROCm graph slots | `sha256:95dcb35b1c7a` | this commit | Strix Halo ROCm/gfx1151 + portable qualification contract | exact lifecycle parity hardware test; release binary `sha256:c824e37fd6ba`; 60-second full-workload development smoke; focused shutdown/qualification tests | passed development smoke | A matched graph-off control grew server RSS by only 4,141,056 bytes across eight complete cycles, while graph-on request-owned capture/release repeatedly added roughly 121-140 MiB, isolating the nonconvergence to native HIP graph churn. ROCm graphs now belong to a bounded reusable slot rather than a logical request row. Rebinding refreshes the exact persistent recurrent/conv buffers in place, makes the new row adopt those handles, resets continuity, and prefers an idle slot that already owns the requested geometry. Request completion leaves graph-bearing slots idle; adapter invalidation destroys graphs before their buffers. The real lifecycle parity test crossed graph buckets, cancellation, unrelated fresh recurrent/KV state, original-prefix continuation, and adapter invalidation with three captures, 96 replays, exact hidden/logit/token parity, and zero fallback. The full graph-on smoke used an explicit 12-graph bound, converged in four cycles with two stable endpoints, then measured 38 requests over 69.96 seconds: RSS grew 421,888 bytes, GPU use 13,656,064 bytes, retained graphs stayed 10 -> 10, slots 3 -> 3, all three ended idle, and 20 measured reuses occurred with zero request/fault/graph/fallback/sync/unexplained-gap failures. It also exposed and fixed a detached-worker teardown race whose core showed `RocmSlice::drop` calling `hipFreeAsync` concurrently with HIP finalization; the server now closes and joins the decode worker before accelerator teardown, and the reproducer exited zero without force or residue. This source-matched dirty-worktree smoke validates the fix but is not the required clean 30-minute receipt. |

| 2026-07-12 | Clean 30-minute reusable-slot ROCm soak | `sha256:95dcb35b1c7a` | `53a582aa` | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260713t011231883902z-rocm-strix-halo-serving-rocm-development-053e89eca9-v1.json`; strict current-source/artifact/commit validation | passed | The clean pushed-source workload converged after four complete cycles with three stable endpoints, then ran 1,094 requests across 151 waves for 1,811.16 measured seconds, including 30 confirmed cancellations and prompt lengths from 136 to 3,160 tokens. It completed 8,734 graph replays, created no new graph slot after baseline, reused slots 551 times, kept three slots resident and idle at final drain, and bounded lazy graph residency from 9 to the configured maximum of 12. RSS grew 37,416,960 bytes and GPU use 95,281,152 bytes, both far below the unchanged 512 MiB gate. All requests and 35,008 output tokens completed with zero request, batching, device-fault, non-finite, graph capture/replay/fallback, synchronization, unexplained-ITL, KV-ownership, cache-lease, or residue failures. The 51 ITL outliers were all attributed; p99/p99.9 ITL were 414.6/509.3 ms. External-yield synchronization had a 0.771 ms maximum. Shutdown was unforced, returned zero after joining the decode worker, and removed the private snapshot. Hosted CI run `29216920797` and qualification-contract run `29216920787` are green with all accelerator jobs skipped. This closes the recurring 30-minute ROCm development gate for the material graph-lifecycle change; the final 24-hour phase soak remains open. |
| 2026-07-12 | Full-model ROCm eager/graph correctness counterexample | `sha256:3fa073dc7459` | this commit | Strix Halo ROCm/gfx1151 development probe | `serving-rocm-graph-correctness-v1`; exact public rollout action-token and selected-token-logprob comparison over four prompt buckets | failed as designed | The new source-bound local gate runs isolated deterministic graph-off and graph-on servers, warms each prompt, then repeats it through prefix reuse. It records exact public rollout provenance rather than accepting text similarity. The graph measurement genuinely executed 188 expected decode replays with three retained graph buckets, four slot reuses, eight prefix hits, zero graph failures/fallbacks, and clean unforced teardown. All four graph-on repeats nevertheless diverged from their own warm trace and the eager oracle. The first short-prompt mismatch occurred at action zero before measured decode replay: warm/eager selected token 15 at logprob -0.0060020443, while the graph-on strict-prefix reuse selected out-of-vocabulary token 248320 at logprob 0.0. A deep-copy and then synchronous CPU-copy experiment on exact-hit logits did not change the counterexample because these non-block-aligned prompts reuse a 128-token KV/GDN strict prefix and recompute the suffix. The remaining bounded hypothesis is graph decode corrupting or aliasing retained strict-prefix KV/GDN state after warmup. The graphs-disabled and pre-captured-graphs correctness gates remain open. |
| 2026-07-13 | Persistent ROCm graph-state address correction | `sha256:2c730b2fe196` | `bcdad5ea` | Strix Halo ROCm/gfx1151 + portable contract | `qualification/receipts/rocm/strix-halo/20260713t031315378590z-rocm-strix-halo-serving-rocm-graph-corre-9c3737d14a-v1.json`; strict receipt/artifact/commit validation; 331 qualification tests; focused graph-slot ownership test | failed, narrowed counterexample | Capture warmup restored GDN state by replacing the request's tensor handles even though the reusable graph slot retained the original handles. The graph therefore recorded request-owned recurrent/conv addresses that were freed at request finish; a later replay wrote through those dangling addresses into a strict-prefix snapshot that had reused the allocation. Rollback now copies values into the persistent slot in place, and a regression asserts both recurrent and conv values are restored without changing either tensor ID. The clean pushed-source run eliminated the prior semantic corruption: graph warm/repeat was exact, all 192 public action token IDs matched eager, 188 measured replays completed through one idle reusable slot with three retained geometries, and eight prefix hits, four slot reuses, zero graph failures/fallbacks, and clean teardown were proven. The exact gate still rejected one narrower numerical difference in the 768-word bucket: at action index one both paths selected token 15, but eager reported logprob -0.0026816889 and graph reported -0.0036571058. This receipt remains failed and both final correctness checkboxes remain open. Hosted CI then exposed only a stale source invariant requiring the removed handle assignments; `6f7790cc` updates it to require in-place restore and reject handle replacement. |
| 2026-07-13 | Exact full-model ROCm eager/graph qualification | `sha256:1c43038c9a38` | `eacf2350` (`e1a0ba15` runtime fix) | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260713t035642923933z-rocm-strix-halo-serving-rocm-graph-corre-9c3737d14a-v1.json`; strict local-artifact/known-commit validation; 333 qualification tests; hosted CI and qualification contract | passed | The final mismatch was not replay state, stream ordering, arena reuse, or the eager LM head. ROCm graphs rounded `max_seqlen_k` to a coarse 512-token cache bucket while eager used the native 64-token FA2 split boundary. At logical length 2,395 this changed the split-K geometry from 2,432 to 2,560 and perturbed BF16 reductions. Graph keys now always match eager's exact FA2 geometry; the larger-bucket environment override was removed so correctness cannot silently become a tuning tradeoff. The source-bound run produced identical eager/graph canonical hashes across all 192 actions, zero token/logprob/output/repeat mismatches, 188 measured native replays, eight prefix hits, four graph-slot reuses, zero measurement captures, failures, fallbacks, or residue, and an idle bounded slot at clean drain. The runner also now allows a bounded successful process group to settle and ignores zombie-only groups while still killing and rejecting live descendants; this removed a false receipt failure without weakening process containment. Hosted CI `29222537358` and qualification-contract run `29222760395` are green. |
| 2026-07-12 | Fail-closed configuration and bounded local Cargo | `sha256:ccacd88adb28` | this commit | Strix Halo host + portable | 55 configuration, 10 logging, and 9 request-log unit tests; 335 qualification-tool tests; real malformed CLI process; bounded-wrapper unit/syntax/refusal checks; single-job 8 GiB/no-swap compile and link scope | passed checkpoint | All authoritative TOML sections now reject unknown keys. Legacy server, model, memory, training, logging, prefix, speculative, streaming, adapter, and request-log environment inputs are fallible and preserve variable plus raw-value context; validation covers semantic values that previously reached runtime clamps or late failures. `RUST_LOG` and tracing directives no longer silently fall back. A real `kiln config` process exited one and named `KILN_PORT` plus `"definitely-invalid"`. During verification, an unbounded filtered Cargo command exposed a developer footgun: without `--lib`, Cargo built unrelated integration targets and linked a 422 MiB server test executable while the 30 GiB unified-memory host entered 8.5 GiB of swap. The checked-in Linux wrapper now refuses overlapping compilers and low-memory starts, defaults to one job, and places the complete build tree in an aggregate systemd `MemoryMax` scope with swap disabled; contributor and CI-policy docs now make target selection explicit. All env-mutating server unit tests now share one poison-tolerant process lock instead of racing through unrelated module locks. The malformed-configuration checkbox stays open only for direct runtime tuning environment reads inventoried in Phase 8.1. |
| 2026-07-12 | Pre-readiness transposed-cache persistence | `sha256:b08189e219f982b3676b3fb9e8c7e1054ed14424c114d26b2f14652c71e1a2a5` | `94aa82d4` | portable source contract | rustfmt; diff hygiene; stale detached-writer and delay-control scan; deterministic cache policy/corruption/read/write regressions; exact runtime-environment ratchet reduced by one read and four mutations; inexpensive default-feature CPU workflow `29227907220` | passed | The unbounded detached writer, delayed write controls, cloned `WeightTensor`, and duplicate transpose are removed. Base-model embedding/projection loaders use the explicit `PersistBeforeReadiness` policy and synchronously persist the transpose bytes they already computed. Lazy native-MTP serving and training use `ReadOnly`: hits may be mapped, while misses, corrupt entries, open failures, and mmap failures perform canonical computation without any rename, removal, write, queue, thread, or delayed work. Optional cache failures cannot fail model loading. The hosted CPU workflow compiled and exercised the model checkpoint without making an accelerator claim. |
| 2026-07-12 | Qualification descendant settlement bound | `sha256:cc05b8e41c0c28d990ffbccdf3b1f876e6b506bdafdfaccb39b1e727be1e0d23` | `942e7cc1` | portable + GitHub Actions counterexample | failing qualification-contract run `29227907217`; three focused lifecycle tests; 100 repeated short-descendant runs with zero termination grace; all 342 qualification-tooling tests from an isolated candidate tree; green rerun `29228263275`; Python compilation and diff hygiene | passed | The hosted runner exposed an exact-boundary race: the synthetic child slept for 100 ms while the test's termination grace also shortened successful process-group settlement to 100 ms. Successful settlement and forced termination are different lifecycle phases. A fixed one-second successful-settlement ceiling now lets bounded descendants finish independently of the operator's termination grace; persistent descendants are still terminated and fail the case. The regression sets termination grace to zero, so it proves cleanup configuration cannot silently remove the settlement window. The green rerun confirms harness determinism without weakening descendant containment or any hardware gate. |
| 2026-07-13 | Mechanical public configuration registry and website contract | `sha256:33f34f024de0c5e42acf15e3cc1aa86927dd797a070975d03a476c9a3e6d8038` | `1ee073cc` with bounded repairs through `dad96333` | portable + documentation portal | 60-field canonical registry; 68-leaf classification and canonical/compatibility/CLI/clear/failure regressions; canonical logging-bootstrap and request-log parity regressions; exact runtime-environment ratchet `1034 -> 1027` reads and `672 -> 670` mutations; 8/8 docs builder tests; 30-page/5-asset validation; release/link/thinking-budget contracts; desktop/mobile Chrome smoke; green CPU CI `29230690259`, qualification `29230690305`, release drift `29230391424`, and Pages `29230391387` | passed | `KILN_<SECTION>_<FIELD>` is executable policy rather than documentation convention. Forty-three old spellings across 42 fields remain warning compatibility aliases; values never appear in warnings, conflicts fail startup, and first-party docs/help/examples teach canonical names. Hosted verification exposed five independent stale assumptions rather than being waved through: release checker `29229452994` still expected an unsectioned name; CI `29229452971` found an invalid temporary static slice; CI `29229685425` caught the mechanically derived `KILN_MODEL_MODEL_ID`; CI `29230034297` caught the corrected 18-canonical-equals-legacy / 43-alias counts; and CI `29230391430` found a source invariant naming a deleted one-off loader. Each was repaired and pushed separately before the final green runs. The complete website reference documents defaults, validation, precedence, clearing, provenance gaps, runtime split reads, and every fixed/dynamic field. Local Cargo correctly remained blocked below the unchanged 15 GiB available-memory floor; all cited automation is inexpensive CPU validation and makes no GPU claim. |
| 2026-07-13 | Fail-closed memory and training admission checkpoint | `sha256:5a0e89d68f9a5870defc14d828a3cf6f95baa11e847f045df0a28dcb84104152` | this commit | portable static + Strix Halo host policy; hardware execution pending | 342 qualification-tooling tests; exact runtime-environment ratchet at 991 reads/628 mutations; Rust 2024 parse/format check across every changed source; 30-document/5-asset validation and 8/8 docs tests; desktop/mobile/cold/empty/failure UI smoke; release, runtime-default, and thinking-budget contracts; independent signature, lifetime, and ownership audit | passed static checkpoint | Memory observations now distinguish physical, configured, primary device-local, separately host-backed, live allocator, cgroup-hierarchy, reservation, and sampler-liveness state. A failed, stale, panicking, or stopped required sampler publishes zero allocation headroom; cgroup v1/v2 ancestors and `memory.high` bound host capacity. Strix Halo's 96 GiB VRAM aperture stays primary while Vulkan's CPU-resident KV and recurrent prefix state must independently fit safe GTT/host headroom, with prefix state reserved before automatic or explicit KV allocation. Startup starts sampler supervision before KV zero-fill and rechecks after allocation. KV autoscaling and training-triggered replacement use atomic checked reservations, one contention replan, and bounded backoff; a replacement may credit only its non-overlapping training guard. ROCm head-major online SDPA charges the complete live conversion, and every materializing attention branch reserves at its allocation boundary. Accelerator identity/cap configuration is typed and fail-closed; unsupported hybrid Vulkan native training is rejected before corpus admission. Server-owned GRPO sources are descriptor-pinned, bounded, identity-checked across multipass preflight, dry run, and training, with route-wide request-body limits and cleanup on every terminal path. `/v1/config`, health, Prometheus, UI, the canonical configuration reference, and the website expose the effective policy and sample status. Cargo correctly refused with 11 GiB available under the unchanged 15 GiB floor, so this checkpoint makes no new compiled or hardware claim; inexpensive hosted CPU validation must be monitored immediately after push. |
| 2026-07-13 | Phase 8.1 fail-closed speculative-decoding authority checkpoint | `sha256:7e04e9b7a159fea147fd0cb240402764d96b6aa0a84ec236e4feda398a647184` | this commit | portable static + Strix Halo host policy; accelerator execution pending | 342 qualification-tooling tests; exact runtime-environment ratchet at 981 reads/625 mutations; 8/8 docs builder tests; 30-document/5-asset site validation and static smoke; release, runtime-default, and thinking-budget contracts; edition-correct Rust formatting and diff hygiene; independent compile/signature, serving-route, ownership, and documentation audits; bounded Cargo admission at the unchanged 15 GiB floor | passed static checkpoint | The loaded typed configuration is the only speculative serving authority. `kiln config`, binary startup before model allocation, and the public real-state constructor reject every effective non-off method with one stable lifecycle/qualification reason. Completion dispatch has no speculative branch and retains ordinary batching; the production loader uses `load_mtp=false`, no speculative prewarm exists, backend native-MTP capability remains diagnostic only, and Desktop cannot enable the feature. The model and server default plus hard ceiling are K=4 pending local K=1/2/4 evidence. Every public high-level `ModelRunner` speculative entry point fails before work; low-level research steps have no serving call site, and the repository's direct caller is the isolated qualification bench, whose enabled arms require the harness contract and strictly validate geometry before allocation. The earlier owner-unsafe exact-K probe, dead admission state, and dead acceptance telemetry were removed. `/v1/config` distinguishes configured intent from serving `off`/unroutable status and intentionally exposes no live routing, admission, scope, or weight-readiness authority. Server SFT now normalizes omitted `train_mtp` to false, rejects explicit true before publication, and checks the invariant again in the worker, preventing deferred MTP upload and uncoordinated alignment during live inference. The hidden Metal override and MTP argmax environment toggle are removed; CUDA and Metal native MTP remain declined. Bounded Cargo correctly refused at 9 GiB available under the 15 GiB floor, so this checkpoint makes no compiled or GPU qualification claim and closes no accelerator gate. |
| 2026-07-13 | Phase 8.1 typed direct-decode rendezvous checkpoint | `sha256:54bc0dbac60e36fcc9c3cc1a4b41ed055b58890036e15b041fbc27d937e79a0c` | `a9bd8330f` + documentation follow-up | portable static + documentation portal; accelerator execution pending | 79-field typed-config registry contract; exact runtime-environment ratchet at 972 reads/611 mutations; 8/8 docs builder tests; 30-document/5-asset validation; static and desktop/mobile Chrome 150 docs smoke; release/link guard; syntax and diff hygiene | passed static checkpoint | Four additional `[batching]` fields make direct streaming effectively-greedy rendezvous mode, maximum width, wait, and mixed-sequence policy startup-authoritative. Canonical names are mechanical; the four old `KILN_DECODE_BATCH*` spellings warn, conflict-check, and fail strictly, including malformed legacy wait values that previously became zero. Every real backend auto-enables the worker; CPU/CUDA/ROCm/Metal/Vulkan preserve exact `(8,0,false)`, `(1,0,false)`, `(8,0,false)`, `(8,100,true)`, and `(64,5000,true)` policies, with width clamped to effective decode width. Configuration diagnostics remain separate from live backend/actor/worker/route state across config, health, debug, CLI, UI, and the website. Worker activity is not routing authority: actor activity shadows the fallback, sampled and non-streaming work bypass it, and under defaults only Metal routes to it. This static checkpoint makes no accelerator correctness or throughput claim and does not close the one-scheduler gate. |
| 2026-07-13 | Complete streaming-prefill execution authority | `sha256:a62f2d8c69d65dc7340580a75f9dc1a7b5f0f70b50955052b16988eeb9d5ee58` | this commit | portable static + documentation portal; accelerator execution pending | exact runtime-environment contract at 963 reads/566 mutations; 342 qualification-tooling tests; generated backend-capability report check; Rust 2024 formatting and diff hygiene; production compatibility-call audits; independent execution-path and identity review; 8/8 docs builder tests, 30-document/5-asset validation, generated-site desktop/mobile Chromium smoke, and complete website/API/reference updates; bounded Cargo admission at the unchanged 15 GiB floor; final CPU CI `29268429061`, qualification `29268429011`, and dashboard/UI smoke `29268887496` green | passed static checkpoint | One startup-resolved policy now reaches ordinary generation, prompt logprobs, GRPO shared-prefix reference forwards, SFT/GRPO/OPD tape and checkpoint replay, local/privileged/merge/live OPD teachers, MTP alignment, and every benchmark prefill. Server teacher construction fails closed without an injected policy. The prompt-logprob teacher fingerprint now uses inference-contract v2 and hashes every execution-policy field, preventing incompatible policy configurations from sharing content identity or logit-cache provenance. The benchmark accepts `--config`, resolves once against selected backend capabilities, and shares that policy with its direct forwards, training runtime, and `ModelRunner`. Standalone compatibility APIs retain backend defaults; production source guards reject their use. The permanent configuration reference, README, quickstart, architecture, changelog, and website API/architecture pages document identity invalidation and restart behavior. The final hosted runs are inexpensive portable validation and make no accelerator claim. |
| 2026-07-13 | Phase 8.1 backend-owned SFT loss routing | `sha256:5de0c1ca68da7b49382d8d8eee1c4961b87e397eb32dc8a7daf7ec885c239fa5` | `a193db663` + test/docs follow-up (admission `307327fca`; budget repair `4c09c043b`) | portable CPU/static; accelerator execution pending | route-specific saturating estimator and boundary tests; admission/queue/trainer/receipt/identity source contracts; exact runtime-environment contract at 950 reads/539 mutations; qualification run `29274470176`; CPU CI runs `29272421238`, `29273482966`, and `29274469856`; 8/8 docs builder tests and generated static/browser smoke | passed portable checkpoint | The first admission commit removed `KILN_USE_FLCE`, selected the backend capability, charged route-specific loss residency, and rejected checkpointed `full_logits`. CPU CI `29272421238` passed 1,021 tests before one focused budget-boundary assertion exposed that a legal maximum-segment plan was still rejected at 30 GiB. `4c09c043b` added explicit 30 GiB reject/31 GiB accept coverage and made CI `29273482966` green. `a193db663` pinned the enum through queue and trainer revalidation, SFT receipt state, and planning identity v4; removed the obsolete Phase A bench/live override; and passed portable CI `29274469856` plus the 342-test qualification contract. The follow-up adds direct v3/v4 resume rejection tests and publishes the contract across the permanent website. These hosted runs make no ROCm, Vulkan, CUDA, Metal, correctness, or throughput claim. |
| 2026-07-13 | Phase 8.1 immutable optimizer authority | `sha256:da62190c4a4dedd29e1fcb6cec02f1aacb807c124b52b5ae691ae99e17b0737a` | this commit | portable static; accelerator execution pending | exact runtime-environment ratchet at 944 reads/530 mutations; seven focused environment-contract tests; generated backend-capability report self-test/generate/check; release-version and Python compilation checks; Rust 2024 formatting and diff hygiene; independent tuple, identity, admission-order, checkpoint, and compatibility-wrapper review; bounded Cargo admission at the unchanged 15 GiB floor | passed static checkpoint | One typed contract now binds exact backend/device identity, resident base dtype, resolved LoRA dtype, optimizer kind, rank, and immutable round-to-nearest execution. CPU uses the F32 reference implementation; CUDA/ROCm, Metal, and Vulkan require their exact supported native hooks, with F16 excluded from training. Submission and dequeue plus direct SFT, inline/streamed GRPO, and OPD fail before physical-memory probes, corpus work, or accelerator ownership and revalidate against the initialized backend. The four backend fallback variables, global training fallback variable, and stochastic-rounding variable have no alias or replacement; legacy stochastic checkpoints fail closed. Review caught and repaired a false broad source assertion, non-exhaustive device matching, Vulkan authority contamination after failed initialization, a CUDA streamed-JSONL open before admission, and standalone memory probes before the cheap tuple guard. Missing expected LoRA gradients remain a separately recorded tape-authority defect. Cargo correctly refused with 8 GiB available, so this checkpoint makes no compiled, accelerator-correctness, or throughput claim; inexpensive hosted CPU validation must be monitored after push. |
| 2026-07-13 | Optimizer admission API, UI, and documentation contract | this commit | `6f6eecad5` with repairs `a2d361717`, `7d529efd0`, `f180dbaf9`, `af216a821`, `75fea561c`, `772576fe8`, `9214c9522`, and `e5e491d78` | portable static/product surface; accelerator execution pending | Rust 2024 formatting and diff hygiene; independent Rust/recipe blocker audit; versioned config-schema tests; fail-closed dashboard state and request guards; server UI static smoke; 8/8 docs builder tests; 30-document site validation; generated-site static smoke; JSON and JavaScript syntax checks | passed static checkpoint | `/v1/config` separates raw optimizer implementations, resident executable tuples, four workload gates, and live-memory admission. Rank state distinguishes backend, model, and effective ceilings. Direct forms and recipes disable unsupported tuples without substitution and expose exact reasons. Distill refresh is a distinct fail-closed workload until exact two-phase preparation and reservation exist. Common admission and dequeue revalidate workload, tuple, full checkpoint-manifest identity, artifact hashes, and effective seed before corpus or governor work. The permanent website and repository references describe the complete schema and non-promises. The listed repair commits address regressions and stale contracts found after the product checkpoint; this row does not claim a current hosted-CI result. Local Cargo remains blocked below the unchanged 15 GiB floor; ROCm/Vulkan execution must follow, so no hardware gate closes here. |
| 2026-07-13 | Phase 8.1 tape-scope, exact-gradient, and frozen-ownership authority | `sha256:6a2d9fd48779d5433b370366ceeecec13deda7f94ba371572ade70123b1ad3a2` | this commit | portable static and documentation portal; accelerator execution pending | Rust 2024 formatting across all 32 modified crate sources; diff hygiene; 13 model tape-routing, 3 GDN frozen-kernel, and 1 ROCm RMSNorm stream/context standalone contracts; exact runtime-environment ratchet at 936 reads/376 mutations; generated backend-capability self-test/check; Python/JavaScript/JSON syntax and release/link checks; 37/37 explicit poison-tolerant CUDA test locks; 8/8 docs-builder tests, 30-document/5-asset build, and generated static smoke; independent compile/API, ownership, and documentation audits; bounded Cargo refusal at 7 GiB available under the unchanged 15 GiB floor | passed static checkpoint | Active training scope is the only tape authority and eight runtime switches are removed without aliases or replacement fields. Inference keeps forward-only paths; graph-preserving RoPE, attention, projection, normalization, GDN, reshape, and loss routes fail closed inside scope. Four unused prefill helpers and the ROCm W8 output leaf explicitly reject active training until their full graphs are recorded. The optimizer requires the exact LoRA ID/shape/backward-dtype/master-device set before mutation, accepts valid zeros, scans finiteness after accumulation, and rejects missing, unknown, duplicate-segment, disconnected registered-input, and non-finite gradients. Bridge fan-in is deterministic and split Q/gate chunks retain the original full A/B IDs, zero-pad each dB slice, accumulate both contributions, and preserve taped reshapes. LoRA A/B are the only trainable model leaves: embeddings, base projections, RMSNorm/GDN norm weights and gate constants, MTP projections, and FLCE/OPD/GRPO heads are constants. Their portable and fused adjoints omit frozen gradients and dWeight GEMMs; fused RMSNorm/GDN kernels compile out dWeight atomics, final-norm training is dx-only, RMSNorm scratch is one hidden-width buffer, and ROCm backward preserves owning context while rejecting capture-context mismatches. Metal finite checking uses a synchronized host correctness fallback. Historical switch documents are explicitly marked non-operational and the permanent website/reference/troubleshooting surface pins the contract. Cargo correctly did not start, and this row makes no ROCm, Vulkan, CUDA, Metal, numerical, stability, memory, or throughput qualification claim. |

| 2026-07-13 | Phase 1.3/8.1 ROCm execution-policy and fail-closed ownership | `sha256:065d8a0ae8675315cb636a1e85a0af21016d94add702df6b573c6392560d4ded` | `59d36171c` | portable static + Strix Halo qualification contract; ROCm execution pending | 98 focused qualification tests; exact runtime-environment contract at 925 reads/376 process mutations; thinking-budget contract; 8/8 docs-builder tests and generated static smoke; Python/Bash/JavaScript syntax; Rust 2024 formatting and diff hygiene; independent crash/correctness re-audit; bounded Cargo refusal below the unchanged 15 GiB floor | passed static checkpoint | Typed startup policy owns ROCm synchronization and graph modes; 22 reasoned wait classes are observable across product surfaces; stream, graph, allocation, capture rollback, and workspace ownership fail closed under sticky per-device quarantine. Review found and repaired first-launch state double advance, warmup retry after partial state, and concurrent hipBLASLt workspace retirement. The source-bound legacy/stream A/B contract is ready, but no compile, GPU, parity, pause, memory, or throughput claim is made. Retained-byte graph budgeting and linearizable quarantine/FFI admission are the next checkpoint. |
| 2026-07-12 | Phase 1.3/1.5/1.6/8.1 ROCm retained-memory, quarantine, and live-phase checkpoint | `sha256:7503eddda785dca76bed000e070e62c9814764d41072c26f366fe0b87ab4025f` | `f806bdc67` | portable source/static + Strix Halo qualification contract; compile and accelerator execution pending | 59 focused qualification tests; exact runtime-environment contract at 925 reads/376 process mutations; 8/8 docs-builder tests; 30-document/5-asset build and generated static smoke; server UI static smoke; thinking-budget, release, runtime-default, Python/JavaScript/JSON syntax, direct Rust formatting, diff-hygiene, installed-HIP-header/symbol, and independent implementation/server/truth audits; bounded Cargo refusal at 8 GiB available under the unchanged 15 GiB floor | passed static checkpoint | Typed policy now adds an independent retained-byte ceiling. Exact queryable allocation identity, owner-aware LRU, pre/post capture admission, matching-device governor reservation, pressure transitions, suppression lifetimes, active graphless-slot accounting, thirteen fallback reasons, five live phases, and typed missing-data reasons span config, health, debug, Prometheus, dashboard, qualification, and the complete website. A linearizable per-device STOP gate, explicit `capture_rollback`, fail-closed FFI submissions, owned host-copy settlement, pageable raw replay H2D contract, and retained uncertain cleanup close the reviewed lifetime gaps; gfx11 capability-query failures now cross FFI as execution errors. This deliberately advances the stall/corruption and bounded typed-config work before later evaluation/performance phases because it is prerequisite to trustworthy local ROCm qualification. Cargo did not start and no ROCm, Vulkan, parity, pause, memory, or throughput claim is made. Next: compile this exact tree when host admission recovers, then run ROCm A/B/byte-pressure/failure/concurrency and the Vulkan baseline before CUDA handoff. |
| 2026-07-12 | ROCm graph checkpoint hosted regression repair | `sha256:cc3e80493e66210a5ca54df36b0f8318f0f80eca6a9e0febb8293060f5d3f142` | `cd5e81014` | portable CPU CI; accelerator jobs skipped by manual-only policy | CI `29303211231`; qualification contract `29303211233`; formatting, cargo-deny, default build/tests, substrate tests, anomaly envelope, CUDA dependency-tree assertion, and qualification tooling/receipt validation | passed | The first hosted run `29302966646` compiled the default tree but exposed one stale exact-spelling assertion in `rocm_graph_state_uses_bounded_reusable_slots`; Pages `29302966717`, UI smoke `29302966639`, release drift `29302966649`, and qualification `29302966626` were green. The repair now checks the actual invariant: every decode/capture path binds geometry to a reusable owner, and the same pre-native `cache_key` reaches final admission. The complete cheap lane is green on the superseding source hash. ROCm/Vulkan builds and hardware behavior remain unclaimed. |

| 2026-07-13 | Source-bound ROCm graph resilience and containment qualification | `sha256:0a0782b7acc4e5f5209040ae901efc5ebe8cc2bc2c14c3876e83419419a83a06` | this commit | portable qualification and documentation contract; Strix Halo ROCm execution pending | 370 qualification-tooling tests; exact runtime-environment contract at 925 reads/376 process mutations; Python compilation; 8/8 docs-builder tests; 30-document/5-asset website build and generated static smoke; diff hygiene | passed static checkpoint | All six ROCm serving drivers now write private typed TOML and require `source=file` attestation instead of constructing public `KILN_*` launch maps. The only launch-environment exceptions are internal debug authorization, `RUST_LOG`, and the still-untyped legacy KV-autoscaler disable flag. The new two-arm resilience workload runs the same deterministic corpus at concurrency 1/8/16/32/64 against one exact release artifact, requires canonical streamed semantic parity, proves headroom capture/replay, forces a typed 64 MiB graph-byte-budget action, bounds graph residency, rejects device faults, dirty teardown, and attributed or unexplained ITL stalls, and emits per-concurrency latency and memory evidence. The companion real-device harness requires all three ignored graph geometry/lifecycle/stale-generation tests to execute and pass under the bounded Cargo wrapper. No compile or accelerator process started below the unchanged 15 GiB host-memory floor, so ROCm correctness, resilience, pause, memory, and throughput gates remain open until clean pushed-source receipts exist. |

| 2026-07-13 | Typed KV autoscaler and maintenance-resize authority | `sha256:4eec157a0521b840822455706a1bd659d703f150dac2537b772dfe8aaf512707` | this commit | portable configuration/product/qualification contract; compile and accelerator execution pending | 84-field mechanical public-config registry; exact runtime-environment contract reduced to 923 reads/376 process mutations; 370 qualification-tooling tests; Rust 2024 formatting; Python compilation; 8/8 docs-builder tests; 30-document/5-asset website build and generated static smoke; diff hygiene; bounded Cargo refusal at 8 GiB available | passed static checkpoint | `memory.kv_autoscale` and maintenance-only `memory.kv_force_blocks` replace the autoscaler's last two direct environment reads. Canonical names are mechanically derived; old names are warning compatibility aliases parsed only by the central registry. The forced target is zero/off by default and a positive value fails unless autoscaling is requested under the maintenance profile. The immutable policy flows into the control thread without an environment read and is reported with exact request, effective state, bounded reason, target, and default/config-file/environment provenance through health, `/v1/config`, and trusted debug state. All six ROCm serving drivers now express autoscaler-off in private TOML and launch with no public `KILN_*` control. Review also corrected the new harness's impossible `source=file` expectation to Rust's public `config_file` vocabulary. Hosted run `29304680991` then exposed Python 3.10's lack of `tomllib`; the test now parses only the generator's closed JSON-scalar TOML subset without an external dependency. No compile or GPU process started below the unchanged 15 GiB floor, so maintenance resize and ROCm behavior remain unclaimed until clean-source receipts exist. |

| 2026-07-13 | Source-bound public adapter and maintenance mutation lifecycle | `sha256:5dd91bbac36e5ec13a4dd6a8949c4a8fd0593aa4aaf0553085fde267b1184625` | this commit | portable qualification/product contract; Strix Halo ROCm execution pending | 378 qualification-tooling tests; exact runtime-environment contract at 923 reads/376 process mutations; strict workload validation; Python compilation; 8/8 docs-builder tests; 30-document/5-asset website build and generated static smoke; release/link and diff hygiene | passed static checkpoint | One exact source-built binary now drives two isolated public lifecycle arms. The experimental arm privately copies and SHA-256 attests a real adapter, proves load/list/health/debug/chat-header revision agreement, requires ordered barrier-swap logs and captured-graph invalidation, unloads, and requires exact canonical base-output restoration. The maintenance arm expresses autoscaling plus a one-block forced target only in typed TOML, requires one exact structured physical resize with finite coordination timing, observes the target through config/health/debug, and proves an HTTP 503 inference rejection changes no batching work counter. Both arms reject device faults, dirty shutdown, and snapshot residue; result details bind the binary, adapter inputs/content revision, generated configs, and semantic outputs. The permanent qualification website documents the invocation, evidence, hard failures, and non-promises. No compile or GPU process started below the unchanged 15 GiB floor, so the Phase 1.7 checkbox remains open until the clean pushed-source Strix Halo receipt exists. |

| 2026-07-13 | Source-bound Vulkan serving baseline contract | `sha256:b60a1b27057d7d8374c95d7799dcc32ecaaa94a4a9d727b1696952d0ed5b2c4e` | this commit | portable qualification/website contract; Strix Halo Vulkan execution ordered after ROCm | focused shared-build and Vulkan-driver tests; strict workload validation; Python compilation; documentation-site validation and generated static smoke; diff hygiene; bounded Cargo admission remains unavailable below 15 GiB | passed static checkpoint | Backend selection is now an immutable build specification: ROCm remains the exact default, Vulkan uses only its feature, and ambient ROCm toolchain variables cannot leak into the Vulkan build. The new required-device/no-skip workload drives synchronized concurrency 1/4/8/12 waves for 25 fixed 32-token requests, binds executable/config/provenance/kernel/semantic identities, proves multi-row batching, records per-wave tail latency and throughput plus memory/synchronization/capacity evidence, and fails on device faults, runtime mutation, batching errors, unexplained outliers, two-second pauses, dirty shutdown, or residue. The permanent website documents the invocation, complete contract, and non-promises. No compile or GPU process started while host admission remained below the unchanged floor, so Phase 6 stays open pending the ordered hardware receipt. |
| 2026-07-13 | Classified runtime-environment inventory and permanent reference | `sha256:12b69425888fefb30006d4283d5f8d416942834931909c1a92cccc1d614f3e17` | this commit | portable source/static and documentation website; accelerator execution unaffected | 391 qualification-tooling tests; exact schema-v2 environment contract/report regeneration; 14 strict workload manifests; Python compilation; 8/8 docs-builder tests; 31-document/5-asset website build and generated static smoke; release/link, runtime-default, thinking-budget, and diff-hygiene gates | passed static checkpoint | The scanner now separates colocated unit tests from production, resolves simple named environment constants, classifies every direct crate-owned access with a rationale, and forbids production process-environment mutation. The generated website and JSON catalog all 923 reads, 376 mutations, 374 literal `KILN_*` names, dynamic call sites, owner paths, and the exact 444-read production migration queue. Of the mutations, 368 are test-only and eight are build-time. The Configuration Reference remains the sole public support authority and links to the inventory; a cataloged name is not promoted merely by existing. This closes only the Phase 8.1 inventory/classification item. Typed policy migration, an explicit experimental namespace/profile, dead-control deletion, and test fixture cleanup remain open. |
| 2026-07-13 | Raw artifact retirement and enforced repository retention | `sha256:42a3922eb7c2691c359c2f5dd3c24b50e45c9a398fb81e7e659922bb833db9d8` | this commit | portable repository and documentation contract; accelerator execution unaffected | 398 qualification-tooling tests; 8 focused policy/index/hash/archive/CLI-boundary tests; writer/importer/locker/validator/dispatch latency-fixture self-tests and retained-hash covered-gate regression; exact staged-index guard; Python compilation; 8/8 docs-builder tests; 32-document/5-asset website build, validation, and generated static smoke; release/link, runtime-default, thinking-budget, and diff-hygiene gates | passed static checkpoint | Removed 4,518 raw `.log`, `.sse`, `.prom`, and oversized CSV files totaling 183,571,706 bytes from the current tree. `docs/audits/removed-raw-artifacts-2026-07-13-v1.json` (`sha256:09039b6bc6d4e76dd1c9674f842cb73e1b867de72e9ca643fef6b44635edbd6e`) binds every removed path at source commit `58138515f0e88071fa793da495e6f86448e070c6` to its byte count and SHA-256 while compact summaries remain. A closed typed policy now forbids common raw evidence formats, caps CSV at 1 MiB and any tracked blob at 10 MiB, permits only path/size/hash/rationale-bound general exceptions, reads exact NUL-delimited index records, and runs in a five-minute CPU-only all-push workflow. Backend-latency imports still hash and parse their downloaded raw log, keep it only in the ignored canonical directory through threshold locking, and allow covered validation from the tracked compact result after the raw payload is gone. Git history was not rewritten; the permanent website documents retention, recovery, and non-claims. Chromium was unavailable locally, so the generated static smoke ran without the optional browser layout pass. |
| 2026-07-13 | Exact verification-test inventory and no-growth ratchet | `sha256:0ddcda2233bb55f0a445fac50726ff70b70f42cc60b6dafc610ab4338dc25a6b` | this commit | portable source/static and documentation website; accelerator execution unaffected | 405 qualification-tooling tests including 6 focused scanner/ratchet regressions; exact 67-test/114-read/946-text-assertion contract and report check; Python/JavaScript/JSON syntax; 8/8 docs-builder tests; 33-document/5-asset website build and generated static smoke; diff hygiene | passed static checkpoint | The generated contract and permanent website now identify every detected Rust ordinary/async test and Python qualification test that reads implementation or qualification-driver source as text, including source reads inherited through helper-call chains while excluding structured fixtures and lexical decoys. Sixty tests inspect implementation text, four inspect qualification drivers, and three inspect generated artifacts; the 33-test backend-capability owner and its 431-assertion mega-test are the largest concentration. Exact checked-in identities make every change reviewable, while three monotonic ceilings refuse count growth and lower after replacements. This closes only the Phase 8.4 inventory item: replacing load-bearing assertions, fail-closed skip handling, mock naming, and redundant-test deletion remain open. Cargo and accelerator processes did not start below the unchanged 15 GiB host-memory floor. |
| 2026-07-13 | Python qualification source-test retirement and shared server launch | `sha256:b15f02e827b934c73129d6ebdd842bc7608f5f2e52b576ca118ff5055b7645eb` | this commit | portable qualification runtime and documentation website; accelerator execution unaffected | 405 qualification-tooling tests; 85 focused serving-driver behavior tests; exact source-test ratchet reduced to 61 tests/108 reads/921 text assertions with zero Python entries; Python compilation; 8/8 docs-builder tests; 33-document/5-asset website build and generated static smoke; repository artifact, runtime-environment, runtime-default, thinking-budget, release/link, and diff-hygiene gates | passed static checkpoint | All seven ROCm serving drivers and the Vulkan baseline now use one fail-closed launcher that requires a source-built binary and regular typed config file, constructs the only supported `--config <path> serve` command, applies the backend-bound sanitized environment, owns the output pipe, and starts the shared structured log collector. Six Python source-inspection obligations were removed rather than renamed. Executable tests now prove launcher arguments and missing-config refusal, one binary build shared across both public-mutation arms, runtime synchronization-dimension rejection, the Vulkan build-spec boundary, synchronized wave dispatch, and structured atomic result publication. The contract dropped every Python source-parsing entry and all three ceilings decreased. The remaining 61 obligations are Rust implementation/generated-artifact tests; this checkpoint does not claim they are resolved or make an accelerator result claim. |
| 2026-07-13 | Fail-closed required-device skip lifecycle | `sha256:c4b140c12d128d8cc6ffbb1e63b23fcc4db705d7f88f5c349236ca93ff9dfd70` | this commit | portable qualification runner and receipt contract; accelerator execution unaffected | focused workload-schema, runner-to-receipt, and receipt-validation skip regressions; 406 qualification-tooling tests; source-test ratchet; Python compilation and diff hygiene | passed static checkpoint | Non-CPU backends must declare `device_requirement=required`; required devices must declare `skip_policy=fail`; every variant must contain a required case. A structured command result with `status=skipped` now has an end-to-end regression proving the runner changes the required result to failed, emits a failed receipt with the explicit skip-policy reason, returns nonzero, and still passes strict receipt validation. Independently, a passed receipt rejects any skipped required result. This closes the Phase 8.4 skip-reporting item without claiming that a hardware case executed. |
| 2026-07-13 | Explicit mock eval test identities | `sha256:f89df657345ac9b8d1c959bc17597368f4cc5717d826ec6c938d6c043a34a085` | this commit | Rust test-target identity and static parse; accelerator execution unaffected | repository-wide mock/e2e/integration naming audit; Rust 2024 formatter parse/check for both renamed targets; exact source-test ratchet and diff hygiene | passed static checkpoint | The only two integration targets whose e2e/end-to-end names concealed injected mock engines or canned generators are now `eval_mock_worker_pipeline` and `qwen3_agentic_mock_executor`. Their module descriptions and the remaining misleading executor test name explicitly state the mock boundary and deny model/backend execution. The `real_model_integration` target remains because it constructs `ModelRunner`/`AppState::new_real` and contains actual model/device qualifications; enum assertions that reject a `Mock` branch do not make it a mock test. Cargo remained blocked below the unchanged 15 GiB floor, so rustfmt provided syntax/edition validation but this checkpoint makes no compiled-test claim. |
| 2026-07-13 | Zero source-text verification and redundant-test removal | `sha256:b13919048d3fd281894083312f031289ac5a6398636a55953dcc443ad8d09a14` | this commit | portable source/static and permanent documentation website; accelerator execution unaffected | corrected zero-test/zero-read/zero-text-assertion ratchet; 9 focused scanner regressions; 409 qualification-tooling tests; capability-report self-test/check; Rust 2024 formatter parse/check; exact runtime-environment contract at 921 reads/376 mutations; 8/8 docs-builder tests; 34-document/5-asset build and Chromium rendered smoke; repository artifact, runtime-default, thinking-budget, release/link, and diff-hygiene gates | passed static checkpoint | Helper-aware scanning exposed 51 obligations missed by the initial 61-entry inventory, including the 36-test resource-concurrency source contract and five ROCm stream source guards. All 112 source-text tests are replaced by or redundant with compiler-enforced types, executable tape/GDN/replay/lifecycle tests, structured canonical metadata validation, and source-bound local qualification as mapped in `docs/VERIFICATION_POLICY.md`. Five source-only targets and seven inline families were deleted; `backend_capability_contract.rs` now retains structured-report and executable tests. The change removes over ten thousand net lines and ratchets recurrence to zero. Bounded Cargo correctly refused below the unchanged 15 GiB available-memory floor, so this makes no new compiled-Rust or hardware claim; cheap hosted CPU checks remain supplemental and will be monitored. This closes the remaining Phase 8.4 replacement and redundancy items. |
| 2026-07-13 | Backend-latency raw-log retention regression closure | `sha256:73e25c177d27cb4d178343b6fb1b47bbbb14cd5677feef189ea451ebada3393b` | this commit | portable latency-evidence and permanent documentation contract; accelerator execution unaffected | 413 qualification-tooling tests including four repository-backed retention regressions; writer/importer/locker/checker/runner/dispatch self-tests; repository-artifact, source-test, runtime-environment, release/link, thinking-budget, Python compilation, and diff-hygiene gates; 8/8 docs-builder tests; 35-document/5-asset build and Chromium rendered smoke | passed static checkpoint | The raw-artifact cleanup correctly changed covered validation to retain the compact JSON and raw-log digest while leaving the raw payload ignored, but one stale Rust source-text assertion still demanded that the raw log be tracked and one published bullet repeated the old rule. Deleting that assertion removed the immediate CI failure but was not accepted as the fix. The validator now rejects an indexed raw-log path even when the checkout file is absent, accepts a clean covered fixture with only tracked compact evidence, and rehashes and reparses an ignored local raw log when present. Repository-backed tests prove all three boundaries plus the writer's clean marker behavior. The cheap qualification workflow now watches every latency tool, manifest/schema, and compact result path, and the complete schema is published on the website. Prior checkpoint CI `29311506669`, qualification `29311506633`, Pages `29311506672`, repository hygiene `29311506631`, and release drift `29311506619` are green; this checkpoint makes no GPU claim. |
| 2026-07-13 | Canonical JSON Schema website references | `sha256:ae5ec10f4a613c929fdb9e4320064c37cf0a575920aef9d9ea0493bafaa38656` | this commit | permanent documentation website; accelerator execution unaffected | 9/9 docs-builder tests including malformed-schema and deterministic generation coverage; 39-document/5-asset build; Chromium rendered smoke; thinking-budget, release/link, repository-artifact, source-test, runtime-environment, JavaScript syntax, and diff-hygiene gates | passed static checkpoint | The website manifest now accepts an explicit `json_schema` document kind and fails closed on wrong extensions, malformed JSON, non-object roots, or missing dialects. Generated pages expose schema identity, root and `$defs` field tables, requiredness, types, constraints, defaults, closed-object status, and exact composition/conditional JSON; they participate in navigation, search, canonical links, sitemap generation, source links, and responsive table/code rendering. The thinking-budget request/resolution/provenance contract plus qualification workload, case-result, and receipt schemas are now generated directly from canonical files rather than copied into another example page. Public config and the full HTTP surface still lack canonical schemas, so Phase 8.5 generation remains explicitly open. |
| 2026-07-13 | Canonical typed-configuration schema and generated reference | `sha256:2bb938ad3766541f127e113b567cea3e1715c53707d563a5b1df4059348710b9` | this commit | portable configuration contract and permanent documentation website; accelerator execution unaffected | strict 92-field/2-template schema-reference-example reconciliation; 13 validator mutation cases; 413 qualification-tooling tests; 9/9 docs-builder tests; 40-document/5-asset build; desktop/mobile Chromium smoke; repository-artifact, source-test, runtime-environment, release/link, thinking-budget, syntax, and diff-hygiene gates | passed static checkpoint | The canonical Draft 2020-12 schema covers all 15 closed typed TOML sections, exact defaults and validation bounds, the dynamic teacher credential map, the deprecated TOML-only `streaming_prefill.enabled` input, 84 mechanically named environment overrides, 63 compatibility aliases, and explicit ROCm profile, graph mode, forced-KV, queue-order, and legacy-toggle conditions. The checker rejects field/count/metadata drift, non-mechanical public names, malformed schema identity, invalid defaults, unknown example keys, and representative invalid configurations. The website renders schema title/identity plus complete configuration-specific tables directly from canonical metadata, adds the reference to navigation/search/sitemap/source links, and verifies its long tables at desktop and mobile widths. This corrects stale 90/82/58/61 counts to 92/84/60/63. Phase 8.5 remains open only for the still-copied HTTP request/response surface. |
| 2026-07-13 | Canonical complete HTTP operation and transport inventory | `sha256:31c07e0db27b34e43d630a22d5b1b125436975b1bbf72f5d2bbe92a86e429e4b` | this commit | portable OpenAPI operation contract and permanent documentation website; accelerator execution unaffected | strict 101-path/112-operation/13-tag validation; five fail-closed contract mutations; 413 qualification-tooling tests; 10/10 docs-builder tests; 41-document/5-asset build; desktop/mobile Chromium smoke; repository-artifact, config-schema, source-test, runtime-environment, release/link, thinking-budget, syntax, and diff-hygiene gates | passed static checkpoint | OpenAPI 3.1 now owns all 53 GET, 47 POST, and 12 DELETE bindings, including embedded assets/redirects, Prometheus text, JSON, multipart, SSE, gzip archives, conditional ETags, and the terminal WebSocket upgrade. Each operation binds its handler/signature owner, one functional tag, parameters/query type, body requirement/type/media, success status/type/media/headers, and the common structured `/v1` error. The generated searchable reference exposes 34 paths missing from the curated API guide; the guide links to the complete contract. Validation rejects missing or duplicate operation identities, placeholder/parameter drift, missing bodies, unsupported media, unresolved references, count/tag drift, owner/signature mismatch, incorrect transports, and unstructured `/v1` errors. The 105 payload components remain honestly open and `migration_pending`, so this closes route/transport inventory but not the Phase 8.5 field-schema and copied-prose retirement gate. |
| 2026-07-14 | Complete inference wire schemas and generated field reference | `sha256:68080f4a66d58fe1f4b3db137184257585dde831e4e4ce24228da5002ab647cb` | this commit | portable schema/product contract and permanent documentation website; accelerator execution unaffected | seven complete OpenAPI inference entrypoints backed by 52 reachable definitions and nine example families; eight adversarial inference mutations plus eight focused shared-validator regressions; exact 17-complete/88-pending component accounting; 421 qualification-tooling tests; 10/10 docs-builder tests; 42-document/5-asset build; desktop/mobile Chromium smoke; repository-artifact, config-schema, source-test, runtime-environment, release/link, thinking-budget, syntax, JSON, and diff-hygiene gates | passed static checkpoint | One Draft 2020-12 schema now owns chat, prompt-logprob, batch, SSE, thinking/performance/config metadata, message/tool, and exact rollout-provenance fields. Request objects accurately retain the runtime's accepted-and-ignored unknown-field policy; emitted responses are closed; OpenAPI resolves the seven public entrypoints to this contract. A shared dependency-free validator replaced the configuration checker's private subset and now verifies external thinking-budget references, every example, response closure, streaming/multiple-choice and adapter conflicts, text-scoring bounds, and sampled-action provenance. Generated pages publish all `x-kiln-*` annotations and media types; the curated API guide delegates field semantics to them. The contract explicitly records the silent unknown-preset fallback and missing sampling-range validation as footguns rather than recommendations. Phase 8.5 remains open for 88 non-inference components and executable runtime router-to-contract enforcement. |
| 2026-07-14 | Complete read-only serving and observability wire schemas | `sha256:753dccebf1a51dfd52ca798b9c778258be847c457887b6b0876642eabccb4f0c` | this commit | portable schema/product contract and permanent documentation website; accelerator execution unaffected | ten complete OpenAPI entrypoints backed by 135 reachable closed definitions and ten example families; exact field-set audit of 111 serialized Rust structs; eight adversarial observability mutations; corrected health 200/503 and debug 403/500 transport contracts; exact 27-complete/80-pending component accounting; 421 qualification-tooling tests; 10/10 docs-builder tests; 43-document/5-asset build; desktop/mobile Chromium smoke; repository-artifact, generator-drift, config/inference/observability schema, source-test, runtime-environment, release/link, thinking-budget, syntax, JSON, and diff-hygiene gates | passed static checkpoint | One deterministically generated Draft 2020-12 schema now owns health, resolved config, trusted model state, model listing, decode telemetry, recent-request/thinking outcomes, and logit-cache statistics, including shared batching, prefill, memory, ROCm graph/synchronization, cache, training support, base-weight, and execution-provenance structures. All emitted objects are closed, serde-skipped fields remain absent rather than nullable, full examples validate, and stale generated output fails publication. OpenAPI no longer claims health is 200-only or that the debug endpoint's direct 403/500 JSON bodies use `ApiError`. The website publishes every definition, annotation, status, and example and the curated API guide delegates field semantics to it. Phase 8.5 remains open for 80 other components and executable runtime router-to-contract enforcement; no Cargo or accelerator process started below the unchanged 15 GiB host-memory floor. |
| 2026-07-14 | Complete adapter, HF/TRL, receipt, and teacher wire schemas | `sha256:b2119208494b3b87d53357d648354402f12b01216f64e668287b1cde51039fbd` | this commit | portable schema/API product contract and permanent documentation website; accelerator execution unaffected | 22 complete OpenAPI entrypoints backed by 80 reachable definitions and 22 example families across 19 operations; exact property-set audit of 51 serialized Rust structs; 18 focused artifact mutations; exact 49-complete/60-pending component accounting; 421 qualification-tooling tests; 10/10 docs-builder tests; 44-document/5-asset build; desktop/mobile Chromium smoke; repository-artifact, generator-drift, config/inference/observability/artifact schema, source-test, runtime-environment, release/link, thinking-budget, runtime-default, latency-validator, syntax, JSON, and diff-hygiene gates | passed static checkpoint | One deterministic Draft 2020-12 schema now owns adapter discovery, load/unload, merge, upload, deletion, detail and legacy audit receipts; immutable SFT/GRPO exports and full manifests; verified PEFT imports; and teacher registration, capability, status, canonical identity, and deletion payloads. It distinguishes runtime adapter/export/import name policies, raw and prefixed digests, exact bundle filenames, skipped versus nullable options, aliases, source exclusivity, GRPO provenance/group bounds, task-format agreement, conditional teacher fields, response closure, and intentionally ignored unknown input fields. All 19 operation bindings plus ETags, `If-Match`, download headers, status codes, and media are enforced. Teacher registration no longer hides a closed request behind arbitrary JSON and now maps extractor failures into the structured API error; deletion emits a typed closed response. Generated website and curated guides delegate field semantics to the canonical contract. Phase 8.5 remains open for 60 components and executable runtime router-to-contract enforcement. Bounded Cargo refused with 11 GiB available under the unchanged 15 GiB floor, so no compile or accelerator claim is made. |
| 2026-07-14 | Complete eval and judgment wire schemas | `sha256:247a0f0747caef9232705b51c3754046016cd1cc75a605236943226471e3f8ff` | this commit | portable schema/API product contract and permanent documentation website; accelerator execution unaffected | 30 complete OpenAPI entrypoints backed by 74 reachable definitions and 30 example families across 25 operations; exact property-set audit of 45 serialized Rust structs; focused positive/negative contract mutations; exact 79-complete/39-pending component accounting; 421 qualification-tooling tests; 10/10 docs-builder tests; 45-document/5-asset build; desktop/mobile Chromium smoke; repository-artifact, generator-drift, config/inference/observability/artifact/eval schema, source-test, runtime-environment, release/link, thinking-budget, runtime-default, latency-validator, syntax, JSON, and diff-hygiene gates | passed static checkpoint | One deterministic Draft 2020-12 schema now owns suites, messages, generation, every scorer variant, comparison, jobs, results, metrics, datasets, uploads, synthesis, and judgment authoring/validation. It preserves open compatibility inputs while closing emitted objects; distinguishes omitted, nullable, and explicit unlimited thinking budgets; enforces source exclusivity, compare bounds, aliases, exact decimal seeds, and observability provenance; and binds all 25 operations. Nine formerly generic JSON responses are typed. The audit fixed the structurally ambiguous fixed-scorer wire shape and prevents `u64` synthesis-seed precision loss while accepting legacy numeric archives. Generated website and eval guide delegate field semantics to the contract. Phase 8.5 remains open for 39 components and executable runtime router-to-contract enforcement. Bounded Cargo refused with 10 GiB available under the unchanged 15 GiB floor, so no local compile or accelerator claim is made. |
| 2026-07-14 | Complete training and agent control-plane wire schemas | `sha256:5e035f3adfff4a0953dba9a72c6344d0524f26b802bca2c49b7a554a177b2bcc` | this commit | portable schema/API product contract and permanent documentation website; accelerator execution unaffected | 52 complete public entrypoints backed by 115 reachable definitions and 53 example instances across 45 operations; exact property-set audit of 74 serialized Rust structures; focused positive/negative mutations; exact 131-complete/0-pending component accounting; 421 qualification-tooling tests; 10/10 docs-builder tests; 46-document/5-asset build; desktop/mobile Chromium smoke; repository-artifact, generator-drift, config/inference/observability/artifact/eval/control-plane schema, source-test, runtime-environment, release/link, thinking-budget, runtime-default, latency-validator, syntax, JSON, and diff-hygiene gates | passed static checkpoint | One deterministic Draft 2020-12 schema now owns native SFT, GRPO, OPD, optimizer and loss variants, exact rollout provenance, every distillation route, admission and preflight, training jobs and checkpoints, embedded agent runs and traces, recipes, corrections, and adapter-library payloads. Inputs preserve the runtime's deliberate compatibility policy while emitted objects are closed; source exclusivity, aliases, bounds, nullable versus skipped fields, exact decimal values, and current unavailable operations are executable constraints. Thirteen formerly generic JSON bodies are typed. The audit removed duplicate flattened training-detail keys and replaced two impossible success contracts with their actual HTTP 501 and 400 errors. Hosted compilation caught and the follow-up repaired the internal checkpoint lookup by carrying its enum-derived control value separately from the flattened public string. Generated website and training guides delegate field semantics to the contract. Phase 8.5 remains open only for executable live-router enforcement. No local Cargo or accelerator process started below the unchanged 15 GiB host-memory floor. |
| 2026-07-14 | Typed operational runtime and executable router enforcement | `sha256:dee3f4f67be65727d45f3ea14829d3a6a8e0f42d7782abd268a493220e522f75` | `a09c892fd` + `c89ee0c76` | portable product contract and hosted default CPU lane; accelerator execution unaffected | exact 98-field/93-canonical-env/69-alias config reconciliation; executable 101-path/112-operation live-router probe; 137 closed observability definitions; environment ratchet 923/376 -> 909/361; hosted CI `29325026823`; Pages `29325026877`; qualification `29325026824`; repository hygiene `29325026912`; 421 local qualification tests; 10/10 docs-builder tests; 46-document/5-asset build; Chromium rendered smoke; every schema generator/checker, release/link, runtime-default, latency, thinking-budget, source-test, syntax, JSON, and diff-hygiene gate | passed | Six formerly handler-local knobs and three agent scalars now use mechanically named typed startup configuration; compatibility aliases are centralized and strict, all request handlers retain one immutable operational snapshot, and `/v1/config` plus the generated website publish it. Terminal and agent execution default to loopback-only with robust IP loopback recognition. The pi executable, pi session root, logit cache, and adapter-library URL resolve once; environment-mutating unit tests are replaced with injected paths or central loader coverage. The batch prepared-prompt and Vulkan FLCE chunk overrides were undocumented dead/unsafe controls and are deleted; FLCE always uses its bounded shape/device-aware policy. Initial hosted run `29324851254` exposed one remaining terminal-global bind-host accessor in credential-free teacher admission; `c89ee0c76` moved that exposure check to the immutable state snapshot. The superseding hosted lane compiled the tree and passed the full default suite including exact production-router/OpenAPI enforcement. Local Cargo remained safely blocked below the 15 GiB floor. This closes Phase 8.5. |
| 2026-07-14 | Correct multi-sample eval aggregation | `git-tree:4367ad530b97a7756bcdc92c33725e3617e2f516` | `43c52b60b` | portable eval/runtime contract and hosted default CPU lane; accelerator execution unaffected | hosted CI `29327354869`; Pages `29327354874`; UI smoke `29327354928`; qualification contract `29327354864`; repository hygiene `29327355026`; release drift `29327354867`; local 76-definition eval and 112-operation HTTP contract validation; 10/10 docs-builder tests; 46-document/5-asset website build; desktop/mobile Chromium docs and server-UI smoke | passed | Suite schema v2 declares closed `single`, `mean@k`, `pass@k`, and odd `majority@k` reducers with exact `n == k` validation before generation. Results retain every seeded raw completion plus one canonical independent record per example; metrics, confidence intervals, weights, slices, paired diffs, failure reruns, browser verdicts, and promotion now consume only reduced examples, while cost and diagnostics remain raw-completion based. Known-answer and exhaustive truth-table tests cover k through 9, malformed groups fail closed, executor tests cover every reducer and override mismatch, and promotion is pinned against completion-zero disagreement. Live results and archives require schema v2 and explicitly reject ambiguous legacy data. CLI, synthesis API/UI, generated contract, and the comprehensive published eval guide expose the same semantics. The hosted default lane compiled the tree and passed the full test suite; local Cargo remained safely blocked at 10 GiB available under the unchanged 15 GiB floor. This closes Phase 4.1. |
| 2026-07-14 | Enforced train/eval separation | `sha256:1e1c3590b361da12ef7de9905e04293b906573f4a5eeaff8ab5e81b04c6b421e` | `f1fbb7fac` through `5e3fcdf51` | portable data/API/UI/documentation contract and hosted default CPU lane; accelerator execution unaffected | hosted CI `29332702186`; Pages `29332377706`; UI smoke `29331460222`; qualification tooling 421/421; focused ROCm harness/manifest/receipt tests 95/95; current 82-definition/32-entrypoint eval schema and 117-definition/52-entrypoint control-plane schema; 102-path/114-operation/133-payload HTTP self-test; 10/10 docs-builder tests; 47-document/5-asset build; Chromium docs and full server-UI matrices; release/link, thinking-budget, formatting, and diff-hygiene gates | passed | Registered rows retain exact and normalized identities plus declared group/session links. Deterministic connected-component manifests materialize train/validation/holdout partitions, exhaustively guarantee every feasible small-corpus partition, and drive named SFT/GRPO, synthesis, public provenance, UI, and generated docs. Held-out post-eval rejects exact, normalized, source-row, group, or session overlap; explicit `train-set-eval` is diagnostic-only and cannot set an accuracy gate. Hosted compilation and the full default suite cleared all migration regressions; derived router counts now come from canonical OpenAPI metadata. Individual files are atomically replaced and hash-bound, but a whole dataset mutation is not yet one crash-transactional directory generation; the final partial-commit gate remains open and this row makes no such claim. This closes Phase 4.2. |
| 2026-07-13 | Fail-closed cross-engine serving benchmark v3 | `sha256:4095b84d5d1fe70d1442633a30f10ddf412ee257e55a5aee59a818fe02fe550f` | `32cbb54f6` | portable benchmark, receipt, campaign, and permanent website contract; accelerator execution pending | 428 qualification-tooling tests; 20 focused benchmark/campaign tests; all four historical v2 serving receipts; 10/10 docs-builder tests; 47-document/5-asset build; desktop/mobile Chromium site smoke; Python compilation and diff hygiene | passed static checkpoint | Every new run fingerprints and rechecks exact weight shards, config, tokenizer, template, runtime artifact, repository, and memory counter. Kiln's runtime artifact must match the executable hash reported by the live process; vLLM requires a canonical immutable-launcher manifest whose embedded model, implementation, runtime-content hash, and `kiln-teacher-v1` identity verify. Five fixed profiles own prompt shape, sampling, comparison mode, and prompt-token uniformity; ordered observed prompt-token counts close tokenizer drift, while exact output parity is required only where cross-engine determinism is meaningful. Absolute memory limits are predeclared verdict gates. The campaign runner executes all five profiles at concurrency 1/8/16/32/64/128, preserves failed counterexamples, pairs matching Kiln/vLLM receipts, and writes a self-hashing summary. Driver v2 remains validation-only for checked-in history. This closes the Phase 5.1 measurement-contract items, not the still-open ROCm/vLLM hardware matrix, optimization, or performance-positioning gates. |
| 2026-07-14 | Request-aware latency diagnostics and bounded telemetry | `sha256:a47ac0e5d838ad9b80d103ceb4818b8d3b0cb80adecc534f2d12ef66c26dd855` | `b15d8c2a6` + `6b9fdc26f` + `a9cfb6520` + `9ea546bed` | portable runtime/API/product contract; accelerator execution unaffected | 1,052 hosted server library tests plus substrate, anomaly, CUDA-dependency, qualification, formatting, license, and repository gates in CI `29339093096`; 428 local qualification tests; exact 102-path/114-operation HTTP contract; 10/10 docs-builder tests and 48-document/5-asset build; desktop/mobile rendered docs smoke; empty/desktop/mobile/cold-start/failure-recovery server UI smoke; schema, syntax, JSON, and diff hygiene | passed static checkpoint | Batching requests now carry actor-ready/delivery timestamps and bounded queue/admission/prefill/decode/delivery candidates through unary, streaming, terminal performance metadata, recent requests, the diagnosis UI, rolling request-local statistics, fixed-cardinality Prometheus metrics, generated schemas, strict qualification parsing, and a comprehensive website reference. Conservative overlap accounting cannot erase unexplained wall time; unsupported backend subphases are nullable rather than invented. A 20,000-token regression bounds the complete measurement path at 100 microseconds/token in debug builds and retained request samples at 8,192/256 KiB. Hosted CI caught a non-`Send` streaming tracker, three stale timing fixtures, two invalid one-sample adaptive-threshold tests, and a real shutdown-path missing delivery timestamp; each was repaired and the replacement run is green with every accelerator job skipped. Direct streaming and request-correlated sampling/readback/GPU-lock/graph/sync/mutation phases remain open. Local bounded Cargo refused at 10 GiB available under the unchanged 15 GiB floor. |
| 2026-07-14 | Direct-stream latency observability and product parity | `sha256:bb609491b79e52929e825eed95063e171a9e9190abe2c87bf02e6a1b7050f19c` | `598229695` | portable runtime/API/product contract; accelerator execution unaffected | hosted CI `29340648131` with 1,053 server library tests plus substrate, anomaly, CUDA-dependency, qualification, formatting, and license gates; Pages `29340646543`; qualification `29340648026`; repository hygiene and release drift; 428 local qualification tests; exact 102-path/114-operation HTTP contract; 10/10 docs-builder tests and 48-document/5-asset build; desktop/mobile rendered docs smoke; full server-UI smoke | passed static checkpoint | Direct-model tokens now preserve producer-ready time through the blocking bridge and record producer delivery, handler receipt, response-body enqueue, request-local ITL, bounded stall attribution, rolling metrics, fixed-cardinality Prometheus observations, terminal performance metadata, recent-request diagnostics, and explicit per-token SSE timing. The public object uses a closed `batching_engine`/`direct` source and generic producer-delivery boundary; direct actor-only phases remain `null` and otherwise unpartitioned intervals remain visibly `unexplained`. Strict schema, qualification parsing, README, operator reference, generated documentation, and the permanent API website agree. This closes the emitted-token and recent-diagnostics gates in Phase 1.6; backend phase correlation remains open. Local bounded Cargo refused at 9 GiB available under the unchanged 15 GiB floor. |
| 2026-07-14 | Request-owned batching decode backend phases | `sha256:5880ab2f3aa229d234fabf7f08dddcd2ff77af51b4fc5ebcf7b20a18354f8bf5` | `38a01f8d9` | portable runtime/API/product contract; exact batching-engine decode boundary; accelerator execution unaffected | hosted CI `29343107991` with 1,055 server library tests plus workspace, substrate, anomaly, CUDA-dependency, 428 qualification, formatting, and license gates; Pages `29343107883`; UI smoke `29343107910`; qualification `29343107873`; repository hygiene `29343107905`; release drift `29343107998`; local exact 102-path/114-operation/133-payload API contract; 140-definition observability schema; 10/10 docs-builder tests and 48-document/5-asset build; desktop/mobile Chromium docs and full server-UI matrices | passed static checkpoint | Decode forwarding now returns one typed token-and-phase envelope. Exact shared-GPU coordination acquisition wait and external-yield synchronization duration follow that owned invocation to every active request blocked by the actor step, then feed request diagnostics, per-token blocking reasons, rolling statistics, fixed-cardinality Prometheus counters, schemas, strict qualification parsing, and the operator UI. A narrower backend candidate competes with broad actor decode only after subtracting the single largest observed subphase, so overlapping candidates are never summed into fabricated coverage. The closed public reason vocabulary now includes every declared backend phase, while unsupported phases remain `null` and zero-count. The implementation explicitly rejects process-global graph before/after deltas as request evidence because concurrent readers could contaminate them. Sampling/readback/graph/mutation correlation and direct-path backend parity remain open. Local bounded Cargo refused at 9 GiB available under the unchanged 15 GiB floor. |
| 2026-07-14 | PID-isolated qualification build containment | `sha256:7418f0e620291bd3f2842f73d361fa7f2095147ce1fbe17253d417627e3af5c4` | `8a7de073b` | portable harness + Strix Halo process containment; ROCm matrix rerun pending | retained and strictly validated failed receipt `20260714t202026239521z-rocm-strix-halo-serving-rocm-graph-resil-b08d6a84da-v1.json`; 431 qualification-tooling tests; real transient-service success, nonzero-status, environment-transfer, private-network, and no-residue probes; Pages `29366572716`; qualification contract `29366572649`; repository hygiene `29366572489`; repository artifact, release-version, source-parsing, Bash/Python, and diff guards; 10/10 docs-builder tests; 48-document/5-asset build and rendered Chromium smoke | passed harness checkpoint | The first clean graph-resilience attempt failed before Cargo could compile because bubblewrap's PID namespace cannot attach its namespaced process to the host user manager as a scope. Source-bound builds now use a unique transient user service with `MemoryMax`, zero swap, private networking, control-group kill, explicit collection, and a hard runtime cap; the measured device process remains inside the original PID/network-isolated case. Closed workload configs record both the service and caller deadlines, with a 60-second teardown window and backend-specific ROCm, Vulkan, and hardware-test limits. The wrapper still defaults to a scope for ordinary developer use. The preserved counterexample is not a GPU failure or a completed Phase 1.7 result; the clean pushed-source headroom-versus-tight-budget rerun is next. |
| 2026-07-14 | ROCm feature-build recovery and closed build environment | `sha256:9359e2abfb45b1cae59a4194ac78910fa8a933c91d71e365d1fc993e1ed001ce` | `af42edc91` + `1d1e833c5` | Strix Halo ROCm/gfx1151 compile and link under private-network containment; graph runtime rerun pending | retained and strictly validated failed receipt `20260714t204214868822z-rocm-strix-halo-serving-rocm-graph-resil-b08d6a84da-v1.json`; exact bounded ROCm server check and release link; local and hosted default workspace build; 431 qualification-tooling tests; real closed-environment secret-exclusion and no-residue probe; superseding hosted CI `29368202298`; qualification contract `29368202332`; repository hygiene `29368202280`; Rust formatting; Python compilation; repository artifact and release guards; 10/10 docs-builder tests; 48-document/5-asset build and rendered Chromium smoke | passed compile checkpoint | The repaired service bridge reached the real source build and exposed three existing ROCm-only Rust errors before GPU startup: `active_rocm_stream` received an owned `Arc`, while graph teardown and health used an unimported `Device`. All three are fixed and the exact `kiln-server --no-default-features --features rocm` binary now checks and links. The versioned closed-source policy derives a minimal Cargo/Rustup, locale, home, user-systemd, and pinned ROCm environment from typed config; ambient flags, target paths, credentials, and API tokens cannot enter the wrapper service. Seventeen compiler-proven unused imports/locals and one uncalled private HIP helper were removed without suppressing feature-conditional warnings. A contained link attempt reached the old 300-second limit at only 2 GiB peak memory, proving it was a false-failure deadline; ordinary ROCm/Vulkan builds now use 840/900 seconds and every dependent total/case deadline carries the cold-build allowance. Initial hosted CI `29368002301` caught an orphaned feature attribute from the warning cleanup; `1d1e833c5` removed it, the exact hosted build passed locally, and the complete replacement lane is green. This is compile/link evidence, not graph execution evidence. |
| 2026-07-14 | ROCm runtime preflight attestation repair | `sha256:250ae478173798b3ff39856d78ea7a1a1df548826110b07b936e787cb173af90` | this commit | Strix Halo ROCm/gfx1151 real startup and graph prewarm; concurrency matrix pending | retained and strictly validated failed receipt `20260714t211116950555z-rocm-strix-halo-serving-rocm-graph-resil-b08d6a84da-v1.json`; 431 qualification tests; 59 focused harness tests; bounded focused Rust route test; formatting; 10/10 docs-builder tests and 48-document/5-asset build; post-restart no-residue and memory check | passed preflight repair | Clean source `c00c89ff` built in 250.1 seconds, loaded the exact model, allocated an 8 GiB KV cache, captured and first-launched a 32-layer HIP graph, completed prewarm, and drained normally. The harness then failed closed because `/v1/debug/model-state` did not include `KILN_MEMORY_RECLAIM_MODE`, making absence impossible to attest despite TOML remaining authoritative. The selected flag map and its real route test now preserve explicit absent state. This is startup/capture and harness evidence, not a completed mixed-concurrency result. |
| 2026-07-14 | ROCm concurrency-64 capacity counterexample | `sha256:250ae478173798b3ff39856d78ea7a1a1df548826110b07b936e787cb173af90` | `6a7587d3c` + this receipt commit | Strix Halo ROCm/gfx1151 headroom graph arm | retained and strictly validated failed receipt `20260714t212406423469z-rocm-strix-halo-serving-rocm-graph-resil-b08d6a84da-v1.json`; exact clean pushed source; automatic CI, Pages, qualification, and repository runs `29369357346` through `29369357420`; post-run no-residue and memory check | failed as designed | The clean run passed source build, model load, typed-config attestation, graph warmup, and concurrency 1/8/16/32, then completed 41/64 headroom requests before the 120-second client deadline while 23 still-unserved requests disconnected with zero generated tokens. Successful completions continued through 112 seconds; 31 bounded slow-decode events maxed at 142.7 ms, nine graph captures succeeded, and no reclaim, resize, device-fault, graph-failure, or unexplained sync event appeared. Host memory stayed flat and teardown restored 25 GiB available with no process or unit residue. This is a queue/service-capacity counterexample under the exact 8-decode/4-staging/12-active policy, not evidence of a device freeze. Failure metrics were zero-filled despite completed waves, exposing a harness evidence-loss defect that must be fixed before retry. |
| 2026-07-14 | Lossless graph-resilience failure evidence | `sha256:f2b567af429e3d2e16279378ae6b00bcda5ef2917cc2cf0abc9a26fe042776d0` | this commit | portable qualification harness and permanent website; ROCm rerun pending | 434 qualification tests including actual failed-result serialization; 10/10 docs-builder tests; 48-document/5-asset build; strict JSON/Python compilation; repository artifact policy and diff hygiene | passed static checkpoint | `RunEvidence` now accumulates exact successful outputs and request timing as each wave completes, plus attempted/failed coverage, latest graph state, peak memory, and attributed/unexplained pauses. Failed case results distinguish unstarted, started-incomplete, and completed arms, preserve the highest full headroom concurrency, and never invent a generic request failure for a build/startup error. Output hashes remain first-class so corrupted evidence cannot be normalized away. A 300-second client and 360-second server containment envelope prevents the correctness matrix from conflating the measured 12-active-request service time with the separate cross-engine performance verdict; all actual TTFT/E2E metrics remain unchanged. No new hardware claim is made. |
| 2026-07-14 | Accepted ROCm graph-budget concurrency matrix | `sha256:f2b567af429e3d2e16279378ae6b00bcda5ef2917cc2cf0abc9a26fe042776d0` | `da9fb3735` + this receipt commit | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260714t214851448470z-rocm-strix-halo-serving-rocm-graph-resil-b08d6a84da-v1.json`; strict receipt and local-artifact validation; no-residue and memory check | passed | Both sequential source-built arms completed all 121 mixed-bucket requests through external concurrency 64, for 242/242 exact fixed-output responses, zero case/request failures, zero output mismatches, zero attributed or unexplained ITL outliers, and zero graph failures. Headroom captured 9/replayed 757 graphs with 98,907,888 bytes peak retained; the 64 MiB arm captured 5/replayed 375 with 65,787,688 bytes peak, 60 pre-capture skips, and 414 closed byte-budget fallbacks. Peak GPU-used readings were 37.652/37.650 GB. P99 TTFT/E2E at concurrency 64 were 213.693/214.131 seconds while p99 ITL was 200.1 ms. This accepts queued correctness, byte-budget behavior, pause policy, and lifecycle under the exact 8-decode/4-staging/12-active profile; it explicitly does not claim native batch width 64 or vLLM-competitive high-concurrency performance. Both snapshots drained, both servers stopped normally, no process/unit residue remained, and host availability returned to 25 GiB. |
| 2026-07-14 | Public adapter probe request-selection counterexample | `sha256:f2b567af429e3d2e16279378ae6b00bcda5ef2917cc2cf0abc9a26fe042776d0` | `f9e05d8f5` + this receipt commit | Strix Halo ROCm/gfx1151 adapter arm | retained and strictly validated failed receipt `20260714t220443062078z-rocm-strix-halo-serving-rocm-public-muta-1437dc4bd3-v1.json`; 435 qualification tests; Python compilation and diff hygiene; post-run no-residue and memory check | failed as designed; driver repaired | The source-bound server loaded and attested the real 30,527,616-byte adapter, published its exact content revision, and performed the public barrier swap. The inference probe then sent the shared mixed-load driver's explicit `"adapter": null`, correctly causing an adapter-to-base per-request transition and authoritative base response headers; the lifecycle driver had intended to select the named adapter. The request helper now accepts an explicit adapter selection and the probe pins `qualification-adapter`, with expected and observed revision identity retained in any future failure. The server stopped normally, removed its private model snapshot, left no process or unit residue, and restored 25 GiB host availability. The old failure serializer nevertheless zero-filled the completed load and reported a dirty shutdown, so lossless partial lifecycle evidence remains required before retry. |
| 2026-07-14 | Lossless public-mutation lifecycle evidence | `sha256:3da08cf04b9dd59a653afaf8f902d52f68fa497886c6ee8c259bbf8afbabf57c` | this commit | portable qualification harness and permanent website; ROCm rerun pending | 437 qualification tests including actual failed-result serialization; Python compilation; 10/10 docs-builder tests; 48-document/5-asset build and generated static smoke; diff hygiene | passed static checkpoint | `RunEvidence` now records each completed build, adapter input hash, public load/unload, semantic output, graph invalidation, forced resize, maintenance rejection, transition, device fault, teardown, and residue boundary. Failed command results retain partial metrics and hashes plus explicit started/completed arm state. Request failure is no longer invented for a build, state, or identity error, while dirty shutdown and residue come only from observed teardown. The shared stream driver preserves explicit base selection by default and accepts a named adapter for the public lifecycle probe. The permanent qualification reference now states both selections and the failure-evidence contract. No new hardware claim is made. |
| 2026-07-14 | Post-prewarm readiness refresh | `sha256:a79e5257490fe0bdc898b55bde18afe7519eb9ec64d677716fe0214423592728` | `0baf0f837` + this receipt/fix commit | Strix Halo ROCm/gfx1151 startup attestation plus portable harness | retained and strictly validated failed receipt `20260714t221700288799z-rocm-strix-halo-serving-rocm-public-muta-1437dc4bd3-v1.json`; 438 qualification tests including forced health advancement during the prewarm-log wait; Python compilation and diff hygiene; post-run no-residue and memory check | failed as designed; harness repaired | The server built, loaded the model, captured a real graph, completed background prewarm, and stopped cleanly, but startup attestation compared trusted debug counters against the older health object fetched before the readiness helper waited for the prewarm-completion log. Live capture counters had legitimately advanced during that wait. Readiness now re-fetches and revalidates health after log completion, so subsequent quiescent health/debug equality remains strict without comparing a stale prewarm snapshot. The retained result correctly reports one build, 30,527,616 adapter bytes, a started/incomplete adapter arm, and zero request, device, dirty-shutdown, or residue failures. No lifecycle request or maintenance mutation ran. |
| 2026-07-14 | Maintenance health status-aware qualification | `sha256:31e3b0fc4115d7bf91fe7abb41e0240bace84aa91993803a0250f9f5f0f1971a` | this commit | portable harness/docs plus controlled Strix Halo ROCm observation; accepted receipt pending | 439 qualification tests; focused exact-503 readiness test; 10/10 docs-builder tests; 48-document/5-asset build and generated static smoke; Python compilation and diff hygiene | passed static checkpoint | A clean `1996a6b6f` run completed the full public adapter load/named-inference/unload/base-restoration arm and observed a structured forced KV shrink from 4,096 blocks to one in 40.229 ms. The maintenance readiness loop then repeatedly rejected the intentionally non-serving `/health` HTTP 503 before result publication, so the outer run was manually interrupted and produced no compact receipt; all contained processes exited and host availability returned to 25 GiB. One status-aware reader now requires HTTP 503 plus `status=maintenance`, failed inference admission, and every other readiness check passing, and is used for both readiness and the post-rejection counter snapshot. The website states that this is offline-mutation readiness, not startup failure. The hardware claim remains pending a complete retained rerun. |
| 2026-07-14 | PID-namespace-safe qualification workspaces | `sha256:d8a235120728c23c0b300060b6c43f12759a36697bdd6f07a8717c0e3b4c572a` | `3f93e5aac` + this receipt/fix commit | all ROCm/Vulkan serving drivers plus Strix Halo adapter arm | retained and strictly validated failed receipt `20260714t222528468208z-rocm-strix-halo-serving-rocm-public-muta-1437dc4bd3-v1.json`; 440 qualification tests; Python compilation; 10/10 docs-builder tests; 48-document/5-asset build and generated static smoke; diff hygiene; exact stale-workspace inspection/removal; post-run no-process and 25 GiB availability check | failed as designed; harness repaired | The clean run completed the entire adapter arm: one 39.881 ms load, named revision-bound inference, one 13.630 ms unload, two ordered transitions, three graph invalidation evictions, exact matching pre/post base-output hashes, zero request/device/output/teardown failures, and clean snapshot removal. Maintenance then failed before startup because the prior externally interrupted process left an 8.7 GiB workspace named with sandbox PID 2, and every subsequent PID namespace reused 2. A shared mode-0700 `mkdtemp` helper now gives mixed load, graph correctness/resilience, pressure, lifecycle, soak, sync A/B, and Vulkan collision-resistant workspaces independent of sandbox PID. The inspected stale snapshot was removed with no referencing process. The maintenance hardware claim remains pending. |
| 2026-07-14 | Accepted ROCm public adapter and maintenance lifecycle | `sha256:d8a235120728c23c0b300060b6c43f12759a36697bdd6f07a8717c0e3b4c572a` | `a001c39fe` + this receipt commit | Strix Halo ROCm/gfx1151 | `qualification/receipts/rocm/strix-halo/20260714t223020349538z-rocm-strix-halo-serving-rocm-public-muta-1437dc4bd3-v1.json`; strict receipt/current-source/local-artifact validation; post-run no-process, workspace, or snapshot residue and 25 GiB availability check | passed | One source-bound binary completed both isolated arms in 72.663 seconds. The 30,527,616-byte adapter loaded in 38.390 ms, its named inference response bound exact content revision `875ba4e8f1ff5710187cbe44d797bd683ad2becfc1e9135f2f6786fa142d641f`, unload completed in 12.007 ms, and pre/post base semantic hashes matched exactly; two ordered public transitions caused three captured-graph invalidation evictions. Maintenance shrank physical KV from 4,096 blocks to one, released 8,587,837,440 bytes, spent 0.0246 ms waiting and 40.242 ms total, exposed the exact target through config/health/debug, and rejected inference with HTTP 503 `inference_disabled_by_profile` without changing batching work. Both arms completed with zero header/output/request/device/shutdown/residue failures and collision-resistant workspaces removed normally. This closes the Phase 1.7 lifecycle-corpus checkbox together with existing prefix, cancellation, slow-consumer, and pressure receipts; it is not a performance claim. |
| 2026-07-14 | Vulkan native-prewarm readiness counterexample | `sha256:d8a235120728c23c0b300060b6c43f12759a36697bdd6f07a8717c0e3b4c572a` | `63cf2e16d` + this receipt/fix commit | Strix Halo Vulkan/RADV | retained and strictly validated failed receipt `20260714t223353042035z-vulkan-strix-halo-serving-vulkan-baseline--8da450e169-v1.json`; 440 qualification tests; 10/10 docs-builder tests; 48-document/5-asset build and generated static smoke; Python compilation and diff hygiene; clean teardown and 25 GiB availability check | failed as designed; readiness repaired | The Vulkan-only release binary built under containment in 297.488 seconds, selected the RADV Strix Halo device, uploaded and dropped CPU weights, initialized exact execution provenance, precompiled pipelines, and populated 2,880 MiB f32 plus 8,020 MiB packed-bf16 decode caches in 8.692 seconds. Health marked inference prewarm complete, but the shared log gate recognized only the generic synthetic-prewarm message and ignored the native `Vulkan decode weight prewarm complete` event; readiness timed out without issuing a measured request. The closed event classifier and permanent qualification reference now recognize that exact causal event. The zero-filled failed metrics also discarded the completed build/startup evidence, so lossless Vulkan partial evidence is required before rerun. No Vulkan serving-performance claim is made. |
| 2026-07-14 | Lossless Vulkan baseline failure evidence | `sha256:c8200815d40e7b1520c59cb80ae46042fac99082ee47fffc7eab3f95b0d8a3b0` | this commit | portable qualification harness and permanent website; Vulkan acceptance rerun pending | 444 qualification tests including failed-result serialization, completed-wave metrics and semantic identity, startup provenance retention, full-lifetime device-event accounting, and post-shutdown gating; Python compilation and diff hygiene | passed static checkpoint | The Vulkan driver now accumulates evidence from build through teardown instead of emitting an all-zero record when a later boundary fails. Compact milestones distinguish unobserved work from measured zero counts; build/config and startup execution identities survive later failure; every completed wave retains actual request, output, latency, termination, and ordered semantic hash evidence; measured-load memory samples remain intact; and typed device/graph/resize/reclaim observations span server start through process shutdown. The acceptance gate consumes those final observations, so a startup or teardown device fault cannot be normalized into a pass. No Vulkan serving-performance or hardware-acceptance claim is made; the next clean pushed-source run must still complete all 25 requests and every exact gate. |
| 2026-07-14 | Vulkan resumable-prefill state-copy counterexample | `sha256:c8200815d40e7b1520c59cb80ae46042fac99082ee47fffc7eab3f95b0d8a3b0` | `ef652f371` + this receipt commit | Strix Halo Vulkan/RADV | retained failed receipt `20260714t225334798301z-vulkan-strix-halo-serving-vulkan-baseline--8da450e169-v1.json`; strict receipt/current-source/local-artifact/known-commit validation; exact ignored-log causal inspection; post-run no-process/residue and 25 GiB availability check | failed as designed; product defect isolated | The clean pushed-source binary rebuilt from cache in 0.392 seconds, selected and attested exact Vulkan execution, completed the 8.694-second 2,880 MiB f32 plus 8,020 MiB packed-bf16 prewarm, and entered the first warmup request. Sixteen bounded prefill phases then reached the resumable split, where recurrent-state snapshotting failed because `Tensor::copy` has no Vulkan deep-copy implementation. The stream emitted no finish reason, no measured wave began, and the new partial serializer retained build/config/startup/teardown identities without inventing a request failure. The server stopped normally in 236 ms, removed its snapshot, and recorded zero device, graph, resize, reclaim, forced-shutdown, nonzero-shutdown, or residue failures. The compact error currently reports only the downstream zero-finish-reason validation while the exact backend error remains in ignored raw evidence; structured generation-error propagation must be fixed with the Vulkan copy implementation before rerun. This is not a Vulkan baseline acceptance or performance claim. |
| 2026-07-14 | Vulkan deep-copy and structured stream-error repair | `sha256:814b86c49ebd79eb0f3115986b6b48f1555218d15071605237bb43a06c31498a` | this commit | Strix Halo Vulkan/RADV tensor runtime plus portable qualification harness; baseline rerun pending | bounded real-device Vulkan copy/mutation/view regression; bounded Vulkan all-target server check; 58 focused mixed-load and 445 total qualification tests; Python compilation, Rust formatting, and diff hygiene | passed repair checkpoint | `Tensor::copy` now gives Vulkan the same fresh-storage snapshot contract used by GDN rollback and resumable prefill. Dense logical ranges use a bounds-checked, queue-ordered device copy into the recycling pool; strided F32/BF16 views use the existing resident gather; unaligned odd-BF16 and byte tensors take value-preserving paths that respect Vulkan's transfer alignment; and every result has a fresh buffer and tensor ID. The device regression proves exact dense and transposed values, pointer independence, and that an in-place source mutation cannot alter its snapshot. The shared SSE reader now validates the closed server error envelope and preserves the exact `generation_error` message as the primary compact failure instead of overwriting it with a zero-finish-reason symptom. No full-server Vulkan acceptance or performance claim is made; the clean pushed-source baseline rerun remains required. |
| 2026-07-14 | Vulkan long-prefill capacity counterexample | `sha256:814b86c49ebd79eb0f3115986b6b48f1555218d15071605237bb43a06c31498a` | `579122050` + this receipt commit | Strix Halo Vulkan/RADV short, mixed, and long synchronized serving waves | retained and strictly validated failed receipt `20260714t230323300327z-vulkan-strix-halo-serving-vulkan-baseline--8da450e169-v1.json`; exact ignored stdout/stderr inspection; all hosted CPU checks for `579122050` green; post-run no-process/unit/workspace/snapshot residue and 25 GiB availability check | failed as designed; capacity and harness defects isolated | The clean source-bound release build completed in 245.873 seconds, native prewarm completed, and the repaired resumable prefill path served the warmup plus all single and four-way requests. Eighteen of 25 measured requests terminated at the exact 32-token length for 576 tokens total; the single wave took 6.467 seconds and the four-way mixed wave took 96.781 seconds. At eight and twelve synchronized requests, seven long-prefill requests reached the fixed 180-second deadline with no first token while the batching actor continued bounded 64-token/four-layer quanta. Observed service evidence was 8 active requests but only a two-row maximum decode batch, 1.243 output tokens/second overall, 319 attributed sub-two-second ITL stalls, four attributed outliers, zero unexplained or two-second pauses, stable 5,369-block KV capacity, and zero device, graph, resize, reclaim, external-sync, policy, sampler, shutdown, or residue failures. This is a real Vulkan service-capacity failure under the declared profile, not a device freeze or allocator mutation. The driver also falsely rejects the public paired `base` response-header sentinel as a loaded adapter on every response, and its bounded compact-details field truncates the otherwise valid structured JSON. Both evidence defects must be repaired before a longer-deadline rerun; the low observed batch width must remain visible rather than being normalized into acceptance. |
| 2026-07-14 | Accepted Vulkan short/mixed/long serving baseline | `sha256:c5e7d485a9afb8319435e87c4eb91808652a2cc3d0a88fbac602b794a10cad66` | `e2efd5dff` + this receipt commit | Strix Halo Vulkan/RADV | `qualification/receipts/vulkan/strix-halo/20260714t232612510178z-vulkan-strix-halo-serving-vulkan-baseline--8da450e169-v1.json`; strict receipt/current-source/local-artifact/known-commit validation; 449 qualification tests; hosted Pages, qualification, and repository checks green; post-run no-process/unit/workspace/snapshot residue and 25 GiB availability check | passed bounded correctness; poor scaling retained | One source-bound server completed all 25 synchronized 1/4/8/12 requests and all 800 expected completion tokens with exact length termination, zero request or batching errors, zero device/graph/resize/reclaim/policy/sampler/sync failures, stable 5,365-block KV capacity, zero unexplained or two-second ITL pauses, zero forced/nonzero shutdowns, and no snapshot residue. The four adaptive outliers were attributed to concurrent prefill; 395 gaps above 250 ms remained below the hard pause gate. Wave durations were 6.473, 93.384, 324.697, and 464.650 seconds with 0.900 tokens/second overall; eight active requests produced only a two-row maximum decode batch and two batched forwards out of 773. Peak sampled unified-device usage was 54,288,658,432 bytes. This closes only the Phase 6 bounded Vulkan short/mixed/long serving item and explicitly preserves weak high-concurrency performance for the still-open vLLM campaign, oracle corpus, and soak gates. |
| 2026-07-14 | Bounded qualification Cargo execution | `sha256:c5e7d485a9afb8319435e87c4eb91808652a2cc3d0a88fbac602b794a10cad66` | this commit | portable harness and Strix Halo host-safety contract; Vulkan corpus execution pending | 451 qualification tests; all 14 workload manifests validated; raw-Cargo search empty; 10/10 docs-builder tests; 48-document/5-asset validation; release/link and repository-artifact guards; Bash syntax and diff hygiene; post-restart no-process/unit residue and 25 GiB availability check | passed safety checkpoint | All checked-in Rust qualification cases now enter the existing bounded transient service through one executable launcher that pins offline Cargo, one job, the unchanged 15 GiB admission floor, zero service swap, private networking, and a 1,740-second cap. A versioned closed environment transfers only the source-build allowlist plus the non-secret `KILN_QUALIFICATION` device gate; credentials, ambient compiler flags, target paths, and unrelated `KILN_*` values remain excluded. Workload validation rejects `cargo` by basename, including absolute paths, and its test enumerates every checked-in manifest so a future workload cannot silently bypass containment or validation. This is a harness-safety result only; the current-source Vulkan correctness and oracle cases remain to be run and retained. |
| 2026-07-14 | Accepted Vulkan tensor and matmul CPU parity | `sha256:9f25964ce481ac7eb09c4e23d86c17ee631b6abd2a00bcd53efc61305d7b4e2f` | `ea4c0775f` + this receipt commit | Strix Halo Vulkan/RADV | `qualification/receipts/vulkan/strix-halo/20260714t235017349724z-vulkan-strix-halo-core-correctness-v1-0a09f3bcee-v1.json`; strict current-source/local-artifact/known-commit validation before documentation mutation; 25 real-device tests; zero failed/ignored; zero output-assertion failures; hosted Pages, qualification-contract, and repository-hygiene runs `29377283760`, `29377283779`, and `29377283732` green; post-run no-process/unit residue and 25 GiB availability check | passed | The required probe selected AMD Radeon 8060S Graphics on RADV Mesa 26.1.3. The bounded private-network service then passed 16 tensor, cast, transfer, reduction, reshape, and autograd parity cases plus nine dense, transposed, batched-BF16, canonical-weight, and LoRA-composition matmul parity cases against CPU expectations. Both Cargo cases entered the closed qualification environment at the unchanged 15 GiB floor, and no hardware skip was accepted. This lower-level receipt loads no model and does not close tokenizer, model-logit, sampling, paged-cache, cancellation, eval, public-serving, or performance gates. |
| 2026-07-14 | Closed qualification case environment | `sha256:9f25964ce481ac7eb09c4e23d86c17ee631b6abd2a00bcd53efc61305d7b4e2f` | this commit | portable runner and permanent website; Vulkan prefill acceptance rerun pending | 452 qualification tests; focused ambient-control/credential exclusion and stable A/B comparison; Python compilation; 10/10 docs-builder tests; 48-document/5-asset validation; release/link and repository-artifact guards; diff hygiene; diagnostic Strix Halo run `20260714t235218976663z` inspected then deliberately not retained | passed harness checkpoint | A clean diagnostic Vulkan prefill run passed the literal 1K/16K actor-fairness arm and exact six-quantum hybrid-model parity with first and following token 13, but live service arguments showed that the runner had inherited ambient `ROCM_PATH` into both Vulkan Cargo cases. The result was not accepted. Every case now starts from versioned `closed-qualification-case-v1`, containing only path, toolchain-home, locale, temporary-directory, user, and user-session plumbing, then receives its exact committed environment overrides plus the runner-owned result and variant identifiers. Undeclared backend controls, device selectors, compiler/linker flags, library paths, product `KILN_*` values, and credentials cannot enter execution or perturb the A/B environment identity. The effective-run artifact records the policy, redacted base, exact overrides, and hashes without capturing the ambient shell. No new Vulkan hardware claim is made until the pushed-source rerun. |
| 2026-07-14 | Headless Vulkan probe counterexample | `sha256:b88f14961fe708c9044c9b8adc4a3177b840d8ee1b3c97bb5a5de9a578aa42ef` | `82c930be4` + this receipt/fix commit | Strix Halo Vulkan/RADV under the closed case environment | retained failed receipt `qualification/receipts/vulkan/strix-halo/20260714t235840251632z-vulkan-strix-halo-prefill-scheduling-v1-6899c96516-v1.json`; strict current-source/local-artifact/known-commit validation before source mutation; executable regression across both Vulkan device-probe manifests; no-process/unit residue and 24 GiB availability check | failed as designed; probe repaired | The closed environment removed ambient `DISPLAY`. `vulkaninfo` still exited zero, enumerated `GPU0` as AMD Radeon 8060S Graphics, and both bounded product arms passed: literal 1K/16K actor fairness plus exact six-quantum hybrid prefill with first/following token 13 and split position 80. Its normal headless stderr said that surface information was being skipped, and the probe's blanket skip regex falsely failed the receipt. Both lower-level Vulkan manifests now require a concrete `GPU<n>:` entry and reject only no-device or explicitly skipped Vulkan/physical-device evidence; a regression proves that headless surface skips pass while a missing or skipped physical device fails. The accepted current-source receipt remains pending the pushed repair. |
| 2026-07-15 | Accepted Vulkan resumable-prefill scheduling parity | `sha256:0a135f0ad1eca6ccc1bfa6df503b9d1f7c9a0f684a993600417135f439fc0f5b` | `d6d14bfaf` + this receipt commit | Strix Halo Vulkan/RADV under `closed-qualification-case-v1` | `qualification/receipts/vulkan/strix-halo/20260715t000104133598z-vulkan-strix-halo-prefill-scheduling-v1-6899c96516-v1.json`; strict current-source/local-artifact/known-commit validation before documentation mutation; all three required cases passed; zero ignored/output-assertion failures; exact closed-base inspection; no-process/unit residue and 24 GiB availability check | passed | The headless probe enumerated `GPU0` without treating its skipped surface information as a device skip. The actor case passed the literal short-decode/1K/16K fairness and cancellation-cleanup contract. The production Vulkan runtime matched monolithic hybrid-model prefill over exact quanta `[17, 17, 17, 17, 12, 1]`, six layer yields, block-aligned recurrent/prefix state, first token 13, following decode token 13, and final KV release. The receipt binds closed case-base hash `sha256:4e2f59070351a66c62498c8952245ba078b66548fd1f7b7b4083b32b2ab41a93`; its effective artifact contains neither `ROCM_PATH` nor `KILN_ROCM_ARCHS`. This closes the current-source resumable-prefill subset, not the public-model tokenizer/logit/sampling/eval corpus or performance gates. |
| 2026-07-15 | Vulkan inference-oracle model-path counterexample | `sha256:420c8e4f3cfd9f1da2bded38d603787a562cfa345ec59ba19ff0f9eecff0f3ac` | `0ee25599e` + this receipt/fix commit | Strix Halo Vulkan/RADV under nested closed case and Cargo environments | `qualification/receipts/vulkan/strix-halo/20260715t001808445031z-vulkan-strix-halo-vulkan-inference-oracles-bfc6c14dd8-v1.json`; strict current-source/local-artifact/known-commit validation; 453 qualification tests; 10/10 docs-builder tests; runtime/source/artifact ratchets and shell syntax; no-process/unit residue and 24 GiB availability check | failed as designed; delivery boundary repaired | The required device probe, two BF16 argmax CPU cases, six-row non-greedy sampling CPU oracle, six FLCE/selected-logprob/gradient CPU-and-TRL cases, and six source-pinned HF tokenizer cases all passed. The final Qwen resident-logit case failed closed before loading weights because the outer manifest supplied its model path but `closed-qualification-test-v1` intentionally forwarded only `KILN_QUALIFICATION` into the inner bounded Cargo service. The two legacy per-test activation variables are now consolidated into manifest-derived `KILN_QUALIFICATION_MODEL_PATH`; that binding is forwarded only by the qualification policy, while ordinary source builds and arbitrary product/secret `KILN_*` values remain excluded. The clean pushed-source acceptance rerun remains required. |
| 2026-07-15 | Accepted Vulkan tokenizer, sampling, selected-logprob, and resident-logit oracles | `sha256:1ac91d8cea7f50eeaa53875fc7fe2559a99a1e1b7703e3110d2e94693e2d1c1a` | `8a1edd250` + this receipt commit | Strix Halo Vulkan/RADV and source-pinned `Qwen/Qwen3.5-4B` | `qualification/receipts/vulkan/strix-halo/20260715t002226653824z-vulkan-strix-halo-vulkan-inference-oracles-bfc6c14dd8-v1.json`; strict current-source/local-artifact/known-commit validation before documentation mutation; six required cases; hosted Pages, CPU CI, qualification, and repository runs `29378841190`, `29378841168`, `29378841146`, and `29378841154` green; post-run no-process/unit residue and 25 GiB availability check | passed bounded correctness; memory ceiling contact retained | The headless RADV probe passed; two BF16 tail-row argmax tests matched CPU; all six sampler tests passed, including six non-greedy rows spanning temperature, top-k, top-p, min-p, repetition/presence/frequency penalties, and seeded categorical choices; six FLCE/GRPO cases matched analytical CPU, finite difference, and pinned TRL/PyTorch references; and six HF-derived Qwen rendering/token/mask/label cases matched exact artifact hashes. The 368.881-second public-model case produced bit-identical resident/nonresident logits across all 248,320 vocabulary entries (`max_abs=0`, `max_rel=0`). The complete workload took 382.453 seconds with zero failures, ignored tests, or output-assertion failures. Systemd recorded 17 GiB peak memory over 368.836 seconds; live cgroup inspection observed cap events but zero OOM/OOM-kill and zero service swap, followed by complete reclamation. This closes the named tokenizer/sampling/selected-logprob and resident-path subsets, not independent HF full-model logits, broader cache/cancellation/eval, or performance gates. |
| 2026-07-15 | Accepted Vulkan cache, cancellation, and live-eval model routes | `sha256:7cd165f542fba1e855a4a915516952b02038e2001fc1ef028d149e04fac54d16` | `bcb245ac7` + this receipt commit | Strix Halo Vulkan/RADV deterministic BF16 hybrid fixture | `qualification/receipts/vulkan/strix-halo/20260715t004650723123z-vulkan-strix-halo-vulkan-model-routes-v1-e2287dab6c-v1.json`; strict current-source/local-artifact/known-commit validation before documentation mutation; required device probe plus one fail-closed model-route case; all 453 qualification tests and Vulkan all-target compile check green; post-run no-process/unit residue and 24 GiB availability check | passed bounded correctness | Three production-runtime contracts proved retained-prefill cancel/discard ownership reclamation, batching-engine multi-turn prefix hits with safe block alignment and lease retirement, and nonbatched KV-plus-GDN split-entry replay with token-identical output. A fourth route ran a one-example, two-token exact-match suite through `LiveEvalGenerator`, the real eval executor, production batching actor, `RealDecodeForward`, and native Vulkan runner; it passed with six prompt tokens, completion `t1 t1`, and no failed, invalid, or errored examples. The route exposed and fixed a product bug where base-model eval falsely requested a content mutation and stable profile rejected it; named adapters still force reload to protect retrained-same-name correctness. The initial Pages run `29379957820` then exposed a stale duplicated `911`-read smoke expectation after the inventory ratcheted to 903; this receipt commit derives all checked inventory counts from the versioned contract and passes the exact assembled-site smoke locally. The 2.760-second workload had zero skips or assertion failures and retained the closed execution identity. This closes the named cache/cancellation/eval subset, not independent HF full-model logits, public-model eval quality, soak, or throughput gates. |
| 2026-07-15 | Accepted independent Vulkan/HF full-model logit oracle | `sha256:b21d95b47650ee831d27a678e85b3842d369b6bc78e0f21ec84c9a9da65bcfa4` | `4d6697c52` + this receipt commit | Strix Halo pinned PyTorch ROCm reference plus Kiln Vulkan/RADV | `qualification/receipts/vulkan/strix-halo/20260715t013710403012z-vulkan-strix-halo-vulkan-hf-full-model-ora-39c1bc8042-v1.json`; strict current-source/local-artifact/known-commit validation before documentation mutation; all 462 qualification tests; bounded Vulkan all-target compile check; exact bubblewrap namespace probe; post-run no-process/unit residue and 24 GiB availability check | passed bounded correctness; ceiling and host-pressure observations retained | A private-network 16 GiB zero-swap HF service pinned Torch 2.13.0 commit `cf30153`, Transformers 5.13.1 Qwen module hashes, safetensors 0.8.0, eager full attention, and the independent torch linear-attention fallback. Its raw F32 logit tensor was bit-identical across diagnostic runs at `sha256:0b902c0d74a8ed54aefefcdab50adeb6fedd7adb3e45a2338c27276e90abeeaf`; cgroup peak was 9,254,346,752 bytes with zero high/max/OOM/OOM-kill events and zero service swap. The 17 GiB zero-swap Vulkan stage kept resident/nonresident Kiln logits bit-identical, then matched HF across all 248,320 values with exact argmax, 10/10 top-token overlap, `0.12433958` maximum and `0.020353988` mean absolute error, and `0.999941539769` cosine. An initial 16 GiB Vulkan diagnostic was OOM-killed and established the invalid lower ceiling. The first runner attempt then exposed that post-exit `systemctl` cannot reconnect from the bubblewrap namespace; service-owned cgroup telemetry and `systemd-run --wait` repaired that boundary and passed the exact namespace probe. The accepted Vulkan service contacted its 17 GiB ceiling without swap/OOM; system-wide swap rose roughly 0.4 GiB while the host stayed above reserve, so soak pressure remains open. This closes the independent Phase 6 CPU/HF oracle gate, not multi-token parity, public HTTP eval quality, soak, throughput, or vLLM competitiveness. |
| 2026-07-14 | Vulkan buffer ownership telemetry and soak diagnosis | this commit | this commit | portable API/qualification plus Strix Halo Vulkan buffer tests | 3 real-device Vulkan buffer tests; Vulkan-feature and feature-free server metrics tests; 472 qualification-tooling tests; canonical 141-definition observability schema and 102-path/114-operation HTTP self-test; 10/10 docs-builder tests; 48-document/5-asset validation and generated static smoke; Rust/Python formatting, compilation, and diff hygiene | passed instrumentation checkpoint; soak remains open | Every successful `VulkanBuffer` bind now records actual Vulkan allocation-requirement bytes, live count/bytes by device-local versus host-visible route, exact process-lifetime high water, and cumulative allocation/free count/bytes. Allocation and bind failures clean up their buffer/memory, destruction preserves Vulkan's buffer-before-memory lifetime rule, and checked live decrements cannot wrap silently. `/health`, Prometheus, the closed generated website/API schema, and the Vulkan soak receipt expose the same state. The soak reconciles count and byte conservation after drain, fails positive post-warmup live ownership growth, records per-cycle churn, and preserves the observed allocation window when stabilization fails before measurement. This checkpoint does not reinterpret RSS growth or relax its gate; the next bounded hardware diagnostic must determine whether the repeated 53-61 MiB curve is owned-buffer growth, churn with matched frees, or page residency in fixed live allocations. |
| 2026-07-15 | Bounded Vulkan recycler and accepted memory model | `sha256:38a6f83501b7da462c16791a98cd9822f21b6caaa6f48f7a7aef6ed3a547db00` | this commit | portable contracts plus Strix Halo Vulkan/RADV | passed current-source 70.845-second measured development smoke; retained failed 512 MiB sampled-peak counterexample in `/tmp`; real-device idle-only eviction test; 109 config tests; Vulkan and feature-free metrics tests; reclaim-policy contract; 472 qualification tests; 99-field/94-env config schema; 143-definition observability and 102-path/114-operation HTTP contracts; 10/10 docs-builder tests; 48-document/5-asset build and generated static smoke; bounded Vulkan all-target compilation | passed bounded development smoke; clean 30-minute receipt pending | The unbounded recycler now has one process-wide 3 GiB typed default, exact allocation-requirement accounting, oldest-idle admission eviction, uncached overflow release, explicit idle trim, and destructor work outside its mutex. Startup configures it before model allocation; coordinated pressure reclaim trims only after exclusive ownership and repeated health/activity checks. Health, config, Prometheus, generated schemas, website configuration, troubleshooting, and qualification docs expose the complete cap/inventory/borrowed/free/hit/miss/eviction/overflow contract. The first measured run rejected only a 676 MiB sampled DRM peak even though end ownership was flat and 118,024 allocations matched frees exactly; the corrected contract preserves the 512 MiB drained process-DRM/RSS leak gates and separately bounds active Vulkan scratch at 1 GiB. The accepted rerun stabilized in four cycles with two flat endpoints, completed 22 requests over 70.845 seconds, matched 120,768 allocations/frees and 93,706,563,776 bytes in both directions, retained 2,275,606,528 bytes under the cap, and recorded zero live/pool growth, request failure, eviction, uncached fallback, host-guard trip, dirty shutdown, or residue. Sampled active DRM growth was 808,316,928 bytes; RSS grew 67,227,648 bytes; minimum host availability was 24,072,265,728 bytes. This proves the bounded memory interpretation but deliberately leaves the clean 30-minute soak open. |
| 2026-07-15 | Vulkan direct-allocation churn counterexample | `sha256:51b070b3dcba093b95d651062b8eef7bab5b139261553656293d164305abdc25` | `5a6f8039e` | Strix Halo Vulkan/RADV | `qualification/receipts/vulkan/strix-halo/20260715t055032942821z-vulkan-strix-halo-serving-vulkan-developme-b5eb848d54-v1.json`; strict current-source/artifact/commit validation | failed safely | The first clean pushed-source 30-minute attempt stabilized in five cycles with two flat endpoints, then completed 166 requests across 39 waves with nine confirmed cancellations and zero request, device-fault, batching, synchronization, unexplained-ITL, KV-ownership, prefix-ownership, shutdown, or residue failures. Exact Vulkan ownership stayed flat: 925,712 allocations matched 925,712 frees and 721,808,052,288 allocated bytes matched freed bytes; live bytes remained 51,840,421,840 and the recycler remained 2,275,606,528 bytes with 609,635 hits, zero misses, zero eviction, and zero uncached fallback. Despite that, RSS rose steadily from 1,128,468,480 to 1,675,059,200 bytes and crossed the unchanged 512 MiB bound by 9,719,808 bytes after 576.42 measured seconds. Device-local ownership was identical on every sampled wave, while allocation/free counts kept increasing, isolating the failure to transient Vulkan buffers that bypass the recycler rather than retained-buffer growth. Host availability stayed above 23,485,243,392 bytes, swap did not grow, the process failed closed, and teardown was clean. The next checkpoint must recycle the hot transient storage and staging routes and demonstrate that allocation counts converge; this evidence does not justify relaxing the RSS bound. |
| 2026-07-15 | Vulkan transient-buffer recycling and zero-churn gate | `sha256:01b2ce6d93397b7b63f3448dfe736b4bb627d254cb7e5cc829f6f170dd921aef` | this commit | portable qualification contract plus Strix Halo Vulkan/RADV unit coverage | 53 real-device Vulkan-kernel tests; 16 real-device Vulkan-storage tests; Vulkan server feature compilation; 472 qualification tests; strict workload validation; 10/10 docs-builder tests; Rust formatting and diff hygiene; bounded source-matched allocation diagnostic | passed implementation checkpoint; measured soak pending | Transient tensor, operator output, recurrent-state replacement, packed upload, readback, and host-to-device staging paths now acquire bucketed buffers from the existing bounded device-local or host-visible recycler. Zero-initialized storage uses a device fill instead of allocating and uploading a host zero vector. A source-matched first pass reduced the first measured wave from 3,714 direct allocations to 2,335; temporary sampled call-site instrumentation then isolated the remaining storage, tensor, GDN, cast, elementwise, and decode-state routes and was removed after converting them. The workload contract now snapshots cumulative allocation count after stabilization and fails the next wave if even one new `VulkanBuffer` allocation occurs; cache hits remain allowed. This turns allocator convergence into an explicit zero-growth gate rather than relying on balanced frees or RSS symptoms. All converted real-device suites pass and a bounded service probe shut down cleanly, but the exact mixed workload has not yet demonstrated zero post-stabilization allocations, so the clean 30-minute receipt and Vulkan soak checkbox remain open. |
| 2026-07-15 | Durable/transient Vulkan allocation boundary | `sha256:01b2ce6d93397b7b63f3448dfe736b4bb627d254cb7e5cc829f6f170dd921aef` | this commit | Strix Halo Vulkan/RADV plus portable API contract | interrupted clean-source mixed-load probe; 54/54 real-device Vulkan-kernel tests; 16/16 real-device Vulkan-storage tests; full Vulkan server feature compilation; Rust formatting and diff hygiene | passed corrective checkpoint; mixed-load rerun pending | Live inspection during the first pushed-source rerun caught a lifetime inversion before measurement: the general CPU-to-Vulkan ownership boundary had admitted long-lived model weights into the scratch recycler. At readiness the entire 3,221,225,472-byte cap was retained and all 152 buffers were borrowed; stabilization had already recorded 241,044 misses and 240,880 uncached allocations. The probe was deliberately interrupted instead of retaining a predictable soak failure. It exited on graceful SIGTERM, joined the server, removed its private model snapshot and workspace, left no process residue, used no service swap, and restored 26 GiB host availability. Generic CPU-to-Vulkan and `VkTensor` constructors are direct again; the new explicitly named recycled constructor is restricted to request-scoped GDN uploads. A real-device regression proves the durable constructor changes borrowed scratch ownership by zero, the recycled constructor changes it by exactly one, and dropping it returns the borrow. This repairs the allocation-class boundary without raising the cap or weakening the zero-churn gate; source-bound stabilization and measurement remain required. |
| 2026-07-15 | Scoped Vulkan model-lifetime allocation phase | `sha256:01b2ce6d93397b7b63f3448dfe736b4bb627d254cb7e5cc829f6f170dd921aef` | this commit | Strix Halo Vulkan/RADV plus portable ownership contract | second interrupted pushed-source readiness probe; focused real-device durable-scope test; full Vulkan server feature compilation; Rust formatting and diff hygiene | passed implementation checkpoint; clean readiness rerun pending | The constructor split removed generic weight-transfer pinning but exposed the broader ownership rule: model-load transpose, cast, and packing operators legitimately reuse inference operators, so their pooled outputs became persistent prepared weights. The second readiness snapshot improved from 152 to 130 borrowed buffers but still pinned 3,221,094,400 bytes of the 3 GiB pool and forced 38,459 uncached allocations before stabilization; decode prewarm then reported 2,880 MiB of F32 and 8,020 MiB of BF16 persistent cache. The probe again stopped cleanly before accepting misleading soak evidence. A process-wide RAII durable-allocation scope now makes both device-local and host-visible recycler entry points allocate directly during model upload and persistent decode-weight prewarm. It is process-wide so parallel preparation inherits it; startup has no requests, and background prewarm already holds the exclusive GPU write gate. Nested scopes are counted and fail on underflow. A real-device regression proves an explicitly recycled tensor changes borrowed ownership by zero inside the durable scope. The cap and zero post-stabilization allocation gate remain unchanged; a clean readiness snapshot must show prepared weights no longer consume scratch before another mixed-load attempt proceeds. |
| 2026-07-15 | Clean Vulkan lifetime-boundary validation and residual churn counterexample | `sha256:89dc3bf2753570839e360153880b84fad2af38c8352619f3808bb8c9616e6524` | `526ae7add` | Strix Halo Vulkan/RADV | source-bound 60-second development-soak diagnostic; release binary `sha256:9be10bf0142dae8c72fca762ef8fd15f4824f90de76375318219d06a0e5746f3`; exact case result | failed safely at the zero-allocation gate | The first clean rerun proved the durable phase fixed the readiness inversion: the first request saw a 2,244 MiB pool with 2,033 MiB free, only 210.8 MiB borrowed, and zero uncached allocations; 2,550,136,832 bytes of startup staging were evicted rather than pinned. Stabilization converged in five cycles with two flat endpoints after 85 requests and five cancellations; final GPU delta was 16,384 bytes, final RSS delta 52,981,760 bytes, total stabilization RSS growth 415,649,792 bytes, retained scratch 3,089,432,576 bytes, and uncached allocations remained zero. The first measured request had zero GPU growth, 6,311,936 bytes RSS growth, no request/device/ownership/shutdown/residue failure, 5,491 recycler hits, zero misses or eviction, and no host pressure or swap growth, but still performed 973 matched direct allocations/frees totaling 188,866,608 bytes. The unchanged zero-growth gate stopped the run after 2.030 measured seconds. This validates the durable/transient boundary and isolates the remaining defect to request paths that still bypass the recycler; it does not satisfy the 60-second or 30-minute soak gates. |
| 2026-07-15 | Vulkan request allocation call-site closure | `sha256:fa4d4230926e68027b760575dab7383afc972661375d9e46279d913bab287c7b` | this commit | Strix Halo Vulkan/RADV plus portable ownership contract | temporary post-prewarm call-site trace removed before commit; 54/54 real-device Vulkan-kernel tests; 17/17 real-device Vulkan-storage tests including isolated ownership reference proof; full Vulkan server feature check; Rust formatting and diff hygiene | passed implementation checkpoint; clean mixed-load rerun pending | One completed post-prewarm warmup request attributed 6,254 raw allocations totaling 27,722,324,420 bytes without weakening the workload's fail-closed environment contract. The generic CPU-to-Vulkan boundary accounted for 4,490 calls; GDN gated norm and causal-convolution input/state/output buffers accounted for six 144-call sites; three readback staging helpers accounted for another 432 calls. Pool misses, lazy persistent resident scratch, and the paged KV cache were separately visible and deliberately remain governed by their existing transient or durable ownership. The shared host upload now recycles request tensors while the process-wide model preparation scope still forces direct base-weight allocation. Runtime-loaded LoRA tensors use a new explicit direct durable upload instead of pinning scratch or activating a process-wide scope during concurrent serving; an isolated-device regression proves durable storage has no recycler reference while transient storage has exactly one. The traced GDN, convolution, and readback buffers now use the bounded recycler. Temporary instrumentation and its non-product environment switch were removed; the cap and zero post-stabilization allocation gate are unchanged. |
| 2026-07-15 | Vulkan pooled-readback logical-size regression | `sha256:2cf69eab0cd30c9936c6cb2974ba294fcdce9745bba528621d104e750f82a387` | `ef49c1b6d` + this commit | Strix Halo Vulkan/RADV plus portable readback contract | clean pushed-source warmup; 54/54 real-device Vulkan-kernel tests with exact logical-length regression; full Vulkan server feature check; Rust formatting and diff hygiene | failed safely, then corrected; clean mixed-load rerun pending | The first pushed-source rerun reached readiness with the expected durable ownership split, but its first warmup failed at gated DeltaNet layer 0 before stabilization: a 98,304-byte logical tensor was backed by a 262,144-byte recycled staging bucket, and the single-output transfer helper exposed all physical bytes to `Tensor::from_raw_bytes_on`. This was a direct correctness regression from pooling the readback, not a numerical or device fault. The qualification harness rejected the response, sent SIGTERM, joined the server, removed the private 9,319,828,096-byte model snapshot and workspace, recorded zero host-guard trip or swap growth, left no process residue, and restored 26 GiB host availability. The common single-output transfer helper now validates the physical buffer is large enough and truncates to its declared logical readback size before returning; all four callers inherit the fix. A real-device GDN regression asserts exact byte length rather than allowing value comparison to ignore a bucket tail. No allocation or memory threshold was relaxed. |
| 2026-07-15 | Vulkan dispatcher-wide logical readback contract | `sha256:5e9948f026e872043213d71c48496ff9b4382783e65f3871a56d68b634589c9a` | `6815fdd49` + this commit | Strix Halo Vulkan/RADV plus portable kernel contract | second clean pushed-source warmup; release binary `sha256:a213b626b5cf31344210d4232c081067eb64093ab82832b7ddfa208a819724c6`; bounded all-target compilation; 54/54 real-device Vulkan-kernel tests; 18/18 real-device GDN integration tests; Rust formatting and diff hygiene | failed safely, then corrected comprehensively; clean mixed-load rerun pending | The first narrow repair changed the single-output helper, but a second source-bound run reproduced the same 98,304-versus-262,144-byte gated DeltaNet failure after 291.224 seconds, proving that diagnosis had selected the wrong branch. The harness again failed on warmup, terminated and joined the server, removed its snapshot and workspace, left no process residue, and restored 26 GiB host availability without a host guard or swap-growth event. The public GDN gates and gated-normalization dispatchers now validate and truncate every branch result to its declared logical bytes. The shared two-stage helper now requires a logical size for every output, rejects sizes larger than backing storage, packs and copies only those bytes, and returns exact slices; paged attention and causal convolution supply their output/state lengths explicitly. Exact-length regressions cover both gate outputs and both cached causal-convolution outputs. Physical recycler bucket capacity is no longer observable through these byte APIs, including debug fallbacks, and no resource threshold was relaxed. |
| 2026-07-15 | Vulkan batched-allocation counterexample | `sha256:247dd87e9504a8502b9d7703941a5f6771798946cdeb694027fb6b205394fc0d` | `8e329e1dc` + this evidence commit | Strix Halo Vulkan/RADV | clean source-bound 60-second diagnostic; raw log `sha256:733653294f0438aceddafd83288768b37bdfd419bf0fca9c244bf7c39ee517ec`; release binary `sha256:4a55f41a51eb8ee91759ab0dd4c8d3f8cb2cd75bb77eecc27fef958ad6c3c565`; all four hosted workflows green | failed safely at concurrency-4 zero-allocation gate; batched trace pending | The logical-size repair cleared the former first-warmup failure, filled the 15-entry prefix-cache ceiling in three waves, and completed five stabilization cycles, 85 requests, and five cancellations. The last two endpoints were exactly flat; final owned GPU and pool growth were zero, the recycler retained 3,137,404,928 bytes under its unchanged 3 GiB cap, uncached allocation stayed zero, and stabilization RSS growth was 417,595,392 bytes. The first measured concurrency-1 wave completed in 1.995 seconds with exactly zero Vulkan allocations, proving the general request path converges. The next concurrency-4 wave completed four requests with no request, device, semantic, ownership, or ITL failure but made 960 matched allocations totaling 1,245,708,288 bytes; live GPU and retained-pool bytes stayed flat, RSS grew 12,582,912 bytes, active sampled GPU growth was 226,590,720 bytes, and all 39,488 pool acquisitions hit. The unchanged gate stopped measurement at 13.891 seconds. Graceful shutdown joined the server, removed the 9,319,828,096-byte private snapshot and workspace, left no process residue, recorded no host-guard or swap-growth event, and restored 26 GiB host availability. Residual churn is now isolated to multi-request/batched execution; this does not satisfy the 60-second or 30-minute soak gate. |
| 2026-07-15 | Recycled Vulkan sampling pipelines | this commit | this commit | Strix Halo Vulkan/RADV plus portable server build | 54/54 real-device Vulkan-kernel tests; 6/6 real-device fused-sampler integration tests; bounded all-target kernel compilation; bounded Vulkan server feature compilation; Rust formatting and diff hygiene | passed implementation checkpoint; clean mixed-load rerun pending | The concurrency-4 counterexample's 960 allocations mapped to the fused batched LM-head sampler's complete per-step family: activation and full-vocabulary logits, seven sampling-control buffers, six optional penalty buffers, the token output, and host-visible token staging. Every request-lifetime member now uses the bounded device-local or host-visible recycler; packed upload staging already used the same pool. The adjacent single-row stochastic sampler now follows the same lifetime contract instead of retaining an equivalent direct-allocation footgun outside the measured route. Both paths continue to copy and map only their declared token bytes even when the pooled backing bucket is larger. Device tests preserve greedy top-1, row-specific penalties, seeded stochastic filters against the CPU oracle, BF16 rows-8 tails, and single-row penalty behavior. Persistent LM-head weights remain outside the recycler, the 3 GiB cap and zero-allocation gate are unchanged, and a pushed-source concurrency sweep must still prove that no other batched route allocates after stabilization. |
| 2026-07-15 | Post-sampler Vulkan allocation counterexample | `sha256:f629cd9c2b4fb8252c4f60b8112ef949fe31c99dc9f315239efebd2f52225817` | `355def481` + this evidence commit | Strix Halo Vulkan/RADV | clean source-bound 60-second diagnostic; raw log `sha256:0d29b1d2154c8fef1aeab958444bda87050b22884acad9ce507c3a52a234decd`; release binary `sha256:31dca918023abbe942d33a1985c6fb6079b3d27138f5b9cf8dc933fca8372f82`; all four hosted workflows green | failed safely at concurrency-4 zero-allocation gate; direct-call attribution pending | Three warmup waves filled the 15-entry prefix cache, then seven stabilization cycles completed 119 requests and seven cancellations. The last two endpoints were flat with zero live-buffer or retained-pool growth, zero uncached allocations, and no evictions; stabilization RSS growth reached 531,251,200 bytes, 5,619,712 bytes below the unchanged 512 MiB limit. Measurement concurrency 1 again had exactly zero allocations and completed in 1.985 seconds. Concurrency 4 completed four valid requests but made 1,056 matched allocations totaling 1,358,954,496 bytes; live GPU and pool retention stayed flat, RSS grew 8,388,608 bytes, and all 38,992 recycler acquisitions hit with zero misses. This proves the sampler conversion did not own the measured direct call sites. The gate stopped measurement at 13.398 seconds; all seven ITL outliers were attributed, no device, semantic, ownership, host-guard, swap-growth, forced-shutdown, or residue failure occurred, the private snapshot was removed, and host availability returned to 26 GiB. |
| 2026-07-15 | Recycled Vulkan batched GDN state transfers | this commit | this commit | Strix Halo Vulkan/RADV plus portable backend contract | exact-value and five-borrow real-device assemble/scatter regression; 54/54 real-device Vulkan-kernel tests; focused Vulkan state-lifecycle test; bounded all-target Vulkan server compilation; Rust formatting and diff hygiene | passed implementation checkpoint; clean mixed-load rerun pending | The residual count has an exact structural signature: Qwen3.5-4B has 24 linear-attention layers, and `1,056 / (24 layers * 2 state kinds) = 22` assemble/scatter row operations. The kt-native batched decode assembled one recurrent and one convolution buffer per layer and later split them back into request rows through two helpers that still allocated every output directly. Both helpers now use the bounded recycler. The first focused test exposed a more serious adjacent contract defect: the old helpers inferred logical row stride from physical capacity, so a recycled 64 KiB row assembled into a 256 KiB bucket could place row 2 in padding and scatter zeros from the wrong offset. Exact recurrent and convolution row byte lengths now flow from typed tensor geometry through both backend interfaces; sources require sufficient capacity, copies use only logical bytes, destinations validate sufficient capacity, and overflow fails closed. A real-device test proves two distinct rows survive assemble and scatter exactly despite oversized backing buckets, accounts for two source, one batch, and two output borrows, and proves all five return on drop. No pool or memory threshold changed. |
| 2026-07-15 | Vulkan state-transfer pool-pressure counterexample | `sha256:313aa83e61a8f7ae198cd7abaed770d2d7ebfffebf818c4b2b19bcecf2618a6f` | `232b07c5d` + this evidence commit | Strix Halo Vulkan/RADV | clean source-bound 60-second diagnostic; raw log `sha256:69b627ea4ce8ea1f107f42b9e9fc9dc2cb78f841daad2e2cafc977e7c396acec`; release binary `sha256:7723aea79b142be2c6c74444b35e43d27e74d671a5fdcdf89d3241e02a96fd69`; exact case result | failed safely at concurrency-4 zero-allocation and ownership gates; in-place scatter pending | The logical-stride state fix reduced the first stabilization cycle from 3,012 raw allocations to 334 and allowed convergence in the minimum four cycles after 68 requests and four cancellations, versus seven cycles and 119 requests before it. Measurement concurrency 1 remained allocation-free. Concurrency 4 then completed four valid requests with 40,573 pool hits but 19 misses totaling 19,136,512 bytes; one 12,582,912-byte buffer was evicted, while live ownership and pool retention each grew 6,553,600 bytes. The zero-growth gates stopped measurement at 13.545 seconds. Cycles 3 and 4 had flat live endpoints but still allocated and freed roughly 400 buffers and 2.4 GiB per cycle, exposing capacity churn hidden by the endpoint-only stabilization signal: production scatter creates replacement row buffers before dropping the previous request rows, temporarily retaining old rows, a batch copy, and new rows. The process shut down cleanly, removed its private snapshot and workspace, left no residue, restored 26 GiB availability, and recorded no device, semantic, unexplained-ITL, host-guard, or swap-growth failure. No threshold was changed. |
| 2026-07-15 | In-place Vulkan batched GDN state scatter | this commit | this commit | Strix Halo Vulkan/RADV plus portable backend contract | 2/2 exact-value real-device state-copy tests including invariant borrow count; 54/54 real-device Vulkan-kernel tests; focused Vulkan state-lifecycle test; bounded all-target Vulkan server compilation; Rust formatting and diff hygiene | passed implementation checkpoint; clean mixed-load rerun pending | Kt-native production scatter now copies each logical recurrent and convolution row from the mutated batch buffer back into the request-owned row buffer already registered for that tensor ID. It no longer allocates replacement rows, changes `Arc` identity, or mutates the row-key maps. The shared device copy validates non-empty inputs, exact logical byte geometry, destination capacity, and overflow, then submits all row copies with explicit compute-to-transfer and transfer-to-compute barriers. The generic split API reuses this primitive after allocating outputs and remains available only for the compatibility bridge. A real-device regression assembles two distinct rows, scatters them into two pre-existing destinations, proves exact values despite bucket padding, and proves borrowed-buffer count is identical immediately before and after scatter. This removes the production path's old-row plus batch plus new-row triple-buffer peak without increasing the 3 GiB cap. |

## Known Starting Defects

These are anchors, not an exhaustive substitute for the checklist:

- ROCm pool trim can synchronize the device every two seconds under pressure:
  `crates/kiln-memory/src/governor.rs`, `crates/kiln-server/src/state.rs`, and
  `crates/kiln-hip/src/lib.rs`.
- Default KV autoscaling can resize/copy live cache capacity between decode
  steps: `crates/kiln-server/src/kv_autoscaler.rs`,
  `crates/kiln-server/src/batching_engine.rs`, and
  `crates/kiln-model/src/paged_kv_cache_kt.rs`.
- ROCm captured graphs omit KV pool generation/identity and are not invalidated
  by physical resize: `crates/kiln-model/src/rocm_graph.rs` and
  `crates/kiln-model/src/generate.rs`.
- A full response channel can sleep the compute actor for up to two seconds:
  `crates/kiln-server/src/batching_engine.rs`.
- GRPO uses one reference tensor for both importance ratio and KL while rollout
  provenance lacks behavior log-probabilities: `crates/kiln-train/src/lib.rs`,
  `crates/kiln-train/src/trajectory.rs`, and
  `crates/kiln-train/src/trainer.rs`.
- Resolved in Phases 4.1 and 4.2: eval `n > 1` aggregated completions rather
  than examples, and synthesis could label training rows as held out:
  `crates/kiln-eval/src/suite.rs`,
  `crates/kiln-eval/src/result.rs`, `crates/kiln-eval/src/synthesis.rs`, and
  `docs/site/evals.html`.
- Thinking-budget configuration, streaming logs, batch/compare surfaces, recent
  requests, and desktop semantics are not yet one conformance-tested contract:
  `crates/kiln-server/src/config.rs`,
  `crates/kiln-server/src/request_log.rs`,
  `crates/kiln-server/src/api/completions.rs`,
  `crates/kiln-server/src/ui/app.js`, and `desktop/`.
