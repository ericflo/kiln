2026-05-09 A100: accepted Vulkan MLP chained dispatch

Change
- Chained each Vulkan MLP gate/up dispatch and down-projection dispatch into a
  single command buffer with a compute-to-compute memory barrier between them.
- This removes one queue submit/wait and one transient descriptor-pool reset
  from each MLP call while preserving the existing shaders and weight-buffer
  routes.
- Rollback env: `KILN_DISABLE_VULKAN_MLP_CHAINED_DISPATCH=1`.

No-profile A/B
- Seed 109:
  - candidate: `974.825019ms` prefill, `80.3875675ms` mean ITL,
    `88.264491ms` p99
  - rollback: `1009.779828ms` prefill, `81.414262ms` mean ITL,
    `90.150941ms` p99
- Seed 110 counter-order:
  - rollback: `965.25146ms` prefill, `81.29783125ms` mean ITL,
    `88.605094ms` p99
  - candidate: `952.165659ms` prefill, `81.043109375ms` mean ITL,
    `96.077606ms` p99
- Both pairs stayed on backend `vulkan` and kept token IDs
  `[271,1206,1423,680,1204,1691,51864,3520,506]`.

Focused profile, seed 111
- Candidate: `944.810553ms` prefill, `82.797547625ms` mean ITL,
  `90.497587ms` p99.
- Rollback: `1009.611486ms` prefill, `83.79880025ms` mean ITL,
  `91.305076ms` p99.
- Outer MLP bucket:
  - `seq_len=64`: `232.760ms -> 230.893ms`
  - `seq_len=1`: `246.136ms -> 234.216ms`
- Vulkan MLP kernel bucket:
  - batch `64` total: `231.757ms -> 229.836ms`
  - batch `1` total: `240.425ms -> 228.764ms`

Validation
- `cargo fmt --check`
- `git diff --check`
- `cargo check -p kiln-vulkan-kernel`
- `cargo check -p kiln-model --features vulkan`
- `cargo test -p kiln-vulkan-kernel --test gdn_parity mlp_decode -- --nocapture`
- `cargo test -p kiln-vulkan-kernel --test gdn_parity -- --nocapture`
- `cargo build --release -p kiln-server --bin kiln-bench --features vulkan`
- `cargo build --release -p kiln-server --bin kiln --features vulkan`
- Live server smoke with `chat_template_kwargs: {"enable_thinking": false}`
  returned HTTP 200 and visible `content == "Red Blue"`.
- `cargo check -p kiln-model --features cuda` was attempted and remains
  host-blocked because `nvcc` is missing.
- `cargo check -p kiln-model --features metal` was attempted and remains
  host-blocked because `objc2` requires an Apple target.

Verdict
- Accepted. The improvement is mainly a Vulkan decode-boundary win; prefill is
  positive but noisy, and p99 was mixed in the unprofiled counter-order pair.
- CUDA and Metal source paths are untouched.

Artifacts
- `docs/audits/vulkan-strix-halo-2026-05-09-a100-mlp-chained-dispatch-*.log`
- `docs/audits/vulkan-strix-halo-2026-05-09-a100-mlp-chained-dispatch-server-response.json`
- `docs/audits/vulkan-strix-halo-2026-05-09-a100-mlp-chained-dispatch-server-metrics.txt`
