# Phase 9 v0.1.0 release-readiness audit

Date: 2026-04-29
Auditor: Cloud Eric
Trigger: PR #651 (Phase 10 closure, merged 2026-04-29) recommended
"Phase 9 v0.1.0 release prep" as the highest-leverage post-Liger pivot
and named this exact audit as the next planning-loop task.

## Status summary

**Verdict: Phase 9 is shipped — the "v0.1.0 release with semantic
versioning" goal is stale.** kiln-v0.1.0 was tagged and published on
2026-04-19 (`gh release view kiln-v0.1.0 -R ericflo/kiln`); the
production line is now at kiln-v0.2.8 (released 2026-04-29 with
Sigstore-signed build provenance, the GHCR `kiln-server` Docker image
auto-publishing, the `docs/site/` landing page live at
https://ericflo.github.io/kiln/, and all 12 findings of
`security-audit-v0.1.md` resolved). Every Phase 9 checklist item in
the project description has either landed or is met by an
already-published artifact.

The single remaining action is a **project-description amendment**:
retire the "Phase 9 v0.1.0 release prep" goal as complete and carry
forward any remaining work as neutral Phase 11 onboarding and
release-readiness polish. The Phase 10 closure doc's claim that "what
remains is an audit pass + the v0.1.0 semver cut" was written without
rechecking the release index — the v0.1.0 cut happened 10 days before
that doc was authored.

## Phase 9 checklist status

| # | Checklist item                                                   | Status     | Evidence |
|---|------------------------------------------------------------------|------------|----------|
| 1 | Security audit (no prompt injection in training, no DoS vectors) | ✅ Shipped | `docs/audits/security-audit-v0.1.md`. 12/12 findings resolved (every finding ends in **Resolution** / **Fixed**). HIGH ×3 (path traversal in delete/load/merge): closed in v0.2.7. MEDIUM ×3 (training queue cap #607, tracked-jobs cap #608, loopback default bind): closed in v0.2.7. LOW ×4 (per-route body limits, composed-cache LRU #621, adapter-dir disk cap #620, cargo-deny pin #612): closed in v0.2.7. NONE ×4 documented as design choices. Documented training-data trust invariants (§3 / item 12) added to README + QUICKSTART. |
| 2 | License review (all dependencies MIT/Apache compatible)          | ✅ Shipped | `deny.toml` enforces an MIT/Apache-compatible allowlist (MIT, Apache-2.0, BSD-2/3, ISC, Unicode-3.0/DFS-2016, Zlib, MPL-2.0, CC0-1.0, 0BSD, CDLA-Permissive-2.0). `cargo-deny check --all-features` runs in CI on every push to `crates/**` / `Cargo.*` / `deny.toml` / `ci.yml` (`.github/workflows/ci.yml:77-92`, action pinned to commit `91bf2b62…` = v2.0.17, cargo-deny 0.19.2). `THIRD_PARTY_LICENSES.md` (267 KB, generated by cargo-about) is uploaded to every `kiln-v*` release (`server-release.yml:412-431`). |
| 3 | Reproducible builds (deterministic Docker image)                 | ✅ Shipped | `deploy/Dockerfile` builds with `cargo build --release --locked --features cuda` (PR #601 added `--locked`). All three CI release jobs (macOS/Linux/Windows in `server-release.yml`) and the GHCR Docker job in `docker-server-release.yml` use `--locked`, so the published artifacts pin to the exact `Cargo.lock` dependency set. SHA-256 checksums shipped alongside every artifact (`*.sha256` files visible on `kiln-v0.2.8`). |
| 4 | CI/CD pipeline (build, test, benchmark, release)                 | ✅ Shipped | 7 active GitHub Actions workflows: `CI` (Linux + macOS Metal + cargo-deny on `crates/**` push/PR), `Kiln server release` (3-platform binary build + Apple notarization + Sigstore attestations on `kiln-v*` tags), `Build kiln-desktop`, `Build and publish kiln-server image` (GHCR push on `kiln-v*` tags + Sigstore attestation), `Pages` (auto-deploy on `docs/site/**` push), `Build kiln-runpod image` (GHCR push of the dev pod image), `Dependabot Updates` (weekly). cargo-deny job verified pinned and load-bearing in `ci.yml`. |
| 5 | Release binaries for Linux x86_64 (CUDA 12+)                     | ✅ Shipped | `kiln-v0.2.8` ships `kiln-0.2.8-x86_64-unknown-linux-gnu-cuda124.tar.gz` (Linux x86_64 CUDA 12.4), `kiln-0.2.8-aarch64-apple-darwin-metal.tar.gz` (macOS Apple Silicon, signed + notarized), and `kiln-0.2.8-x86_64-pc-windows-msvc-cuda124.zip` (Windows x86_64 CUDA 12.4). Each comes with a `.sha256` sidecar and a Sigstore-signed in-toto attestation linking the SHA-256 digest to the workflow run (verifiable with `gh attestation verify <artifact> -R ericflo/kiln`, see `SECURITY.md` §"Supply-chain provenance"). Release notes record `supported_sm: [80, 86, 89, 90]` and `min_cuda: 12.4`. |
| 6 | Docker image on GitHub Container Registry                        | ✅ Shipped | `ghcr.io/ericflo/kiln-server` published on every `kiln-v*` tag via `docker-server-release.yml` (PR #559+). GHCR API confirms tags `0.2.8`, `latest`, `0.2.7`, `0.2.6` are present (queried via `gh api /users/ericflo/packages/container/kiln-server/versions`). Sigstore build provenance attached via `actions/attest-build-provenance@v2`, push-to-registry, verifiable with `gh attestation verify oci://ghcr.io/ericflo/kiln-server:<tag> -R ericflo/kiln`. |
| 7 | Landing page / project website                                   | ✅ Shipped | `docs/site/index.html` is the GitHub Pages source; `Pages` workflow auto-deploys on any `docs/site/**` change (`.github/workflows/pages.yml`). Live at https://ericflo.github.io/kiln/, linked from `README.md:13`. Already references `v0.2.8` as the latest release in three places (lines 126, 258, 264, 270, 276) and the canonical landing-page-bump pattern (single-PR doc-only deploy) is established (PR #624 scaffold, plus the `kiln-post-release-doc-sweep` agent note documenting the no-GPU bump-on-release path). |
| 8 | v0.1.0 release with semantic versioning                          | ✅ **Shipped 2026-04-19** | `gh release view kiln-v0.1.0 -R ericflo/kiln` returns the published release with assets (Apple Silicon Metal tarball + sha256), `published: 2026-04-19T02:34:15Z`. Subsequent semver-correct patch and minor releases have followed: kiln-v0.1.1 (2026-04-20), kiln-v0.1.2 (2026-04-20), kiln-v0.2.0 (2026-04-24), kiln-v0.2.2 → kiln-v0.2.8 (2026-04-25 → 2026-04-29). Desktop tags (`desktop-v*`) are versioned independently. The repo workspace `Cargo.toml` `version = "0.2.8"` matches the latest `kiln-v*` tag. Workspace inheritance (`version.workspace = true`) is in place across all 12 crates. |

## Versioning reconciliation

The kiln project description's eighth Phase 9 checklist bullet —
"v0.1.0 release with semantic versioning" — is the only item that
needs reconciliation, and it is reconciled by acknowledging that it
**already happened 10 days before this audit was written**. Concrete
release timeline:

```
kiln-v0.1.0  2026-04-19  ← the "v0.1.0 cut" goal, met
kiln-v0.1.1  2026-04-20
kiln-v0.1.2  2026-04-20
kiln-v0.2.0  2026-04-24  ← first 0.2 line (Sonic-3 etc)
kiln-v0.2.2  2026-04-25
kiln-v0.2.3  2026-04-26
kiln-v0.2.4  2026-04-26
kiln-v0.2.5  2026-04-26
kiln-v0.2.6  2026-04-26
kiln-v0.2.7  2026-04-28  ← security audit closure
kiln-v0.2.8  2026-04-29  ← Sigstore provenance + dep bumps + landing-page scaffold
```

PR #651's framing ("what remains is an audit pass + the v0.1.0 semver
cut") was written without rechecking the release index — the v0.1.0
tag predated that doc by 10 days. This audit is the audit pass; there
is no remaining v0.1.0 cut to do.

**What "v0.1.0" was probably meant to mean** in the project
description, charitably: *the release-readiness milestone* — the moment
where kiln's release artifacts, supply-chain metadata, docs, and
onboarding path are coherent enough for cold-reader developers. That
milestone is satisfied by the current `0.2.x` line, with Phase 11 left
to keep the onboarding surfaces polished.

**Recommendation:** Amend the kiln project description's "Phase 9:
Public Release Preparation" section to:

* Strike the "v0.1.0 release with semantic versioning" bullet (already
  shipped on 2026-04-19; the goal as written cannot be met any more).
* Add a new explicit note in its place — suggested:
  "**Release-readiness baseline** on the kiln-v0.2.x line: release
  artifacts, provenance, docs, landing page, demo link, and cold-reader
  onboarding checks are green; keep v0.3.0-ahead changes separate from
  Phase 11 onboarding polish." This is the *actual* remaining work the
  original "v0.1.0 cut" goal was reaching toward.
* Optionally mention that the `0.2.x` line is the current release line
  and `0.1.x` was the early-access run.

Do **not** silently rewrite the project description from this audit
PR — that decision belongs to Eric (it's a positioning question, not
a doc-fact correction). This audit's job is to flag the staleness and
recommend a neutral release-readiness amendment.

## Punch list

After walking each Phase 9 checklist item against current `main`, the
punch list of remaining work is:

1. **Project description amendment** (no GPU; Eric-driven; not a code
   PR). Strike the stale `v0.1.0 release with semantic versioning`
   bullet from the kiln project description, add a release-readiness
   milestone in its place per the recommendation above. Effort:
   ~10 minutes of editing in the Cloud Eric project UI.

That is the only remaining item. Every code, CI, doc, and release
artifact called for by the Phase 9 checklist is already in place on
the kiln-v0.2.8 line. The post-v0.1 deferred Phase 8 items
(multi-GPU tensor parallelism, model-agnostic mode) explicitly
**should not** be queued as Phase 9 blockers per the planning-loop
policy.

## Recommended next planning-loop task

The single highest-leverage next task — concrete, verifiable,
no-GPU — is:

> **Title:** `kiln project description: replace stale v0.1.0 cut goal
> with release-readiness milestone`
>
> **Description:** Apply the project-description amendment recommended
> in `docs/audits/PHASE9_V0_1_0_READINESS.md`: strike the
> `v0.1.0 release with semantic versioning` Phase 9 checklist bullet
> (already shipped 2026-04-19; the goal as written is met) and replace
> it with a forward-looking "Release-readiness baseline on the
> kiln-v0.2.x line" goal that names the shipped artifacts,
> provenance, docs, demo link, onboarding checks, and the boundary for
> hypothetical v0.3.0-ahead changes.
> Doc-only / project-config-only; no PR against ericflo/kiln required.
>
> **Effort:** ~10 minutes. **GPU required:** no. **Cycle backoff
> after:** 30 minutes (steady state, Phase 9 cleared, no v0.1.0 cut to
> watch).

After that amendment lands, the next Phase 9 cycle should treat Phase
9 as closed and continue Phase 11 onboarding polish, or take the two
ranked post-Phase-10 alternatives from `PHASE10_CLOSURE.md` §"Pivot
recommendations" only if the planning policy has moved beyond Phase
11: GDN training-time streaming follow-ups (PR #635/#636/#637 in
flight) or a LoRA precision study targeting the FP32 SGEMM ~42%
hotspot. Per planning-loop policy, do **not** end the cycle on
"awaiting Eric" without a corresponding `ce approval-create` if a
genuine product decision is required; otherwise pick the
higher-leverage option and execute it.

## References

* `gh release list -R ericflo/kiln` and `gh release view kiln-v0.1.0`
  / `gh release view kiln-v0.2.8` (release index of record)
* `gh api /users/ericflo/packages/container/kiln-server/versions`
  (GHCR image-tag verification)
* PR #651 — `audits: phase 10 closure` (the doc that recommended this
  audit)
* PR #627 — `ci: attach build provenance attestations to release
  artifacts` (Sigstore evidence for item 3 + item 5)
* PR #624 — `docs(site): scaffold landing page + GitHub Pages
  workflow` (item 7)
* PR #612 — `chore: pin cargo-deny-action to a specific commit SHA`
  (item 2 / §12 closure)
* PR #607 — `security: cap training queue depth` (item 1 / §4)
* PR #608 — `security: cap tracked-jobs map size and TTL Completed/
  Failed entries` (item 1 / §4)
* PR #620 — `security: cap total adapter_dir disk usage on upload`
  (item 1 / §8)
* PR #621 — `security: LRU eviction for .composed/ adapter cache`
  (item 1 / §8)
* PR #601 — `docker: use --locked in cargo builds` (item 3)
* `docs/audits/security-audit-v0.1.md` (item 1, full evidence)
* `docs/audits/PHASE10_CLOSURE.md` (closure doc that triggered this
  audit)
* `SECURITY.md` §"Supply-chain provenance" (item 5 verification
  recipe)
