2026-05-09 A077: Vulkan GDN in-projection inner-stage profile

Change:
- Added profile-only `KILN_PROFILE_VULKAN_GDN_IN_PROJ_KERNEL_STAGES=1`
  timing in the Vulkan GDN in-projection dispatcher.
- The profiler covers the default single-submit path and the older multi-submit
  fallback path. Normal runtime behavior is unchanged when the env var is unset.
- CUDA and Metal source paths are untouched.

Measured profile:
- Command:
  `KILN_PROFILE_PAGED_LAYERS=1 KILN_PROFILE_GDN_STAGES=1 KILN_PROFILE_FULL_ATTN_STAGES=1 KILN_PROFILE_MLP_STAGES=1 KILN_PROFILE_VULKAN_MLP_KERNEL_STAGES=1 KILN_PROFILE_VULKAN_GDN_IN_PROJ_KERNEL_STAGES=1 KILN_BENCH_LOG_TOKENS=1 ./target/release/kiln-bench --model-path Qwen3.5-4B --paged --latency-only --latency-warmup-runs 1 --prompt-tokens 64 --max-output-tokens 8 --seed 77 --quiet`
- Artifact:
  `docs/audits/vulkan-strix-halo-2026-05-09-a077-gdn-in-proj-inner-profile.log`
- Backend JSON reported `vulkan` on
  `AMD Radeon 8060S Graphics (RADV STRIX_HALO)`.
- Measured pass generated token IDs:
  `[271,1206,1423,680,1204,1691,51864,3520,506]`.
- Latency: `2193.262508ms` prefill, `99.140491875ms` mean ITL,
  `98.900620ms` p50 ITL, `100.821073ms` p99 ITL, `9` tokens.

Measured-pass GDN in-projection inner totals:
- `batch=64 single_submit=true total`: `363.733ms` / `24` calls
  (`15.156ms` mean).
- `batch=64 read_host_visible`: `246.909ms` / `24` calls.
- `batch=64 record_submit_wait`: `108.461ms` / `24` calls.
- `batch=64 create_tensors`: `2.794ms` / `24` calls.
- `batch=64 create_out_buffers`: `1.778ms` / `24` calls.
- `batch=64 create_x_stage_write`: `1.696ms` / `24` calls.
- `batch=1 single_submit=true total`: `133.239ms` / `192` calls
  (`0.694ms` mean).
- `batch=1 record_submit_wait`: `82.976ms` / `192` calls.
- `batch=1 read_host_visible`: `36.940ms` / `192` calls.

Measured-pass surrounding buckets:
- GDN `seq_len=64`: `in_proj` `364.616ms`, `recurrent` `238.993ms`,
  `conv` `151.255ms`, `out_proj` `123.227ms`, `gated_norm` `109.423ms`.
- GDN `seq_len=1`: `in_proj` `138.207ms`, `recurrent` `105.897ms`,
  `out_proj` `63.238ms`, `gated_norm` `48.718ms`.
- MLP `batch=64`: total `876.746ms`, `gate_up_dispatch` `657.821ms`,
  `down_dispatch` `129.983ms`, `readback` `77.298ms`.

No-profile server correctness smoke:
- Command:
  `KILN_MODEL_PATH=Qwen3.5-4B KILN_PORT=18422 ./target/release/kiln serve`
- Server log selected Vulkan GPU
  `AMD Radeon 8060S Graphics (RADV STRIX_HALO)` and enabled the live greedy
  decode batcher with backend `vulkan`.
- Direct chat with
  `chat_template_kwargs: {"enable_thinking": false}` returned HTTP 200,
  `finish_reason == "stop"`, and visible `content == "blue green"`.
- Artifacts:
  - `docs/audits/vulkan-strix-halo-2026-05-09-a077-server-smoke.log`
  - `docs/audits/vulkan-strix-halo-2026-05-09-a077-direct-chat-response.json`
  - `docs/audits/vulkan-strix-halo-2026-05-09-a077-server-metrics.txt`

Validation:
- `cargo fmt --check`
- `cargo check -p kiln-vulkan-kernel`
- `cargo check -p kiln-model --features vulkan`
- `cargo build --release -p kiln-server --bin kiln-bench --features vulkan`
- `cargo build --release -p kiln-server --bin kiln --features vulkan`
- `git diff --check`
- CUDA and Metal checks were attempted and remain environment-blocked on this
  Linux host before project typecheck:
  - CUDA: `nvcc --version` / `No nvcc found in PATH or standard locations`.
  - Metal: `objc2` requires an Apple target.

Verdict:
- Keep the profile-only instrumentation.
- The current 64-token GDN in-projection bucket is dominated by output
  transfer/readback for `batch=64`, with compute/submit next. Optimizing this
  by tweaking only arithmetic tile shape is unlikely to be enough; the next GDN
  in-proj attempt should reduce CPU-bound output movement, keep more adjacent
  GDN stages resident, or fuse away the tensor split/readback boundary.
- Endpoint correctness remains good on the no-profile Vulkan GPU path when the
  caller uses the real template config rather than prompt-text hacks.
