Vulkan Strix Halo A080 summary, 2026-05-09

Change:
- Added a large-batch Vulkan MLP hybrid path:
  packed-BF16 gate/up weights plus cached F32 down weight.
- Default enabled when packed-BF16 MLP weights are available and
  `row_count >= 8`.
- Rollback:
  `KILN_DISABLE_VULKAN_MLP_BF16_GATE_UP_F32_DOWN=1`

No-profile same-binary A/B, seed 81:
- Rollback:
  - prefill `1470.046758ms`
  - mean ITL `87.12934975ms`
  - p99 ITL `87.890651ms`
  - token IDs `[271,1206,1423,680,1204,1691,51864,3520,506]`
- Candidate:
  - prefill `1384.762731ms`
  - mean ITL `86.617229125ms`
  - p99 ITL `87.867494ms`
  - same token IDs

Profile:
- Candidate flags for prefill MLP:
  `bf16_weights=true down_bf16_weights=false rows2=false down_rows2=true`
- A079 `batch=64` MLP inner total:
  - total `678.129ms`
  - gate/up `426.340ms`
  - down `234.627ms`
  - readback `6.541ms`
- A080 `batch=64` MLP inner total:
  - total `558.304ms`
  - gate/up `413.934ms`
  - down `127.721ms`
  - readback `6.615ms`

Correctness:
- Bench candidate and rollback both reported backend `vulkan` on
  `AMD Radeon 8060S Graphics (RADV_STRIX_HALO)`.
- Server smoke with
  `chat_template_kwargs: {"enable_thinking": false}` returned visible
  `content == "blue green"` and `finish_reason == "stop"`.

Validation:
- `cargo fmt --check`
- `cargo check -p kiln-vulkan-kernel`
- `cargo check -p kiln-model --features vulkan`
- `cargo test -p kiln-vulkan-kernel --test gdn_parity mlp_decode_bf16_packed_weights_match_cpu_reference -- --nocapture`
- `cargo build --release -p kiln-server --bin kiln-bench --features vulkan`
- `cargo build --release -p kiln-server --bin kiln --features vulkan`
- `git diff --check`
- CUDA check attempted and host-blocked by missing `nvcc`.
- Metal check attempted and host-blocked because `objc2` requires an Apple
  target.

Verdict:
- Accept as a Vulkan-only prefill win.
- CUDA and Metal source paths are untouched.
