target/
*.log
# Generated by tauri-build on every compile — contains JSON schemas for the
# Tauri ACL and capabilities system. Regenerated deterministically from
# tauri.conf.json + capabilities/*.json, so not worth tracking.
gen/
