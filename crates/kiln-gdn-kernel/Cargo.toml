[package]
name = "kiln-gdn-kernel"
version.workspace = true
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Vendored Gated DeltaNet (GDN) chunk forward-substitution CUDA kernel with C-ABI wrapper"
build = "build.rs"

[dependencies]
half = "2"
# Phase 7 closeout (#1082) — kiln-tensor-typed surface is the only path.
kiln-tensor = { workspace = true, features = ["cuda"] }
kiln-kt-bridge = { path = "../kiln-kt-bridge", default-features = false, features = ["cuda"] }

[build-dependencies]
cc = "1"
