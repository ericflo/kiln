[package]
name = "kiln-graph-vulkan"
description = "Vulkan CapturedGraph scaffold for the Phase 5 replay vocabulary."
version.workspace = true
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true

# Per Phase 5 of #1082, kiln-graph-vulkan implements the shared
# `kiln_graph::CapturedGraph` trait as a scaffold. Production Vulkan replay
# still lives in `crates/kiln-model/src/vk_decode_resident.rs` and
# `crates/kiln-vulkan-kernel/src/cmd_batch.rs`, where command batching remains
# the native replay primitive.
#
# Internal-only — no semver commitment.

[dependencies]
kiln-graph = { workspace = true }
kiln-tensor = { workspace = true }
thiserror = { workspace = true }
