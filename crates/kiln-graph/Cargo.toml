[package]
name = "kiln-graph"
description = "Backend-agnostic replay vocabulary and capture scaffolding for kiln."
version.workspace = true
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true

# Phase 5 of #1082. This crate ships the backend-agnostic replay vocabulary
# (CapturedGraph, ReplayPlan, CaptureSession, AllocatorMode, and typed
# capture errors). Production decode replay still lives in the model-level
# runners until Phase 5.x moves or wraps those runners behind this contract.
#
# Internal-only — no semver commitment until Phase 7 ships.

[dependencies]
kiln-tensor = { workspace = true }
thiserror = { workspace = true }
